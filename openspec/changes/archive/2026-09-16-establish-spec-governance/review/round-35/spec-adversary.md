Verdict: FAIL
- [ ] F1 major openspec/trace/gaps.json The hand-made low count hides a gap in the gate that merges a pull request. The CI run measured 83 not-covered lines for `src/layers/flights/enrichment.js`; the entry records the low count 86. `compareCoverageEntry` uses `entry.low` as the allowed count for a changed file, so a later change that edits this file and leaves 3 more not-covered lines gives no error. The same holds for `src/data/manager.js` with 2 lines. The ranges `src/data/manager.js` [134, 139] and `src/layers/flights/enrichment.js` [83, 88] keep the measured low counts, and I checked that they give no error and no stale entry for the width limit, for the CI samples and for both local runs. Write the measured low counts, or add a known limit that names the 2 and the 3 not-covered lines that these low counts allow above the measured counts.
- [ ] F2 minor proposal.md "The table gives the lowest count and the highest count that the runs gave" is not true for two of the seven rows. Name the two rows that are not measured counts in the sentence above the table.
- [ ] F3 minor openspec/trace/history.jsonl The unstable lines record the ranges [134, 141] and [83, 91]. The ledger holds other counts. The history is the record of each ledger change, and it does not record the change that the person made.
- [ ] F4 minor design.md The design does not say that two counts in the ledger come from a person. The archived design is the reference for the later change `harden-gate-ledger`, so record the manual change there as well.
- [ ] F5 minor openspec/trace/gaps.json The file is not the output of `writeLedger`. The 58 non-ASCII characters in the untraced test names are now `\uXXXX` escapes. Write the file with the command of the project.
- [ ] F6 minor openspec/trace/gaps.json For both files the low count is below the count that one of the two environments measures. A later change that edits either file gets `LEDGER-LARGER-GAP` in that environment, because the gate compares a changed file with the low count. Name this in the limit `range-slack` or in `deterministic-tests`.

Notes:
- **The next pull request passes.** `compareWithBase` with this ledger as its own base gives no errors; `compareLedger` gives no errors and 0 stale entries for the CI samples and for both local runs. The measured low counts 134 and 83 with the high counts 141 and 91 would give `LEDGER-UNSTABLE-TOO-WIDE`, so the reason in the proposal for a correction is real.
- **Ledger, history and samples agree on identity.** The cache holds exactly 4 samples per file for all 580 files, and every CI sample hash equals its ledger hash. Round 34 F4 is closed.
- **The rebuild changed nothing else.** Against `b967f7e`, only 5 coverage entries differ.
- **The `ci-samples` limit text is right:** the CI job keeps its results for 7 days, and no script in the project makes samples from CI results.
