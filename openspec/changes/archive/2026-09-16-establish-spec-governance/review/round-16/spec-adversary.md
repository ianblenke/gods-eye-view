Verdict: FAIL
- [ ] F1 major scripts/spec/lib/inventory.mjs:3 The inventory takes only files with a code extension (`CODE_FILE`). No gate looks for code in a tracked file with another extension or with no extension, and no gate stops a code file that loads such a file. Node runs these files. `require` reads a file with an unknown extension as CommonJS, and `import` reads a file with no extension as ESM in a `"type": "module"` package. The coverage run measures the code of these files, but `measureCoverage` reads only the files in the inventory, so the counts are lost. The test guard does not check their source either (test-guard.mjs:269). I made a fixture in `review-spec-r16/exp1.mjs` that runs the real `runGates` `init` and then `check`. In the fixture, `src/math.js` loads `src/impl.dat` with `createRequire(import.meta.url)('./impl.dat')` and loads the file `src/impl` (no extension) with `import { sub } from './impl'`. Each of the two files has a branch and a function that no test runs. The check gave `Coverage: 1 files, 1 complete` and `Gates passed.`, and `gaps.json` has no coverage entry. Other tracked files can hold code in the same way: a shell script with no `.sh` extension (the known limit `script-coverage` says that "the gate stops for a new or longer shell script", but this is true only for `.sh` files), an `.htm` or `.xhtml` file, and an `.svg` file with a `<script>` element or an `onload` attribute (the repository has 5 `.svg` files). Also, `planCi` (scripts/spec/lib/ci.mjs:24) and `checkUntracked` (inventory.mjs:52) use the same extension test. Thus a pull request that changes only `src/impl.dat` needs no archived change and no review. No known limit names this case. `pattern-checks` names only test files and HTML syntax. Correction: make the test guard record each script that a test process parses from a file in the root that is not in the inventory and is not a test file, and stop the gate for each such file (for example `COVERAGE-NOT-IN-INVENTORY`). Also add tracked files that start with `#!` and `.htm`, `.xhtml` and `.svg` files to the inventory or to a stop rule. Add a scenario and a gates.test.mjs test with a `require` of a file with an unknown extension and an `import` of a file with no extension. Or add a known limit, for example: "The inventory selects code files by extension. Code in a file with another extension or with no extension gets no count, and a diff that changes only such files needs no archived change. The spec adversary checks each new or changed file that has no code extension."

Round-15 findings:
- **Corrected (recorded):** F1, F2 and F3. The new known limit `pattern-checks` (proposal.md) names HTML code with a less common syntax (an unquoted attribute value, a character reference in a URL, a `srcdoc` attribute) and test file imports in forms that the patterns do not find. design.md:134 has the sentence about text patterns and the limit.
- **Corrected:** F4. The gates.test.mjs:721 test is tagged `coverage-gate-043`. It runs `check` with `src/uses.js` that imports `./helper.test.mjs`. It asserts status 1 and `ERROR COVERAGE-TEST-IMPORT src/uses.js:1`, so it checks the THEN line and the AND line (file and line). links.json:247–249 lists both tests.
- **Corrected:** F5. The `uncalled-code` limit now names files that no test loads, counted by physical lines, and event handler attributes counted as one line each.

Other checks:
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected before review.md is written. There are no warnings.
- **Trace files:** `ids.json` has `coverage-gate-043` with the change `establish-spec-governance`. `openspec/trace/retired-ids.json` does not exist, so no test uses a retired ID. The 6 history lines all name `establish-spec-governance`.
- I did not find other new gaps that are outside the known limits.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r16/`. I ran the experiment on the local Node v26. `require` of a file with an unknown extension works the same way on all Node versions. `import` of a file with no extension in a `"type": "module"` package also works on Node 24, but I did not run it there.