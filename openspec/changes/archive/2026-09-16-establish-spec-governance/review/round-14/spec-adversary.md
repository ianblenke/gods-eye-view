Verdict: FAIL
- [ ] F1 major scripts/spec/lib/coverage.mjs:5 The pattern `INLINE_SCRIPT` skips each script element that has the text `src=` anywhere in its start tag. The pattern uses `\bsrc\s*=`, and a hyphen or a quote is a word boundary. Thus the pattern also skips `<script data-src="x">` and `<script class="src=1">`. A browser runs the code in these elements, because they have no `src` attribute. I tested the real `inlineScriptLines` and `measureCoverage` (`review-spec-r14/html.mjs`). For `<script data-src="x">` with two lines of code, the count was 0, and the record was `complete: true, lines {total: 0, uncovered: 0}`. A complete file gets no gap in `currentGaps`, so it needs no ledger entry. Thus a change can add a new HTML file, or add a script to `index.html`, with code that no test covers, and the gates do not stop. The known limit `script-coverage` says that the gate measures inline scripts as gaps. It does not name this case. The coverage-gate-012 test (src/tooling/spec/coverage.test.mjs:93) uses only real `src` attributes. Correction: match `src` only as a full attribute name, for example `(?:^|\s)src\s*=` outside the quoted values. Add `<script data-src="x">code</script>` to the coverage-gate-013 test, and assert that the gate counts its code lines.
- [ ] F2 major scripts/spec/lib/coverage.mjs:57 `inlineScriptLines` counts only the code in `<script>` elements. It does not count JS code in event handler attributes such as `onload="..."` or `onclick="..."`, or in `javascript:` URLs. I tested `<body onload="fetch('/api'); window.go = 1">` (`review-spec-r14/html.mjs`). The count was 0 and the file was complete. Thus a change can put code that no test covers into HTML attributes, and the gates do not stop. The known limit `script-coverage` (proposal.md:58) names only shell files and inline scripts, and coverage-gate-012 and coverage-gate-013 name only script elements. Correction: count the lines of event handler attributes and `javascript:` URLs as not-covered code, or stop the gate for them. If you do not do this, add this case to `script-coverage`, and add an AND line to coverage-gate-012 that says that the gate does not measure code in attributes.
- [ ] F3 major scripts/spec/lib/inventory.mjs:4 The inventory does not include any file with a name that ends with `.test.mjs` (`isCodeFile`, inventory.mjs:32–34), and the coverage run excludes `**/*.test.mjs` (scripts/spec/gates.mjs:71). No gate checks whether a code file imports a test file. Thus an author can put code in a new file such as `server/providers/core.test.mjs`, add one test with a tag and an assertion, and export the rest of the code. A code file can then import that file. The gate does not measure this code, and no known limit names this case. No tracked code file imports a test file now, so a stop does not break old code. Correction: stop the gate when a file in the inventory imports a file with a name that ends with `.test.mjs`, and add a coverage-gate scenario and a test for this stop. Or add this case to "Known limits and later changes".
- [ ] F4 minor openspec/changes/archive/2026-09-14-establish-spec-governance/proposal.md:68 The known limit `branch-identity` says that the counts stay the same only when the change also covers or deletes the same number of other lines, branches or functions. A change does not always have to do this. V8 does not count the branches or the inner functions of a function that no test calls. Also, the gate counts a file that no test loads by its physical lines. Thus a change can add branches and inner functions on the current lines of such a function or file, and each count stays the same. I tested this on local Node (`review-spec-r14/v8exp/`). The new version added four branches and three inner functions on the same lines of an uncalled function. The lcov counts of the two versions were equal (FNF 2, FNH 1, BRF 2, BRH 2, LF 7, LH 4). `compareLedger` gave only `LEDGER-STALE`. The ratchet command wrote only a `hash` line. After the ratchet command, `compareLedger` gave no errors, and `compareWithBase` with `sameAsBase: () => false` and `outsideChanged: true` gave `[]`. The spec adversary check in `branch-identity` covers each changed code file with a ledger entry, so this case does not get past that check. Correction: add a sentence to `branch-identity`. For example: "V8 does not count the branches or inner functions of a function that no test calls, and the gate counts a file that no test loads by its physical lines. Thus a change can add code on the current lines of such a function or file, and no count changes."

Round-13 findings:
- **Corrected:** F1. `branch-identity` (proposal.md:68) now names lines, branches and functions. It names new code that no test covers, and each changed code file that has a ledger entry.
- **Corrected:** F2. gap-ledger-033 has the new AND line (specs/gap-ledger/spec.md:117). The archived spec is the same as `openspec/specs/gap-ledger/spec.md`. The gap-ledger-033 test (src/tooling/spec/ledger.test.mjs:400–410) makes the case: `src/flaky.js` is at 100% with the hash of its entry and has each low count at 0. The test asserts that the ratchet command keeps the entry and writes no history line. If you remove ledger.mjs:399–401, the test fails. The gate output has no `TRACE-ID-CHANGED` error, so the registry has the new scenario hash.

Gate output: the only error is `REVIEW-MISSING`, which is expected. There are no warnings. The ledger has no new entries and no entries with an origin other than `pre-spec`. The six history lines name `establish-spec-governance` and agree with the six ranges in the proposal. All the spec files of the change are the same as `openspec/specs/`. I did not change any file in the repository. My test files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r14/`.