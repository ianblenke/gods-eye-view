Verdict: FAIL
- [ ] S159 major openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:173 "The author corrects each minor finding, records it as a known limit, or keeps it with a reason in `review.md`." This does not agree with the other prose of the change. Step 10 in `.claude/commands/opsx/review.md:26` gives different choices for each agent. A minor finding of the spec adversary goes into "Known limits and later changes", and the step does not let you correct it or keep it. A minor STE finding is kept or corrected in the proposal or the design, and the step does not let you record it as a known limit. The sentence also has two meanings: "in `review.md`" can belong only to "keeps it with a reason" or to all three choices. Write: "The author adds each minor finding of the spec adversary to the section "Known limits and later changes" in the proposal. The author keeps each minor finding of the STE adversary, or corrects it in the proposal or the design. `review.md` records the decision for each minor finding."
- [ ] S160 major .claude/agents/spec-adversary.md:62 "With PASS, also list each minor finding, so that the author can correct it or record it as a known limit." This does not agree with step 10 of `.claude/commands/opsx/review.md:26`. After a PASS, that step tells you to add each minor spec finding to the known limits, and it does not start a new round. Only step 9, after a FAIL, lets you correct minor findings. Write: "With PASS, also list each minor finding, so that the author can record it as a known limit." Or change step 10 so that it also lets you correct a minor spec finding.
- [ ] S161 major .claude/commands/opsx/review.md:26 "If the two agents give the verdict PASS with minor findings" Articles and nouns. This has two meanings. It can mean that the two verdicts are PASS and at least one agent has minor findings. It can also mean that each agent gives PASS and each agent has minor findings. With the second meaning, no step handles the minor findings when only one agent has them. "minor spec finding" can also mean a finding about a spec, not only a finding of the spec adversary. Write: "If the two agents give the verdict PASS, and one or the two agents give minor findings, do not start a new review round for them. Add each minor finding of the spec adversary to the section ... Keep each minor finding of the STE adversary, or correct it ..."
- [ ] S162 minor openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:85 "The hash keeps the requirement, the scenario name and the scenario lines as separate parts." One word, one meaning. The same paragraph uses "contains" for the relation between the hash and its input, and S155 replaced "keeps" with "contains" for this reason. The sentence agrees with the code, because `scenarioHash` puts the parts in separate JSON fields. The sentence does not tell why this is important. Write: "The hash contains the requirement, the scenario name and the scenario lines as separate parts. Thus a line that moves from one part to another part changes the hash."
- [ ] S163 minor openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:85 "When the text changes, the ratchet command writes the new hash" Articles and nouns. Line 83 is now a separate paragraph, so "the text" has no noun before it in its paragraph. It can be the scenario text or the requirement text. The two readings give the same rule, because each change makes a new hash. Write: "When the hash of a scenario changes, the ratchet command writes the new hash only if a changed test has a tag with the ID of that scenario."
- [ ] S164 minor .claude/agents/spec-adversary.md:54 "A heading with a tab character is not read." Voice. This is a description, and the active voice is possible. Write: "The gate does not read a heading with a tab character."

Notes for the caller:
- **Gate output:** It says "STE: 0 errors, 0 warnings.", so there are no STE-PASSIVE or STE-ING warnings to examine. The only error is REVIEW-MISSING, which is not an STE problem.
- **How I found the changed prose:**
  - For `openspec/`, I compared the files with the round-23 copy in `scratchpad/review-spec-r23/repo`.
  - That copy has no `.claude/` folder. For the agent files and `review.md`, I used `git diff`, which is read-only, and I checked the parts in your list.
  - `proposal.md`, `tasks.md`, `README.md` and `openspec/ste` did not change.
- **Round 23 findings:** all four are corrected.
  - **S155:** The "Thus" sentence now follows the requirement sentence. The spaces at the end of a line are now in the design. The new sentence has S162.
  - **S156:** It is corrected with the given text.
  - **S157:** It is corrected in the requirement text of the delta spec and the main spec, in design.md:91, in the spec-trace-053 title and in the test name at `src/tooling/spec/specs.test.mjs:346`.
  - **S158:** It is corrected in the delta spec and the main spec.
- **New prose with no findings:**
  - The new spec-trace-047 AND line agrees with the code and the test: the `moved` and `before` checks, and `scenarioHash('a', ['b'])` compared with `scenarioHash('a\nb', [])`.
  - `.claude/agents/ste-adversary.md` has no findings.
  - Steps 11 to 14 of `review.md`, and the change in the step reference to "step 11", have no findings.
  - The `git restore` and `git rm` parts of step 9 have no findings.
- **Not reported:**
  - "can refer to" in the ste-adversary.md:53 example uses REFER with a meaning that STE does not approve. I did not report it, because proposal.md:3 has "refers to" in the same sense and earlier rounds accepted it.
  - design.md:173 "PASS when all its findings are minor" does not name the case with no findings, but the agent files do.
- **Safety:** I did not read `.env`. I changed no files in the repository and wrote no notes.
