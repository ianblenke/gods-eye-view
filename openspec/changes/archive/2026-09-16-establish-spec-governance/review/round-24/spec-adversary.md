Verdict: PASS
- [ ] F1 minor .claude/commands/opsx/review.md:26 Step 10 lets the author change files after the two agents give PASS, and step 11 takes the tree hash only after these changes. So the `Reviewed-Tree:` hash is the hash of a tree that no agent read. design.md:173 and the agent output rule (.claude/agents/spec-adversary.md:62) go further than step 10. They say that the author "corrects each minor finding" with no new review round. For a spec finding, that correction is a change to code and tests. Step 10 allows only a known limit for a spec finding, so the documents do not agree. design.md:177 still says "the hash stays correct until a file changes". The known limit `review-attestation` says only that the tree hash stops an old review. It does not say that the hash can include changes made after the review. Experiment (`review-spec-r24/exp2/run2.mjs`): I used the real `computeTreeHash` and `checkChangeReview` on a copy. I took the hash of the reviewed tree (`30390a3f…`). Then I changed a code file in the diff and the proposal, wrote the new hash (`89389b20…`) in `review.md` and ran the check. It gave no errors. No record connects an agent output to the tree that the agent read. To correct it, make design.md:173 and the agent output rule agree with step 10: after a PASS, only text in the proposal and the design can change. A code correction needs a new review round. Also add to `review-attestation` that the tree hash includes the step-10 changes to the proposal and the design, which no agent read.
- [ ] F2 minor scripts/spec/lib/review.mjs:129 With the new verdict rule, the severities in a finding decide the verdict. But the review gate checks only the `Verdict: PASS` line of each agent output. It does not stop for a PASS output that has a critical or a major finding. It also does not check that `review.md` records each finding of the current agent outputs, which step 12 requires. Experiment (`review-spec-r24/exp2/run.mjs`): I wrote the agent output "Verdict: PASS" with the lines `- [ ] F1 critical …` and `- [ ] F2 major …`. `review.md` had only `- [x] none`. `checkChangeReview` gave no errors. So an agent output that does not obey its own verdict rule passes the gate. A minor finding can also be missing from `review.md` and from the known limits. To correct it, add a change-review scenario and a test. The gate must stop for an agent output that has `Verdict: PASS` and a finding line with the severity `critical` or `major`. The gate must also stop for a finding ID (`F<n>`, `S<n>`) of `review/<agent>.md` that `review.md` does not name. As an alternative, name this in the known limit `review-attestation`.

Notes:
- **Round-23 F1 is corrected.** `scenarioHash` (specs.mjs:38–42) now hashes `JSON.stringify` of `{ requirement: { name, text }, name, body }`, so each part stays separate. I ran the round-23 experiment again with the real `loadSpecs` and `lintSpecs` on Node 24.21.0 (`review-spec-r24/exp1`). Spec a now gives the scenario hash `8202f8e8…` and spec b gives `0b2d7bf1…`. I also put the old line-feed join back into a copy (`review-spec-r24/mut`). The spec-trace-047 test then fails at specs.test.mjs:257, so the new assertion can fail. The new AND line in spec-trace-047 is the same in the main spec and in the delta spec. JSON escapes lone surrogates, so the hash input is also well formed for such text.
- **Scope since round 23:** The only changed files are specs.mjs, specs.test.mjs, spec-trace (main and delta), design.md, ids.json, links.json and the process files. The name of the spec-trace-053 scenario and of its test changed only in wording, and links.json agrees. `gaps.json` and `history.jsonl` did not change. `retired-ids.json` does not exist.
- **Tests:** specs.test.mjs and review.test.mjs pass on a copy with Node 24.21.0 (43 tests).
- **Tasks:** Test task 1.17 (spec-trace-047) is before code task 1.24. Task 9.8 "Correct each finding" does not agree with the new step 10. That is a prose matter for the STE adversary.
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected. `MaxListenersExceededWarning` is a Node warning, not a gate warning.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r24/`.
