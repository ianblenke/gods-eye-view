Verdict: FAIL
- [ ] S183 major openspec/specs/coverage-gate/spec.md:247-248 (change delta specs/coverage-gate/spec.md:243-244) "node:test skips each test that starts a test run with the guard ... each test that starts no test run with the guard runs as usual" Does not agree with the code. `src/tooling/spec/testGuard.test.mjs:26` sets `GUARDED_RUN` and gives it to every test in the file except the tests for `coverage-gate-045` and `-046`. So on Node 24.14.0, node:test skips tests that start no test run, for example "[coverage-gate-018] reads the relative file of a script URL" (it only calls `fileOfScriptUrl`), "[coverage-gate-018] checks only the files in the inventory ..." (a local `createGuard`) and the `withGuardEnv`/`wrapChildProcess` tests. The phrase "a test run with the guard" also has two meanings: a test that starts a guarded run, or a test that runs in a process with the guard (this file installs the guard at the top). design.md:70 "The tests skip each test run with the guard." has the same two meanings, and so do the code comments at testGuard.test.mjs:25 and gates.test.mjs:11 and the JSDoc of `missingTestContext`. Remove `GUARDED_RUN` from the tests that start no run, or write: "THEN node:test skips each test in `testGuard.test.mjs`, except the tests for `coverage-gate-045` and `coverage-gate-046`, and each test in `gates.test.mjs` that starts a test run with the guard".
- [ ] S184 major openspec/specs/gap-ledger/spec.md:119 (delta :115) "each not-covered count and each total count is in the band of the count in the entry / THEN the gate does not stop the build for that file" Does not agree with the code. The code does not test the total count. It tests the covered count (total minus not-covered) against the band. Both counts can be in the band while the covered count is not. I checked this with `compareLedger`. An entry with branches 3 of 100 and a current count of 8 of 95 (both counts differ by 5) gives `LEDGER-LOST-COVERAGE` "a.js has 87 covered branches. The ledger records 97." Write: "AND each not-covered count is in the band of the count in the entry, and each covered branch count and covered function count is not below the covered count in the entry minus the band".
- [ ] S185 major openspec/specs/gap-ledger/spec.md:114 and :125 (delta :110, :121) "The band of a count is 5, or 2% of the count" / "a covered count is below its band" Does not agree with the code, and there are two meanings. "its band" for a covered count reads as 5, or 2% of the covered count. `compareCoverageEntry` uses `band(entry[metric])`, which is 2% of the not-covered count. With 1,000 covered and 10 not-covered branches, the spec gives a band of 20, but the code stops at a loss of 15 ("b.js has 985 covered branches. The ledger records 1000."). design.md:126 "5, or 2% of the count" and the `compareLedger` JSDoc "by at most the band of each count" have the same problem. Write: "a covered branch or function count is below the covered count in the entry by more than the band of the not-covered count in the entry" (or change the code to use the covered count).
- [ ] S186 major openspec/specs/gap-ledger/spec.md:113-131 (requirement "Count band for unchanged files") Does not agree with the other specs. Four places in the same spec still stop with no exception. `gap-ledger-004` (:42) stops for more not-covered lines than the entry. `gap-ledger-054` (:58) stops for a smaller covered count in a file with the entry hash. `gap-ledger-008` (:102) stops for a current gap that is smaller than its entry. The requirement "Ratchet rule" (:34) and the Purpose (:4) say the same. The new scenarios 065 and 066 say the gate does not stop in these cases, and the code agrees with 065/066. design.md:117 also says "It also stops when the covered branch count ... becomes smaller" with no band. Add the exception to each place, for example in 004, 054 and 008: "AND the file is not an unchanged file with counts in its band (see Count band for unchanged files)". In 008, use "AND the file is not an unchanged file".
- [ ] S187 major openspec/specs/gap-ledger/spec.md:114 "An unchanged file is a loaded code file with the content of the base commit and of its ledger entry." One word, one meaning. The same spec already uses "unchanged file" with two other meanings. In `gap-ledger-018`, `-054` and `-028` (and design.md:117) it means "content hash equal to the hash in its ledger entry". In `gap-ledger-041` and `-056` it means "content equal to the base commit". The spec now has three meanings. The texts also use other words for the new term: proposal.md:65 "a file that the diff does not change" (the band also needs the entry hash and a loaded file) and the test name gap-ledger-065 "a file that the diff does not change". Give the new term its own name, for example "a stable-base file" or "a file in the band", and use it in the requirement, 065 to 067, design.md:124-128, proposal.md:65 and the 065 test name.
- [ ] S188 major openspec/specs/coverage-gate/spec.md:115 (delta :111) "tells you to run the gates with make, which uses a clean copy of the project". The same text is in the `GATES-LOCAL-ENV` message at scripts/spec/gates.mjs:329. Does not agree with the code. The `make gates` copy (`git ls-files --cached --others --exclude-standard`) includes untracked files that Git does not ignore. `.gitignore` ignores only `.env`, and `git check-ignore .env.local` finds no rule. So a non-empty `.env.local`, which this scenario covers, goes into the copy, and the gate stops again under make. design.md:44 correctly says "copies the files that Git tracks or does not ignore". Write: "tells you to run the gates with make, which does not copy the files that Git ignores, or to make the file empty" (or ignore `.env.*` in `.gitignore`).
- [ ] S189 minor openspec/changes/archive/2026-09-15-establish-spec-governance/design.md:44 "The gates also stop for a dotenv file that Git does not track and that is not empty." `localEnvFiles` reads only the project root, and coverage-gate-047 says "the project root". Write: "for a dotenv file in the project root that ...".
- [ ] S190 minor openspec/specs/coverage-gate/spec.md:241 (delta :237) "a process with `GEV_SPEC_OUT` in its environment has a `node:test` module without the function `getTestContext`" Articles and nouns / scope. `installGuard` returns before this check in a worker thread and in a process with no inspector. A test runner without an inspector therefore writes no `COVERAGE-NO-TEST-CONTEXT` error. The build still stops through the test processes, so the meaning is clear. Write: "WHEN the test guard starts in the main thread of a process with an inspector session and `GEV_SPEC_OUT`, and `node:test` has no function `getTestContext`".
- [ ] S191 minor src/tooling/spec/ledger.test.mjs test name gap-ledger-067 "a file that is not loaded or not true" Approved words / one word, one meaning. The specs say "untrue coverage", and a file is not "true" or "not true". Write: "does not use the band for a changed file, a file that no test loads or a file with untrue coverage".
- [ ] S192 minor openspec/changes/archive/2026-09-15-establish-spec-governance/proposal.md:65 "a band of 5, or 2%," / "removes or weakens tests" Articles and nouns: the sentence does not say what the 2% is of. Approved words: I am not sure that "weaken" is an approved STE verb. Write: "a band of 5, or 2% of the count when that is more, ..." and "a change that removes tests or removes assertions from tests".
- [ ] S193 minor coverage-gate-047 test name and spec :113 (delta :109) "before the test runs" Verbs. "runs" can be a noun (the gate's test runs) or a verb (before the test runs). Both readings give the same result here. Write: "before the gate starts the test runs".
- [ ] S194 minor scripts/spec/lib/ledger.mjs:202 "so its entry is not stale"; Makefile:5 "so these folders are copied back"; src/tooling/spec/testGuard.test.mjs:384 "this folder is removed by a later exit listener" Approved words and voice. "stale" is not an approved STE word, and the two comments use the passive voice where the active voice is possible. Write: "so the gate does not record its entry as not current"; "so the target copies these folders back"; "a later exit listener removes this folder".
- [ ] S195 minor scripts/spec/lib/test-guard.mjs JSDoc of `missingTestContext` "Node versions before the function `getTestContext` in `node:test` cannot count the assertions of a test." Articles and nouns: the guard counts assertions, not a Node version. Write: "On a Node version without the function `getTestContext` in `node:test`, the guard cannot count the assertions of a test."

Notes:
- **Round 28 corrections:** S180, S181 and S182 are all in proposal.md (lines 33 and 57).
- **Facts checked, no finding:**
  - design.md:18/21 and proposal.md:3 (120 files, 289 of 3,533 tests) agree with the gate output. The 289 traced names are all in `src/tooling/spec/`, so "only the tests of this change" is correct.
  - "Node 24.14.0 has no `getTestContext()`" is correct. `node:24.14.0-bookworm-slim` gives `undefined`, and the gate image (v24.21.0) gives `function`. The CI `verify` matrix runs `npm test` on 24.14.0.
  - ci-gates-006: the new AND line agrees with the `upload-artifact` step, which has `if: always()` and the path `.gev-cache/spec/`.
  - ci-gates-007: the THEN and AND lines agree with `GATES_COPY` and `GATES_BACK`.
  - gap-ledger-067 "untrue coverage" agrees with the requirement's word "loaded", because `measureCoverage` records untrue files as not loaded.
  - The tasks 3.37–3.41, 4.64–4.67 and 7.24–7.26 are all in the imperative.
- **Gate output:** "STE: 0 errors, 0 warnings.", so there are no STE-PASSIVE or STE-ING warnings to examine. The REVIEW-TREE error is expected.
- **How I compared:** git shows the untracked archive and main specs as deleted against `2069f91`. So I extracted the `2069f91` copies to the scratchpad and ran `diff -r`. The only changed prose is in design.md, proposal.md, tasks.md, and the ci-gates, coverage-gate and gap-ledger specs, in both the delta and the main copy.
- **Safety:** I did not read `.env`, did not change any repository file, and ran only read-only git commands. I ran `docker run --rm` twice to print a Node version. My scratch files are only in `.../scratchpad/review-ste-r29/`: the `old/` copies and `band.mjs`, which reproduces S184 and S185.
