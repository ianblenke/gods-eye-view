Verdict: FAIL
- [ ] F1 major scripts/spec/lib/test-guard.mjs:132 `withGuardEnv` gives the gate values only to the process that `node:child_process` starts. That process can be a shell. A shell can start Node with other values, so the Node process gets no guard. `exec` and `execSync` always use a shell. I tested this with a copy of `scripts/spec` and a fixture project, like gates.test.mjs does:
  - `src/math.js` has `add` and `sub`. The test calls only `add`. Another test writes a fake script with the `sourceURL` of `src/math.js` that calls `sub`, then runs `execSync(\`GEV_SPEC_OUT= ${process.execPath} fake.cjs\`)`.
  - Without `GEV_SPEC_OUT=`, the check gave `COVERAGE-FAKE src/math.js`. With `GEV_SPEC_OUT=`, the check gave `Coverage: 1 files, 0 complete, 0 not loaded, 0 untrue.` and `LEDGER-STALE`. The ledger gap became smaller, so the ratchet command would record a closed gap from fake coverage.

  This path uses `node:child_process`, so the `guard-results` limit does not include it. coverage-gate-025 and coverage-gate-031 are therefore not true for a process that a shell starts. Make the gate stop for coverage from a process that wrote no guard results. For example, at the exit of each process with the guard, compare the process IDs of the coverage files in `NODE_V8_COVERAGE` with the `guard-<pid>.jsonl` files. Add a scenario and a gate test that uses `execSync` with `GEV_SPEC_OUT=`. If you do not do this, add to the `guard-results` limit that a shell or another program that a test starts can start Node without the gate values.
- [ ] F2 major scripts/spec/lib/test-guard.mjs:234 In a worker thread, the guard reads `GEV_SPEC_OUT` from the environment of the worker. A test sets that environment with the `env` option of `new Worker`. Without `GEV_SPEC_OUT`, the guard returns at :234, before `wrapChildProcess` (:245). The child processes of the worker then do not get the gate values. I tested this with the same copy of the gates and the test from gates.test.mjs:683:
  - With `new Worker(code, { eval: true })`, the check gave `COVERAGE-FAKE src/math.js` and `1 untrue`.
  - With `new Worker(code, { eval: true, env: <process.env without GEV_SPEC_OUT> })`, the check gave `0 untrue` and no `COVERAGE-FAKE`.

  Thus coverage-gate-039 ("the guard gives the gate values to the child processes that the worker starts") is not true. Give the gate values to a worker from the main thread, not from the `env` option of the test. For example, wrap `Worker` in the main-thread guard so that it adds the gate values to `env`, or send the values in the worker options. Add this case to the coverage-gate-039 gate test. Also change the `worker-coverage` limit and design.md:49. They say that the guard does not load into a worker thread, but coverage-gate-039 says that it does.
- [ ] F3 minor scripts/spec/lib/ledger.mjs:133 For an entry with a range, `sameTotals` accepts any truthy `totals` value. gap-ledger-055 says that the gate stops for a loaded entry that has no total branch count or no total function count. I tested a copy of `ledger.mjs` with a loaded range entry and counts in the range. `totals: {}`, `totals: { branches: null, functions: null }` and a false `totals: { branches: 1, functions: 1 }` each gave `{"errors":[],"stale":[]}`. False totals can also hide lost branch coverage across two changes:
  - Change 1 writes `totals.branches: 5` for a range entry with branches 3 to 5. The gate does not stop.
  - In change 2, the real total is 50, and the not-covered branches go from 5 to 12. The covered count goes from 45 to 38. `compareLedger` gives only `LEDGER-STALE`, because `coveredCount` of the entry is 0 (:173). The ratchet command writes branches 12 with the reason `shown by test`. Then `compareWithBase` with this base and `sameAsBase: () => true` gives no errors, because the base covered count is 0 (:313).

  Stop for a loaded range entry when `totals.branches` or `totals.functions` is not a number. Also stop in `compareWithBase` when the totals of an unchanged file are not the base totals and the history of the change has no line for that file. Add the `{}` case and the null case to the gap-ledger-055 test (ledger.test.mjs:158).
- [ ] F4 minor .claude/commands/opsx/review.md:24 `git checkout -- openspec/specs` copies the files from the Git index, not from the commit before the archive. On this branch, the archived spec files are already in the index (`git status` shows `A` and `AM` for `openspec/specs/*/spec.md`). A later change with `MODIFIED` or `REMOVED` requirements can stage the archive output in the same way. Then this step keeps the archived specs, and the next `openspec archive` applies the delta again. Tell the author to copy `openspec/specs` from the commit before the archive into the index and the working tree, for example with `git restore --source=<commit before the archive> --staged --worktree openspec/specs`.

Round-4 findings:
- **Corrected for the reported paths:** F1. On the local Node, I used a copy of the guard with the correct inventory hash:
  - A nested `node --require strip.cjs --test fake.test.mjs`, where `strip.cjs` removes `GEV_SPEC_OUT`, now writes `COVERAGE-FAKE` from the test process.
  - A worker with its default environment now gives the gate values to its child process.
  - A runner preload that sets `NODE_OPTIONS` to `''` also did not remove the guard on Node v26. The reason is that the v26 runner gives its preloads to each test process on the command line. I could not check this on 24.21.0. `lockGuardEnv` does not lock `NODE_OPTIONS`.
  - F1 and F2 in this round are new paths.
- **Corrected:** F2. coverage-gate-039 has its own tests (testGuard.test.mjs:308, :327 and gates.test.mjs:683). The worker test is no longer tagged coverage-gate-024. spec-trace-045 is on a gate test with `git mv` (gates.test.mjs:705).
- **Corrected for entries without `totals`:** F3. F3 in this round is the remaining case.
- **Corrected:** F4. `idsOfChangedTests` compares each test with all base test files after it removes the spaces at the end of each line. The `scenario-text-change` limit is in the proposal.
- **Corrected:** F5. The step now moves the folder to `openspec/changes/<name>` and runs the ratchet command. F4 in this round is the remaining problem with `git checkout`.

Gate output: the only error is `REVIEW-MISSING`, which you expected. There are no warnings. `ids.json` has 199 IDs, and each one names establish-spec-governance. `links.json` has no empty entry. Each of the four `unstable` history lines names the change, has a `[low, high]` range for each metric, and agrees with `gaps.json` and the proposal table. Each loaded ledger entry has number totals.

I did not change files in the repository. I did the experiments in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r5/`. The copies of `scripts/spec` are in `copy/`, the gate fixtures are `worker-gate.mjs` and `shell-gate2.mjs`, and the ledger test is `ledger-exp.mjs`. I used the local Node v26.8.2. The copied registry, ledger and guard test files pass (88 tests). F3 uses only ledger logic. F1 and F2 depend on how shells and the worker `env` option work, and Node 24 has the same behavior. I did not run F1 or F2 on 24.21.0.