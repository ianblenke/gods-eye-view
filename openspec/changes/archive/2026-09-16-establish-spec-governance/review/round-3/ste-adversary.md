Verdict: FAIL
- [ ] S67 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:52 "A change to a code file during the run does not hide this, because the hashes are from the start of the run." S46 is not complete here. One word, one meaning: the noun "change" still means a modification in this sentence, but the name of an OpenSpec change in other places. Write: "The hashes are from the start of the run. Thus a code file that changes during the run cannot hide this."
- [ ] S68 openspec/changes/archive/2026-09-14-establish-spec-governance/proposal.md:60 "write a record that obeys the record rules" One word, one meaning: "record" is a noun here, but a verb in all the other places (S13, S51). The test names also use "entry" for the same thing (gov-tests-r3.txt:159 "writes one entry for each test"). I am not sure that "obey" is an approved STE verb. Write: "write an entry that agrees with the rules for entries". Also: design.md:41 "stops for a record with tags" → "stops for an entry with tags"; specs/spec-trace/spec.md:63 "in the record of the test" → "in the entry of the test"; specs/spec-trace/spec.md:150 "Stop for a test record that does not agree with its name" → "Stop for a test entry that does not agree with its name"; specs/spec-trace/spec.md:151 "the tags of a test record" → "the tags of a test entry"; gov-tests-r3.txt:202 and :203 "a test record" → "a test entry". Fix the openspec/specs copy too (line + 4).
- [ ] S69 gov-tests-r3.txt:201 "stops for a result record that a test forges in the result folder" Approved words: "forge" is not an approved STE word. "record" is also a noun here (S68). Write: "stops for a result entry that a test writes in the result folder".
- [ ] S70 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/change-review/spec.md:62 "Stop for an agent output without a pass" Verbs: this uses the verb "pass" as a noun. Write: "Stop for an agent output without the verdict PASS". Also gov-tests-r3.txt:20 "stops for an agent output without a pass" → "stops for an agent output without the verdict PASS". Fix the openspec/specs copy too (line + 4).
- [ ] S71 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/coverage-gate/spec.md:150 "the text of the guard import" Verbs: "import" is a verb everywhere else ("a test imports"), and here it is a noun. Write: "the text that imports the guard". Fix the openspec/specs copy too (line + 4).
- [ ] S72 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/coverage-gate/spec.md:111 "The gate reads the content hash of each code file before the test runs start." One word, one meaning: in the other files "read" means to read a file, and design.md:58 uses "calculates" for this action. Write: "The gate calculates the content hash of each code file before the test runs start." Fix the openspec/specs copy too (line + 4).
- [ ] S73 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/coverage-gate/spec.md:78 "Run the tests without inherited Node options" Approved words: I am not sure that "inherit" is an approved STE word or a technical verb. Write: "Run the tests without the Node options of the gate environment". Also gov-tests-r3.txt:49 "runs the tests without inherited Node options and with the test guard" → "runs the tests without the Node options of the gate environment and with the test guard". Fix the openspec/specs copy too (line + 4).
- [ ] S74 gov-tests-r3.txt:64 "gives untrue coverage to a file in the coverage report that no guard checked" One word, one meaning: in other places, "gives ... coverage to a file" sets the coverage count ("gives 0% coverage"). Here it means "records the file as untrue". The scenario (coverage-gate-024) says "records the file as not loaded and as untrue". Write: "records a file in the coverage report that no guard checked as not loaded and as untrue".
- [ ] S75 gov-tests-r3.txt:163 "records an untagged subtest of a tagged parent as untraced" Approved words and verbs: "tag" is a noun in this change, and "tagged" and "untagged" use it as a verb. The spec says "a subtest with no tag". Write: "records a subtest without a tag under a parent with a tag as untraced".
- [ ] S76 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/spec-trace/spec.md:21 "Stop for a duplicate ID in the main specs" One word, one meaning: the same spec uses "the same ID" for this (lines 26 and 39). I am not sure that "duplicate" is an approved STE word. Write: "Stop for the same ID in two scenarios of the main specs". Also gov-tests-r3.txt:155 "stops for a duplicate ID in the main specs" → "stops for the same ID in two scenarios of the main specs". Fix the openspec/specs copy too (line + 4).
- [ ] S77 gov-tests-r3.txt:38 "reads the total and covered counts for each loaded file" Articles: an article is possible here and in the places below. Write: "reads the total and the covered counts for each loaded file". Also: gov-tests-r3.txt:53 "checks only inventory files inside the root" → "checks only the inventory files inside the root"; gov-tests-r3.txt:91 "makes ledger entries smaller" → "makes the ledger entries smaller"; gov-tests-r3.txt:148 "does not read scenario headings in fenced code and keeps code lines in the hash" → "does not read the scenario headings in fenced code and keeps the code lines in the hash"; proposal.md:11 "records closed gaps" → "records the closed gaps"; proposal.md:13 "checks new prose" → "checks the new prose"; design.md:26 "Measure coverage of code" → "Measure the coverage of code"; design.md:84 "compares lines, branches and functions" → "compares the not-covered lines, branches and functions"; design.md:146 "Findings are Markdown check boxes." → "The findings are Markdown check boxes."; design.md:166 "Link tests to scenarios and find trace errors" → "Link the tests to the scenarios and find the trace errors"; design.md:168 "List tracked and untracked code files and test files" → "List the tracked code files, the untracked code files and the test files"; design.md:169 "Read lcov" → "Read the lcov report"; design.md:171 "keep samples" → "keep the coverage samples"; design.md:172 "STE lint rules" → "The STE lint rules"; design.md:175 "Tests for the files above" → "The tests for the files above"; design.md:180 "Process rules for people and agents" → "The process rules for people and agents"; openspec/config.yaml:11 "Run gates in the Docker image" → "Run the gates in the Docker image"; openspec/config.yaml:12 "Unit tests are src/**/*.test.mjs. Tests use" → "The unit tests are the files src/**/*.test.mjs. The tests use"; openspec/config.yaml:16 "Write tests from the scenarios. Then write code" → "Write the tests from the scenarios. Then write the code"; openspec/config.yaml:46 "Use active voice. Use simple present, simple past or simple future tense." → "Use the active voice. Use the simple present, the simple past or the simple future tense."
- [ ] S78 openspec/config.yaml:14 "This process applies to every change:" Approved words: in STE, "apply" means "put a material on a surface" (S38). One word, one meaning: the other files use "each" for "every". Write: "This process is for each change:". Also openspec/config.yaml:17 "Every scenario has a stable ID. Every test names" → "Each scenario has a stable ID. Each test names".
- [ ] S79 openspec/config.yaml:22 "An adversarial review agent reviews each change before archive. The review result goes in the change folder as review.md." Verbs: this uses the verb "archive" as a noun. Approved words: in STE, "go" means "move" (S33). The text also does not agree with design.md:148–150, where two agents do the review after the archive command. Write: "Two adversarial review agents review each change after the archive command. The change folder contains the review result in review.md."
- [ ] S80 openspec/config.yaml:40 "review records" Approved words and one word, one meaning: "record" is a verb in the other files (S13). Write: "review files".
- [ ] S81 openspec/config.yaml:56 "one automated test can check it" Approved words: "automated" is not an approved STE word (S24). Write: "one automatic test can check it".
- [ ] S82 openspec/config.yaml:30 "Do not change or reuse an ID." Approved words: I am not sure that "reuse" is an approved STE verb. Write: "Do not change an ID or use it again." Also openspec/config.yaml:54 "Do not change or reuse a scenario ID." → "Do not change a scenario ID or use it again."
- [ ] S83 openspec/config.yaml:61 "State how each gate measures the requirement." Approved words: I am not sure that "state" is an approved STE verb. Write: "Tell how each gate measures the requirement."
- [ ] S84 openspec/config.yaml:65 "Add a final task group for the gates and the adversarial review. Include make gates, the review agent run and review.md." Approved words: "final" is not an approved STE word. The approved word is "last". Write: "Add a last task group for the gates and the adversarial review. Include make gates, the run of the review agents and review.md."
- [ ] S85 openspec/config.yaml:8 "Stack:" Approved words: in STE, "stack" does not mean a set of software. Write: "Software:".
- [ ] S86 openspec/config.yaml:35 "Separate two or more IDs with a space" One word, one meaning: the test names use "separate" as an adjective (gov-tests-r3.txt:162), and specs/spec-trace/spec.md:81 says "with one space between the IDs". Write: "Put one space between two IDs".
- [ ] S87 AGENTS.md:13 "Get a passed review from the two review agents before you merge." Verbs: "merge" needs an object here. Write: "Get a passed review from the two review agents before you merge the change."
- [ ] S88 .claude/commands/opsx/review.md:15 "The archive does not change the scenario text" One word, one meaning: "archive" is also the archive folder (AGENTS.md:43), and design.md uses "the archive command" for the command. Write: "The archive command does not change the scenario text".
- [ ] S89 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:196 "Rollback: remove the CI job and the `gates` target." Approved words: "rollback" is not an approved STE word. Write: "To remove this process, remove the CI job and the `gates` target."

Notes for the caller:
- Round-2 check: 20 of the 21 findings (S47–S66) are corrected everywhere. S46 is correct in the places it named, but the same problem is still in design.md:52 (see S67). The rewritten texts for S52 (coverage-gate/spec.md:98) and S57 (the spec-trace purpose) have no STE problems.
- The merged specs in openspec/specs/ are the same as the change delta specs, at line + 4. The STE lint shows 0 warnings. The `allowedIng` list in openspec/ste/words.json has no changes in this round.
- I did not read openspec/changes/archive/2026-09-14-establish-spec-governance/review.md (it is not there) or .env. I made no changes to files in the repository.