Verdict: FAIL
- [ ] F1 major scripts/spec/lib/ledger.mjs:144 A person can hide lost branch coverage in a file that did not change. The gate checks branches and functions only when the file hash changed (ledger.mjs:144). The base check accepts more branches when the file has one fewer not-covered line (ledger.mjs:284). design.md:83 gives the reason: "A new test cannot add code, so new tests cause the new branches." That reason is wrong when a change removes or changes a test. I tested this on the local Node:
  - With both tests, `x.js` had `BRF:5 BRH:5 LF:5 LH:5`. When I removed one test, it had `BRF:4 BRH:2 LF:5 LH:5`. The file lost 2 branches and no lines. `a ? b : c` and `b || c` on a line that runs do this.
  - I used a ledger entry with lines 10 and branches 5, and a run with lines 9 and branches 12. `compareLedger` gave only `LEDGER-STALE`. `ratchetLedger` wrote branches 12 with the reason `shown by test`. After that, `compareLedger` and `compareWithBase` (with `sameAsBase: () => true` and `outsideChanged: true`) gave no errors.
  
  Record the total branch count and the total function count in each ledger entry. Accept more not-covered branches or functions in a file that did not change only when the total count became larger by the same number or more. Add a scenario and a test where a change removes a test and covers one more line. If you do not do this, add the risk to Known limits.
- [ ] F2 major scripts/spec/gates.mjs:124 The gate does not read the exit status of a test run that wrote its result file. So a test can make a failed test look like a passed test. I tested this on the local Node with a copy of `trace-reporter.mjs` and `trace.mjs`:
  - `src/a.test.mjs` has `[demo-001] real check`, which fails. A test in `src/b.test.mjs` found the result file through `/proc/<ppid>/cmdline`. It waited for `"status":"fail"` and wrote `"status":"pass"` at the same place in the file.
  - `node --test` stopped with status 1. The record showed `"status":"pass"`, and `evaluateTrace` gave `errors: []`. It also marked `demo-001` as verified by the failed test. The guard counts an assertion before the assertion throws (test-guard.mjs:98-100), so `TRACE-NO-ASSERTION` does not occur.
  
  The `guard-results` limit (proposal.md:60) names only a new record. It does not say that a test can change the records of other tests. Stop for a run with a status that is not 0 when no record of that run has the status `fail`. Add a scenario and a test for this. Also add the change of other records to the `guard-results` limit.
- [ ] F3 minor scripts/spec/lib/test-guard.mjs:131 A child process that a test starts with `node:child_process` can still run without the guard. `withGuardEnv` adds the guard import at the end of `NODE_OPTIONS`. Node runs all `--require` preloads before `--import` preloads. So a `--require` file can remove `GEV_SPEC_OUT`, and then `installGuard` returns null (:186). I tested this with a copy of the guard, the correct inventory hash and `NODE_V8_COVERAGE` set:
  - `NODE_OPTIONS="--import=<guard>"` with a `new Function` fake wrote a guard file with `COVERAGE-FAKE`.
  - `NODE_OPTIONS="--require=strip.cjs --import=<guard>"`, where `strip.cjs` is `delete process.env.GEV_SPEC_OUT;`, wrote no guard file. It still wrote a coverage file.
  
  design.md:55-56, coverage-gate-031 and the sentence "A process that a test starts without `node:child_process` does not get the guard" all say that such a child always gets the guard. Load the guard before each other preload, for example as the first `--require` option in `NODE_OPTIONS`. If you do not, add this path to the `guard-results` limit.
- [ ] F4 minor scripts/spec/lib/registry.mjs:140 Round-2 F4 is only partly corrected. The text of a test goes to the next test or to the end of the file. So a comment after the closing line of a test counts as a change to that test. registry.test.mjs:133-135 makes this a required result: `// end` after the last test gives `demo-002`. spec-trace-043 also counts each test in a new test file as a changed test, so a renamed test file lets a person change the text of every scenario that the file names. This contradicts spec-trace spec.md:181: "a test with a tag that names the ID MUST also change." End the text of a test at its last line, and compare a renamed file with its old path, for example with `git diff -M`. If you do not, add these limits to Known limits.
- [ ] F5 minor .claude/commands/opsx/review.md:22 Step 9 says: "correct each finding. Then start again at step 3." The change is already archived (step 2). A correction to a scenario, a test tag or a test name needs the ratchet command, but the ratchet command stops for a change that is not active (gap-ledger-027, gates.mjs:291). This round moved the archive folder from `2026-09-13-…` to `2026-09-14-…` by hand. Tell the author to move the change back to `openspec/changes/<name>`, run the ratchet command and archive the change again. Or let the ratchet command accept the archived change of the diff.
- [ ] F6 minor scripts/spec/lib/ledger.mjs:470 The `unstable` history line records only the line range (`after: [next.low.lines, next.lines]`). history.jsonl line 3 for `src/data/labelArbiter.js` shows `"before":18,"after":[18,18]`, but the change put the branch range 50 to 52 in gaps.json. Line 2 for `annotationResolver.js` also does not show branches 137 to 138. So the history does not show the branch and function ranges that the change allowed. Record the low count and the high count of each metric in the history line, and assert them in the gap-ledger-035 test (ledger.test.mjs:407-411).
- [ ] F7 minor src/tooling/spec/ledger.test.mjs:255 The gap-ledger-047 THEN line is "the file has its keys in sorted order". The test has only one `untracedTests` file key, so it cannot find an unsorted `untracedTests` map. Also, `writeLedger` writes `version` before `coverage` (ledger.mjs:57-62, gaps.json:2), so the top-level keys are not in sorted order. Add a second `untracedTests` file in the wrong order and assert the order. Also sort all keys, or change the THEN line to name the maps that have sorted keys.

I checked the round-2 findings:
- **Corrected:** F1, F3, F5, F7, F8 and F9.
- **Corrected for the reported paths:** F2. F3 in this round is a new path.
- **Corrected:** F6. Each new history line must name the checked change. Any file outside `openspec/` in the diff stops a wider range. The fixed width limit keeps each high count less than 5, or 2%, above the counts that the runs give.
- **Partly corrected:** F4. The rest is F4 in this round.

Gate output: the only error is `REVIEW-MISSING`, which you expected. There are no warnings.

I did not change files in the repository. I ran the tests in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r3/` with copies of `scripts/spec/lib`. They used the local Node v26, not 24.21.0. F1 and F6 use only ledger logic and do not depend on the Node version. F2 and F3 depend on reporter and preload behavior that Node 24 also has.