Verdict: FAIL
- [ ] F1 minor scripts/spec/lib/specs.mjs:39 The scenario hash joins the requirement name, the requirement text, the scenario name and the body lines with a line feed only. Nothing marks where one part stops and the next part starts. Thus, if a line moves from the end of the requirement text into the scenario name, and the old scenario name moves to the first body line, the joined text does not change. The scenario hash does not change. Requirement spec-trace-047 (openspec/specs/spec-trace/spec.md:188) says: when the text of a requirement changes, the hash of each scenario of that requirement changes. design.md:83 says: "Thus a change to a requirement is also a change to each of its scenarios." I ran the real `loadSpecs`, `lintSpecs`, `checkRegistry` and `compareRegistryWithBase`, and OpenSpec 1.3.1, on copies (`review-spec-r23/exp1`). The main spec `radio` (spec a) had the requirement `Play` with the lines "The radio MUST play the stream that the user selects.", "Origin: spec-first" and "The radio MUST NOT play a stream with the type exe.". It had the scenario "Play a selected stream `radio-001`" with a WHEN line and a THEN line. In spec b, the MUST NOT line is not in the requirement text. It is the scenario name, "The radio MUST NOT play a stream with the type exe. `radio-001`". The first body line is "Play a selected stream". Results: both specs gave the same `radio-001` hash (`29deb5f9…`). Only the requirement hash changed. There were no load errors and no lint errors. `checkRegistry` with the registry of spec a gave no errors on spec b. `compareRegistryWithBase` with no changed test gave no errors. `openspec validate --specs --strict` said "valid: true" for both specs. Thus an author can remove a MUST or MUST NOT line from a requirement and change no test. The registry does not use the requirement hash, so `TRACE-ID-CHANGED` and `TRACE-ID-BASE-CHANGED` do not stop the build. An archived MODIFIED delta that is equal to spec b passes spec-trace-051. The known limit `scenario-text-change` is only about a changed test. Correction: in `scenarioHash`, hash a structure that keeps the parts separate, for example `JSON.stringify({ requirement: requirement && { name, text }, name, body })`, or put a fixed prefix on each part. Then run the ratchet command. Add a case to the spec-trace-047 test (src/tooling/spec/specs.test.mjs:238) where a line moves from the requirement text to the scenario name. The case must assert that the scenario hash changes.

Notes:
- **Round-22 F1 is corrected.** `readDeltaNames` (specs.mjs:307–346) uses the same regular expressions as OpenSpec 1.3.1 `splitTopLevelSections`, `parseRemovedNames` and `parseRenamedPairs`. I compared `readDeltaNames` with `parseDeltaSpec` of the pinned package on 200,000 random delta texts (`review-spec-r23/repo/fuzz.mjs`). In no case did OpenSpec read a removed name or a renamed pair that the gates did not read. In 10,864 cases the gates read more names, for example when a section title occurs two times. That can stop a correct change, but it does not let an incorrect change pass. The name check (`has`) compares trimmed names in the same way that `buildUpdatedSpec` does. The spec-trace-052 and spec-trace-053 tests assert the codes, the files and the messages for the heading form, the no-space form, the list form, the FROM/TO form and a TO line with no FROM line.
- **Round-22 F2 is corrected.** `scenarioHash` uses `trimEnd`, and the requirement text keeps the raw lines (specs.mjs:39, :82, :180). The spec-trace-047 test asserts that the hash changes when the indentation changes in a fence line, a nested list line and a scenario body line. It also asserts that spaces at the end of a line do not change the hash.
- **Tasks and IDs:** Test task 1.23 (spec-trace-053) comes before code task 1.24. `retired-ids.json` does not exist, so no retired ID is used. `ids.json` and `links.json` contain spec-trace-053 and the new name of the spec-trace-051 test.
- **Tests:** specs.test.mjs and specLint.test.mjs pass on a copy with Node 24.21.0 (36 tests).
- **Ledger:** `gaps.json` and `history.jsonl` did not change since round 22.
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected. `MaxListenersExceededWarning` is a Node warning, not a gate warning.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r23/`.
