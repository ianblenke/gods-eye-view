Verdict: FAIL
- [ ] S1 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:79 "If it changes in the same change that first loads it" One word, one meaning. "change" is the name of an OpenSpec change and also a modification. Use the noun "change" only for an OpenSpec change. Write: "If the author changes the file in the same OpenSpec change that first loads it". Also fix: specs/spec-trace/spec.md:161 "A change to the text of a scenario MUST include a change to a test that names the ID" → "When the text of a scenario changes, a test that names the ID MUST also change"; specs/spec-trace/spec.md:173 "without a test change" → "when no test changed"; design.md:93 "Thus a code change cannot use a range" → "Thus an author who changes code cannot use a range"; proposal.md:38 "Process change:" → "New process:"; specs/ci-gates/spec.md:25 "Check a diff without code changes" → "Check a diff that changes no code"; gov-tests.txt:5 and :37 "checks a diff without code changes without a change name" → "checks a diff that changes no code, without a change name". Fix the copies in openspec/specs/*/spec.md too (line + 4).
- [ ] S2 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/coverage-gate/spec.md:53 "the gate waits until each run stops" One word, one meaning. Everywhere else, "stop" means that a gate fails the build. Here it means that a run is complete. Write: "the gate waits until each run is complete". Also gov-tests.txt:112 "waits until each run stops" → "waits until each run is complete". Fix the openspec/specs copy too.
- [ ] S3 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:89 "because the load changes the result" One word, one meaning. Everywhere else, "load" is a verb that means import a file. Here it is a noun for processor work. Write: "because the other test runs at the same time change the result".
- [ ] S4 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:9 "stops fake coverage" One word, one meaning. The specs use "untrue coverage" for the same thing. "fake" is not an approved STE word. Write: "stops untrue coverage". Also design.md:20 "fake coverage" → "untrue coverage". I am not sure that "untrue" is in the STE dictionary. I accept it as a technical name because it is the ledger field `untrue`.
- [ ] S5 AGENTS.md:34 "Somebody edits the ledger files to hide a gap." One word, one meaning. The other files use "a person" for the same thing. Write: "A person edits the ledger files to hide a gap."
- [ ] S6 gov-tests.txt:71 "writes the ledger with sorted keys and appends history lines" One word, one meaning. The specs use "adds a history line" for the same action. Write: "writes the ledger with sorted keys and adds history lines".
- [ ] S7 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/spec-trace/spec.md:103 "records the scenario as not verified" Approved words and one word, one meaning. I am not sure that "verify" is an approved STE word. The same spec also uses "untested" (lines 113 and 118) for the same state. Write: "records the scenario as not tested". Also: :107 the same fix; :92 "Scenario verification" → "Scenario tests"; openspec/specs/spec-trace/spec.md:4 "the spec scenarios that it verifies" → "the spec scenarios that it tests"; design.md:20 "tags on tests that verify nothing" → "tags on tests that test nothing"; design.md:63 "does not verify a scenario" → "does not test a scenario"; AGENTS.md:51 "the old tests that verify each scenario" → "the old tests of each scenario". Fix the openspec/specs copies too.
- [ ] S8 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/change-review/spec.md:10 "the review gate accepts the change" Approved words. I am not sure that "accept" is an approved STE verb. Most scenarios already say "does not stop the build". Write: "the review gate does not stop the build". Use the same fix in: change-review:7, :78, :80, :82, :84; spec-lint:40; gap-ledger:44, :54, :64, :78, :92, :118; coverage-gate:106; spec-trace:26, :28; design.md:67, :93; and in gov-tests.txt:54, :56, :58, :61, :79, :84, :93, :109, :110, :121, :129, :151 and :154. For example, write "Do not stop for gaps that are equal to the ledger" and "does not stop for gaps that are equal to the ledger". Fix the openspec/specs copies too.
- [ ] S9 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/gap-ledger/spec.md:186 "Refuse to ratchet when a gap is larger" Approved words and verbs. "refuse" is not an approved STE word. "ratchet" is the name of a command, and here the text uses it as a verb. Write: "Stop the ratchet command when a gap is larger". Also: :190 → "Stop the ratchet command without a change name"; :194 → "Stop the ratchet command for a change that is not active"; spec-trace:173 "Refuse to register changed text without a test change" → "Stop the ratchet command for changed text when no test changed"; gov-tests.txt:74–76 → "stops the ratchet command when a gap is larger / without a change name / for a change that is not active"; gov-tests.txt:88 → "stops the ratchet command for changed text without a changed test that names the ID". Fix the openspec/specs copies too.
- [ ] S10 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/spec-trace/spec.md:178 "Stop for an ID that disappeared" Approved words. I am not sure that "disappear" is in the STE dictionary with this meaning. Write: "Stop for an ID without a scenario". Also gov-tests.txt:89 "stops for an ID that disappeared and is not retired" → "stops for an ID without a scenario that is not retired". Fix the openspec/specs copy too.
- [ ] S11 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/gap-ledger/spec.md:8 "`openspec/trace/gaps.json` does not exist" Approved words and one word, one meaning. I am not sure that "exist" is an approved STE verb. change-review uses "is not there" for the same state. Write: "`openspec/trace/gaps.json` is not there". Also: :13 "exists" → "is there"; :22 "does not exist in the base commit" → "is not in the base commit"; :147 → "is not there"; proposal.md:36 "the gaps that exist in the base commit" → "the gaps in the base commit"; AGENTS.md:47 "for code that exists" → "for old code"; gov-tests.txt:48 "does not replace a ledger that exists" → "does not replace the current ledger". Fix the openspec/specs copy too.
- [ ] S12 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/ste-lint/spec.md:19 "Ignore code and links" Approved words. I am not sure that "ignore" is an approved STE verb. "Git ignore" and "coverage ignore comment" are technical names, but these uses are not. Write: "Do not check code and links". Also gov-tests.txt:137 "ignores fenced code, inline code, URLs and scenario IDs" → "does not check fenced code, inline code, URLs or scenario IDs"; gov-tests.txt:124 "ignores scenario headings in fenced code" → "does not read scenario headings in fenced code". Fix the openspec/specs copy too.
- [ ] S13 openspec/specs/gap-ledger/spec.md:4 "closes without a record" Approved words and one word, one meaning. The same sentence uses "Record" as a verb. I am not sure that "record" is an approved STE noun. Write: "closes and the ledger does not show it". Also: specs/spec-trace/spec.md:10 "the record has the capability" → "the gate also records the capability"; specs/change-review/spec.md:12 and gov-tests.txt:94 "the review record is not there" → "the `review.md` file is not there"; gov-tests.txt:108 "checks the review record of each archived change" → "checks the `review.md` file of each archived change"; gov-tests.txt:186 "writes one record for each test" → "writes one entry for each test"; design.md:154 "make the coverage record" → "make the coverage data"; AGENTS.md:28 "for the review record" → "for `review.md`"; design.md:129 "Review record and agents" → "Review file and agents". Fix the openspec/specs copies too.
- [ ] S14 openspec/specs/change-review/spec.md:4 "Make sure that two adversarial review agents approve each change before merge, and keep the record of the review." Approved words and verbs. I am not sure that "approve" is an approved STE verb, because the dictionary has "approved" as an adjective. The sentence also uses "merge" as a noun and "record" as a noun. Write: "Make sure that each change gets the verdict PASS from the two adversarial review agents before you merge it. Keep the output of the review."
- [ ] S15 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/ci-gates/spec.md:19 "archive the change before merge" Verbs. The text uses the verb "merge" as a noun. Write: "archive the change before you merge it". Also, with the same rule: specs/change-review/spec.md:4 "in its folder before merge" → "in its folder before you merge the change"; design.md:20 "ledger edits" → "changes to the ledger files"; proposal.md:10 "without a ledger update" → "and the ledger does not change"; design.md:7 "| Measure | Result |" → "| Item | Result |". Fix the openspec/specs copies too.
- [ ] S16 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/ste-lint/spec.md:32 "Lint only Markdown with the lint command" Verbs. "lint" is the name of a tool, and here the text uses it as a verb. Write: "Check only Markdown with the lint command". Also: gov-tests.txt:35 "lints only Markdown" → "checks only Markdown"; gov-tests.txt:24 and :27 "ratchets a change" → "runs the ratchet command for a change"; design.md:156 "Compare, make and ratchet the gap ledger" → "Compare and make the gap ledger, and run the ratchet command". Fix the openspec/specs copy too.
- [ ] S17 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:59 "Thus it knows if a test is a leaf test" Approved words. In STE, "if" is a conjunction for a condition. Here it means "whether". Write: "Thus it can find each leaf test". Also specs/spec-trace/spec.md:60 "the gap ledger rules decide if the build stops" → "the gap ledger rules control the result of the build". Fix the openspec/specs copy too.
- [ ] S18 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/gap-ledger/spec.md:54 "Accept a file that was not loaded and is now loaded" Voice. This is a description, and the active voice is possible. Write: "Do not stop for a file that no test loaded and a test now loads". Also: :59 "a loaded file that is no longer loaded" → "a loaded file that no test loads now"; design.md:79 "A file that was not loaded has no branch count." → "A file that no test loaded has no branch count."; gov-tests.txt:56 → "does not stop for a file that no test loaded and a test now loads with the same content"; gov-tests.txt:57 → "stops for a loaded file that no test loads now"; gov-tests.txt:148 "an -ing word that is not allowed" → "an -ing word that the allowed list does not contain". Fix the openspec/specs copy too.
- [ ] S19 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:40 "Known limits and follow-up changes" Approved words. "follow-up" is not an approved STE word. Write: "Known limits and later changes". Also :42 "Each one has a follow-up change." → "Each one has a later change."; :44 "until the follow-up change measures" → "until the later change measures".
- [ ] S20 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:46 "A backfill change must make the tests deterministic." Approved words. "deterministic" is not an approved STE word. Write: "A backfill change must change the tests so that each run gives the same counts." Also design.md:93 "until a backfill change makes its tests deterministic" → "until a backfill change makes its tests give the same counts on each run".
- [ ] S21 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:14 "This version includes the fixes." Approved words. In STE, "fix" is a verb that means "attach". It is not a noun for a correction. Write: "This version corrects these problems."
- [ ] S22 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:47 "The tree hash stops a review that is out of date, but it does not prove that a review agent wrote the output." Approved words. "out of date" is an idiom, and "prove" is not an approved STE word. Write: "The tree hash stops an old review, but it does not show that a review agent wrote the output."
- [ ] S23 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:48 "a test can remove it on purpose" Approved words. "on purpose" is an idiom. Write: "a test can remove it".
- [ ] S24 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:24 "The automated checks" Approved words. "automated" is not an approved STE word. The approved adjective is "automatic". Write: "The automatic checks".
- [ ] S25 openspec/changes/archive/2026-09-13-establish-spec-governance/proposal.md:21 "the ban on coverage exclusions" Approved words. "ban", "exclusion" and "exclude" are not approved STE words. Write: "the rule against coverage ignore comments and coverage filter options". Also specs/coverage-gate/spec.md:77 "No coverage exclusions" → "No coverage ignore comments or filter options"; specs/coverage-gate/spec.md:11 "Exclude test files" → "Do not include test files"; gov-tests.txt:43 "excludes test files from the inventory" → "does not include test files in the inventory". Fix the openspec/specs copy too.
- [ ] S26 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:22 "Keep the gates free of new packages." Approved words. In STE, "free" does not mean "without". Write: "Do not add new packages to the gates."
- [ ] S27 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:37 "One allocation test takes 8 minutes" Approved words. In STE, "take" does not mean "last for a time". Write: "One allocation test runs for 8 minutes".
- [ ] S28 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:39 "starts each run with a clean environment" Approved words. In STE, "clean" means "without dirt". Write: "starts each run with a new environment".
- [ ] S29 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:45 "returns at once" Approved words. "at once" is an idiom. Write: "returns immediately".
- [ ] S30 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:49 "A difference is a violation." Approved words. "violation" is not an approved STE word, and the other files use "error" for the same thing. Write: "A difference is an error."
- [ ] S31 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:55 "found one such case" Approved words. In STE, "case" is a noun for a container. Write: "found one test file that does this".
- [ ] S32 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:57 "Test names carry the scenario IDs" Approved words. In STE, "carry" means "hold and move". Write: "Test names contain the scenario IDs".
- [ ] S33 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:67 "The ID goes at the end of the scenario heading" Approved words. In STE, "go" means "move". Write: "The ID is at the end of the scenario heading".
- [ ] S34 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:135 "The review happens after the archive command" Approved words and voice. I am not sure that "happen" is an approved STE verb. The active voice with a clear subject is possible. Write: "The agents do the review after the archive command".
- [ ] S35 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:150 "source checks and hook blocks" Approved words. In STE, "block" is not a noun for a check that stops something, and the guard records module hooks. Write: "source checks and module hook checks".
- [ ] S36 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:179 "Remove the old test runner discovery" Approved words. "discovery" is not an approved STE word. Write: "Remove the part of the old test runner that finds the test files".
- [ ] S37 openspec/changes/archive/2026-09-13-establish-spec-governance/specs/change-review/spec.md:63 "check the fixed lines of each archived change" Approved words. In STE, "fixed" means "attached". Write: "check the necessary lines of each archived change". Fix the openspec/specs copy too.
- [ ] S38 AGENTS.md:3 "These rules apply to each person" Approved words. In STE, "apply" means "put a material on a surface". Write: "These rules are for each person".
- [ ] S39 AGENTS.md:50 "from the current behavior" Approved words. I am not sure that "behavior" is an approved STE noun. Write: "from what the code does now".
- [ ] S40 .claude/agents/spec-adversary.md:9 "the scripted gates did not stop" Approved words. "scripted" is not an approved STE word. Write: "the gate scripts did not stop".
- [ ] S41 .claude/agents/spec-adversary.md:29 "The test must set up the WHEN condition." Verbs. "set up" is a phrasal verb. Write: "The test must make the WHEN condition."
- [ ] S42 gov-tests.txt:104 "computes the tree hash from the change files and the diff files" Approved words. I am not sure that "compute" is an approved STE word or technical verb. The approved verb is "calculate". Write: "calculates the tree hash from the change files and the diff files".
- [ ] S43 gov-tests.txt:159 "keeps the promisified result of the real execFile" Approved words. "promisified" is not an STE word. I am not sure that it is a technical name. Write: "keeps the result of util.promisify for the real execFile".
- [ ] S44 openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:53 "At exit, the guard writes" Articles. An article is possible here. Write: "At the end of the process, the guard writes". Also: design.md:147 "from specs" → "from the specs"; design.md:155 "Merge base, base files and diff" → "The merge base, the base files and the diff"; design.md:158 "`review.md`, agents and the tree hash" → "`review.md`, the agent files and the tree hash"; proposal.md:34 "(pinned Node version and Git)" → "(the pinned Node version and Git)"; specs/gap-ledger/spec.md:4 "the date it opened" → "the date that it opened"; .claude/commands/opsx/review.md:15 "does not change scenario text, tests or code" → "does not change the scenario text, the tests or the code"; gov-tests.txt:39 "from kept samples" → "from the kept samples"; gov-tests.txt:41 "the merge base, base files and the diff" → "the merge base, the base files and the diff"; gov-tests.txt:122 "reads the ID, requirement, scenario, origin, file, line and hash" → "reads the ID, the requirement, the scenario, the origin, the file, the line and the hash"; gov-tests.txt:125 "reads the first line, origin and scenario format" → "reads the first line, the origin and the scenario format".
- [ ] S45 openspec/changes/archive/2026-09-13-establish-spec-governance/tasks.md:200 "Run `openspec archive establish-spec-governance` and replace each TBD purpose line." Instructions. Each task must give one instruction. Write two tasks: "Run `openspec archive establish-spec-governance`." and "Replace each TBD purpose line." Also: tasks.md:202 "Close each finding and save the agent output in the `review` folder." → two tasks; .claude/commands/opsx/review.md:8 "run `openspec list --json` and ask the user to select a change" → "run `openspec list --json`. Ask the user to select a change."; review.md:14 "stop and correct them first" → "stop. Correct them first."; review.md:16 "again and keep the output. Run `git diff --stat origin/main` and keep the output." → "again. Keep the output. Run `git diff --stat origin/main`. Keep the output."; review.md:17 "and keep the output" → ". Keep the output."; review.md:23 "and keep the hash" → ". Keep the hash."
