Verdict: FAIL
- [ ] S196 major openspec/specs/gap-ledger/spec.md:138 (delta :134) "a band file has a count above its entry, and each count is in the band of the entry count" Articles and nouns / one word, one meaning. The line has two meanings. (a) "a count" can mean only the not-covered counts, or also the covered and total counts. (b) "in the band" can mean a count within the band on both sides of the entry count, or a count that is only not above the entry count plus the band. The code (`largerInBand`) uses only the not-covered line, branch and function counts, and it checks only the upper side. I ran `ratchetLedger` with `sameAsBase` true on an entry of 400/50/1 and a current gap of 300/51/1. The command kept the entry with 400 lines and wrote no history line, but 300 is not "in the band" of 400 (the band is 8). This also does not agree with proposal.md:65 "so the ledger keeps the old count until the next ratchet run". The ratchet run also keeps the old count here. The name of the gap-ledger-068 test and design.md:130 "for a larger count in the band" have the same problem. Write: "AND a not-covered line, branch or function count of a band file is above its entry, and no not-covered count is above the entry count plus the band of the entry count", and in proposal.md:65 write "... the ledger can keep the old count also after a ratchet run".
- [ ] S197 major openspec/specs/gap-ledger/spec.md:343, :355, :327 (delta :339, :351, :324) gap-ledger-013 "a current gap is larger than its entry / THEN the command stops and does not change the ledger", gap-ledger-028 "for an unchanged file with more not-covered branches and a covered branch count that is not smaller / THEN the command writes the larger branch count", gap-ledger-010 "has fewer not-covered lines than its entry / THEN the command writes the smaller count". These do not agree with gap-ledger-068 or with the code. A band file is a type of unchanged file, and these three scenarios have no band exception. S186 added the exception to 004, 054 and 008, but not to these ratchet scenarios. I checked a band file with the entry 10/50/1 (totals 1000) and the current gap 10/53/1 (totals 1003). 028 says that the command writes 53, but the code keeps 50 and writes no history line. For 013, a count above the entry but in the band does not stop the command. For 010, fewer lines together with one more branch keeps the old line count (see the "mixed" case in the gap-ledger-068 test). Add to each scenario: "AND the file is not a band file with each larger count in the band (see Count band for band files)".
- [ ] S198 major openspec/specs/gap-ledger/spec.md:44 and :63 (delta :40, :59) "or its count is above the band" / "or the covered count is below the band" Articles and nouns: two meanings. The requirement defines the band as an amount ("The band of a count is 5, or 2% of that count"). Thus "above the band" can mean "above 5". With an entry of 5 and a current count of 8, that reading stops the build (8 > 5), but the code does not stop (8 ≤ 5 + 5). gap-ledger-066 uses the correct words. Write: "AND the file is not a band file, or its not-covered line count is above the entry count plus the band of the entry count" and "AND the file is not a band file, or the covered count is below the covered count in the entry minus the band of that count".
- [ ] S199 major openspec/changes/archive/2026-09-15-establish-spec-governance/design.md:130 "The not-covered lines use the band of the entry count. The covered branches and functions use the band of the covered count." Two meanings. After "entry count" in the sentence before, "the covered count" can mean the current covered count. design.md:118 defines the covered count without "entry" or "current". The code uses `band(before)`, the covered count in the entry. The two readings give different results. For example, with an entry covered count of 251 and a current count of 245, the entry reading allows 245 (band 6), and the current reading stops (band 5). The spec (065) and the JSDoc of `compareLedger` say "in the entry". Write: "The covered branches and functions use the band of the covered count in the entry."
- [ ] S200 minor openspec/specs/coverage-gate/spec.md:115 (delta :111) "tells you to make the file empty or to run `make gates`" and scripts/spec/gates.mjs:330 "Make the file empty, or run the gates with make, which does not copy ignored files." The statement is correct, but for an untracked `.env.local` that Git does not ignore, the advice does not help. `make gates` copies that file, and the gate stops again. The gate-047 test uses `.env.local` as its example. Write: "Make the file empty. When Git ignores the file, you can also run the gates with make."
- [ ] S201 minor openspec/specs/gap-ledger/spec.md:116 (delta :112) "The coverage gate MUST allow a small count difference and each smaller gap for a band file." One word, one meaning. In this spec the ledger comparison is "the gate" or "the gates", and "coverage gate" is the name of the coverage measurement. The requirement also does not name the ratchet command, but gap-ledger-068 is in this requirement. Write: "The gates and the ratchet command MUST allow ...".
- [ ] S202 minor openspec/specs/gap-ledger/spec.md:4 and :34 "A band file can differ from its entry by a small band." / "A band file is an exception" Articles and nouns. The term "band file" comes before its definition. A band file is not an exception for a count outside the band, and it can also have each smaller gap. Write: "A file with the base content and the entry hash can differ from its entry by a small band, and it can have a smaller gap." and "A band file with counts in its band, or with a smaller gap, is an exception, as ...".
- [ ] S203 minor openspec/changes/archive/2026-09-15-establish-spec-governance/design.md:132 "The gate uses no band for a changed file, a new gap or a file that no test loads." The list does not include untrue coverage, but gap-ledger-067 does. Write: "..., a file that no test loads or a file with untrue coverage."
- [ ] S204 minor openspec/changes/archive/2026-09-15-establish-spec-governance/proposal.md:57 "A larger difference stops the gate." A difference outside the band that makes the gap smaller does not stop the gate (066). Write: "A larger gap outside the band stops the gate."
- [ ] S205 minor openspec/specs/coverage-gate/spec.md:249 (delta :245) "and the skip reason is false on a Node version with the function" Approved words / articles. A reason is text, so it cannot be "false". The THEN part also gives a condition that is the opposite of the WHEN condition. Write: "AND the other tests of the gates have no skip reason" and put the second part in its own line: "AND on a Node version with the function, the skip option of each test is false".
- [ ] S206 minor Makefile:3-4 "a copy of the files that Git tracks or does not ignore, as in CI" / "Ignored local files, such as .env, change the coverage" and the JSDoc of `localEnvFiles` "Vite and the key setup read these files". Here "as in CI" is not fully correct, because the CI checkout has only tracked files. "change" and "read these files" say more than is true, for example for an untracked `.env.example`. Write: "a copy without the files that Git ignores, near to CI" / "can change the coverage" / "Vite and the key setup can read these files".
- [ ] S207 minor .claude/agents/spec-adversary.md:34 "The gate allows a small loss of coverage in a band file. Thus also check each changed test ..." Articles and nouns. The agent file does not define "band file", and "each changed test" is not the same as proposal.md:65 "each removed or changed test". Write: "... in a band file (see the requirement "Count band for band files"). Thus also check each removed or changed test and each changed code file that a band file uses."
- [ ] S208 minor openspec/specs/ci-gates/spec.md:61 (delta) "each copy step stops the target when it fails" The filter step `{ grep -vxFf ... || true; }` does not stop the target when grep fails (status 2). Write: "each copy step, except the filter for deleted files, stops the target when it fails".

Notes:
- **Round 29 findings:** all are corrected.
  - **S183:** only 31 tests use `GUARDED_RUN` now, and the gate-046 test asserts that list.
  - **S184, S185:** the text uses the covered count in the entry, which agrees with `band(before)`.
  - **S186:** the exception is in 004, 054, 008, the Ratchet rule, the Purpose and the design bullet. The ratchet scenarios still need it (S197).
  - **S187:** the new term is "band file".
  - **S188:** the message is now correct, but the advice does not help in one case (S200).
  - **S189 to S195:** corrected.
- **Facts checked, no finding:**
  - 290 of 3,534 tests and 120 files agree with the gate output.
  - The "31 tests" agree with 3 + 28 `GUARDED_RUN` tests.
  - CI runs `npm test` on 24.14.0.
  - The ci-gates-006 AND line agrees with `ci.yml`.
  - `.gitignore` ignores `pinokio/ENVIRONMENT`.
  - Tasks 3.37–3.41, 4.64–4.68 and 7.24–7.26 are in the imperative.
  - The delta specs and the main specs have the same text.
  - The gate output has "STE: 0 errors, 0 warnings", so there were no STE-PASSIVE or STE-ING warnings to examine.
- **For the spec adversary, not an STE problem:** a covered-count loss in the band with the same not-covered counts is not "larger" for `largerInBand`. The ratchet command then records the lower covered count and a `totals changed` history line. The next change can lose another band. This agrees with `unchanged-band` for each change, but the loss can add up over many changes.
- **Safety:** I did not read `.env`, did not change a repository file, and ran only read-only git commands. My only scratch file is `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-ste-r30/band068.mjs` (it reproduces S196 and S197), plus `prose.diff` in the same folder.
