Verdict: FAIL
- [ ] F1 major scripts/spec/lib/specs.mjs:91 The scenario hash does not include requirement text that is in a code fence. The hash has fence lines only when a scenario is open (specs.mjs:85, :91). Fence lines before the first scenario do not go into `requirementText` (specs.mjs:174). Requirement spec-trace-047 says that the hash MUST change when the text of a requirement changes. I ran the real `parseSpecFile` on a copy (`review-spec-r21/exp1`). The requirement `Play` had "The radio MUST play only the stream formats in this list:" and then a fence with `mp3` and `aac`. I added `flac` and `exe-download` in the fence. The hash of `radio-001` did not change, and there were no errors. When I changed the plain text line, the hash changed. Thus an author can change a requirement in `openspec/specs` and change no test. `TRACE-ID-CHANGED` and `TRACE-ID-BASE-CHANGED` do not stop the build. The known limit `scenario-text-change` is only about a changed test. Correction: in `parseSpecFile`, add each fence line of a requirement that has no open scenario to `requirementText`. Add a case with changed fence text to the spec-trace-047 test in specs.test.mjs:236. The case must assert that the hash changes.
- [ ] F2 major scripts/spec/lib/specs.mjs:242 `checkArchivedChange` compares only scenarios. It does not compare requirements. The requirement "Archived change specs" (openspec/specs/spec-trace/spec.md:249) says that the main specs MUST contain the delta specs of the change. The gates also do not lint archived delta specs: `lintSpecs` and the OpenSpec count check read only main specs and active changes. I ran the real `loadSpecs`, `checkArchivedChange`, `lintSpecs` and `checkOpenSpec` with OpenSpec 1.3.1 in the Docker image (`review-spec-r21/exp2`). The archived change had two items. (a) Under `## ADDED Requirements` it had `### Requirement: Stream check` with "The radio MUST stop a stream that has no audio for ten seconds." and no scenario. This requirement was not in `openspec/specs`. (b) Under `## REMOVED Requirements` it had `### Requirement: Play`, but the main spec still had `Play`. All four checks gave no errors. When I moved the same folder to an active change, `openspec validate --strict` gave "ADDED "Stream check" must include at least one scenario". Thus an archived change can show a requirement that is not in the main specs and that has no test. No known limit names this. Correction: in `checkArchivedChange`, stop the build for each added or modified requirement that the main spec of its capability does not have with the same name and text. Also stop for each removed requirement name that the main spec still has. Run `lintSpecs` on the requirements of the archived delta specs, or run `openspec validate` on them. Add spec-trace scenarios and specs.test.mjs tests for (a) and (b).
- [ ] F3 minor scripts/spec/lib/spec-lint.mjs:44 The tasks check (spec-lint-006) reads only active changes (`specs.changeIds` comes from `listActiveChanges`). CI stops the build for an active change (`CI-ACTIVE-CHANGE`) and checks only the archived change. Thus CI never checks that `tasks.md` names each scenario ID. An author who archives the change without step 4 of AGENTS.md can merge a change that has no test tasks. No known limit names this. Correction: when the gates check an archived `--change`, run the tasks check on `<archive folder>/tasks.md` for the scenario IDs of its delta specs. Change the requirement at openspec/specs/spec-lint/spec.md:81 and add a scenario and a test.

Notes:
- **Round-20 F1, corrected:** `checkOpenSpec` runs `validate --specs --strict` and compares the scenario counts of each requirement from `show --json` (openspec.mjs:68–112). The gates now stop for each heading that is not a title, a section, a requirement or a scenario (specs.mjs:161–163). I read the OpenSpec parser (`markdown-parser.js`). I found no heading that OpenSpec reads as a requirement or a scenario and that the gates skip without an error. OpenSpec finds spec files only at `openspec/specs/<name>/spec.md`, the same as the gates.
- **Round-20 F2, corrected:** `parseSpecFile` changes each `\r\n` and each `\r` to `\n` (specs.mjs:79). The spec-lint-017 test checks all three line ends.
- **Other corrections:** I checked spec-trace-048/049/050, coverage-gate-044, change-review-004/022/023 and ci-gates-010 against their tests. The tests make the WHEN condition and assert the THEN lines. Each test task comes before its code task in tasks.md. The history line for `server/providers/aircraft/opensky.js` (branches 35 to 51) comes from the new worst-record rule, because `liveProviders.test.mjs:130` imports the file with a query string. That rule is stricter, so I do not report it.
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected. `MaxListenersExceededWarning` is a Node warning, not a gate warning.
- **Not reported, weak test:** The last assertion of the spec-lint-020 test (openspec.test.mjs:66–67) accepts two results. It does not check the path that the default `resolve` gives. The real gate run checks the CLI version, so this does not open a gap.
- **Not reported, false stop:** `AGENT_VERDICT` (review.mjs:12) starts with `^\s*`, which can match a line feed. An agent output with an empty line before "Verdict: PASS" gives `["\nVerdict: PASS"]` (`exp3`), and the gate stops with `REVIEW-AGENT-NOT-PASS`. This stops a correct output. It does not let a bad output pass. Use `^[ \t]*` to correct it.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r21/`.
