Verdict: FAIL
- [ ] S218 major openspec/changes/archive/2026-09-15-establish-spec-governance/proposal.md:66 `range-band-ratchet` "For a band file with a range, the ratchet command keeps the high count and writes the current total counts." This sentence has no condition, so it disagrees with the code and with `range-totals` on line 67, which says "while each not-covered count is in its range ... The ratchet command then keeps the old total counts". I ran `ratchetLedger` with `sameAsBase` true on a range entry of lines 44 (low 40), branches 35 (low 35) and functions 3, with totals 64/19. There are three cases:
  (a) The current lines are 42, which is in the range, and the total branches are 70. The command keeps the whole entry, with totals 64, and writes no history line. This does not agree with "writes the current total counts".
  (b) The current lines are 38, which is below the low count. The command writes a high count of 42 and a low count of 38, with the reason `smaller`. This does not agree with "keeps the high count".
  (c) The current lines are 47, which is above the high count but in the band. The command keeps 44 and writes totals 70. Only this case agrees with the sentence.
  Write: "For a band file with a range and a not-covered count above its high count, the ratchet command keeps that high count and writes the current total counts."
  Can wait: yes. Only 2 entries have a range, and the text is known-limit prose. The spec and the code are correct.
- [ ] S219 major openspec/specs/gap-ledger/spec.md:4 Purpose "A file with the content of the base commit and of its entry is a band file." One word, one meaning. The new definition does not agree with the definition in the requirement "Count band for band files" (line 116: "A band file is a loaded code file with true coverage. ..."). It also does not agree with gap-ledger-067, which uses no band for a file that no test loads or that has untrue coverage. It does not agree with `isBandFile`, which needs `loaded` and `!untrue`. With the Purpose definition, an unchanged file that no test loads is a band file. With the requirement, it is not. Write: "A loaded code file with true coverage, the content of the base commit and the content hash of its entry is a band file."
  Can wait: yes. The requirement, gap-ledger-067, design.md:128 and the code all agree. Only the Purpose summary is incomplete.
- [ ] S220 minor openspec/specs/gap-ledger/spec.md:63 (delta :59) gap-ledger-054 "its covered count is outside the limit in `gap-ledger-066`". Articles and nouns. The line before names two counts, "the covered branch count or the covered function count", but "its covered count" is singular. The result is the same with each reading. Write: "or that covered count is outside the limit in `gap-ledger-066`".
- [ ] S221 minor openspec/specs/coverage-gate/spec.md:250 (delta :246) "each test that needs the guard". One word, one meaning. The THEN line says "each test that needs the guard to count assertions", which is two phrases for the same group of tests. Write: "the skip option of each test that needs the guard to count assertions is false".
- [ ] S222 minor openspec/changes/archive/2026-09-15-establish-spec-governance/proposal.md:66 "The change `harden-gate-ledger` corrects this." Verbs. The change does not exist yet: `openspec/changes/` has only `archive`. The simple present can make a reader look for a change that is not there. Write: "The later change `harden-gate-ledger` will correct this."

Notes:
- **Round 31 findings:**
  - **S209:** corrected. The gap-ledger-013 AND line and the gap-ledger-068 WHEN AND line now point to the limits in 066. Those limits have one direction only: lines above the entry plus the band, covered counts below the entry minus the band. So the mixed case from round 31 (entry 10/50/1 with 1000 branches, current 13/10/1) has one reading: 013 does not apply. I ran it again, and the command does not stop. It writes branches 10 and keeps lines 10.
  - **S210:** corrected. The "entry or current count" problem is gone because 054 now points to 066. S220 is a minor point in the new wording.
  - **S211:** corrected. The title and the test name agree with `bandFileCounts` and with the assertions `noisy`, `mixed`, `backfill` and `drift`.
  - **S212:** corrected. design.md:130 now says "with no range".
  - **S213:** corrected. The name is the same in gates.test.mjs:408 and in the coverage-gate-046 list at testGuard.test.mjs:427.
  - **S214:** corrected. S221 is a minor point in the new wording.
  - **S215:** corrected. The new sentence agrees with the code. The new `range-band-ratchet` limit has S218.
  - **S216:** corrected. gates.mjs:330 and the test at gates.test.mjs both say "can change", and the Makefile comment says "which is almost the same as CI".
  - **S217:** corrected in its order: the definition now comes before the term is used. The definition itself is incomplete (S219).
- **Delta and main specs:** apart from the Purpose, which only the main spec has, they have the same text for 013, 054, 066, 068 and coverage-gate-046.
- **Outside this round's scope (not reported):** gap-ledger-013 still says that the command stops for each larger gap in a file that is not a band file. For a non-band unchanged file, gap-ledger-028 and the code do not stop. This conflict was there before round 31.
- **Safety:** I did not read `.env`, did not change a repository file, and ran only read-only git commands. My only scratch file is `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-ste-r32/rangeband.mjs`. It reproduces S218 and the S209 check.
