Verdict: FAIL
- [ ] F1 major openspec/trace/gaps.json The ranges of `src/data/manager.js` (lines 134 to 141, 7 wide) and `src/layers/flights/enrichment.js` (lines 83 to 91, 8 wide) will stop the gate on every run after this pull request merges. `compareWithBase` checks the width of every entry that has a range, not only new or wider ranges. The check is skipped now only because the base has no ledger. After the merge, each later pull request has this ledger as its base, and the gate gives `LEDGER-UNSTABLE-TOO-WIDE`. The ledger also does not agree with scenario `gap-ledger-043`. To correct it, make each range at most 5 wide: manager.js `low.lines` 136 and enrichment.js `low.lines` 86. I tested this change: `compareWithBase` then gives no errors, and `compareLedger` gives no errors for the last local run and for both CI runs.
- [ ] F2 minor proposal.md The limit `range-band-ratchet` still says "Only two ledger entries have a range." The ledger now has 13 entries with a range.
- [ ] F3 minor proposal.md The limit `ci-samples` tells a later author to "add the samples of that run". The conversion script is only in the author's scratchpad, and the CI job keeps the gate results for only 7 days. Name this in the limit, or add the tool.
- [ ] F4 minor proposal.md Some new ranges rest on runs that are not the checked tree: five local runs from before round 29, and one CI run of the debug branch with other test files. Say this in the text, or run the stability command again with only the samples of runs on this tree.

Notes:
- **Conversion script:** `ci-samples.mjs` uses the gates' own functions, checks each hash against the CI inventory and skips files with other content. All 1,157 CI sample lines have the same keys as the local samples. With the HEAD ledger, the CI data gives the same `LEDGER-LARGER-GAP … 141 … allows 139` error that CI gave.
- **Ranges and history:** each low and high count is equal to the lowest and highest sample of that metric. The 11 new `unstable` history lines agree with the ledger and name this change.
- **CI on the next push:** `compareLedger` with `sameAsBase` gives no errors for both CI runs and for the last local run.
