Verdict: FAIL
- [ ] F1 major scripts/spec/lib/specs.mjs:88 In a main spec, the spec lint can still pass a requirement that has no scenario. The new rule SPEC-LINT-NO-SCENARIO (spec-lint.mjs:28) does not see such a requirement in two cases. (a) `parseSpecFile` sets `removed` or `renamed` from the section name, and it does this in main specs too. A requirement under `## REMOVED Requirements` in `openspec/specs/<capability>/spec.md` is skipped at spec-lint.mjs:20. A requirement under `## RENAMED Requirements` never goes into `requirements`. (b) `REQUIREMENT_HEADING` (specs.mjs:7) matches only `Requirement:` with an upper-case R. The line `### requirement: Stream check` is not read as a requirement, and its text goes into the requirement above it. I ran the real `loadSpecs` and `lintSpecs` on a main spec that has a correct requirement with a scenario, followed by `### Requirement: Stream check` with "The radio MUST stop a stream that has no audio for ten seconds." and no scenario (`review-spec-r18/exp1/`, `review-spec-r18/exp2/`). With that requirement under `## RENAMED Requirements`, under `## REMOVED Requirements`, or under `### requirement:` in lower case, the result was no spec errors and no lint errors. For the same files, `openspec validate --strict` gave errors: "Main spec contains delta header" and "appears outside the main ## Requirements section" for (a), and "Requirement must have at least one scenario" for (b). An author can also move a tested requirement under `## REMOVED Requirements` in the main spec, retire its IDs and remove its tests, and keep the MUST text. Thus a requirement with no test passes the gates. No known limit names this case. Correction: stop the build for a delta section header (`ADDED`, `MODIFIED`, `REMOVED` or `RENAMED Requirements`) in a file in `openspec/specs/`. Also stop for a requirement heading outside `## Requirements` in a main spec. Read the requirement and scenario headings without regard to letter case, or stop for a heading that matches only when case is ignored. Add scenarios to spec-lint and tests in specLint.test.mjs for these cases. Or run `openspec validate --specs --strict` in the gates.
- [ ] F2 minor openspec/specs/change-review/spec.md:60 The round-17 F4 correction is only partly done. change-review-015 and design.md:159 now say that the tree hash uses only the diff files outside `openspec/changes/`. But change-review-019 still says "the hash of the change files and the diff files", and the comment at scripts/spec/lib/review.mjs:45 says the same. The `tree` command shows the same hash as `computeTreeHash`, and that hash skips each diff file in `openspec/changes/` (review.mjs:64). No gap passes because of this. Correction: change the THEN line of change-review-019 and the comment at review.mjs:45 to say "the diff files outside `openspec/changes/`".

Round-17 findings:
- **F1, recorded as a known limit:** proposal.md has `guard-self-coverage`. It names the guard preload and says that the spec adversary checks each change to `scripts/spec/lib/test-guard.mjs`.
- **F2, recorded as a known limit:** proposal.md has `gate-code`. It names the gate code of the pull request and says that a person must check `scripts/spec/`, `.github/workflows/` and `openspec/trace/`.
- **F3, corrected:** spec-lint.mjs:28–30 adds `SPEC-LINT-NO-SCENARIO`. Its scenario is `spec-lint-011` (openspec/specs/spec-lint/spec.md:44). The test at specLint.test.mjs:63 uses deepEqual on the full error (code, file, line and message). It also shows that a removed requirement gives no error. The rule is in `ids.json`, in `links.json` and in tasks.md:23, where the test task comes before code task 1.22. The main spec and the delta spec are the same. F1 of this round lists the remaining paths.
- **F4, corrected for change-review-015 and design.md:159:** F2 of this round lists the remaining text.
- **F5, corrected:** ci-gates-007 has the new AND line. ciFiles.test.mjs:23–33 asserts the `gates`, `gates-init`, `ratchet`, `stability`, `lint` and `tree` recipes, and the definitions of `CHANGE_ARG` and `BASE_ARG`. `Makefile` agrees with them. Check 3 in .claude/agents/spec-adversary.md now reports a file without a code extension only when its code has no scenario or no test.

Other checks:
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected before review.md is written. There are no warnings.
- **Changed files:** The files that changed after the round-17 review are `spec-lint.mjs`, `specLint.test.mjs`, `ciFiles.test.mjs`, the specs, the trace files, the agent file, and proposal.md, design.md and tasks.md.
- **Ledger:** The ledger still has 468 keys (268 coverage entries and 200 untraced test files). The 6 history lines name `establish-spec-governance`.
- **Retired IDs:** `openspec/trace/retired-ids.json` does not exist.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r18/`. F1 uses only JS parser logic, so the Node version has no effect on it.