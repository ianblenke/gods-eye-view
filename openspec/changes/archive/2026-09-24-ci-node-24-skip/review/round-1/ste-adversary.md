Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:7 "The same errors show in the CI runs of 2026-09-22 to 2026-09-24. Earlier faults hid them." Agreement with the other prose. "show" and "hid" contradict each other. The archived `ci-leak-cleanup/proposal.md:5` says that `npm run format:check` stopped the job "Node 24.14.0" and that the later steps did not run. Its spec-adversary review confirms that `ci.yml` has no `continue-on-error`. So the step `npm test` did not run in the runs that the format fault stopped, and those runs could not show the two errors. "Earlier faults" and "them" name nothing. "hid" may not be an approved STE word, and I am not sure. Name the runs where the job reached `npm test`, and name the fault that stopped the other runs. If the lead measured the errors only after `ci-leak-cleanup`, write: "The step `npm run format:check` stopped the job before the step `npm test` in the runs of 2026-09-22 to 2026-09-24, so no run showed these errors. The first run after the change `ci-leak-cleanup` shows them."
- [ ] S2 major openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:3 "Both check that the gate reports a live timer." Agreement with the code. `[coverage-gate-050]` (gates.test.mjs:999-1013) asserts status 0 and `doesNotMatch(/GATES-TEST-LEAK/)`. It checks that the gate does not report a live timer. Write: "Both test the gate check for a live timer."
- [ ] S3 major openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:7 and design.md:26 "Each other test of this file that runs the gates has the option `GUARDED_RUN`." and "The two tests need the guard, because they run the gates on a fixture." Agreement with the code. Both claims are false as written. These tests call `runGates` through `run()` and have no option: `[coverage-gate-016]`, `[gap-ledger-030]`, `[spec-trace-002 coverage-gate-008 coverage-gate-017]`, `[coverage-gate-047]`, `[coverage-gate-026]`, `[coverage-gate-035]`, `[coverage-gate-030]` and `[gap-ledger-053]`. `[coverage-gate-030]` even starts a real test run through `spawnSync`. The real reason is different. Each new test starts with `passes(root, ['init'])`, which needs status 0 (gates.test.mjs:982 and :1001). On Node 24.14.0 the gate reports the error `COVERAGE-NO-TEST-CONTEXT` (gates.mjs:237-239), so `init` stops with status 1. The design also shortens the scenario words "needs the guard to count assertions" to "needs the guard", so one thing has two names. Write in proposal.md:7: "Each other test of this file that needs a gate run without the error `COVERAGE-NO-TEST-CONTEXT` has the option `GUARDED_RUN`." Write in design.md:26: "The two tests need the guard to count assertions, because each test calls `passes(root, ['init'])`, and the gate stops with `COVERAGE-NO-TEST-CONTEXT` when the guard cannot count assertions."
- [ ] S4 major openspec/changes/archive/2026-09-24-ci-node-24-skip/tasks.md:3 (also proposal.md:3) "the test `[coverage-gate-049]` that stops for a live timer", "One test has the tag `[coverage-gate-049]`" Articles and nouns. gates.test.mjs has two tests with this tag: line 980 "stops for a test that leaves a live timer" and line 1016 "stops for a real child process that leaves a live timer, with no stub". "stops for a live timer" fits both. An instruction can act on the wrong test, because the second one already has the option. Write in tasks.md:3: "Add `GUARDED_RUN` to the test `[coverage-gate-049]` that has no option and stops for a test that leaves a live timer." Write in proposal.md:3: "One test has the tag `[coverage-gate-049]` and no option, and the other has the tag `[coverage-gate-050]`."
- [ ] S5 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:3 "The CI job "Node 24.14.0" fails two tests" and "pass since the change" Verbs. A job does not fail a test, and "pass since" gives the meaning of a perfect tense. Write: "Two tests of `src/tooling/spec/gates.test.mjs` fail in the CI job "Node 24.14.0"." and "The other jobs of the workflow pass after the change `ci-leak-cleanup`."
- [ ] S6 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:5 "cannot count assertions without it" Articles and nouns. "it" can mean `getTestContext` or `node:test`. Write: "without that function".
- [ ] S7 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:5 "accepts Node 24.14.0 or later, and the CI matrix runs 24.14.0" Agreement with the code. `package.json` engines is `>=24.14.0 <25 || >=26 <27`, so Node 25 is not accepted. Also "runs 24.14.0" has no noun. Write: "accepts Node 24.14.0 and later Node 24 versions, and Node 26. The CI matrix runs Node 24.14.0."
- [ ] S8 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:7 "when `getTestContext` is missing" Words that end in -ing. "missing" is not a technical name, and the other prose says "has no getTestContext" for the same fact. Write: "when `node:test` has no function `getTestContext`."
- [ ] S9 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:12, design.md:26 and :30, tasks.md:5 "the list of skipped tests that the test `[coverage-gate-046]` pins", "tests that carry `GUARDED_RUN`", "its list", "the list in the test" One word, one meaning. The test checks two lists, one for `testGuard.test.mjs` (line 407) and one for `gates.test.mjs` (line 413). "the list" and "its list" do not say which. The tests are skipped only on a Node version without the function, so "skipped" is wrong on Node 24.21.0 and Node 26. "pins" and "carry" are figurative, and one relation has four verbs (has, uses, carry, gets). Write in proposal.md:12: "Add the names of the two tests to the list for `gates.test.mjs` in the test `[coverage-gate-046]`. The list names each test that has the option `GUARDED_RUN`." Write in design.md:26: "checks the two lists of tests that have `GUARDED_RUN`". Write in tasks.md:5: "Add the names of the two tests to the list for `gates.test.mjs` in the test `[coverage-gate-046]`."
- [ ] S10 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/design.md:22 "A skip is the existing way to handle a Node version without the function." Words that end in -ing, and approved words. "existing" is not a technical name. "handle" has a figurative meaning that STE does not approve. Write: "The file already uses a skip for a Node version without the function."
- [ ] S11 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/design.md:5 "the run reports", "The test that has the same tag `[coverage-gate-049]` and starts a real child process already has the option." Verbs, and articles and nouns. "run" is a verb that the text uses as a noun. Two tests fit the second sentence: gates.test.mjs:1016 and testGuard.test.mjs:460. Both have the option, so the claim is true, but the reference is not clear. Write: "On Node 24.14.0 the gates report `COVERAGE-NO-TEST-CONTEXT`, and the tests fail. In the same file, the test `[coverage-gate-049]` that starts a real child process already has the option."
- [ ] S12 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/design.md:34 "The same checks run on Node 24.21.0 and Node 26 in the same workflow, and the job "Spec gates" runs the gate itself." Articles and nouns. The matrix has Node 24.14.0 and 26.x. Node 24.21.0 is only in the job "Spec gates". "the gate itself" does not say which gate. "The leak check" has three names in the change: "the leak check", "the gate reports a live timer" and `GATES-TEST-LEAK`. The tests in testGuard.test.mjs:479-501 still record a live timer on Node 24.14.0. Only the report of the gate has no test there. Write: "The gate report of a live timer has no test on Node 24.14.0. The job "Node 26.x" runs the same tests on Node 26, and the job "Spec gates" runs them on Node 24.21.0."
- [ ] S13 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/design.md:38 "The change is in a test file." Agreement with the other prose. proposal.md:17 lists two changed test files. Write: "The change is in two test files."
- [ ] S14 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/proposal.md:23 "so the tests of the gates that need it skip there" Articles and nouns. "it" can mean "the guard" or "assertions". Write: "so the tests of the gates that need the guard skip there."
- [ ] S15 minor openspec/changes/archive/2026-09-24-ci-node-24-skip/tasks.md:9-17 Instructions. design.md:30 says that the job "Node 24.14.0" is the acceptance test on the push, but no task checks it. The archived `ci-leak-cleanup` had the same gap (its S47). Write: "2.8 Confirm that the job "Node 24.14.0" passes after the push."

Checked, with no finding:
- **The fix:** `missingTestContext()` returns a message when `node:test` has no `getTestContext` (test-guard.mjs:237-239). `GUARDED_RUN = { skip: missingTestContext() }` skips a test on that Node version and gives `skip: false` on a Node version with the function. The guard writes `COVERAGE-NO-TEST-CONTEXT` (test-guard.mjs:296-302), and the gate reports it as an error with no file (gates.mjs:237-239). So the option is the right fix for Node 24.14.0.
- **The two tests need the guard:** each starts with `passes(root, ['init'])` (gates.test.mjs:982 and :1001).
- **The pinned list:** testGuard.test.mjs:444-446 has the three names in the order of the file (lines 980, 999 and 1016, after line 870). Each name equals the test name letter for letter. The name of `[coverage-gate-046]` is unchanged. The regex `^test\('([^']*)', GUARDED_RUN, ` matches the new names.
- **Workflow claims:** `ci.yml` has the matrix `[24.14.0, 26.x]`, `.node-version` is 24.21.0, and the job "Spec gates" runs `gates.mjs ci` on the pinned Node.
- **Spec:** the scenario `coverage-gate-046` text is unchanged. The requirement "Run exit" already covers the live-timer check, so "no spec delta" and "Add a scenario" hold.
- **Tasks:** each task starts with an imperative verb and gives one instruction, or one action on two files (1.4).
- **Not run:** my tools have no shell. I ran no `git diff`, no `node --test` and no mutation. I could not check that no assertion changed apart from the pinned list. I read the current files against the documents.
