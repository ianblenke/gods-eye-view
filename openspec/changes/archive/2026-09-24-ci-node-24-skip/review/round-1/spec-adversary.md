Verdict: PASS
- [ ] F1 minor proposal.md:3 (also tasks.md:3, design.md:5) The text does not match the tests. "Both check that the gate reports a live timer" is false for [coverage-gate-050]. That test asserts `GATES-TEST-LEAK` is absent, and its name says "does not stop". The tag [coverage-gate-049] is on two tests in `gates.test.mjs`, at lines 980 and 1016. Task 1.1 ("the test [coverage-gate-049] that stops for a live timer") and design.md:5 ("the test that has the same tag ... starts a real child process") both fit either test. Line 1016 already has `GUARDED_RUN`. It does not start a child process itself, because the gate does. Give the full test names and remove "Both check that the gate reports".
- [ ] F2 minor proposal.md:7, design.md:22 and design.md:26 "Each other test of this file that runs the gates has the option GUARDED_RUN" is false. [coverage-gate-030] (`gates.test.mjs:586`) calls `run(root, ['init'], { spawn: forger })`. The forger calls the real `spawnSync`, so real test processes run under the guard, with no option. It passes on Node 24.14.0 only because none of its assertions (status 1, `COVERAGE-EXTRA-RESULT`, the `Trace:` counts) depends on the extra `COVERAGE-NO-TEST-CONTEXT` error. D2 says the two tests "need the guard, because they run the gates on a fixture". That reason also fits [coverage-gate-030]. Change the criterion to "the test asserts a status or an output that the guard error changes", or name [coverage-gate-030] as an accepted exception.
- [ ] F3 minor proposal.md:7 "The same errors show in the CI runs of 2026-09-22 to 2026-09-24. Earlier faults hid them." The two sentences contradict each other. The archived ci-leak-cleanup proposal says a failed `format:check` stopped the jobs before the test step. Say in which runs the job reached `npm test`, or name the run IDs.
- [ ] F4 minor proposal.md:21-23 (Known limits) Two gaps are not named in the Known limits section.
  - **Local gates cannot prove the fix.** `make gates` runs on Node 24.21.0, where both tests run before and after the change. Only the CI job "Node 24.14.0" runs the skip path. design.md:30 says the job is the acceptance test, but the precedent ci-leak-cleanup lists this limit in the proposal as `local-gates-cannot-prove-it`.
  - **Hidden later steps.** `scripts/run-unit-tests.mjs:75-76` returns when the parallel phase fails. In the failed runs, the two allocation probes did not run on 24.14.0, and neither did the build step. `GEV_REQUIRE_ALLOCATION_GATE=1` forces the probes on 24.14.0. The goal "Make the job pass" is therefore not proven for those steps, and the CI run after the push is the first run that reaches them. Add known limits for both.

Checked by reading, with no fault found:
- **Pinned list.** The 33 names in `testGuard.test.mjs:413-447` match the `GUARDED_RUN` tests of `gates.test.mjs` in file order. The two new names come before the already guarded [coverage-gate-049] at line 1016.
- **Test names.** The names of both tests equal the names in `openspec/trace/links.json`, so no test was renamed.
- **GUARDED_RUN fits.** `missingTestContext()` returns a message on a Node version without `getTestContext` and false on one with it. The scenario coverage-gate-046 asks for a skip on such a version. Both tests start with `passes(root, ['init'])`, which fails on `COVERAGE-NO-TEST-CONTEXT`.
- **Scope of the change.** No code file, no scenario and no ID changed. No history line or ledger entry is needed. Neither changed file is in `scripts/format-scope.json`, so `format:check` is not affected. The gate output has no other error.

Method note: this session had no shell tool. I did not run `node --test` or any mutation, and I did not run `git diff`. I read the tree at branch ci-node-24-skip (head 308e3ba, working tree). The base is origin/main b1267c6. I could not compare assertion bodies with origin/main. I compared names against `links.json`, and I read the bodies of tests 049 and 050 as they are now.
