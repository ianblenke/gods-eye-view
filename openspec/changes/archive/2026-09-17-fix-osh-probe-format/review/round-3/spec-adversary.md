Verdict: FAIL

- [ ] F1 major `openspec/changes/archive/2026-09-17-fix-osh-probe-format/design.md:88` The new sentence justifying the third measured row is not true as written. `listOfSystems()` accepting `items` only gets the array to `oshPages`; `fetchSystemsUpstream()` then passes it to `mapOshSystems()`, which at `src/data/oshSystems.js:236` keeps a record only when `feature.geometry.type === 'Point'`. A page in the server's default (non-GeoJSON) shape carries no top-level `geometry`, so its items are dropped one frame after `listOfSystems()` yields them — the route answers `200` with a short `count`, no error and no log line. `proposal.md:9` records only that `f=json` "answered `200` with an `items` array"; the item shape was never measured, so "That page still works" is an assumption, not a measurement. The only test of that shape, `[osh-014]` at `src/data/oshProxy.test.mjs:288`, drives a systems next link with no `f` and has its page-2 fixture return `features` — it encodes the same assumption instead of checking it. Yes, the rule should always send our format: append `OSH_LIST_FORMAT` whenever `keepsFormat`, regardless of whether the candidate carries an `f` key (drop the candidate's `f` unconditionally, add ours after the loop). That is one line, it keeps the F1 datastreams fix exactly as it is (`current` never has `f` there), it holds D27's rule that every URL this provider sends is one it built, and it retires this whole question. If you keep the current rule instead, the design sentence must be replaced by a named known limit saying page 2 may be silently dropped by the mapper.

- [ ] F2 minor `server/providers/osh/get.js:155` and `design.md:86` "`current` is the URL this provider actually sent, so its own `f` key says whether this list asks for a format at all" is wrong from page 3 onward. `current` is page N-1's URL, which this provider built from page N-2's link. One link that omits `f` clears `keepsFormat` for the rest of the walk: page 3 then drops an `f` the link does carry. The format latches off and never returns. The always-force fix in F1 removes this too; otherwise correct the sentence to say `current` is the previous page's URL, not the list's intent.

- [ ] F3 minor `openspec/specs/osh/spec.md` `osh-040` The WHEN line is "a next-page link carries an `f` key", so all four lines are silent on the shape D27 now spends a paragraph on: a systems link that omits `f`. No THEN line states what page 2 requests, and no test drives it with a non-GeoJSON page 2. Add a WHEN/THEN for the omitted-`f` link.

- [ ] F4 minor `server/providers/osh/get.js:170` A candidate with two `f` keys (`?page=2&f=json&f=xml`) passes `isSamePageWalk` and the loop appends `OSH_LIST_FORMAT` once per key, so the wire carries `f` twice. `osh-040`'s "the walked request's `f` value is `OSH_LIST_FORMAT`" is singular and does not cover it. The F1 fix (append once after the loop) also fixes this.

- [ ] F5 minor `design.md:3` "five tests that catch the class of fault" is stale after the round-2 and round-3 additions. The change adds eight tagged tests (`osh-037` three, `osh-038` one, `osh-039` one, `osh-040` three), which is what D28 at `design.md:78` itself enumerates, plus the changed `osh-009`. `links.json:718-732` agrees.

Confirmed, question 3: the move is correct. A throw from `oshListUrl()` now lands in `probeOnce`'s catch at `base.js:72`, which pushes `{candidate, status: null, reason: failureReasonFor(error)}` and the loop continues to the next candidate — one miss, not a rejected pass. `osh-012` holds: `baseErrorCode()` sees no 401/403, so a data route answers `502 base_unresolved`, and the failure record carries the candidate name only, never the URL, so "report the name, never its URL" is intact. One cosmetic residue: `failureReasonFor()` labels a local URL fault `network_error`.

Also verified unchanged by the round-2 edits: the `osh-040` `f=json` case genuinely fails a repair-not-replace implementation; the datastreams case pins
