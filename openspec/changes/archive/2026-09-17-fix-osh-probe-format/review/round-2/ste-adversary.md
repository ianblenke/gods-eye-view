Verdict: FAIL

Paths below are relative to `/home/ianblenke/docker/gev-osh`. `C/` = `openspec/changes/archive/2026-09-17-fix-osh-probe-format/`.

- [ ] S1 major C/design.md:68 "`osh-037` tags two tests" Disagrees with the code. Three tests carry `[osh-037]`: `src/data/oshGet.test.mjs:458`, `:476` and `src/data/oshProxy.test.mjs:643`. The new throws test made the count stale. Write: "`osh-037` tags three tests".
- [ ] S2 major C/design.md:68 "The helper has one loop and no branch, so it opens no gap." Disagrees with the code. `server/providers/osh/get.js:30` is an `if` with an `||`, so the helper has two branches. The throws test covers both. Write: "The helper has one loop and one check with two branches. The `osh-037` throws test covers each, so it opens no gap."
- [ ] S3 major C/design.md:29-35 the `oshListUrl()` code block Disagrees with the code. The block has no safety check; `get.js:30-32` throws on a foreign origin or a path outside the root. The D24 prose above it also names only `new URL()` and `URLSearchParams`. Add the `if` and the `throw` to the block, and one sentence: "It then throws when the built URL does not keep the root's origin and does not start with the root's path."
- [ ] S4 major C/design.md:65 "`osh-038` could not catch this fault, because its round-trip assertion passes on an already-rebuilt, canonical query." Disagrees with the code. `oshProxy.test.mjs:654` also asserts that no decoded value holds a space and that an `f` value decodes to the GeoJSON format; the old rebuild gave `application/geo json`, so both fail. `osh-038` misses the fault because its fixture body (`oshProxy.test.mjs:10`) carries no `links`, so no walk runs. Write: "`osh-038` could not catch this fault, because its fixture list has no next link, so no page walk runs."
- [ ] S5 major C/proposal.md:11 "decodes it and re-encodes it the same way, so a next-page link round-trips" Disagrees with the record and with this change. Round 6 of `osh-fusion` (`review/round-6/spec-adversary.md:5`) reported that page 1 and page 2 **decode identically**, not that the bytes round-trip; D25 of this design says the same literal "re-serializes to `f=application%2Fgeo+json`, a different string". Write: "It reported that page 1 and page 2 decode to the same value, so the walk added no fault. That was true, and it was not the question: both pages decoded to `application/geo json`, the value the server refuses."
- [ ] S6 major C/specs/osh/spec.md:36 and openspec/specs/osh/spec.md:261 "a URL with a Location of the root's origin plus `path`" Disagrees with the code and with the new AND line under it. `new URL('systems', 'https://osh.example/api/')` gives `/api/systems`, which `oshGet.test.mjs:462` pins; line 37 requires the built URL to keep the root's path. Also "Location" names an HTTP header. Write: "gives a URL with the root's origin, the root's path plus `path`, and a query of the `URLSearchParams` form of `query`".
- [ ] S7 major C/tasks.md:3 "the ADDED requirement with `osh-037`, `osh-038` and `osh-039`" Disagrees with the spec delta, which has four scenarios. Write: "...with `osh-037`, `osh-038`, `osh-039` and `osh-040`."
- [ ] S8 minor C/design.md:63, server/providers/osh/get.js:154 and src/data/oshProxy.test.mjs:708 "echo" / "echoes" Approved words. "echo" is not an approved STE verb, and "sent back into" is two readings. Write: "A server can write the format this provider sent into its next link." In the test name: "even when a server writes a different f value".
- [ ] S9 minor C/design.md:63 "never the link's own bytes" One word, one meaning. `osh-040` and proposal.md:20 say "the link's own value" for the same thing. Write: "never the link's own value".
- [ ] S10 minor C/specs/osh/spec.md:54 and openspec/specs/osh/spec.md:279 "whatever value a server writes there" Approved words. Write: "with any value a server writes".
- [ ] S11 minor src/data/oshGet.test.mjs:476 "would leave" Verbs. Use the simple present. Write: "oshListUrl throws when the bu
- [ ] S11 minor src/data/oshGet.test.mjs:476 "would leave" Verbs. Use the simple present. Write: "oshListUrl throws when the built URL leaves the root's origin or path".
- [ ] S12 minor C/proposal.md:39 "is the stronger guard, and the scan is the third guard" One word, one meaning. "stronger" compares two, "third" counts three, and the text names no first or second. Write: "is the stronger of the two guards, and the scan is the weaker one."
- [ ] S13 minor C/proposal.md:39 "has an empty literal at scan time, and a query written outside the five scanned files, passes too" Articles and nouns. The literal is not empty, its value is; the comma before "passes" splits the subject from its verb. Write: "leaves an empty value in the literal at scan time. A query in a file outside the five passes too."
- [ ] S14 minor C/design.md:57 "a run with no `&`, `=` or space" Disagrees with the code. `PAIR_TOKEN` at `oshProxy.test.mjs:681` uses `\s`, which is any white space. Write: "a run with no `&`, no `=` and no white space".
- [ ] S15 minor server/providers/osh/get.js:157 "rather than carries the server's bytes" Verbs. The form after "rather than" must match "names". Write: "and never carries the server's bytes".
- [ ] S16 minor C/proposal.md:15 "it builds a URL with `URLSearchParams`" The bullet does not name the safety check the spec now requires at line 37. Add: "It throws when the built URL would leave the root's origin or the root's path."

No host name, system id, datastream id, count or place name entered the repository. Every value is a fixture (`osh.example`, `attacker.example`, `sys-fixture-1`, `ds-fixture-1`, `obs-fixture-1`), and the proposal carries only the statuses `400`, `302` and `200` with the message text, no host. I found no missing lint warning: the new prose has no word that ends in -ing and no contraction; the faults above are all ones the lint cannot see.
