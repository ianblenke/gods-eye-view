Verdict: FAIL
- [ ] F1 major server/providers/osh/get.js:167 The forced `f` is not scoped to the list that sent one. `fetchDatastreamsUpstream` (osh.js:118) deliberately sends no `f`, but `f` is on `OSH_PAGE_LINK_QUERY_KEYS`, so a server that writes `f=json` into a datastreams next link now makes page 2 request `f=application%2Fgeo%2Bjson` — a format the owner never measured for that route. `oshPages` throws on any non-2xx, so where the walk used to page, it now fails the whole datastreams request. This contradicts the Non-Goal in design.md:20 and makes `osh-datastreams-no-format` (proposal.md:40, "The datastreams list still names no format") false for page 2 onward. Either force `f` only when the first-page URL carried one (pass the wanted format into `buildNextPageUrl`, or drop an `f` the provider did not send), or state the datastreams consequence in `osh-040` and in the known limit.
- [ ] F2 major src/data/oshProxy.test.mjs:708 `osh-040` drives exactly one next-link value, `f=application/geo+json`, whose decoded form is `application/geo json`. An implementation that repairs rather than replaces — `key === 'f' ? value.replace(/ /g, '+') : value` — passes every assertion in the test, yet the THEN line says "never the link's own value" and the WHEN says "whatever value a server writes there". The assertion is weaker than the scenario. Add a second link whose `f` is a legitimately different value (`f=json`), and assert the walked request still decodes to `application/geo+json`. You measured that case by hand; it is one line in the test.
- [ ] F3 minor openspec/changes/archive/2026-09-17-fix-osh-probe-format/design.md:26 The `oshListUrl()` body quoted in D24 has no safety check, and no decision in the design records the throw added for round-1 F4. The only record of that behaviour outside the code is the AND line of `osh-037`. Update the D24 code block and say why the check is there and why it cannot fire today.
- [ ] F4 minor server/providers/osh/base.js:64 The `oshListUrl()` call sits outside `probeOnce`'s `try`. Today `candidateRoots()` normalises the trailing slash and `path` is the literal `systems`, so the check cannot fire — but if a later candidate shape ever makes it fire, one bad candidate rejects the whole probe pass instead of recording a miss, which the `osh-012` path is built to do. Move the call inside the `try`, or say in D24 that the throw is meant to abort the pass.
- [ ] F5 minor openspec/changes/archive/2026-09-17-fix-osh-probe-format/proposal.md:39 `osh-query-scan-is-text` is honest about source escapes, concatenation and unscanned files, but not about two shapes it also misses: `BAD_BYTE` is tested only against the value half of a pair, so a hand-encoded key (`'limit%3D1'`, or `'systems%3Ff=x'`) matches no pair and passes; and neither rule mentions `#`, while `oshListUrl()` keeps a fragment from `path`. `osh-038` catches the first at run time, nothing catches the second. Name the value-only rule in the limit.
- [ ] F6 minor openspec/changes/archive/2026-09-17-fix-osh-probe-format/design.md:69 Three stale counts. D28 says `osh-037` tags two tests; links.json:718 lists three. D28 says "The helper has one loop and no branch"; it now has a two-clause branch. design.md:3 says "two tests that catch the class of fault"; the change adds five. tasks.md:3 (1.1) still omits `osh-040` from the scenarios the spec file holds.

Verified as correct: the `osh-009` pin and its decode assertion match `base.js`; `ids.js`'s `limit=1&resultTime=latest` and the `` `?${OSH_OBSERVATIONS_QUERY}` `` template both pass the `osh-039` rules as D26 claims; the widened `[^&=\s]+` key class does catch `'?f[]=...'` and `'?p.f=...'`; `osh-040` genuinely fails before the fix; both arms of the `key === 'f'` ternary and both throw conditions are exercised, so the 100%-coverage claim holds; history.jsonl carries no line for this change, matching "opens no gap"; `osh-037`–`osh-040` are new ids, none from retired-ids.json.

I could not find a bypass of the F4 assertion. Every reachable `root` comes from `candidateRoots()`, which forces a trailing slash, so the `startsWith` prefix is segment-aligned; `//attacker.example/x`, an absolute URL and `../x` all throw, and a `path` that carries its own query is overwritten by `url.search`. The one residue is the fragment noted in F5.
