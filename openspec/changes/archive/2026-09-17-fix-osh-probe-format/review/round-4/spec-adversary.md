Verdict: PASS

**The five round-3 corrections are right.** I re-derived each one.

- **F1/F2 (the latch).** I exercised the new `buildNextPageUrl()` body against ten shapes: a link with a raw `+` in `f`, a link with a different valid `f` (`f=json`), a link that omits `f`, a link with `f` twice, a link with an empty `f` value, a `current` with an empty `f` value, a datastreams walk, a `;`-smuggled second pair, an uppercase `F`, a dot-segment path and a trailing-slash path. Every result is the one `osh-040` states: `f=application%2Fgeo%2Bjson` written exactly once when `current` has an `f` key, no `f` key at all when it does not, the `;` pair encoded whole as one `limit` value (`limit=1%3Bf%3Dtext%2Fhtml`), and a stop for `F` and for `systems/`. Deciding from `current` rather than from the link does close the latch: page 3's `current` is page 2's rebuilt URL, which always carries our `f` when page 1 did. I could not find a shape it gets wrong. The one residue is a link that echoes `current` byte for byte — the walk then refetches that page to the 20-page cap and repeats its items — but that loop predates this change and the rebuild does not create it.
- **F3 (`osh-040`).** The scenario now states exactly what the code does. Its WHEN names the three link shapes, and each is driven: `f` present (oshProxy.test.mjs:708, :732), omitted (:780), twice (oshGet.test.mjs:312). Each AND has an assertion — the encoded wire form at :728, the dropped key at :777, the three-page latch at :834. Nothing in the code is unstated and nothing stated is undriven.
- **F5 (the count).** Eleven is right: `osh-037` 3, `osh-038` 1, `osh-039` 1, `osh-040` 6. `links.json` lists the same eleven names, and they match the test titles verbatim. D28's per-id split and "one loop and one check with two clauses" both hold.
- **The Impact.** Correct. `gaps.json` banks 52/407 for `src/data/labelArbiter.js`; 405−50 = 407−52 = 355, so the covered count is unchanged, which is why `toleranceCounts()` wrote the gap's own counts and `compareLedger()` raised no `LEDGER-LARGER-GAP`. Both history lines name `fix-osh-probe-format` at the right commit, and `banked-branch-count` does exist in `2026-09-17-harden-gate-ledger`. No osh provider file has a `gaps.json` entry, so the 100% claim holds.

Three minors, each open for you to accept by name:

- [ ] F1 minor `openspec/changes/archive/2026-09-17-fix-osh-probe-format/proposal.md:32` The changed-files list gained the four trace files but not `openspec/specs/osh/spec.md`, which this change modifies. `harden-gate-ledger` lists its spec files. Add it, or say the list excludes spec text.
- [ ] F2 minor `openspec/changes/archive/2026-09-17-fix-osh-probe-format/design.md:7` D-level "Changed files" still names six files while the proposal now names ten. A round-3 edit updated one list and not its neighbour.
- [ ] F3 minor `openspec/changes/archive/2026-09-17-fix-osh-probe-format/proposal.md` The gate reports 6 STE warnings and no text in the change names them. The "Accepted lint warnings" line that answered round-1 F5 is gone from the proposal. Name them, or state that they are older than this change.
