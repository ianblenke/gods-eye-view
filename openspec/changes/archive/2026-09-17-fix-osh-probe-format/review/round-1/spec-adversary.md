Verdict: FAIL

- [ ] F1 major `server/providers/osh/get.js:153` The fault this change fixes is still live on the page walk, and `osh-038` does not see it. `buildNextPageUrl()` rebuilds the query from `candidate.searchParams`, and `f` is in `OSH_PAGE_LINK_QUERY_KEYS` (line 101). A server next link that writes `f=application/geo+json` decodes to `application/geo json` and re-serializes to `f=application%2Fgeo+json` — measured: `node -e` gives `decodes to "application/geo json"`. Page 2 then carries the same broken value, `oshPages` throws `OSH page HTTP 400`, and the whole list fails. `osh-038` cannot catch it: the rebuilt query is already canonical, so the round-trip assertion passes, and the one assertion that would catch it (`no decoded value holds a space`) never runs, because no fixture drives a next link with an `f` key (`src/data/oshGet.test.mjs:131,264,320,340,360,402` and `src/data/oshProxy.test.mjs:296` all use `page`, `limit` or `_method`). The proposal's Why states round 6's conclusion "was true"; it is not true for a `+` a server writes. Add a next-link fixture with `href: 'systems?page=2&f=application/geo+json'` driven through `oshProxy`, and make `buildNextPageUrl` re-encode the decoded value to the named format (or drop `f` and re-add `OSH_LIST_FORMAT`). Add the scenario under the new requirement.

- [ ] F2 minor `src/data/oshProxy.test.mjs:675` The `osh-039` scan misses a literal query whose key holds a non-word byte. `LITERAL_QUERY = /\?[A-Za-z_][\w-]*=/` and `PAIR_TOKEN = /(?:^|[?&])[A-Za-z_][\w-]*=([^&]*)/g` both need `[A-Za-z_][\w-]*`, so `'?f[]=application/geo+json'` and `'?p.f=application/geo+json'` pass both rules. An escaped byte (`'f=application\x2Bjson'`) also passes, because `BAD_BYTE` reads source text, not the built string. `osh-query-scan-is-text` names only the plain-word value, the concatenation and the unscanned file, so the limit is not complete. Add these two forms to that limit, or widen the key class to `[^&=\s]+`.

- [ ] F3 minor `openspec/specs/osh/spec.md:281` The `osh-038` WHEN names four routes, "the status, systems, datastreams and observations routes". The test (`src/data/oshProxy.test.mjs:654`) drives only `/systems`, `/datastreams` and `/observations`. The probe still runs, so the intent holds, but the WHEN is wider than the test. Drop `status` from the WHEN, or drive it.

- [ ] F4 minor `server/providers/osh/get.js:23` Nothing enforces the JSDoc contract "`path` is a fixed literal at each call site". `new URL(path, root)` with a variable path would leave the root, and unlike `ids.js` there is no `assertObservationUrl()` re-check. `osh-005` keeps the request a GET, so the loss is a wrong read, not a write. No limit names it. Add a limit, or assert `url.origin === root.origin` and that `url.pathname` starts with `root.pathname`.

- [ ] F5 minor `openspec/changes/archive/2026-09-17-fix-osh-probe-format/proposal.md:47` The gate output reports 7 STE warnings. "Accepted lint warnings" names one, `tasks.md:12 STE-ING "missing"`. That one is correct: task 2.4 is at the 20-word hard limit. Name the other six, or state that they are older and outside this change.

- [ ] F6 minor `src/data/oshProxy.test.mjs:17` `stripComments` moved from inside the `osh-005` test to module scope, shared with `osh-039`. A later loosening for the scan now also weakens the one-call-site guard. Behaviour is unchanged today. Note the coupling in the design, or give each test its own copy.

Confirmed sound: `osh-005` still pins four files and one `fetch` call site (the helper added none); `osh-039` re-pins the same four; `osh-034` sees no new address; `ids.js` keeping `OSH_OBSERVATIONS_QUERY` is correct — every byte of `limit=1&resultTime=latest` is already canonical, `assertObservationUrl` fails closed as the limit claims, and `osh-039` passes it (the `` `?${...}` `` template escapes `LITERAL_QUERY` because `$` is not `[A-Za-z_]`). Tests precede code in `tasks.md`; `ids.json` and `links.json` match; no history line is right, since no gap moved.
