Verdict: FAIL

- [ ] S1 major openspec/changes/archive/2026-09-17-fix-osh-probe-format/proposal.md:44 "A reword that removes the `-ing` word would push the task past that hard limit." Text does not agree with the facts. Task 2.4 is 20 words; "It must fail because no export exists." is also 20 words and carries no `-ing` word. The record is therefore not honest, though the word count it gives is right. Write: "Task 2.4 already sits at the 20-word task limit. This change keeps the warning, because `missing` here is an adjective, not a noun."
- [ ] S2 major proposal.md:36 "the server accepts `f=application%2Fgeo%2Bjson` on the systems list, and a datastreams request with no `f` key" Text does not agree with the other prose. Line 38 says "The owner measured only the systems list", and the Why (lines 5 and 9) measures the systems path alone. Write: "...on the systems list, and the same list with no `f` key."
- [ ] S3 major design.md:47 "`get.js` already holds the one page-link URL builder that both `base.js` and `osh.js` import." Text does not agree with the code. `base.js` imports `OSH_LIST_FORMAT`, `oshGet` and `oshListUrl`; `osh.js` imports `OSH_LIST_FORMAT`, `oshListUrl` and `oshPages`. Neither imports `buildNextPageUrl`. Write: "`get.js` already holds the URL builders, and both `base.js` and `osh.js` import from it."
- [ ] S4 major design.md:63 "the added requirement has one MUST sentence" Text does not agree with the spec delta. `specs/osh/spec.md:31` has two sentences with MUST: one MUST and one sentence with two MUST NOT clauses. Write: "the added requirement has a MUST sentence, a MUST NOT sentence, an origin line and three scenarios".
- [ ] S5 major specs/osh/spec.md:42 "a client requests the status, systems, datastreams and observations routes" Text does not agree with the test. The `osh-038` test (src/data/oshProxy.test.mjs:654) requests `/systems`, `/datastreams` and `/observations` only. The probe runs from `/systems`. `design.md:51` names the right four fetches. Write: "a client requests the systems, datastreams and observations routes".
- [ ] S6 minor design.md:63 "`osh-037`, `osh-038` and `osh-039` each tag a test" Text does not agree with the code. `osh-037` tags two tests, one in `oshGet.test.mjs` and one in `oshProxy.test.mjs`, as tasks 2.4 and 2.5 ask. Write: "`osh-037` tags two tests, `osh-038` and `osh-039` one each".
- [ ] S7 minor proposal.md:15 "The helper builds a URL from a path and a query object with `URLSearchParams`." Text does not agree with the code. `oshListUrl(root, path, query)` takes three parameters. Write: "The helper builds a URL from a root, a path and a query object, with `URLSearchParams`."
- [ ] S8 minor specs/osh/spec.md:49 and design.md:57 "no quoted string in those files holds a `key=value` pair whose value has..." Text is wider than the code. `PAIR_TOKEN` reads a pair only at the start of a string or after `?` or `&`. Write: "no quoted string starts a `key=value` pair, at its start or after `?` or `&`, whose value has...".
- [ ] S9 minor proposal.md:5 "it answered `302` to the server's web root" Articles and nouns. "to" can name the target of the answer or the target of the redirect. Write: "it answered `302` with a `Location` of the server's web root".
- [ ] S10 minor proposal.md:37 "the scan is the third line" Articles and nouns. "the third line" names no third item; the text names only two guards before it. Write: "the scan is the third guard".
- [ ] S11 minor proposal.md:19 "it refuses a literal query written next to a URL" Voice, and the words do not name the rule. The scan refuses any quoted string with `?` and a `key=value` pair. Write: "it refuses a quoted string that holds `?` and a `key=value` pair".
- [ ] S12 minor design.md:38 "the serializer owns the bytes on the wire" Approved words. STE has no such meaning of "own". Write: "the serializer writes the bytes on the wire".
- [ ] S13 minor design.md:47 "page-link URL builder" Articles and nouns. Four nouns in a group. Write: "the builder of a page-link URL".
- [ ] S14 minor tasks.md:21 "This change does not alter their behaviour." One word, one meaning. The change uses "change" everywhere else. Write: "This change does not change their behaviour." (or "does not touch their behaviour").
- [ ] S15 minor test name, src/data/oshProxy.test.mjs:654 and :643 "decodes f to the expected format" and "the queries built by oshListUrl" Voice. Write: "decodes f to the GeoJSON format" and "the queries that oshListUrl builds".

No leak. The four Markdown files carry no host name, no system id, no datastream id, no count from the owner's server and no place name. The measured statuses 400, 302 and 200 appear with no host.

Accepted warning: honest on the word count, wrong on the claim that no reword fits (S1).

Byte-for-byte requirements: "API root resolution" and `osh-010`, `osh-011`, `osh-012` stay true under the two new AND lines of `osh-009`. The pinned probe URL at oshBase.test.mjs:20 matches spec line 9 exactly. No finding there.
