Read the proposal, design, tasks, delta spec, `scripts/spec/lib/ledger.mjs`, the staged diffs and `openspec/trace/gaps.json` (verified 405/23 totals and the tolerances). Report below.

---

Verdict: FAIL

- [ ] S1 major `/home/ianblenke/docker/gods-eye-view/src/tooling/spec/ledger.test.mjs:167` "stops for fewer covered branches or functions in an unchanged file" Test names. The name states a behaviour the code no longer has. `gap-ledger-078` at line 153 of the same file proves the opposite: entry 23/3 against current 22/3 is fewer covered branches (20 to 19) and the gate does not stop. The scenario title became "Stop for a loss of covered branches or functions"; the test name did not follow. Write: `[gap-ledger-054] stops for a loss of covered branches or functions in an unchanged file`.
- [ ] S2 major `.../fix-covered-count/specs/gap-ledger/spec.md:39-40` "the file has more not-covered branches or functions than its entry, and fewer covered branches or functions" then "the loss of that metric". Two meanings. One reading pairs the two clauses across metrics: more not-covered branches and fewer covered functions. That reading makes THEN untrue (entry branches 0/5, now 2/7 gives loss 0; entry functions 0/2, now 0/1 gives loss 0; no stop). "that metric" then has no antecedent. Write: "- **AND** for the branches or for the functions, the file has both more not-covered counts than its entry and fewer covered".
- [ ] S3 major `/home/ianblenke/docker/gods-eye-view/openspec/changes/archive/2026-09-17-fix-covered-count/proposal.md:20` "The requirement "Count tolerance" gets the loss rule in the scenario `gap-ledger-072`." Does not agree with the delta. `gap-ledger-070` also got the loss rule ("a loss of branches or functions above the tolerance also stops the build"), and "What Changes" at lines 8-10 names only -054, -072 and -078. Write: "...gets the loss rule in the scenarios `gap-ledger-070` and `gap-ledger-072`.", and add a bullet for -070 to "What Changes".
- [ ] S4 minor proposal.md:7 "increase"/"decrease"; proposal.md:31 and design.md:40 "rise"/"fall"; design.md:52 "a stable not-covered count". One word, one meaning. Three word pairs for the two differences, and "stable" for what line 31 calls "no rise". Use "rise" and "fall" everywhere, and at design.md:52 write "with no rise of the not-covered count". The same applies to the JSDoc of `lossOf` in `scripts/spec/lib/ledger.mjs:41-43`.
- [ ] S5 minor design.md:38,40 "catch" ("What this change does not catch", "catch this signature"). Approved words. "catch" is not approved in this sense, and proposal.md:31 writes the same idea as "stop the build for this signature". Write: "What this change does not stop" and "stop the build for this signature in most files".
- [ ] S6 minor design.md:5,36 "absorbs" ("its tolerance of 8 absorbs a move of the total", "This absorbs a move of one branch"). Approved words. Write: "a move of the total stays inside its tolerance of 8" and "This allows a move of one branch".
- [ ] S7 minor Verbs, phrasal. proposal.md:3 "the fall in the total reads as a lost branch" — write "the gate reads the fall in the total as a lost branch". design.md:28 "the part that both counts agree on" — write "the part that is in both counts". design.md:28 "shows in both differences equally" — write "is equal in both differences". design.md:32 "It turns one false stop into another false stop" — write "It gives another false stop".
- [ ] S8 minor One word, one meaning. "show": the gate shows counts (delta spec:42), a test shows branches (delta spec:23), a loss shows in a difference (design.md:28). "read": the gate reads the tolerance (live spec `gap-ledger-069`) and a fall reads as a loss (proposal.md:3). Keep "show" for the gate output only; see S7 for the two rewrites.
- [ ] S9 minor proposal.md:31 "The open corner is a file with 25 or more functions and a function with at most the line tolerance in lines." Does not agree with the code. The corner also needs the tolerance conditions: without them `tolerance` is 0, so a function loss of 1 stops the build. Write: "The open corner is a file with the tolerance conditions, 25 or more functions, and a function with at most the line tolerance in lines."
- [ ] S10 minor design.md:40,52 and proposal.md:31 "signature", "the open corner". Approved words. Neither is an approved STE word in this sense, and I am not sure of an approved replacement. Write: "gives these counts" for the first, and "The known limit names the file that can hide it" for the second.

Checked and not reported: the two requirement texts and the carried scenarios are byte-identical and stay true under the changed lines. The measured numbers (23/22 branches, 3 not covered, tolerance 0 for 23 and 8 for 405) all agree with `gaps.json` and `toleranceOf`. `gap-ledger-078` and the new `gap-ledger-072` bodies are true against `lossOf`. Tasks all start with an imperative verb.
