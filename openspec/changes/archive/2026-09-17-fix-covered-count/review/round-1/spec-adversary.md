Verdict: FAIL

- [ ] F1 major scripts/spec/lib/ledger.mjs:50 `Math.min` is signed, so when the not-covered count *falls* the loss is negative and that metric loses its check entirely, at any tolerance, including 0. Real shape, no ≥25 functions needed: an outer function F containing 3 already-uncovered nested callbacks. V8/lcov never compiles nested functions of an uncalled function, so deleting F's only caller gives functions total 10→7, not covered 3→1, covered 7→6: ncDiff -2, covered fall 1, loss -2, no stop. Branches move the same way. Only the line metric is left, and F's own body (say 4 lines) is inside the line tolerance of any file with ≥100 lines. A real covered function is lost and every metric passes. Fix: use the min only when the not-covered count rose — `const fall = before - now; return gap[metric] >= entry[metric] ? Math.min(gap[metric] - entry[metric], fall) : fall;`. I checked this against all seven of your cases and every case in ledger.test.mjs: each keeps its result.
- [ ] F2 major openspec/changes/archive/2026-09-17-fix-covered-count/proposal.md:31 (and design.md:40) The known limit names the wrong corner. "The function metric and the line metric stop the build for this signature in most files. The open corner is a file with 25 or more functions and a function with at most the line tolerance in lines" is false for F1: the function metric does not fire at 10 functions. If you keep the rule, the limit must say that a metric whose not-covered count falls is unchecked for a covered loss regardless of tolerance or file size.
- [ ] F3 minor openspec/specs/gap-ledger/spec.md:61 `gap-ledger-078` says "not larger", which admits the falling half that F1 exploits, and the test covers only the equal half (23/3 → 22/3, and functions 3 → 2 with the same count). Narrow the line to "equal to", or add the falling case and its assertion.
- [ ] F4 minor openspec/specs/gap-ledger/spec.md:64 "gives no other error" claims more than the code: the WHEN does not bound the line count, and a larger one still gives LEDGER-LARGER-GAP; untrue coverage gives COVERAGE-FAKE.
- [ ] F5 minor openspec/specs/gap-ledger/spec.md:68 "more not-covered branches or functions ... and fewer covered branches or functions" reads as allowing the two halves on different metrics. The code needs both on one metric. Say "of the same metric".
- [ ] F6 minor src/tooling/spec/ledger.test.mjs:172 The test name and links.json keep "stops for fewer covered branches or functions" while the scenario title at spec.md:66 is now "Stop for a loss of covered branches or functions".
- [ ] F7 minor proposal.md:24 The Impact lists `openspec/trace/history.jsonl` as changed. The tree does not change it, and the `gap-ledger-073` test asserts an empty history.
- [ ] F8 minor proposal.md:29 The heading is "## Known limits". All seven other archived proposals use "## Known limits and later changes".
Add that sentence in the next change that already touches that requirement.

Since F1 is being fixed, the sentence to add changes with it: state the bound the code will then honor — for a metric of a file with the tolerance conditions, the allowed difference is the loss, which is the fall of the covered count when the not-covered count did not rise, and the smaller of the two differences when it did. Put it in the requirement text of "Count tolerance" at `openspec/specs/gap-ledger/spec.md:238`, not only in the scenarios, because the scenarios are where the rule currently lives and a reader of the requirement alone gets a bound the code does not keep.
