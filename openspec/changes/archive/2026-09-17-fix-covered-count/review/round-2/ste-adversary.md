Verdict: FAIL

- [ ] S1 major openspec/changes/archive/2026-09-17-fix-covered-count/design.md:31 "A file that no longer calls a function gives this shape." Code disagreement. The loss rule runs only in the unchanged-file branch of `compareCoverageEntry` (scripts/spec/lib/ledger.mjs:225); a file that stops calling its own function gets a new content hash and takes the `changed` branch instead. The proposal states it correctly at proposal.md:34. Write: "A caller in another file that no longer calls a function gives this shape."
- [ ] S2 major openspec/changes/archive/2026-09-17-fix-covered-count/proposal.md:25 "Changed files: `scripts/spec/lib/ledger.mjs`, `src/tooling/spec/ledger.test.mjs`, `openspec/specs/gap-ledger/spec.md`, `openspec/trace/ids.json`, `openspec/trace/history.jsonl`." Prose disagreement with the tree. `git status` also shows `openspec/trace/gaps.json` and `openspec/trace/links.json` as changed. The ratchet run writes the ledger, so `gaps.json` is the one file this change cannot omit. Write the list with `openspec/trace/gaps.json` and `openspec/trace/links.json` added.
- [ ] S3 minor design.md:31 "V8 drops the function's own nested, already uncovered functions from the total" One word, one meaning. The test comment at src/tooling/spec/ledger.test.mjs:186 says "removes" for the same action. Write: "V8 removes".
- [ ] S4 minor design.md:31 "already uncovered functions" One word, one meaning. Each other line writes "not covered" or "not-covered". Write: "already not-covered functions".
- [ ] S5 minor design.md:31 "the loss must skip the not-covered difference when it falls" Approved words. "skip" has no approved STE meaning here, and "it" can read as the loss or as the count. Write: "the loss must use only the fall of the covered count when the not-covered count falls".
- [ ] S6 minor proposal.md:25 "two history lines, where an earlier run of this change added none" Approved words. "where" gives a place, not a contrast. Write: "two history lines. An earlier run of this change added none."
- [ ] S7 minor proposal.md:28 "Across this change's ratchet runs, the ledger recorded" Approved words. "across" goes from one side to the other; it is not "during". Write: "The ratchet runs of this change recorded".
- [ ] S8 minor proposal.md:28-29 "the ledger recorded `src/data/labelArbiter.js` at 50 and at 52 not-covered branches" and "Each run gives that file 50 or 52 not-covered branches" One word, one meaning. Two bullets state the same measurement with two verbs. Delete the first sentence of line 28 and start line 28 with "This change never touches `src/data/labelArbiter.js`."
- [ ] S9 minor proposal.md:35 "a change rehashes its six scenarios" Approved words. "rehash" is not an STE word, and "hash" elsewhere in this change is only the content hash. Write: "a reword gives each of its six scenarios a new hash".
- [ ] S10 minor proposal.md:35 "A later change that already changes that requirement can add this sentence." Articles and nouns. "this sentence" names no sentence; the limit gives none. Write: "A later change that already changes that requirement can make the requirement text exact."
- [ ] S11 minor proposal.md:35 "gets a bound that the code does not keep for the covered count" Approved words. "keep" is "to retain", so "the code does not keep a bound" has two readings: the code breaks the bound, or the code drops it. Write: "gets a bound that is smaller than the bound the code uses for the covered count when the total count moves".
- [ ] S12 minor src/tooling/spec/ledger.test.mjs:186 "Deleting the only caller of a function removes its nested functions too" Words that end in -ing. "Deleting" is a verb used as a noun. Write: "When you delete the only caller of a function, V8 removes its nested functions too".
Not reported, as instructed: the "Count tolerance" requirement text itself. The limit `requirement-text-less-exact` reads correctly against the code apart from S9 to S11.
