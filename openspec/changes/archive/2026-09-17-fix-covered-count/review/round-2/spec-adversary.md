Verdict: FAIL

- [ ] F1 major `openspec/specs/gap-ledger/spec.md:70` (and the delta copy at `.../fix-covered-count/specs/gap-ledger/spec.md:40`) The word "loss" is used in `gap-ledger-054`, `gap-ledger-070` and `gap-ledger-072` and is defined nowhere in the spec — neither in the "Count tolerance" requirement text nor in any scenario. Worse, `gap-ledger-054`'s second WHEN says the file has "both more not-covered counts than its entry and fewer covered", which excludes the exact shape the F1 ternary exists for. The new `fewerNested` case in the `054` test (`src/tooling/spec/ledger.test.mjs:440-442`: functions 3/10 not covered and total → 1/7, so not covered **falls** 3→1 while covered falls 7→6) asserts a stop that no scenario states. An author who replaces `lossOf`'s ternary with a plain `Math.min` — the bug your round 1 caught — still satisfies every scenario as written, and deleting that one unrequired assertion is a usual edit the trace gate does not stop. Define the loss in the `054` WHEN (the smaller of the rise of the not-covered count and the fall of the covered count, or the fall alone when the not-covered count falls) and widen the WHEN to cover a falling not-covered count.
- [ ] F2 major `openspec/changes/archive/2026-09-17-fix-covered-count/proposal.md:34` The limit `uncalled-function-branches` is true but not complete, so it does not name the loosening this change makes. `loss = min(rise, fall)` passes whenever the **rise** is at most the tolerance, whatever the fall is: for a tolerant file, covered branches or functions may fall by 40 while the not-covered count rises by 1 and the gate is silent. The pre-change rule (`now < before - tolerance`) stopped that. This is the same uncalled-function shape as design.md:31, in its commoner variant: the outer function becomes not covered (rise 1) and V8 drops its nested functions from the total (fall 1+k). The limit names only the "not-covered count unchanged" case, so this one is in neither the gates nor the limits. Either add the clause ("or the not-covered count rises by at most the tolerance, on a file with the tolerance conditions") to the limit, or keep the old covered-count comparison as a second check for tolerant files — it costs nothing on the measured data, since `labelArbiter` holds 355 covered in both runs and `placeSearch` has tolerance 0.
- [ ] F3 minor `.../proposal.md:25` The Impact's changed-files list omits `openspec/trace/gaps.json` and `openspec/trace/links.json`, both of which this diff changes. `gaps.json` is where the banked 52/407 the next two Impact lines discuss actually lives, and `simplify-ledger` lists it by the same convention. Add both.
- [ ] F4 minor `.../design.md:51` "Each new assertion fails against the code before this change" is untrue. Both new `054` assertions pass against the pre-change code: for `measuredLoss` (covered 20→17) and for `fewerNested` (covered 7→6, tolerance 0) the old covered-count rule also raised `LEDGER-LOST-COVERAGE`. They discriminate only against the intermediate `Math.min` form. Narrow the claim to the assertions that do fail (`070`, `072`'s `noLoss`, `073`'s measured case).
- [ ] F5 minor `.../design.md:31` "The not-covered count then falls" holds only when two or more nested functions drop; with one, the uncalled function's own +1 cancels it and the count is unchanged or rises. Say "can fall".
- [ ] F6 minor `.../proposal.md:35` `requirement-text-less-exact` is honest and I accept keeping it open, but it does not say enough: it omits the direction. The code allows **more** than the stated bound, never less, so a reader who trusts the requirement expects a stop that does not come. Add "the code allows a larger covered-count difference than the tolerance" to that limit.

The Impact numbers check out: `placeSearch` still 23 branches with 3 not covered in `gaps.json`; `labelArbiter` 405−50 = 407−52 = 355 covered in both; two history lines, both naming `fix-covered-count`, with the totals line that `LEDGER-TOTALS-NOT-BASE` requires. The `072` arithmetic (9 vs 8 at the edge) and the `078` cases are correct.
