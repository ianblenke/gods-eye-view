**Verdict: PASS** — no major. The two cases are described correctly in the delta spec and the known limit; one sentence in `design.md` lost a qualifier in the round-3 rewrite.

```
Verdict: PASS
- [ ] F1 minor design.md:43 "In the first case the gate records the entry as not current." False when the file has the tolerance conditions, which is the very case this change exists for. compareLedger pushes stale only when `!tolerant` (ledger.mjs:271), so placeSearch 23/3 -> 22/3 with sameAsBase gives {errors: [], stale: []} — asserted at ledger.test.mjs:160. gap-ledger-078 splits this correctly in its last two ANDs; the design sentence keeps only the "without the tolerance conditions" half. The wording is STE S1's dictation, so the loss of scope came in with the fix, not from the author. Fix: "In the first case the gate records the entry as not current, unless the file has the tolerance conditions."
- [ ] F2 minor design.md:31 and proposal.md:34 S8 added "with covered branches of its own" to proposal.md:34 precisely to separate it from the identical sentence at design.md:31, which describes the opposite shape (the not-covered count falls). The qualifier was then added to design.md:31 too, so the two sentences are again identical in the clause meant to tell them apart. It also fits design.md:31 least well: for the branches, the uncalled function's own covered branches turning uncovered pushes the not-covered count up, against the fall that sentence explains. Fix: drop the qualifier at design.md:31; its distinguishing feature is the removed nested functions.
- [ ] F3 minor src/tooling/spec/ledger.test.mjs:186 S5 named this line and quoted its text, but the corrected sentence landed at design.md:31 instead. Line 186 still reads "A caller in another file that no longer calls a function removes its nested functions too" — the caller, not V8, as the agent. STE's call whether it still matters; I report it only because the fix went to the neighbour and not to the cited line.
- [ ] F4 minor design.md:51 (line not edited this round) "The two new `gap-ledger-054` assertions, for a not-covered count that falls, fail only against the middle `Math.min` form." Only one of the two is. The other (ledger.test.mjs:185, placeSearch 3/23 -> 5/22) has the not-covered count rising: loss = min(2, 3) = 2 > 0, so it stops under the old code, the middle form and the shipped code alike. Fix: "The new `gap-ledger-054` assertion for a not-covered count that falls fails only against the middle `Math.min` form."
```

**1. The two cases, read against `lossOf` and `toleranceOf`.** The no-stop set for a real fall is exactly `0 <= rise <= tolerance`: `rise >= 0` gives `loss = min(rise, fall)`, `rise < 0` gives `loss = fall`. Case 1 is `rise = 0`, case 2 is `0 < rise <= tolerance`, and case 2 is unreachable without the tolerance conditions because `compareCoverageEntry` defaults to `() => 0`. `gap-ledger-054`, `gap-ledger-078` and the `uncalled-function-branches` bullet all say this, and the 400/30 -> 361/31 numbers check out: rise 1, fall 40, tolerance `min(8, floor(361 * 0.04)) = 8`, no stop. A reader can predict the gate from the prose alone, with F1 the one exception.

**2. Neighbours.** The S1 fix at design.md:43 did not leave its old claim standing anywhere else; nothing carries "corner", "still does not stop" or the line-tolerance reason any more. F2 and F3 are the two places where this round's edits moved but did not settle.

**3. Impact.** Verified against the tree: `gaps.json` has a zero-byte diff and is correctly absent from the list; the six named files are exactly the six modified paths. history.jsonl gains four lines, two pairs (branches 50->52 with totals 405->407, then back), matching "two ratchet runs added two history lines each" and naming `src/data/labelArbiter.js`. 405-50 = 407-52 = 355 covered. `gaps.json` holds placeSearch at 23 branches with 3 not covered.