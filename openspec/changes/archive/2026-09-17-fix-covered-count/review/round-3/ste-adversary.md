Verdict: FAIL

- [ ] S1 major `openspec/changes/archive/2026-09-17-fix-covered-count/design.md:43` "The corner needs the tolerance conditions, because the line tolerance is 0 without them." Untrue for the first of the two cases the paragraph names. When the not-covered count stays the same the loss is 0, so `lossOf` (scripts/spec/lib/ledger.mjs:48) gives no error with or without the tolerance conditions: `gap-ledger-078` and src/tooling/spec/ledger.test.mjs:162 show only LEDGER-STALE, and the ratchet then banks the smaller total. The reason also names the line tolerance, which governs no branch or function metric here; for the second case the branch tolerance is the operative one. Write: "The second case needs the tolerance conditions, because the tolerance is 0 without them. In the first case the gate records the entry as not current."
- [ ] S2 major `.../proposal.md:25` "The counts of that file moved and then moved back." Articles and nouns. "that file" names no file in this bullet. The bullet lists seven files, one of which (`scripts/spec/lib/ledger.mjs`) has coverage counts, so the sentence reads as that file. The four history lines are all for `src/data/labelArbiter.js`, which the proposal names only three bullets later. Write: "The branch counts of `src/data/labelArbiter.js` moved and then moved back."
- [ ] S3 minor `.../design.md:31` "the loss must use the fall when the not-covered count falls" Judged: dropping "alone" left "the fall" with the wrong nearer antecedent, because the sentence before names two falls and the "when" clause names the not-covered one. The code block above anchors `fall`, so a reader recovers, but the 25-word limit is the only reason the count is not named. Write two sentences: "`Math.min` of a negative difference and a positive one returns the negative number. So the loss must use the fall of the covered count when the not-covered count falls."
- [ ] S4 minor `.../proposal.md:35` "gets a bound the code does not keep for the covered count" Judged: the new sentence "The code can allow more than that bound, never less" gives the direction and removes the ambiguity I named, so the split works. But "keep" is "to retain" in tasks 4.1 to 4.6, in `gap-ledger-073` and at proposal.md:27. The same bullet already writes "the code follows them". Write: "a bound the code does not follow for the covered count".
- [ ] S5 minor `src/tooling/spec/ledger.test.mjs:186` "A caller in another file that no longer calls a function removes its nested functions too" Judged: the S1 correction belongs here, but the merge with S12 made the caller the agent of "removes". V8 removes them, and "its" now has two readings. Write: "When a caller in another file no longer calls a function, V8 removes its nested functions too".
- [ ] S6 minor `.../design.md:43`, `.../design.md:55`, `.../proposal.md:34` "The corner", "this corner" Approved words. "corner" is the place where two lines meet; it has no approved meaning of "rare case". Each of the three places writes "two cases" or "this shape" one sentence earlier. Write "case" each time.
- [ ] S7 minor `.../proposal.md:29` "The covered branch count stays 355 for both counts" One word, one meaning. "count" is the total, the not-covered count and the covered count in the two bullets around it. Write: "for both runs".
- [ ] S8 minor `.../proposal.md:34` "A caller in another file that no longer calls a function gives this shape." One word, one meaning. design.md:31 gives the same sentence for the opposite shape, where the not-covered count falls. Both are true, of different functions. Write: "A caller in another file that no longer calls a function with covered branches of its own gives this shape."

Not reported, as instructed: the "Count tolerance" requirement text. The last AND of `gap-ledger-054` ("the tolerance, which is 0 without the tolerance conditions") now agrees with `gap-ledger-078` and with the default of `compareCoverageEntry`; the loss definitions in `gap-ledger-054` and the numbers in `uncalled-function-branches` (30 of 400 to 31 of 361, 40 covered branches) agree with `lossOf` and `toleranceOf`. The Impact list is correct: `openspec/trace/gaps.json` has no diff now, because the second ratchet run moved `src/data/labelArbiter.js` back to 50 of 405.
