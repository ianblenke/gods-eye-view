**Verdict: PASS** (four minor findings; no major — I could not find a shape the new `054` lines let a weaker implementation pass, and the two limits are true and complete except for one direction word).

```
Verdict: PASS
- [ ] F1 minor openspec/specs/gap-ledger/spec.md:59-64 (gap-ledger-054) The rewrite dropped the positivity condition. The old text carried "AND the covered count is smaller than in its entry"; the two new AND lines are purely definitional, and the fourth line's first disjunct ("the file does not have the tolerance conditions") then holds for every unchanged non-tolerant file. Literal reading: any unchanged file without the tolerance conditions stops the build, loss or no loss. The code does not do this, and gap-ledger-078's own third bullet and its test (codes = ['LEDGER-STALE'] for 23/3 -> 22/3, no sameAsBase) assert the opposite. Fix: make the last line "AND the loss of that metric is above the tolerance, which is 0 without the tolerance conditions" — that matches compareCoverageEntry's default tolerance = () => 0 and removes the contradiction in one edit.
- [ ] F2 minor openspec/changes/archive/2026-09-17-fix-covered-count/proposal.md:109 (uncalled-function-branches) The limit is true and complete about scope but wrong about direction. "still does not stop" claims the corner predates this change; both named cases are new. Before this change the rule was stop iff fall > tolerance, so rise 0 / fall 1 / tolerance 0 (the placeSearch shape) and rise 1 / fall 40 / tolerance 8 (the 400/30 -> 361/31 shape) both stopped. Replace "still does not stop" with "no longer stops". Also, "One measured run moved from 30 not covered of 400 to 31 of 361" reads as a V8 observation; it is a shape measured against the code, not a coverage run — say so.
- [ ] F3 minor proposal.md:100 The Impact lists `openspec/trace/gaps.json` under changed files. It is not changed: `git status` shows 11 modified/added paths and gaps.json is not among them, and `git diff HEAD -- openspec/trace/gaps.json` is empty. The round-2 F3 fix overcorrected. Remove it (this is consistent with the next Impact line: the ratchet run kept the placeSearch entry, so nothing was written).
- [ ] F4 minor proposal.md:100 "The final ratchet run added two history lines. An earlier run added none." history.jsonl gains four lines naming this change, in two pairs: branches 50->52 with totals 405->407, then branches 52->50 with totals 407->405. One run cannot write both directions for the same file and metric, so two runs added two lines each. Correct the sentence to "Two ratchet runs added two history lines each: the file's counts flipped and flipped back."
```

**Scope of the two limits — complete.** The no-stop set is `min(rise, fall) <= tolerance`, i.e. `rise <= tolerance` or `fall <= tolerance`. The second is the bound the requirement already grants and is unchanged from the old rule; the first is exactly the limit's two cases (rise 0, or 0 < rise <= tolerance), and it names both with numbers. `requirement-text-less-exact` is accurate, including "never less": a stop needs both differences above the tolerance, so the code never stops for a difference at or below it. Its "six scenarios" count is right (069-074).

**Your rejection of my round-2 second fix: accepted, and your reasoning holds.** I had scoped that second check to tolerant files, but `src/search/placeSearch.js` in the failing run *is* tolerant (unchanged, same as base, true coverage) and its branch total of 23 puts `toleranceOf` at 0. The old comparison `now < before - 0` fires on 23/3 -> 22/3, so the second check reintroduces the exact stop this change exists to remove. Your alternative — name the variant in `uncalled-function-branches` with the measured numbers and defer a stricter rule — is the right call for this change.

No finding on `lossOf` itself. I tried `Math.min(rise, fall)` (blocked by the `fewerNested` case), `fall` alone (blocked by 078), and `rise` alone (blocked by both `fewerNested` and the 072 edge case at rise 9 / fall 8 / tolerance 8). The message satisfies 054's final AND: two current counts, two entry counts.
