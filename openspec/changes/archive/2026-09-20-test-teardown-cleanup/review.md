# Review: test-teardown-cleanup

Verdict: PASS
Reviewers: spec-adversary, ste-adversary
Date: 2026-09-20
Gates: make gates CHANGE=test-teardown-cleanup passed
Rounds: 3
Scope: diff 6a5e581
Reviewed-Tree: d3575375b773d8225cbb2de9b91391ab4a9ce0c01b8ce25cb6ac08b766432398

## Findings

- [x] Round 1 spec-adversary F1 (minor): a task/scenario count wording mismatch in `tasks.md` section 5 ("two tests" where the diff shows one test with two timers). Corrected.
- [x] Round 1 ste-adversary S1–S19 (19 minor): inconsistent terminology for "live timer" across five different names, several `-ing` words the STE lint's own check does not catch, passive voice in descriptions, and two `tasks.md` items packing more than one instruction per checkbox. All corrected.
- [x] Round 1's first spec-adversary attempt ran against the wrong repository copy (a Claude subagent launched with `isolation: "worktree"`, which built its worktree from `gods-eye-view` rather than the tree the change actually lives in) and reported a false PASS. A corrected re-run, pointed at the right directory, found the real defect below. Recorded as the reason this round needed a second spec-adversary launch, and as the origin of the mid-review switch to `codex` for review agents.
- [x] Round 2 spec-adversary F1 (major): the round-1 fix for `src/annotations/annotationEngine.test.mjs` passed `outlineRetryDelaysMs: []` to skip its retry wait entirely, instead of clearing a real timer like the other six files. That silently dropped the only test exercising the real link between `engine.clear()` and `isStale()`'s read of its state after a real wait — the isolated retry-function unit tests use a fake `isStale` and never touch this path. Corrected: kept the default delays, captured the real timer id, cleared it once `engine.clear()` ran and the task's own promise settled, and added one new assertion (the one exception to "no assertion changes" in this whole change).
- [x] Round 2 spec-adversary F2 (minor): "no code file changes, so no coverage requirement moves" was imprecise — a test-only change can still move a coverage total for a file it exercises differently, which happened here for `src/data/groundFloor.js`. Corrected.
- [x] Round 2 ste-adversary S20, S21 (major): the absolute "no test name and no assertion" line conflicted with the new exception, and a task line's phrasing could be read as one added assertion per test rather than one for the whole change. Both corrected with an explicit carve-out and unambiguous wording.
- [x] Round 2 ste-adversary S22–S24 (minor): a task item packing several instructions (the same fault as round 1's S18/S19), several perfect-tense verbs, and two more non-technical `-ing` forms. All corrected.
- [x] Round 2's own fix (the round-1 correction above) was itself still broken: it captured the real retry-wait timer id but cleared it in the test's own teardown before the timer ever fired, so `isStale()` still never ran and the new assertion only proved a timer got created, not that the retry logic actually reads `clear()`'s state after a real wait. This was caught by the round-3 spec-adversary review, not by the lead in advance — see below. Corrected in round 3 by mocking the clock from before `annotate()` ever runs (not from after `clear()`), so the retry wait genuinely runs to completion on the mock clock and `isStale()` genuinely runs. Proved with a named mutation: forcing `isStale()` to always answer false makes the fetch count rise from 2 to 4, and the new assertion catches it.
- [x] A related, incidental defect the lead found and fixed while writing the round-2 correction: `isStale()`'s check is `myGen !== generation || controller.signal.aborted || !annotations.has(anno.id)`, and `clear()` increases `generation` before it aborts its controllers. So the generation check short-circuits the abort-signal read, which is unreached from `clear()`'s only call site. The change's own prose (round 2's wording) had wrongly attributed the mechanism to the abort signal; round 3 corrects it to name the generation counter throughout.
- [x] Round 3 spec-adversary F3, F4 (minor): `design.md`'s account of the round-2 draft's fault conflated it with a separate, third, previously-unreviewed draft the lead wrote and discarded while fixing round 2's finding (that third draft enabled the mock clock only after `engine.clear()` ran, so the real timer it raced against already existed outside the mock clock's control — the one draft that genuinely would have left a real timer running past the test). And a file count said "six files" get a plain timer clear, when only five do (one uses the mock clock, and `testGuard.test.mjs` has no fix at all). Both corrected: all three drafts are now named and distinguished, and the count is accurate.
- [x] Round 3 ste-adversary S22, S27–S30 (minor): one more packed-instruction task item, several more `-ing` words and phrasal verbs, and one task line not starting with an imperative verb. All corrected.
- [x] Round 3 ste-adversary S25, S26 (major): both quoted text that had already been corrected — by the very same round's spec-adversary findings F3/F4 — at the moment the ste-adversary agent took its read of the file. The two review agents ran in parallel against the same starting commit; the spec-adversary's corrections landed before the ste-adversary's report was read, but after the ste-adversary had already taken its own snapshot. The lead independently re-verified both flagged phrases were absent from the current file by direct text search. The gate's own `REVIEW-AGENT-NOT-PASS` check reads only the literal `Verdict:` line in `review/ste-adversary.md`, so a disposition note next to a `Verdict: FAIL` line does not satisfy it, and there is no accept-a-major path past round 3. The user was asked and chose a narrow, targeted re-check with the same reviewer over editing the verdict line directly.
- [x] A narrow ste-adversary re-check, scoped only to confirming S22 and S25–S30 (not a new full round — same diff, same commit base), gave `Verdict: PASS`: all seven were resolved. It found three further minor findings on the corrections themselves (S31–S33: two more `-ing` forms, one passive-voice sentence, one "would have" construction outside the permitted simple tenses). All three corrected, though not required for the PASS verdict already given.

## Known limit carried into `proposal.md`

`src/tooling/spec/testGuard.test.mjs`'s leak did not reproduce in any isolated run — five repeats by the implementer, three more by the lead, and confirmed clean again during the change's own full-project `make ratchet` runs across all three rounds. No fix was made; no test or timer was ever named to fix. This is recorded as an open known limit, not silently dropped.
