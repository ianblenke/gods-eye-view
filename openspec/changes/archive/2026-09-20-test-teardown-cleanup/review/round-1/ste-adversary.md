Verdict: PASS

Reviewed the full change folder at openspec/changes/archive/2026-09-20-test-teardown-cleanup/ (proposal.md, design.md, tasks.md). No delta specs in this change. Confirmed the merge (2483a2e..e86489c) touches no line of AGENTS.md, .claude/agents/ or .claude/commands/opsx/review.md, so nothing there is in scope. Confirmed against the actual test diff that no `test(`/`it(`/`describe(` line changed anywhere — the "no test name changes, no assertion changes" claim in the prose holds. No test names to check (none added or renamed).

Findings: all minor (no major — no two-meaning ambiguity, no disagreement with code/specs found).

- [ ] S1 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/proposal.md:3 "any file whose process leaves a live timer at exit" Approved words. Not sure "whose" is an approved STE relative pronoun — it is not in openspec/ste/words.json and STE's approved pronoun set is narrow. Write: "a file with a process that leaves a live timer at exit".
- [ ] S2 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:3 "the file whose process still holds a live timer at exit" Approved words. Same concern as S1. Write: "the file with a process that still holds a live timer at exit".
- [ ] S3 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:3 "files `teardown-guard` touches" Articles and nouns. Omitted relative pronoun. Write: "files that `teardown-guard` touches".
- [ ] S4 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/proposal.md:3,5 and design.md:3,5,11,49 One word, one meaning. Five names for the same thing: "live timer" (proposal:3, design:3), "stray timer" (proposal:5), "dangling timer" (design:5), "timer still armed"/"armed" (proposal:5, design:5,49), "stray resource" (design:11). Write: pick one term (e.g. "live timer") and use it everywhere.
- [ ] S5 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:16 "the existing coverage-gate requirement" Words that end in -ing. "existing" is not a technical name. Write: "the coverage-gate requirement already in place".
- [ ] S6 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:22 "the losing side's timer keeps running after the winning side resolves... by moving the wait behind whatever guard already exists, or by a clear inside a hook that always runs... by capturing the timer id and clearing it" Words that end in -ing. None of losing/running/winning/moving/capturing/clearing is a technical name. Write: "the timer on the side that loses stays live after the side that wins resolves... moves the wait behind whatever guard already exists... captures the timer id and clears it".
- [ ] S7 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:22 "The first shape is fixed by... The second is fixed by..." Voice. Passive in a description where the active is possible. Write: "The fix for the first shape moves the wait... The fix for the second shape captures the timer id and clears it...".
- [ ] S8 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:24 "A single shared teardown helper was considered and rejected." Voice. Write: "This change considered and rejected a single shared teardown helper."
- [ ] S9 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:28 "This was confirmed by reproducing `teardown-guard`'s own guard environment against the file directly." Voice + Verbs. Passive, plus "reproducing" as a gerund noun. Write: "`teardown-guard`'s own guard environment, run directly against the file, confirmed this."
- [ ] S10 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:28 "by running the file many times" Words that end in -ing. Write: "by many runs of the file".
- [ ] S11 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:48 "A cleared timer was doing useful work." Verbs. Past continuous, not simple past. Write: "A cleared timer did useful work."
- [ ] S12 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:48 "not a repeating poll a feature depends on" Words that end in -ing. Write: "not a poll that a feature depends on and that repeats".
- [ ] S13 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/design.md:49 "no failing case to prove it against" Words that end in -ing. Write: "no case that fails, to prove it against".
- [ ] S14 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/proposal.md:27 "by running the file's child-process tests under load instead" Words that end in -ing. Write: "by many runs of the file's child-process tests under load instead".
- [ ] S15 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/tasks.md:6 "A leaking file prints every result" Words that end in -ing. Write: "A file that leaks prints every result".
- [ ] S16 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/tasks.md:13,49 "the losing side of a race" / "each losing timer" Words that end in -ing. Write: "the side of a race that loses" / "each timer on the side that loses".
- [ ] S17 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/tasks.md:70 "no failing case to prove a fix against" Words that end in -ing. Write: "no case that fails, to prove a fix against".
- [ ] S18 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/tasks.md:1 "Read `AGENTS.md` first, then read `teardown-guard`'s own `design.md` for the diagnostic method." Instructions. Two sequential instructions in one task item, not actions at the same time. Write: split into "1.1 Read `AGENTS.md` first." and "1.2 Read `teardown-guard`'s own `design.md` for the diagnostic method."
- [ ] S19 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/tasks.md:14-16 (and the same pattern repeated at 23-25, 32-34, 41-43, 50-52, 59-61, 73-74) e.g. "Run the file. Confirm every test still passes, with the same names and assertions." Instructions. Each of these task items packs two or three sequential instructions (Run/Confirm, or Run/Confirm/Report) under one checkbox, not actions at the same time. Write: give each task item one instruction — split "Run", "Confirm" and "Report" into separate line items, or separate checked tasks.

No findings against check 9 (OpenSpec words) — no SHALL/SHOULD/MAY anywhere in the change's prose. No noun groups over three found.
