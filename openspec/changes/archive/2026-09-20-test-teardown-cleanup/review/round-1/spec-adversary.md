Reviewed test-teardown-cleanup at commit e86489cde5bf4ad79cbf5ffc598f540ae1f0ad90 (branch test-teardown-cleanup, /home/ianblenke/docker/gev-leak-cleanup), full diff against base a5b678b (the six changed test files, trace files, and the three new change-folder docs — no production code file changed, confirming the proposal's claim).

Verdict: PASS
- [ ] F1 minor openspec/changes/archive/2026-09-20-test-teardown-cleanup/tasks.md:48-52 Section 5 says "Find the two tests and the two timers that outlive them" and "Report the two tests that held a timer," but the diff for src/annotations/annotationEngine.test.mjs only changes one test ("outline queue: clear drops queued-but-unstarted upgrades..."). The two timers are the two concurrently in-flight upgrades (OUTLINE_UPGRADE_CONCURRENCY = 2) inside that single test, not two separate tests. Reword to "one test, two timers" so the record matches the diff.

What I checked closely, per your specific asks:

1. No fix weakens what a test proves. I traced each of the six diffs against the production code it exercises:
   - sharelink.celestial.test.mjs: `clearTimeout(manager._debounceTimer)` — confirmed `completeInitialRestore()` → `_scheduleUpdate()` really does arm a real timer (src/sharelink.js:462-463); the clear is necessary and inert otherwise.
   - localServices.test.mjs: intercepts setTimeout via `t.mock.method`, but server/standalone/key-setup.js has exactly one setTimeout call site (the 250ms restart, key-setup.js:330), fired once per successful save — no risk of losing an earlier call under the single capture variable. (Minor style note only, not a finding: the fix clears the same timer three redundant ways — t.mock's own auto-restore, `t.after`, and an inline `clearTimeout` — harmless.)
   - gevRealtime.test.mjs: `renderFreshCesiumFrame` calls setTimeout exactly once per invocation (gevRealtime.js:2336, the losing side of a postRender-vs-timeout race), so capturing "the" timerId per test is correct, not lossy.
   - manager.test.mjs: both races capture `giveUpTimer` synchronously at Promise construction and clear it unconditionally after `Promise.race`, regardless of which side wins — correct in both tests.
   - militaryInstallations.test.mjs: verified `resolveGroundFloorCellsBounded` (src/data/groundFloor.js:506) is called unconditionally, even with an empty points array (src/layers/installations/ingestion.js:147), so the second test's "0 elements" scenario still arms a real, previously-orphaned 1200ms timer — the fix targets a real leak, not a phantom one. In the first test, task notes flagged two candidate await sites (lines 575 and 632 in the pre-fix file); I confirmed only the fetch-mock's injected delay (575) is an orphaned race loser, while the polling loop's setTimeout (632) is fully sequential/awaited and was correctly left untouched.
   - annotationEngine.test.mjs: this is the one case where a fix changes *behavior under test* rather than just adding cleanup — it passes `outlineRetryDelaysMs: []` instead of clearing a timer id, which changes which internal branch of `resolveOutlineWithRetry` executes for the two in-flight (then-aborted) queue tasks. I checked whether this drops real coverage/verification of the retry/backoff/isStale logic: it doesn't — that logic is already directly and thoroughly unit-tested elsewhere in the same file (lines 91-150) with a fake `waitFn`, independent of this test. Gate output and the ledger diff (only one totals-changed entry, for an unrelated file) confirm no coverage regression resulted.

2. "No test name, no assertion changed" — verified literally true across all six diffs; only timer-capture/clear code and one config parameter were added.

3. testGuard.test.mjs known-limit handling is legitimate: the file has zero diff (confirmed via git diff), tasks.md 8.1-8.4 documents 5+3 independent repro attempts (implementer + lead) all clean, 8.2 is honestly left unchecked with "no reproduction means no failing case to prove a fix against," and the proposal's "Known limits" section names this gap explicitly — matches AGENTS.md rule 13 (don't guess a fix with no failing case) and the spec-adversary's known-limits check.

Also checked: the one ledger/history.jsonl entry this change produced (src/data/groundFloor.js branch-total 135→136, "totals changed") is expected/spec'd behavior per gap-ledger spec.md's Requirement "Count tolerance" (gap-ledger-057), and matches an identical prior pattern for the same file from an earlier unrelated change (simplify-ledger, 2026-09-16) — not a hidden regression. Trace files (links.json, gaps.json) show no new scenario tags, consistent with the no-spec-delta claim.
