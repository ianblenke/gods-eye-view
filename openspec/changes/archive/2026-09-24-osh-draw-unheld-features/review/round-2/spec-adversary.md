Verdict: PASS
- [ ] F1 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:13 The lead's correction of S11 and F11 is incomplete. S11 named "small part of the whole" at proposal.md:13, and the line still says "the measured fresh set is a small part of the whole". These lines also state measured shape of the owner's server: proposal.md:40 and :44 ("a location that carries a uid also carries an id"), design.md:115 (same claim), and design.md:5 ("the dropped case is common"). The diff changed proposal.md:40 and design.md:115 from "the measured server" to "the owner's server", which keeps the claim. Constraint 2 is not broken, because no name, id, count or place appears. But the claim has no test (`osh-no-live-test`), and proposal.md:44 uses it to dismiss `osh-feature-key-collision`. Remove the proportion and the shape claims, or record them as accepted by name in `review.md`.
- [ ] F2 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:3 The rewritten first sentence says the collection "does not hold every feature that its location streams name". The unchanged third sentence says "The features the streams name are not in the collection at all". Read together, the third sentence says no named feature is held. Spec `osh-042` and its tests say a held feature is moved by its location. Write "The features that the layer does not hold are not in the collection at all."
- [ ] F3 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:70 and :72 (also proposal.md:19) The corrected D54 text is still false in three places.
  - "the layer creates and removes no entity" (line 70). `update()` calls `_dataSource.entities.removeAll()` (src/layers/osh/index.js:458) and adds a new entity for each feature (index.js:521) on every refresh. Write "the set of entity ids does not change".
  - "Only the label, the entity properties and `placed.streamFeatures` differ" (line 70, and the same list in proposal.md:19). After a failed features read, `pollSelected()` sets `name: featureRecord?.name ?? null` (index.js:191). The detail of a held feature then shows its id in place of its name, until the next refresh. No test pins this and no known limit names it. Add it to the list, or add a known limit.
  - "as it does today" (line 72) has no fixed time. Once this change lands it is false, because a failed read now removes only the features that have no fresh location. Write "as it does before this change".
- [ ] F4 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/specs/osh/spec.md:223 (synced at openspec/specs/osh/spec.md:491) The clause "the layer keeps no record of a stream-drawn feature after a refresh or after `destroy()`" is false when read literally. After a refresh that draws the feature, `_placedFeatureById` (index.js:423) holds its record. The click handler reads that record (index.js:258) to set `selectedId` to the location's `systemId`, as the clause at line 219 requires. The rule the code follows is different: the layer does not carry the record from one refresh to the next, and `destroy()` clears it. Write the clause that way. The tests for it are the `[osh-059]` removes-its-entity test and the `[osh-059]` destroy test. If you change the delta, sync the main spec.
- [ ] F5 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:24, :37, :58, :88 The lead added or moved these four lines with four spaces of indent, so each one nests under the item above it. A6 (line 37) reads as a sub-step of A5, and L12 (line 88) as a sub-step of L11. A13 (line 38) sits between A6 and A7, and L7d (line 90) sits after L13, so the ledger order is A5, A6, A13, A7. Use the same indent as the sibling items. Put A13 after A12 and L7d after L7c, or leave the order and say why.

Note: I had only Read, Grep and Glob. I did not run `node --test` or any mutation. I checked the 34 ledger ids of `tasks.md` against the tests by reading, and each fails the test the ledger names. That includes A13 and L7d, the two entries for F1 and F2 of round 1. I read the tree at HEAD 95e18a5a932be9657423ffbbfa07696861ae1d80 on branch osh-draw-unheld-features.

Round-1 corrections that I checked and found right:
- F1: the newer-first test at src/data/oshSystems.test.mjs:664 fails under a last-wins rule.
- F2: the `[osh-032]` layer test at src/data/oshLayer.test.mjs:2424 selects held FEATURE_A. Its host `sys-fixture-1` is the id of SYSTEM_A, so it goes through `pollSelected()` (index.js:186-193). It asserts `<h3>Feature A</h3>` and `Placed by Stream Beta (42 s)` with literal values.
- F3: the `[osh-059]` unplaced test now asserts `placed.stream` at zero (oshLayer.test.mjs:2478). The `[osh-057 osh-059]` text is gone from every file that is not under `review/`.
- Sync: the synced `openspec/specs/osh/spec.md` matches the delta for `osh-032`, `osh-042`, `osh-057`, `osh-058`, `osh-059` and `osh-060`.
- Carried scenarios: the Records scenarios and `osh-028` to `osh-033`, `osh-046` and `osh-049` are identical to the archived `osh-location-streams` delta.
- Trace: `links.json` holds both new test names. `gaps.json` has no entry for the three changed source files.

Constraints 1 to 4 hold for the diff. It changes no provider file, uses only `foi-fixture-`, `sys-fixture-` and `ds-fixture-` ids, calls no real server, and renames no existing test.

The gate output does not itemise the 296 STE warnings, so I could not tie any of them to this change. The one error, REVIEW-MISSING, clears when `review.md` exists.
