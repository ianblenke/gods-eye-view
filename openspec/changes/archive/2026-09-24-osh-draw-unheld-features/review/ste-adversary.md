Verdict: PASS
- [ ] S31 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:19 "A features getter that throws sets `partial:true`. It changes the label, the entity properties and `placed.streamFeatures` for one refresh." Voice and articles. The getter does not set `partial`, and the layer sets it (index.js:399, osh-046). "It" can mean the getter or `partial:true`. The same wrong actor is at design.md:19 ("Then a features getter that throws keeps that id and that position") and design.md:62 ("only a throw sets `partial:true`"; "throw" is a verb used as a noun). My round-1 S7 text had this flaw. Write: "When the features getter throws, the layer sets `partial:true`. For that refresh, the layer changes the label, the entity properties and `placed.streamFeatures`." design.md:19: "Then, when the features getter throws, the layer keeps that id and that position." design.md:62: "and only a getter that throws makes the layer set `partial:true`."
- [ ] S32 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:66 "names a feature by the `foiId` of its record" Articles and nouns. An unheld feature has no record. A location is also a record (osh-052 "location records"). The same phrase is at design.md:19 and proposal.md:19 ("the `foiId` of the record"). The context makes the meaning likely, so this is minor. Write, as osh-060 says: "a location whose `foiId` is the `id` of the held feature".
- [ ] S33 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:70 "Only the label, the entity properties and `placed.streamFeatures` differ." This does not agree with the code or with the new limit `osh-failed-read-click-host`. With no held record, the detail shows the id and not the name (index.js:191 reads `_featureRecordsById` only). The host of a click can change. The paragraph before (line 68) gives a case where the entity id changes, so "Only" reads as unconditional. Write: "For a location with the `foiId` of the held feature, only the label, the entity properties, the name in the detail, the host of a click and `placed.streamFeatures` differ."
- [ ] S34 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:44 "a location can carry a uid that equals the id of a held feature but is not its uid. The layer then has two records with one id. Cesium refuses the second entity. ... the map is partly empty" Approved words and agreement with the code. oshSystems.js:163 uses `foiId || foiUid` as the key, and :156 matches a held id from `foiId`. So the collision needs a location with no `foiId`. The text does not say this. "refuses", "partly empty" and "needs" are figurative. Write: "a location that carries no id, and a uid that equals the id of a held feature but is not the uid of that feature. The layer then has two records with one id. Cesium throws an error for the second entity, so `update()` returns false and sets `error`. The map has only some of its entities for that refresh. On the owner's server, a location that carries a uid also carries an id, so only an unusual location causes this."
- [ ] S35 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:32 "A1: restore the `continue` for an unheld feature. Fails the draws-at-the-location test." Verbs. The new sentence "Fails ..." has no subject. It reads as an imperative in the wrong form, and "fail" is intransitive. The same pattern is on tasks.md:32-44, :73-90 and :102-104. It is also at design.md:92 ("The test that mutant M16 fails", "must fail both tests"), design.md:42 ("a test still fails on it") and tasks.md:45, :91, :105 ("that no test fails on"). Write: "Then the draws-at-the-location test fails." design.md:92: "The test that fails when mutant M16 runs keeps its name and every assertion." "The host-fallback mutation must make both tests fail." tasks.md:45: "If no test fails for a mutation, that is a finding. Stop. Add a test that fails for it, and then continue."
- [ ] S36 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:24 "    - `[osh-058]` a newer location that comes first ..." Structure. Four added lines have a 4-space indent, so Markdown makes each one a sub-item of the line before: tasks.md:24, :37 (A6 under A5), :58 and :88 (L12 under L11). Each is a sibling. A13, L13 and L7d (tasks.md:38, :89, :90) also break the id order. The heading at tasks.md:47 ("The layer: `osh-059`, `osh-060` and the `osh-057` clause") does not name `osh-032`, which task 3.1b now covers. Write: a 2-space indent for each of the four lines, and "The layer: `osh-059`, `osh-060`, `osh-032` and the `osh-057` clause".
- [ ] S37 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:31 "Run each mutation below on the adapter. Report one line for each mutation: the id and the test that failed." Instructions. This task gives two instructions in sequence, as 5.4 did in round 1 (S26). The same text is at tasks.md:72 and :101. Write: "Run each mutation below on the adapter." and add a sub-item "Report one line for each mutation: the id and the test that failed."
- [ ] S38 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:38 "Only a fresh location draws, and a location that is not fresh does not draw." Meaning. The second clause repeats the first, so the freshness bound of "bounded twice" is empty. proposal.md:13 still says "the measured fresh set is a small part of the whole". S11 named that line, and the lead reports S11 as corrected. It states a measured proportion of the owner's server, which nothing in the repository shows, and it is the borderline case of constraint 2. If the lead accepts it, accept it by name. Write in design.md:38: "The freshness gate lets only a location that is not older than `OSH_FRESH_MAX_AGE_MS` draw." Write in proposal.md:13: "Only a fresh location draws, so a stream that stopped more than an hour ago adds nothing."
- [ ] S39 minor src/data/oshSystems.test.mjs:664 "a newer location that comes first wins over an older location for one unheld feature" Test names. "wins over" is a phrasal verb, and "comes first" does not say where. Write: "a location that is newer and first in the list gives the drawn feature its position, and an older location for the same feature does not". Also src/data/oshLayer.test.mjs:2424 "shows its name": "its" can refer to the detail, the feature or the stream. Write: "shows the feature's name and Placed by with the age of the location". These tests are new, so a rename is allowed. It needs the same change in tasks.md:24 and :62 and a `make ratchet`.
- [ ] S40 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:3 "does not hold every feature" One word, one meaning. The collection "does not hold" the feature in proposal.md:3, proposal.md:39 and design.md:5. It "does not have" the feature in design.md:24 and :44, which are changed lines. One relation has two words. Write "does not have" for the collection in proposal.md:3, proposal.md:39 and design.md:5, and keep "hold" for the layer.
- [ ] S41 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:11 "One host can have many features. So the host's name is the only name a location carries" Meaning. The "So" claims that a host with many features is the reason for the name. The real reason is that a location carries the name of its system. design.md:7 has this middle sentence and the proposal does not. Write: "One host can have many features. A location carries the name of its system, not the name of a feature. So the host's name is the only name a location carries for an unheld feature, and many features share it." Also proposal.md:5 "But this server does not meet the premise of the rule." "meet" is figurative, and I am not sure it is approved. Line 7 says the same in the next paragraph. Write: "But the premise is not always true on this server."
- [ ] S42 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/specs/osh/spec.md:226 "one refresh holds a feature" (also :227 "the second refresh has a features getter", :229 "all three first refreshes hold", :232 "the fourth refresh removes") Articles and nouns. These changed lines keep the refresh as the subject of "holds", "has" and "removes". S27 named this pattern. "all three first refreshes" is unclear. Write: "the layer holds a feature in the first refresh, and gets a fresh location whose `foiId` is the feature's `id`", "in the second refresh the features getter throws, with the same location", "the first, second and third refreshes each hold the one entity `osh-foi:<id>`", "in the fourth refresh the layer removes the entity".
- [ ] S43 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:20 "A moved feature carries the stream fields for it." Round-1 corrections that the lead reports as done are not in the tree. S24: proposal.md:20, and design.md:98 "It draws it as a feature entity". S25: "grouping" at tasks.md:52 and in the test name at src/data/oshLayer.test.mjs:2196. S30: design.md:86 "An exception would need a position memory and a re-add branch" and design.md:104 "To draw nothing was weighed here too ... It is rejected." S22: "sits on one feature" (design.md:34) and "hard ceiling" (design.md:38). S16: "the feature list has no such feature" (spec.md:233). Apply the "Write:" text of those findings there, or list them as kept by name.

Note: this review had no shell tool. I ran no test and no mutation. I found no change to a request (constraint 1), no real server call (3), and no rename of an existing test (4). Every new fixture id has the synthetic prefix (2). S38 is the only borderline case of constraint 2.
