Verdict: FAIL
- [ ] F1 major src/data/oshSystems.js:165 The "newer wins" rule for an unheld feature has no test that pins it. The only test (src/data/oshSystems.test.mjs:627-662) lists the older location first and the newer second. Replacing the condition `!existing || isNewer(...)` with `true`, a last-wins rule, still passes every test, by reading. The spec (osh-058) says "at the newer location". Design D52 says the location list has no order across streams. Add a test with the newer location first and the older second. Assert the newer `lon` and `datastreamId`. Add "always overwrite" to the A-mutation ledger.
- [ ] F2 major src/layers/osh/index.js:186 and :192 The layer wiring of `Placed by` for a held feature that a stream moves has no test. The osh-032 clause and D57 require that line for a moved feature. Every layer test that asserts `Placed by` selects a stream-drawn feature (oshLayer.test.mjs:2297, 2330). No layer test selects a held feature with a `detailHost`. Changing line 186 to `effectiveFeature?.locationSource === 'stream'`, or adding `&& !featureRecord`, passes all tests, by reading. Add a layer test that holds `FEATURE_A`, gives it a fresh location and a literal age, clicks it and asserts the `Placed by` text and the feature name. Tag it `[osh-032]`.
- [ ] F3 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:58-61 (also design.md:90, oshLayer.test.mjs:1778) Task 3.2 says to retag a test `[osh-057 osh-059]`, and D56 says a separate `[osh-057 osh-059]` test exists. No test has two tags, so both texts are false. Task 3.2 also contradicts task 2.1, which forbids renaming an existing test. The osh-059 clause "`placed.stream` does not count it" therefore has no `[osh-059]` test that asserts it. Only the `[osh-057]` test at line 1799 asserts it. The trace gate cannot see this hole. Add a new test tagged `[osh-059]` that asserts `placed.stream === 0` and no `osh:` entity for the system of an unheld-feature location. Correct D56 and task 3.2 to match the tests.
- [ ] F4 minor src/data/oshSystems.js:163-166 A location with only `foiUid` draws under that key. If the uid equals the id of a held feature but is not its `uid`, `features` holds two records with one id. Cesium then throws in the draw loop (index.js:521). `update()` returns false, sets `error`, and the map is partly empty for that refresh. Skip a drawn key that a held id already uses, or add a known limit.
- [ ] F5 minor proposal.md:31 The proposal says `osh-location-streams` is merged but not archived, and says to archive it first. Tasks 1.1 and design Context say it is archived, and the archive folder exists. Remove or correct the bullet.
- [ ] F6 minor design.md:110 and design.md:70 The Risks bullet says the OSH layer sets neither `disableDepthTestDistance` nor `heightReference`. `entityAlwaysOnTop()` (index.js:34-38) sets both on the feature points, and osh-061 covers it. D54 also says "the fourth option in D52", but that option is the seventh. Correct both.
- [ ] F7 minor tasks.md:1-105 Every box is unticked, although the change is archived and its gates ran. The task list records no mutation result, though tasks 2.4, 3.5 and 4.3 say to report one line for each. Tick the finished tasks and record the results, or say where they are.
- [ ] F8 minor specs/osh/spec.md:211 (openspec/specs/osh/spec.md:479) The osh-059 clause "the map holds no entity `osh:<systemId>` for that location's system" is false when the host has a `Point` or another location places it. State that the unheld-feature location does not place its system.
- [ ] F9 minor src/data/oshSystems.test.mjs:236 and :493 Two existing test names now contradict osh-042: "is dropped for an unknown feature" and "dropped entirely". The first no longer asserts anything about the unknown feature. Constraint 4 blocks a rename. Record it as a known limit, and cover the drawn feature in a new test.
- [ ] F10 minor proposal.md:38 The limits section is named "Known limits left open". The review command and the spec adversary read "Known limits and later changes". Rename the section, or add that heading for the minor findings.
- [ ] F11 minor proposal.md:3, 13, 40 and design.md:5, 7, 111 The prose describes measured properties of the owner's server: "one host owns most of the features that lack their own record", "a location that carries a uid also carries an id", and the ages "from seconds to more than a day". There is no name, id, count or place, so this is not a listed constraint 2 violation. It is still stream-shape metadata. The owner must decide whether to keep it.
- [ ] F12 minor src/data/oshLayer.test.mjs (about 60 existing tests) The change rewrote the teardown of the existing layer tests to `t.after(() => layer.destroy(viewer))`, and no proposal, design or task lists that. Each rewritten test counts as a changed test for `idsOfChangedTests` (scripts/spec/lib/registry.mjs:202-216). That satisfies TRACE-ID-CHANGED-NO-TEST for any tag in the file with no real edit. Here osh-057 has a real changed test (line 1778), so nothing is hidden now. I saw no dropped assertion. Name the rewrite in the Impact section.
- [ ] F13 minor src/layers/osh/index.js:258 On a failed features read, a click on a drawn feature takes its host from the location's `systemId`, not from the feature record. D54 and osh-060 say that only the label, the properties and one count differ. No test pins this. Add it to the known limits.
- [ ] F14 minor gate output ERROR REVIEW-MISSING is the only error and clears when `review.md` exists. The 302 STE warnings are not itemised, so I could not tie any of them to this change.

Note: I had only Read, Grep and Glob, so I could not run `node --test`, `git diff` or any mutation. I traced every mutation of the ledger by reading. All 27 (A1 to A12, L1 to L12, D1 to D3) redden the named test, and F1 and F2 are the unlisted survivors. Checked without a diff: constraint 1 (no request added or changed, no provider file touched) and constraint 3 (all tests use fake sources) hold. Constraint 2 holds except for the F11 prose, and all fixture ids use the `foi-fixture-`, `sys-fixture-` and `ds-fixture-` prefixes. For constraint 4 I found no rename among the tests I read, but I could not compare against origin/main. I read the working tree of branch osh-draw-unheld-features, which has no commit. The base is origin/main 1f92646f.
