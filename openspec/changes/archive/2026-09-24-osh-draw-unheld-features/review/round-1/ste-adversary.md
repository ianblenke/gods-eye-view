Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:31 "which `main` has merged but not archived. Archive that change before this one runs its gates." One word, one meaning: this disagrees with design.md:11 ("`main` has archived that change"), with tasks.md:4 and tasks.md:7 (archived 2026-09-20, commit `66af434`), and with the folder `openspec/changes/archive/2026-09-20-osh-location-streams/`. It also uses the present perfect, "has merged". design.md:11 has the same tense problem ("has archived"). Write in proposal.md: "The base text of `osh-042` and `osh-057` is the delta of `osh-location-streams`. That delta is in `openspec/specs/osh/spec.md`." Write in design.md:11: "`main` archived that change on 2026-09-20, so ..."
- [ ] S2 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:110 "The OSH layer sets neither `disableDepthTestDistance` nor `heightReference` ... A separate change owns that fix. This change adds entities that show the same defect until it lands" This does not agree with the code or the spec. `osh-marker-depth` (D59 to D63) is archived. `entityAlwaysOnTop()` at src/layers/osh/index.js:34 sets both properties. `osh-061` and `osh-062` are at openspec/specs/osh/spec.md:572 and :580. A stream-drawn feature goes through the same loop (index.js:530). Write: "**The new entities follow `osh-061` and `osh-062`.** A stream-drawn feature draws in the same loop as a held feature, so its point is on top at every distance and it hides beyond the horizon."
- [ ] S3 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:70 "The fourth option in D52 would skip the draw when `partial` is true." The option that draws only when the features read succeeded is the seventh (design.md:46). The fourth is "Leave the rule and explain it". Write: "The seventh option in D52 skips the draw when `partial` is true."
- [ ] S4 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:90 "The layer gains one separate test tagged `[osh-057 osh-059]`" Three texts disagree with each other and with the code. tasks.md:60 says "Retag it `[osh-057 osh-059]`". tasks.md:83 says "the extended `[osh-057 osh-059]` test". src/data/oshLayer.test.mjs:1778 is an extended test that keeps its old name and its tag `[osh-057]`. openspec/trace/links.json:1123 holds that name, and constraint 4 forbids a rename. No test has the tag `[osh-057 osh-059]`. Write in design.md:90: "The layer extends the test `[osh-057]` a fresh location that names a feature moves the feature entity and never places a system. That test keeps its name and its tag. It also asserts no system entity and `placed.stream` at zero for a location that names an unheld feature." Write in tasks.md:60: "Keep its name and its tag `[osh-057]`. This is the changed `[osh-057]` test that the trace gate needs." Write in tasks.md:83: "L12: run the A2 mutation again with the layer suite. It must fail the extended `[osh-057]` test." The new tasks.md:83 text also starts with an imperative verb, as check 7 requires.
- [ ] S5 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:3 "`osh-042`, `osh-057` MODIFIED; the rest carried" and tasks.md:6 "Carried under Systems layer: ... `osh-032` ..." tasks.md:7, proposal.md:28, section 4 and the delta spec all treat `osh-032` as changed: it gets two new clauses. Line 3 leaves it out of MODIFIED, and line 6 lists it as carried. Write in line 3: "`osh-042`, `osh-057` and `osh-032` MODIFIED". Remove `osh-032` from the carried list in line 6.
- [ ] S6 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:9 "Write `design.md` with D52 to D57." design.md has D58 (design.md:102), and D58 is the decision that names the gates. Write: "D52 to D58".
- [ ] S7 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:19 "A failed features read changes only the label and one count, for one refresh." Three texts give three different readings. design.md:19 says "so a failed features read changes no entity". design.md:68 says "Only the label, the entity properties and `placed.streamFeatures` differ", then "changes the label and one count". Delta osh-060 also sets `partial:true`. The code (index.js:536, :547, :399) changes the label, the entity properties and the count. It also sets `partial`. The entity id and the position stay. Write in proposal.md:19: "A features getter that throws changes the label, the entity properties and `placed.streamFeatures` for one refresh, and sets `partial:true`. The entity id and the position stay." Write in design.md:19: "... so a features getter that throws keeps the entity id and the position." Write in design.md:68: "So a failed read changes the label, the entity properties and `placed.streamFeatures` for one refresh."
- [ ] S8 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:66 "Both give the same entity id `osh-foi:<id>` and the same position." The same claim is at proposal.md:19, design.md:68 ("no entity is created or destroyed on the way") and delta osh-060 (specs/osh/spec.md:228 and :232), and it is false for one input. A held feature has its record id (src/data/oshSystems.js:203-217). A location that names it only by `foiUid` moves it under that record id. The test at src/data/oshSystems.test.mjs:255 shows this. When the features getter throws, the same location draws the feature under its `foiUid` (oshSystems.js:163). The entity id changes, the layer removes one entity and adds another, and index.js:572 clears the selection. The `[osh-060]` test uses a `foiId` that equals the record id, so it does not show this. Write in design.md:66: "When the location carries the `foiId` of the held record, both cases give the same entity id and the same position." Add the same condition to osh-060. Add to `osh-feature-key-split` (proposal.md:40): "A location that names a held feature only by uid moves it under its record id. When the features getter throws, that location draws it under the uid, and the entity id changes for that refresh."
- [ ] S9 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/specs/osh/spec.md:211 "the map holds no entity `osh:<systemId>` for that location's system" The same line is at openspec/specs/osh/spec.md:479. It has two readings: "this location adds none" and "the map has none". The second is false when the system has a `Point` or another fresh location, because the loop at index.js:460 draws that system. Write: "that location adds no entity `osh:<systemId>`, and adds nothing to `getStats().placed.stream`".
- [ ] S10 major openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:62 "A failed features read is one that throws, that answers `keyRequired:true` alone, or that answers `truncated:true`." One word, one meaning: "failed" has two meanings. osh-046 and osh-060 (specs/osh/spec.md:227) use "throws". This sentence adds two more cases. design.md:70 then says "`partial:true` already tells the viewer that a read failed". That is false for the two added cases, because `partial` stays false for `keyRequired:true` alone and for `truncated:true` (index.js:399-405). Write: "A failed features read is a features getter that throws. Two other reads also leave a feature unheld: a read that answers `keyRequired:true` alone gives no feature, and a read that answers `truncated:true` gives some of them. Only a throw sets `partial:true`, and `truncated:true` shows the second case." Write in design.md:70: "`partial:true` already tells the viewer that a getter threw." Also change "permanent" and "transient" in design.md:62-63 to "The first cause does not change from one refresh to the next. The second cause lasts for one refresh."
- [ ] S11 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:7 "a small fraction of the fresh locations" Constraint 2, borderline. The text has no id, name, host or number. It does state the shape of the owner's server: proportions ("small fraction", "small part of the whole" at proposal.md:13, "most", "few"), a range ("ages run from seconds to more than a day", design.md:7), and "only a minority of locations are fresh" (design.md:38). Design.md:5 and design.md:7 have the same kind of text. Write, for example: "Here a location can name a feature that the collection does not hold, and the layer discards those locations." Keep the general rule and remove the claims about the measured server. If the lead decides these are allowed, accept this finding by name.
- [ ] S12 minor src/data/oshSystems.test.mjs:236 "and is dropped for an unknown feature" and :493 "is dropped entirely" The names of these two existing tests disagree with osh-058, because a location that names an unknown feature now draws a feature. Constraint 4 forbids the rename. Add one sentence to design.md:90: "The names of the tests at oshSystems.test.mjs:236 and :493 say that the location is dropped. Since `osh-058`, the location draws a feature. The names stay, because the trace and the ratchet use them." The author names the person who accepts this finding.
- [ ] S13 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:48 "`[osh-059]` the detail for a selected stream-drawn feature shows its id, its host id and Placed by with the location's age" Task 3.1 lists ten `[osh-059]` tests. src/data/oshLayer.test.mjs:2330 has an eleventh, and openspec/trace/links.json:1153 holds it: "the detail for a selected stream-drawn feature with no host shows Placed by with the datastream name and age". Add it to task 3.1.
- [ ] S14 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:9 "held feature ... unheld feature" Approved words. The design defines "held" and "unheld" as technical terms, and that is acceptable. But "hold" also has three other meanings: "the collection does not hold the feature" (design.md:44), "the pinned file counts under `osh-005` hold" (proposal.md:27), and "`osh-060`'s one-id rule cannot hold" (design.md:36). In STE, "hold" means to keep something in a position. Write: "the pinned file counts under `osh-005` stay true", "`osh-060`'s one-id rule cannot stay true", "the collection does not have the feature".
- [ ] S15 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:3 "Its location streams name many more features than that collection holds" Approved words. "name" is used as a verb in most new lines: proposal.md:3, proposal.md:5, design.md:9, delta specs/osh/spec.md:52, :85 and :209. "carry" is used to mean "have": proposal.md:9 "A fresh location already carries everything an entity needs", spec.md:88 and :89. I am not sure of the approved verb for "name". "refer to" is a possible one. Write: "Its location streams refer to many more features than that collection has." and "A fresh location already has everything an entity needs."
- [ ] S16 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/specs/osh/spec.md:84 "Draw a feature the layer does not hold from its fresh location alone `osh-058`" One word, one meaning. There are five problems. (a) "draw" is used for the adapter that adds a record (spec.md:52, :84) and for the layer that adds an entity (osh-059). The body of osh-058 says "`features` gains one placed feature". (b) "location" means the record (proposal.md:17, "gives a placed feature at the location") and its position (src/data/oshSystems.test.mjs:516, "draws that feature at the location"). Use "position" for the position. (c) "the layer does not hold" (spec.md:52, :85), "the feature list does not hold" (spec.md:209) and "the feature list has no such feature" (spec.md:232) name the same test. Also "feature collection" (proposal.md:3) and "features getter" name the same source. Pick one word for each. (d) "mutant M16" (design.md:90) and "the host-fallback mutation" (design.md:90) name one thing. (e) "the M16 pin" (design.md:88) and "pins" (design.md:76) use "pin" for a test. Write in spec.md:84: "Add a feature that the layer does not hold, from its fresh location alone `osh-058`". Write in spec.md:209: "a fresh location that names a feature the layer does not hold".
- [ ] S17 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:9 "a feature record the features read gave this refresh" Verbs used as nouns. "read" is used as a noun in "the features read", "a failed features read", "a by-id read" and "a restored read". These are at proposal.md:19, proposal.md:39, design.md:9, :46, :62-:70, spec.md:227, :232, and the test names at src/data/oshLayer.test.mjs:2475. The words "features read gave" can also read as a verb. Write: "a feature record that the features getter gave in this refresh", "when the features getter throws", "a request by id".
- [ ] S18 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:7 "A label built from it would fill the map with one word." Verbs: the conditional "would" is not an approved tense. It also appears at design.md:34, :36, :38 (in "would collapse" and "would need"), :42, :44, :70, :74, :84, :109. It is used in options that the design rejects, so the simple present is correct. Write, for example: "A label that uses the host's name fills the map with one word." and "the host's marker sits on one of them and claims to be the system" becomes "the host's marker shows the system at the position of one feature".
- [ ] S19 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:30 "The count is bounded by the freshness gate and by each stream's page limit." Voice: passive in descriptions where the active voice is possible. Write: "The freshness gate and the page limit of each stream limit the count." Same problem at design.md:3 ("is dropped"), design.md:38 ("is already bounded twice"), design.md:68 ("no entity is created or destroyed"), spec.md:218 ("is not counted under `placed.streamFeatures`"), spec.md:145 ("is placed by a stream"), tasks.md:7 ("The base was checked"), proposal.md:20 ("A feature placed by a stream shows"). Write: "The layer drops a fresh location that names a feature it does not hold." "`placed.streamFeatures` does not count a held feature that a fresh location moves." "when a stream places the selected feature". "The author checked the base against the archived spec."
- [ ] S20 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/specs/osh/spec.md:214 "never stands in for the feature's name" Verbs: "stands in for" is a phrasal verb. The same phrase is at design.md:74. Write: "never replaces the feature's name". Also design.md:76 "`osh-045` already gives a feature with no name no label" has a double "no" that is hard to parse. Write: "`osh-045` says that a feature with no name gets no label".
- [ ] S21 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:31 "Reddens the draws-at-the-location test." Approved words: "redden" is not an approved verb. It is used in tasks.md (lines 30, 31-42, 68-83, 94-98), design.md:90 and design.md:104. The strings "draws-at-the-location test", "systems-unchanged test", "not-fresh test" and "keeps-its-selection test" are hyphen chains that the reader must decode. Write: "A1: restore the `continue` for an unheld feature. The test `[osh-058]` a fresh location that names an unheld feature draws that feature at the location must fail." Use the same pattern for each line.
- [ ] S22 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:34 "That is the M16 failure with a count attached." Approved words: idioms and figurative words. These include "sits on one feature" (design.md:34), "hard ceiling" (design.md:38), "it stays red" (design.md:42), "no entity is created or destroyed on the way" (design.md:68), "for a case the feature rule never had" (design.md:84), "a reasoned no is a legitimate outcome" (design.md:100), "changes shape" (design.md:117), "the degraded state" (proposal.md:39), "trustworthy" (design.md:96), "plausibly" (design.md:94), "trusts" (design.md:112). Write plain words. For example: "Mutant M16 still fails under this change." "The count has an upper limit." "Its age is visible, so the viewer can judge the position."
- [ ] S23 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:5 "Its premise fails on this server." Approved words: I believe these are not approved STE words. I am not sure of some of them. "premise" (proposal.md:5, :7), "discards" (:7), "fraction" (:7), "mirrors" (:9, design.md:50, :80), "cluster", "own", "lack" (design.md:7), "permanent" and "transient" (design.md:62), "demands" (design.md:104), "ledger" (tasks.md:10, and no ledger exists in tasks.md), "gains" (proposal.md:28, spec.md:86). "where" is used as a conjunction at proposal.md:7 ("draws a few entities where it has positions for many more"). Write: "The rule is correct when the layer holds every feature that a location names. On this server the layer does not hold them all." "The layer does not use most of the fresh positions that it reads." "The map draws a few entities, but the layer has positions for many more."
- [ ] S24 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/proposal.md:18 "It counts under `getStats().features`" Articles and nouns: "It" can refer to the layer or to the stream-drawn feature. Write: "The layer counts it under `getStats().features` and under a new `getStats().placed.streamFeatures`." In the same way, proposal.md:20 "A moved feature carries the stream fields for it" has an unclear "it". Write: "The record of a feature that a location moves also has the four stream fields, so the `Placed by` line can show them." At proposal.md:9, "That mirrors the placeholder" has an unclear "That". Write: "This is the same as the placeholder that `osh-057` draws for a system with no held record." At design.md:94, "It draws it as a feature entity" has two "it" words. Write: "The change draws the feature as a feature entity".
- [ ] S25 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:14 "Keep existing `[osh-042]` tests unchanged without renaming them." Words that end in -ing: "existing" and "renaming" (tasks.md:14, :15, :16), "naming" (design.md:90, "a fresh location naming an unheld feature"), "grouping" (tasks.md:50, src/data/oshLayer.test.mjs:2196). Not in `allowedIng`, and not technical names. Task 2.1 also gives three instructions (Keep, Do not rename, Add). Write: "2.1 Keep the name of each `[osh-042]` test that the file has now." Sub-bullets: "Do not rename a test that the file has now. A new name can stop the ratchet on a file with untraced entries." and "The new `[osh-042]` test in 2.2 satisfies the trace gate for the new clause." Write in design.md:90: "a fresh location that names an unheld feature". Write in the test name: "... give three entities, and the layer does not group them". Update openspec/trace/links.json:1156 with the ratchet.
- [ ] S26 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/tasks.md:105 "Run `/opsx:review osh-draw-unheld-features`, correct the findings, and record the result in `review.md`." Instructions: task 5.4 gives three instructions in sequence, not at the same time. Split it into 5.4, 5.5 and 5.6. Also tasks.md:43, :84 and :98 "Stop and add the test before you continue." give two instructions in sequence. Write: "If a mutation makes no test fail, stop. Add a test that fails for that mutation, and then continue." tasks.md:33 does start with an imperative, but the "Assert ..." lines under the test names are instructions inside description bullets. Keep them, but start each with the test tag.
- [ ] S27 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/specs/osh/spec.md:231 "a fourth refresh whose features getter throws with no fresh location for that feature removes the entity" The fourth refresh is an action, and it is in a THEN clause. Move it to the WHEN clause. Also spec.md:229 uses "label" as a verb ("label the entity with the feature's name"). Write: "the entity has the feature's name as its label". Also spec.md:226 "one refresh holds a feature" has "refresh" as the subject of "holds". Write: "the layer holds a feature in one refresh, and gets a fresh location that names it". Also spec.md:223 "so `destroy()` and a refresh alone forget it" has an unclear "alone" and an unapproved use of "forget". Write: "the layer keeps no record of a stream-drawn feature after a refresh or after `destroy()`". Also spec.md:88 "keeps `uid` from ..., and null for ..." has no verb in the last clause. Write: "takes `uid` from the location's `foiUid` and `systemId` from the location's `systemId`, and has null for `name`, `description` and `validTime`".
- [ ] S28 minor src/data/oshSystems.test.mjs:580 "a location with both keys draws a feature keyed by foiId" Test names, new tests: check 3, check 4 and check 6. "keyed by" uses "key" as a verb (:580, :603). "one feature is drawn" is passive (:627). "a held feature is moved, not drawn a second time" is passive (:687). "the drawn feature's location" is unclear (:725): it means the location that draws the feature. Write: "a location with both keys draws a feature that has the `foiId` as its id", "a location with `foiUid` alone draws a feature that has the uid as its id", "... wins, and the function draws one feature", "a location moves a held feature and does not draw it a second time", "a location that draws a feature leaves systems and unplaced unchanged". Renaming needs a trace update (`make ratchet`). New tests, so the rename is allowed.
- [ ] S29 minor src/data/oshLayer.test.mjs:2450 "destroy() forgets the stream-drawn feature and zeroes placed.streamFeatures" Test names, new tests. "forgets" is not an approved use and "zeroes" is a noun used as a verb (:2450). The `[osh-060]` name (:2475) has no verb and "none once the location is gone" is unclear. The `[osh-059]` name at :2330 has "the datastream name and age", where "age" has no owner. Write: "destroy() removes the stream-drawn feature and sets placed.streamFeatures to zero", "the entity id stays the same in a refresh that holds the feature, in a refresh where the features getter throws and in a restored refresh, and the layer removes the entity when the location is gone", "... shows Placed by with the datastream's name and the location's age". Also src/layers/osh/detail.test.mjs:309 "a feature placed by a stream shows Placed by with the datastream's name and the age" has "the age" with no owner, and a passive participle. Write: "a feature that a stream places shows Placed by with the datastream's name and the age of the location".
- [ ] S30 minor openspec/changes/archive/2026-09-24-osh-draw-unheld-features/design.md:52 "Last come the four stream fields `datastreamId`, `datastreamName`, `phenomenonTime` and `ageMs`." Verbs: this sentence uses inverted word order, with the verb before the subject. Write: "The record ends with the four stream fields ...". At design.md:100: "To draw nothing was weighed here too, because a reasoned no is a legitimate outcome. It is rejected." is passive and has an unclear "It". Write: "The author also weighed the option to draw nothing. The author rejects it, because the fresh positions are the live data that this layer exists to show." At design.md:84: "An exception would need a position memory and a re-add branch" has a conditional, "re-add", and a noun group. Write: "An exception must keep the position of the entity and add the entity again after a refresh."

Note: no shell in this review, so I ran no test and no mutation. I found no violation of constraints 1 or 3. I found no id that lacks the `foi-fixture-`, `sys-fixture-` or `ds-fixture-` prefix. I found no host, no place name and no numeric count from the owner's server (S11 is borderline).
