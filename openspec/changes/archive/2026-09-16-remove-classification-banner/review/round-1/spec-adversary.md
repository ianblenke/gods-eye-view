```
Verdict: FAIL
- [ ] F1 major src/hudMarkings.test.mjs:13 The marking check is case sensitive (`hud.includes('TOP SECRET')`), but `src/ui/styles/overlays.css:119` gives `#intel-hud *` the rule `text-transform: uppercase`. So `<span class="hud-top-bar-left">Top Secret // Si-Tk // Noforn</span>` passes all three tests and the user still sees "TOP SECRET // SI-TK // NOFORN". The known limit `hud-markings-word-list` names another word, not another case. Compare in one case, for example `hud.toUpperCase().includes(marking)`, for both files.
- [ ] F2 major openspec/specs/hud-markings/spec.md:16 The requirement says the three parts keep the center part "in the middle". With `justify-content: space-between` the center of the middle part is at `(W + L - R) / 2`, so it is in the middle only when the left part and the right part have the same width. After the change the left part is empty and the right part is "PAGE 1/1", so the center part moves left by half the width of "PAGE 1/1". The design at design.md:23 gives this as the reason to keep the empty span. No test checks the position. Give `.hud-top-bar-left` and `.hud-top-bar-right` the same width (`flex: 1 1 0`), or remove the claim about the middle from the requirement and from design.md:23 and record it as a known limit.
- [ ] F3 major openspec/specs/hud-markings/spec.md:8 The requirement covers the HUD, but each scenario reads only `src/hud.js` and `src/ui/styles/overlays.css`. The HUD also has `index.html`, `src/ui/styles/responsive.css`, `src/ui/styles/cockpit.css` and the cockpit files `src/cockpit*.js`. A marking in one of these files does not stop the build. The known limits name the text-only method and the word list, not this file scope. Read each HUD source file and each HUD style file in the test, or record the file scope as a known limit.
- [ ] F4 minor src/hud.js:6 The module comment still says the HUD renders "classification banners". Line 59 says the same. The change corrected only the comment of `_buildDOM`, so these two lines are now untrue. Remove "classification banners" from both lines.
- [ ] F5 minor openspec/specs/hud-markings/spec.md:26 The AND line of `hud-markings-003` asserts that `overlays.css` has no rule for `hud-classification`. This result belongs to the requirement "No false classification marking", not to "Layout of the top bar". The requirement about the marking then has no scenario for the style file. Move the AND line to a scenario under the first requirement.
- [ ] F6 minor openspec/changes/archive/2026-09-16-remove-classification-banner/proposal.md:23 The Impact section says the change "removes two lines of `src/hud.js`". The diff removes one line and empties the text of a second line, and the ledger records 293 to 292. Correct the count.
- [ ] F7 minor openspec/changes/archive/2026-09-16-remove-classification-banner/review.md The gate output has the error REVIEW-MISSING. This is the expected error for a change before its review. The gate output also starts with the tail of a Node warning ("Use `node --trace-warnings ...`"); the head of that warning is not in the file, so its cause is not visible. Capture the full gate output in the next round.
```
