```
Verdict: FAIL

- [ ] S1 major tasks.md:7 "Remove the two rules from `src/ui/styles/overlays.css`" One word, one meaning. The change removes one rule (`.hud-classification`, old line 214) and one selector line from a second rule that stays (old line 353). proposal.md:12 and design.md:27 call these "the rule" and "its one selector line". "the two rules" can mean the reader removes the whole minimal-variant rule, which hides five more elements. Write: "Remove the rule `.hud-classification` and its selector line for the minimal variant from `src/ui/styles/overlays.css` until the tests pass."
- [ ] S2 major proposal.md:23 "The change removes two lines of `src/hud.js` and adds no line." Does not agree with the code. `git diff --numstat` gives 3 added and 4 removed lines for `src/hud.js`: the change rewrites two comment lines and keeps the element `hud-top-bar-left` as an empty line. Write: "The change removes four lines of `src/hud.js` and adds three lines. The new lines are two comment lines and an empty element."
- [ ] S3 major specs/hud-markings/spec.md:16 "so that the center part stays in the middle" Does not agree with the code. Same words in openspec/specs/hud-markings/spec.md:20 and proposal.md:11. The rule at `src/ui/styles/overlays.css:127` has `justify-content: space-between`, so with an empty `hud-top-bar-left` the center part moves to the left by one half of the width of "PAGE 1/1". The three classes do not put the center part in the middle, and `hud-markings-003` does not measure the position. Write: "The top bar of the HUD MUST keep three parts." Give the reason in design.md, or give the left part the width of the right part.
- [ ] S4 minor proposal.md:9 "Remove the text" One word, one meaning. proposal.md:9 says "the text" and design.md:12 says "each false classification marking" for the same item. proposal.md:29 and design.md:23 also use "text" for the content of a file. Write: "Remove the marking "TOP SECRET // SI-TK // NOFORN" from the top bar of the HUD."
- [ ] S5 minor design.md:23 "The rule `.hud-top-bar` has `justify-content: space-between`" Articles and nouns. Two rules have the name `.hud-top-bar`. The rule at line 144 has only `top: 70px`; `justify-content: space-between` is in the group rule at line 127. That rule also has `display: none`, so the bar is a flex row only in the operator variant. Write: "The group rule for `.hud-top-bar` and `.hud-bottom-bar` has `justify-content: space-between`."
- [ ] S6 minor design.md:31 "asserts the words" Approved words. "assert" is the name of the Node module, not an approved STE verb. Write: "checks the words".
- [ ] S7 minor design.md:31 "The coverage gate measures only the types" Approved words. "measure" needs a quantity. The gate measures coverage, and the types select the files. Write: "The coverage gate measures coverage only for the file types".
- [ ] S8 minor openspec/specs/hud-markings/spec.md:4 "This capability gives the rules for the markings" Instructions. Each other Purpose starts with a verb in the imperative (change-review, ci-gates, coverage-gate, gap-ledger, spec-lint, spec-trace, ste-lint). Line 4 also gives an action to a capability ("The capability also keeps the three parts"). Write: "Keep each classification marking of a government out of the HUD. The program shows open data only. Keep the three parts of the top bar."
- [ ] S9 minor specs/hud-markings/spec.md:8 "has one of the words "TOP SECRET", "SI-TK", "NOFORN"" Approved words. Same words in openspec/specs/hud-markings/spec.md:12. "TOP SECRET" is two words and "SI-TK" is not a word. Write: "has one of the markings".
- [ ] S10 minor proposal.md:5 "so the marking is not true" / "two bad effects" Approved words. A marking is not true or false; a statement is. I am not sure that "false" and "bad" are in the STE dictionary; STE gives "not correct" for "false". Same use of "false" in proposal.md:5, design.md:12 and the two spec files. Write: "so the marking is not correct" and "A marking that is not correct causes two problems."
- [ ] S11 minor src/hudMarkings.test.mjs:22 "keeps the three parts of the top bar and removes the dead style rules" Verbs. The test removes nothing; it reads the file and checks it. "dead" is not an approved STE word. Write: "keeps the three parts of the top bar and has no rule for the classification".
- [ ] S12 minor specs/hud-markings/spec.md:9 "THEN the test stops the build" Does not agree with the code. Scenario `hud-markings-001` does not give the check `hud.includes('hud-top-bar')` at src/hudMarkings.test.mjs:15, and scenario `hud-markings-003` does not give the check `overlays.includes('.hud-top-bar-left')` at src/hudMarkings.test.mjs:27. Add an **AND** line to each scenario for the check that it does not give.
- [ ] S13 minor proposal.md:29 "a list of marking words" Articles and nouns. "marking words" can mean "words that mark". Write: "a list of the markings".
```

Note on `allowedIng`: "marking" and "markings" are correct additions. Each of the 14 uses in the new prose is a countable noun with an article, a number or a determiner ("a classification marking", "the markings that the HUD shows", "the two markings", "each false classification marking"). No use is a verb form or a gerund. "classification marking" is a technical name of the United States government marking system. The one weak use is "marking words" in proposal.md:29; see S13.
