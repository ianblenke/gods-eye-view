Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:46 "The upstream project replaced that cache with a cache for each service, so the test has no code to check." Agreement with the code, and two possible meanings.
  - `src/voice/gevActions.js` still has the cache and the sharing of a request in progress. `cachesFor(service)` is at lines 301-322, and `reverseGeocode` uses `reverseGeocodeCache` and `reverseGeocodeInFlight` at lines 3823-3848.
  - The dropped 14th test checked exactly this: "a later call for the same coordinate joins the fetch in progress or uses the result in the cache". On `main` it called the seams `_reverseGeocodeForTest` and `_resetReverseGeocodeForTest` (`src/voice/gevActions.js:2984-2993` in `/home/ianblenke/docker/gods-eye-view`).
  - In the clone, `src/` has no match for either seam, and `src/voice/gevActions.test.mjs` has no test that names a cache or a reverse lookup. So the code exists and has no test. Only the seam is gone.
  - "has no code to check" can mean "the cache is gone". `design.md:36` ("it does not exist now") can be read the same way. The wording "the catch ... turns a rejected reverse lookup into" also uses "catch" as a noun and "turn" outside its approved meaning.
  - Write: "The fork had a test of `credential-boundary-014` for the cache of the reverse lookup in `src/voice/gevActions.js`. The upstream project replaced that cache with a cache for each service, and the test seams that the test used are gone. The code of the new cache is still in `src/voice/gevActions.js`, and no test covers it. The `catch` clause in that file that changes a rejected reverse lookup to "no place" has no test. A backfill change for the voice actions writes both tests."
  - Write in `design.md:36`: "and the clone does not have this test".
- [ ] S2 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:52 "because the covered code depends on timing" Words that end in -ing. "timing" is new in this round. It is not in `allowedIng` and it is not a technical name. Also "the covered code depends on" is unclear: what changes is which lines a test runs. Write: "because a test runs different lines when its events happen at different times".
- [ ] S3 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:36 "and smaller counts for 7 files" Numbers. `openspec/trace/history.jsonl` has `smaller` lines for 8 files: `scripts/read-dotenv-value.mjs`, `server/providers/gbfs.js`, `src/bloom.js`, `src/data/labelArbiter.js`, `src/data/lifecycle.js`, `src/layers/bikeshare/model.js`, `src/sources/live/vessels.js` and `src/ui/visualEffects.js`.
  - The last ratchet run (commit `fa65ea2`, lines 1469-1472) added `labelArbiter.js` (branches 52 to 50) and wrote the `lifecycle.js` drop (184 to 177) again.
  - The number 7 is right only without `lifecycle.js`. Its drop is from the hand-written adopt line, not a gap that the fork left. `labelArbiter.js` is a count that changes between runs.
  - Write: "and smaller counts for 8 files. Two of them, `src/data/labelArbiter.js` and `src/data/lifecycle.js`, are counts that change between runs (`sync-counts-change-between-runs`)."
- [ ] S4 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:43 "The author of this change checked ... The person who accepts this change on `main` reads" (also :3, :47, :53 and design.md:5) One word, one meaning.
  - The change has three roles: "the owner" (:3, :47), "the person who accepts this change on `main`" (:43, :53, design.md:5, :56) and "the author of this change" (:43, :47, :53).
  - Only the second role has a definition, and it is in `design.md:5`. The proposal uses it first, before the definition.
  - No line says if the owner is the person who accepts the change. Round 2 S14 raised this and it is still open. "The owner decides in `review.md`" can name a different person from the one who accepts the change.
  - Write once, in the proposal before its first use: "The owner of the project is the person who accepts this change on `main`. Rule 21 of `AGENTS.md` calls this person 'the person who merges a change'. The author of this change is the agent that wrote it." Fix the two sentences if they are not true. Then use one name for one person.
- [ ] S5 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:47 "The rule is that a test name that exists on `origin/main` does not change." Agreement. `AGENTS.md` has no such rule. Its rule 3 only says to put the scenario ID at the start of a test name. A reader cannot find the source of the rule. Write the source: for example "The owner's rule for this change is that ..." or the number of the rule or gate that says it.
- [ ] S6 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:17 "the files of the merge commit that have a gap" (also :34 "the adopted gaps of the files of the merge commit") One word, one meaning.
  - A commit has all files in its tree. The command `adopt` takes only the files that the merged commit changed and that differ from the base commit (design.md D5, `gap-ledger-089`).
  - `design.md:66` says "the files that the merge commit brings from the upstream project".
  - Write in both places: "the files that the merge commit brings from the upstream project".
- [ ] S7 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:43 "a person edited them after the merge. Only the upstream project changed them before the merge." (also :44 "A later merge commit from the upstream project") Verbs used as nouns, and one word with two meanings.
  - "merge" is a noun here. In round 2, S11 asked to write "merge commit" for the result. These two places still use "the merge".
  - I suggested the words in :44 in round 2. They give "merge commit" two meanings: the commit that this change makes, and a later commit that "the upstream project" makes.
  - "edited" is not an approved word, as far as I know (I am not sure). The approved verb is "change".
  - Write in :43: "Three more code files have an adopt line. The fork did not change them, only the upstream project did, and a person then changed them in this change."
  - Write in :44: "A later change of these two tests in the upstream project can conflict with these edits, when the fork merges that change."
- [ ] S8 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:12 "The HTTP geospatial provider and the function `createGoogleGeocoder` call the server route `/api/google/geocode`" One word, one meaning, and agreement with the code.
  - Round 2 S13 raised this and it is still open. The inline code did not fix the meaning.
  - `src/search/defaults.js` uses `createGoogleGeocoder` two times. The first use calls `/api/google/geocode` (line 48). The second use calls `/api/geocode` (line 62). The function itself calls nothing. It takes a `request` function.
  - Write: "The HTTP geospatial provider, and the first use of `createGoogleGeocoder` in `src/search/defaults.js`, call the server route `/api/google/geocode`, with no key in the request."
- [ ] S9 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:12 "Keep the behavior of the OSH layers and of the geocoders as it was in the fork. Change only how they connect to the application." Agreement with the other prose, and an unbounded "only" claim. The known limits `sync-share-token` (the share-link token changes from `o` to `3`) and `sync-third-geocoder-call` (a third request) change the behavior. Round 2 S7 gave the words of this goal. Write: "Keep the behavior of the OSH layers and of the geocoders as it was in the fork, except for the two changes in the known limits `sync-share-token` and `sync-third-geocoder-call`. Change only how they connect to the application."
- [ ] S10 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:38 "The scenario text of `credential-boundary-014` names the HTTP geospatial provider, and not the voice actions." (also proposal.md:49 "makes three calls") Articles and nouns, and one word with two meanings.
  - The sentence is about the reverse lookup, but it now follows the sentence about the forward lookup. Round 2 S3 asked to move it to the paragraph at line 36. Move it to the end of that paragraph.
  - "call" has two meanings. It means a network request in "makes three calls". It means a call of the function in the scenario ("a later call makes no fetch").
  - Write in `design.md:38` and `proposal.md:49`: "A forward lookup that finds no place makes three requests: to the server route, to Photon and to the local Nominatim route `/api/geocode`." Also write "One test of `credential-boundary-013` counts the three requests".
- [ ] S11 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:50 "A change of the scenario rehashes all scenarios of its requirement, so a later change does it." Verbs. "does it" is a substitute verb, and it can mean "changes the scenario" or "rehashes". "rehashes" has no definition in this change. Write: "A change of the scenario `osh-005` changes the hash of each scenario of its requirement, so a later change must write the new text of the scenario."
- [ ] S12 minor openspec/changes/archive/2026-09-26-upstream-sync/tasks.md:64 "The only errors must be review errors, and errors of a count that changes between runs." Instructions, and an exception clause that does not bound itself. It does not say which errors or which files. The known limit `sync-counts-change-between-runs` gives two examples. The ledger history shows a third file with a changed count between the last two ratchet runs (`src/data/labelArbiter.js`, 52 to 50 branches), so the list of two is not complete. Write: "Run `make gates CHANGE=upstream-sync`. The only errors must be review errors, and `LEDGER-LARGER-GAP` or `LEDGER-STALE` errors for a file with a count that changes between runs (`sync-counts-change-between-runs`). Write each such error and its file in `review.md`."
- [ ] S13 minor openspec/changes/archive/2026-09-26-upstream-sync/specs/credential-boundary/spec.md:18 "a later call for the same coordinate, rounded to four decimals, then makes no fetch" (also `openspec/specs/credential-boundary/spec.md`) Voice. "rounded" is a passive participle with no agent. "the coordinate ... rounded" can also mean that the caller rounds it. The provider rounds it (`src/search/http.js:98`, `toFixed(4)`). Write: "**AND** a later call for the same coordinate, which the provider rounds to four decimals, makes no fetch". Change the live spec in the same way.
- [ ] S14 minor openspec/changes/archive/2026-09-26-upstream-sync/specs/osh/spec.md:155 "a page that has the hosts of the panel" (also `openspec/specs/osh/spec.md`) One word, one meaning. The requirement (:145), the title (:154) and the test name say "the hosts of the page". The first line of the scenario says "the hosts of the panel". Round 2 S13 asked for one phrase. Write: "**WHEN** a test builds the application catalog in a page that has the elements `osh-panel`, `osh-panel-detail` and `osh-panel-video`".
- [ ] S15 minor src/app/layers/osh.test.mjs:46 "gives the OSH systems layer of the catalog the production source and the hosts of the page" Test names, articles and nouns. With two long objects, "the catalog the production source" can read as one noun group. The test is new, so the rename is free. Write: "builds the OSH systems layer in the catalog with the production source and the hosts of the page". Change the key in `openspec/trace/links.json` (line 1537) with it.

Checked and correct:
- **Terms.** "adopt line" and "untrue coverage" agree with `openspec/specs/gap-ledger/spec.md`. The conditions of D5 and "merged commit" agree with `gap-ledger-089`, `-091`, `-096` and `-097`. The words of rule 21 in `design.md:5` and `:56` agree with `AGENTS.md:34`: the person who merges a change, and the upstream remote must have the second parent of the merge commit. The terms "the upstream project", "the merge commit" and "the merged commit" have one meaning in the four documents, except at `proposal.md:17`, `:34` and `:44` (S6, S7).
- **Numbers that agree with the tree.**
  - `openspec/trace/history.jsonl` has 799 adopt lines of `upstream-sync` (797 and the 2 hand lines), 272 test lines with `lines:null` and 17 lines with `untrue:true`. So 525 code files agree.
  - It has 16 `closed` lines. The 3 code files and 1 test file with `file removed` agree. The examples 511, 562 and 66 agree.
  - The adopt counts of the three edited files are 3/4/1, 2 and 4/2. The hand lines are 44/30/2 and 184/102/15. The adopt run measured 40 and 177 lines, and the ledger has 177/100 for `lifecycle.js`. The list of 8 code files and 10 test files agrees with `adopt-summary.txt`.
  - The ceiling of ten seconds (`server/providers/transitHistory.test.mjs:164`) and the `clearTimeout` in `within` (`src/sdr/controller.test.mjs:365`) agree.
  - 13 tests of `credential-boundary-014` are in the new file. `main` has 14, and the dropped one is "a later call for the same coordinate joins the fetch in progress ...".
- **Rules that the design states and the specs do not.**
  - The token rule agrees with `validateLayerStateRegistry` (`src/data/layerState.js:641-645`), which runs when the module loads.
  - The boundary rule agrees with `check:boundaries` (`package.json:46`) and with `src/app/layers/osh.js`.
  - `src/data/localLayers.js` has only one importer, the test of `osh-033`. The panel comes before `scene-runtime` in `src/ui/templates/context.html:250-255`. The layer order from `traffic` to `directions` is in `src/app/constructCatalog.test.mjs:65-66`.
- **Scenarios and tests.**
  - `credential-boundary-014` agrees with `src/search/http.js:96-113`: an HTTP error status throws, the memory needs a non-null Google status, and a fetch failure or a JSON failure rejects.
  - `osh-096` agrees with the new test: the route `/api/osh/systems` is asked, `hidden` is false, and the detail has `System A`. The delta specs equal the live specs in the changed places.
- **Test names.** `openspec/trace/links.json` has the 13 names of `credential-boundary-014`, the name of `osh-095` and the new name of `osh-096`. All agree with the test files. I read all 15 names. The only wording problems are the old words that `sync-test-names` records. The two renames agree with the assertions (`assert.rejects`).
- **Word list.** `openspec/ste/words.json` in the clone equals the file on `main`. The change adds nothing to `allowedIng`. "wiring" and "real-looking" are gone. No word of `words.json` and no `SHALL`, `SHOULD` or `MAY` is in the change. Tasks 2.6 to 3.14 each give one instruction and start with an imperative verb. I found no passive verb in an instruction. The only passive form in a description is "rounded" (S13).

Not checked:
- I could not run Git or code, and I got no lint output. I found the -ing words by hand.
- I could not check 61 pull requests, 241 commits, 1002 and 650 files, 15 live timers, 0.8 seconds or the old ceiling of two seconds. `sync/diffstat.txt` says 1025 files changed, and I do not know which diff it counts.
- I could not sum 60486 lines, 5866 branches, 1683 functions and 3414 untraced tests.
- I ran no mutation. I did not check that the 15 registry and catalog layers agree (29 and 30). I did not check the claim that "the code of this change in these files is covered" (`proposal.md:43`).
- The gate claims of `sync-counts-change-between-runs` (exact compare in this change, tolerance after the push to `main`) are for the spec adversary. I did not check `docs/CURRENT-STATE.md`, because the diff does not change it.
