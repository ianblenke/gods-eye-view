Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-25-upstream-sync/design.md:48 "The reviewer reads the Git diff between the merged commit and HEAD for these files." One word, one meaning. The same known limit in `ledger-adopt` (proposal.md:49) says "The person who merges the change reads the Git diff". Round 3 of that review made the change because the review agents cannot run Git. This line and proposal.md:32 name two people for one duty. Write: "The person who merges this change reads the Git diff between the merged commit and HEAD for these files."
- [ ] S2 major openspec/changes/archive/2026-09-25-upstream-sync/design.md:44 "It adopts a file when two things are true." Agreement with the spec. Scenario `gap-ledger-089` and `ledger-adopt` design.md:29 give three conditions. The missing one is that the file has a gap: a code file with a not-covered count above 0, or a test file with untraced tests. As written, the command can adopt a file with no gap, and `gap-ledger-091` forbids this. Write: "It adopts a file when three things are true. First, the file has a gap: it is a code file with a not-covered count above 0, or a test file with untraced tests. Second, the content of the file is not equal to its content in the base commit. Third, the merged commit changed the file since its merge base with the base commit."
- [ ] S3 major src/search/reverseGeocodeRoute.test.mjs:126 and :133 "a fetch failure gives no place", "a response that is not JSON gives no place" Test names. `createHttpGeospatialProvider().reverseGeocode` does not give a null place for these two cases. `json()` in src/search/http.js:68-83 lets the fetch error and the `SyntaxError` through, and both tests use `assert.rejects`. Each name states the opposite of its assertion. These 13 tests are in a new file, so `links.json` gives them new keys. If the owner still forbids a rename, the name must stay true, and it cannot. Write: "a fetch failure makes the reverse lookup reject" and "an answer that is not JSON makes the reverse lookup reject". Line 133 also uses "response", where the other names use "answer".
- [ ] S4 major openspec/changes/archive/2026-09-25-upstream-sync/design.md:34 "It also remembers a definitive answer with a Google status for one coordinate." Agreement with the code and the spec. The code (src/search/http.js:111) adds a coordinate to `noPlace` only when the answer gives no place and the Google status is not null. It does not remember an answer that has a place, but the sentence says it does. "definitive" is not defined and is not an approved word. The scenario `credential-boundary-014` has no line for this rule, and the test at reverseGeocodeRoute.test.mjs:206 pins it under that ID. Write in design.md: "It also remembers, for one coordinate, an answer that has a Google status and gives no place." Add to the scenario: "**AND** it remembers an answer that has a Google status and gives no place, so a later call for that coordinate makes no fetch". Then name a mutation for this line in tasks.md 2.4.
- [ ] S5 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:3 "changed 106 files in the same way or in a way that conflicts" Numbers. The scratchpad `both-files.txt` lists 32 files that both sides changed. I found nothing that gives 106. If 106 is right, say what it counts and give the 32. Also, `ledger-adopt` design.md:66 and :92 say "about 1800 untraced tests". proposal.md:32 says 3413 tests in 272 test files. If both count the same thing, one number is old.
- [ ] S6 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:39 "and eight test files" Numbers and instructions. The person who reads the diff (see S1) cannot find the eight test files, because the text does not name them. The scratchpad list has 18 adopted files that both sides changed (8 code, 10 test). It has 3 code files that the text does not name: `server/providers/places/google.js`, `server/standalone/vite.config.js` and `src/voice/gevActions.js` (1668 lines). The text has 3 code files that the scratchpad does not have: `src/app/constructCatalog.js`, `src/search/defaults.js` and `src/search/http.js`. design.md:58-61 also lists `src/voice/gevActions.js` as an edited file. Name the 8 test files and state the rule for the list. I confirmed only that all 8 code files in the text have an adopt line.
- [ ] S7 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:54 "The code files that this change adds or edits by hand keep the rules of the gates. The new files are ... both stay at 100% coverage. The gap of a hand-merged file is inside the adopted count of that file." Agreement. The first sentence is true only for the two new files. `constructCatalog.js`, `defaults.js` and `http.js` have adopt lines (history.jsonl:719, 1045, 1047), so their gaps are allowed. "stay" is wrong for a new file. Write: "The new code files `src/app/layers/osh.js` and `src/layers/osh/hosts.js` have 100% coverage. The gates allow the gap of an edited file that the command `adopt` adopted, up to its adopted count."
- [ ] S8 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:34 "Upstream also has a Nominatim provider as a last resort, so a miss makes three calls." Articles and nouns. "a miss" has no referent. The three calls (server route, Photon, Nominatim) belong to the forward lookup in src/search/defaults.js:36-70. The reverse lookup makes one call. The sentence follows the reverse-geocoder sentences, so it reads as if it were about them. Write: "Upstream also has a Nominatim provider as a last resort. A forward lookup that finds no place makes three calls: the server route, Photon and Nominatim."
- [ ] S9 minor openspec/changes/archive/2026-09-25-upstream-sync/specs/credential-boundary/spec.md:16 "for the life of the page" (also design.md:34, and the test name at src/search/reverseGeocodeRoute.test.mjs:75) Agreement with the code. The change moved the memory into the provider (http.js:62-67, "for its life"). Each provider that the application creates has its own `unconfigured` flag: `defaultGeospatial` and each `createDefaultPlaceSearch()` call. The change made "page" wrong. Write: "for the life of the provider".
- [ ] S10 minor openspec/changes/archive/2026-09-25-upstream-sync/specs/credential-boundary/spec.md:18 "the same place shape as before this change" (also the test name at src/search/reverseGeocodeRoute.test.mjs:85) Articles and nouns. The line is carried text, but the change made it ambiguous. "this change" was `credential-boundary`, and the delta of `upstream-sync` now holds it. A spec and a test name do not name their change. Write: "a configured answer gives the fields `formattedAddress`, `locality`, `region`, `country`, `types`, `labels` and `streetLabels`".
- [ ] S11 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:34 "The reverse geocoder remembers" One word, one meaning. One thing has these names: "the reverse geocoder" (proposal.md:11, design.md:32 and :34), "the provider" (design.md:34), "the HTTP geospatial provider" (spec.md:14) and "the reverse lookup" (test names at :75, :154-206). "the Google forward geocoder" (proposal.md:11) is a fifth name for the forward side. Write "the HTTP geospatial provider" for the object, and "reverse lookup" only for the request.
- [ ] S12 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:32 "The person who merges this change checks the merged commit against the upstream remote." One word, one meaning. "merge" has four meanings in the four documents. (1) The Git merge of the upstream branch (proposal.md:5, :9). (2) Putting this change on `main`, as in "the person who merges this change" (proposal.md:32, design.md:48). In a change that is itself a merge, a reader can take this as the person who ran the Git merge. (3) Upstream accepted a pull request (design.md:3 "the upstream project merged 61 pull requests"). (4) "merged" for the source ("merged commit", "merged files", "merged code") and for the result ("merged tree"). Write "the tree of the merge commit" for the result. Write "the person who accepts this change on `main`" and add "(rule 21 of `AGENTS.md` calls this person 'the person who merges a change')" once.
- [ ] S13 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:31 "wrote one history line for each of 797 files" One word, one meaning. The term in `gap-ledger` and `ledger-adopt` is "adopt line". Write: "wrote one adopt line for each of 797 files". Also design.md:44 "test files without scenario IDs" has a different meaning from the spec term "test file with untraced tests". Write: "test files with untraced tests".
- [ ] S14 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:5 "This change makes the merge." Verbs. "merge" is a noun here. Write: "This change merges the upstream branch." In the same file: proposal.md:11 "The function `resolveApiKey` is gone." (not an STE verb form, and unscoped; `server/providers/places/google.js` still has `resolveApiKey`, and the earlier design calls it an "input") becomes "The browser search code has no `resolveApiKey` input."; proposal.md:25-26 "get new text" and "gets `osh-095`" become "have new text" and "has `osh-095`"; design.md:28 "get the OSH modules" becomes "The change adds the OSH modules to the boundary lists in `scripts/package-boundaries.json`."
- [ ] S15 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:16-40 Approved words. I am not sure about each word, but none of these is an approved STE word in this use.
  - "owns the token `o`" (proposal.md:10) becomes "uses the token `o`".
  - "the templates ... hold the markup" (design.md:18) becomes "contain".
  - "A test of upstream pins the order" (design.md:26) becomes "checks".
  - "intact" (design.md:9) becomes "so that they work as before".
  - "that comes with the merge" (design.md:10) becomes "that the merge brings".
  - "as the package boundaries of upstream demand" (design.md:28) becomes "as the rule of the package boundaries of upstream says".
  - "and no more the voice actions" (design.md:34) becomes "and not the voice actions".
  - "when the promise wins" (design.md:38) becomes "when the promise settles first".
  - "takes one feature area" (proposal.md:34) becomes "has one feature area".
  - "a miss" becomes "a lookup that finds no place".
- [ ] S16 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:16 "upstream owns the structure of the application" Articles and nouns. "upstream" is a noun with no article, and it is a third name beside "the upstream project" and "the upstream branch". Places: proposal.md:10 (two times), :12, :30; design.md:16, :18, :24, :26, :28, :32, :34. Write "the upstream project" in each place. In design.md:16, "the fork as its first parent" should read "the newest commit of the fork as its first parent". A branch is not a parent, and `ledger-adopt` design.md:3 has the right text.
- [ ] S17 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:10 "builds the layer in the new file `src/app/layers/osh.js`, and its style sheet moves to" Articles and nouns. "its" can refer to the catalog, the layer or the file. The style sheet belongs to the panel. "builds the layer in the new file" is also not exact: the catalog calls `createApplicationOsh()` from that file. Write: "The application catalog builds the layer with `createApplicationOsh()` from the new file `src/app/layers/osh.js`. The style sheet of the panel moves to `src/ui/styles/osh-panel.css`."
- [ ] S18 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:28 "The module `createOshPanelHosts` moves to `src/layers/osh/hosts.js`. The application wrapper then imports only the package entry" One word, one meaning. `createOshPanelHosts` is a function, and the module is `hosts.js`. "The application wrapper" is a new name for the file `src/app/layers/osh.js`. Write: "The function `createOshPanelHosts` moves to the module `src/layers/osh/hosts.js`. The file `src/app/layers/osh.js` then imports only the package entry `src/layers/osh/index.js`". That claim is true in the code.
- [ ] S19 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:12 "Do not change the behavior of the OSH layers or of the geocoding, only the place where they connect." Articles and nouns and -ing. "geocoding" ends in -ing and is not in `allowedIng`. It is a technical noun, so add it to the list or write "the geocoders". "only the place where they connect" is loose. Write: "Change only the place where the OSH layers and the geocoders connect to the application." design.md:18 "The runtime files follow the format of upstream." Both "runtime files" and "format" have no definition, and proposal.md:12 says "the OSH files". Write: "The OSH files follow the Prettier format of the upstream project."
- [ ] S20 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:24 "The registry has 29 layers, and the application catalog has 30 layers." Articles and nouns. Both numbers are correct (layerState.test.mjs:200, constructCatalog.test.mjs:42). The text does not say why they differ by one, so a reader thinks it is an error. The catalog test shows `local-adsb` has the disposition `local-only`, but I did not confirm that this is the reason. State the reason. design.md:26 "after recent imagery" should read "after the recent-imagery layer", as in the spec.
- [ ] S21 minor openspec/changes/archive/2026-09-25-upstream-sync/specs/osh/spec.md:145 "It MUST place that layer after the recent-imagery layer." Agreement. The scenario (line 151), the test and design.md:26 say "right after". The requirement is weaker than its scenario. Write: "It MUST place that layer right after the recent-imagery layer." Also "the production source" (line 145) and "the standalone layer sources" (line 149) use "source" for two things: the OSH source from `createOshSource()` and the input object of the catalog. The scenario checks neither the production source nor the hosts of the page. Write in the requirement: "with the production OSH source and the hosts of the page". Write in the scenario: "with the standalone layer inputs".
- [ ] S22 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:22 "The panel of the OSH layer is an `aside` element" Agreement. The test at src/data/osh.test.mjs:217 needs `<aside`. The scenario `osh-094` says only "one element with the id `osh-panel`". Write in the scenario: "the page has one `aside` element with the id `osh-panel` and the attribute `hidden`". This is a change of the scenario body, which you are already making.
- [ ] S23 minor openspec/changes/archive/2026-09-25-upstream-sync/tasks.md:26 "Keep the tests for `osh-049`, `osh-057`, `osh-059`" Agreement. The delta spec does not carry `osh-059`. It belongs to the requirement "Feature entities", which the change does not modify. Remove `osh-059`. The other 20 IDs in tasks 2.1 to 2.7 match the delta scenarios.
- [ ] S24 minor openspec/changes/archive/2026-09-25-upstream-sync/tasks.md:31 "Merge the upstream branch and resolve the conflicts." Instructions. Each of these gives two instructions in one task: lines 31, 32 ("Move ..., and give ..."), 33 ("Write ... and place ... until 2.3 passes"), 43 ("Run `make lint`, and correct ...") and 45 ("Run the review ... and write `review.md`"). The order is not the same as time at once. Split them. Also "until 2.3 passes" and "until 2.4 passes" (line 34): a task number does not pass. Write: "until the test of `osh-095` passes" and "until the tests of `credential-boundary-014` pass".
- [ ] S25 minor openspec/changes/archive/2026-09-25-upstream-sync/tasks.md:29-36 Agreement with the proposal. proposal.md:12 and design.md:28 say the change formats the OSH files with the Prettier version of upstream, lists the OSH modules in `scripts/package-boundaries.json`, and moves `createOshPanelHosts` to `src/layers/osh/hosts.js`. No task in section 3 has any of these. Add three tasks that start with "Format", "List" and "Move".
- [ ] S26 minor src/data/layerState.test.mjs:195 "[osh-033] registers the OSH systems layer with the token o, and localLayers.js exports it" Test names. The name says the token `o`, and the assertion at line 198 and the scenario say `3`. The name exists on `origin/main`, so no rename fixes this. Record it in proposal.md, section "Known limits and later changes": "`sync-osh-test-names`: The name of the test of `osh-033` says the token `o`, and the test checks the token `3`. The name stays, because a test name that exists on `origin/main` does not change." The name at src/data/osh.test.mjs:212 keeps "index.html" for a page with expanded templates. That is acceptable, and it needs no line.
- [ ] S27 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:40 "measures wall-clock time for 15000 vehicles ... A quadratic ingestion of 15,000 rows still takes far longer than that." One word, one meaning. The number has two forms (15000 and 15,000), and the thing has two names (vehicles and rows). "15 timers were alive" (design.md:38) should read "15 live timers", the term of `coverage-gate-049` and proposal.md:13. I could not check that a quadratic ingestion takes more than ten seconds. No task in tasks.md names a mutation for it.
- [ ] S28 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:13 "Fix two upstream tests that the gates stop." Articles and nouns. The gates stop the build, not a test. "a wall-clock ceiling that fails under the load of the gates" says a ceiling fails, but the test fails. Write: "Fix two upstream tests that make the gates stop the build." and "the test fails at a wall-clock ceiling, because the gates run many test processes at the same time."
- [ ] S29 minor openspec/changes/archive/2026-09-25-upstream-sync/design.md:48 "Rule 21 of `AGENTS.md` asks the person who merges this change to check that the upstream remote has the merged commit." Voice and agreement. Rule 21 says "must check" and also "must record the result in `review.md`". "asks" is weaker, and proposal.md:32 says the same rule in other words ("checks the merged commit against the upstream remote"). Write in both places: "Rule 21 of `AGENTS.md` says that the person who merges this change must check that the upstream remote has the merged commit, and must record the result in `review.md`."
- [ ] S30 minor openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:30 "the files of the sections above" Articles and nouns. The sections above name no files. The list is in design.md, section "Files that the change adds or changes". Write: "the files that design.md lists".

Not checked:
- Sums: 60497 lines, 5868 branches, 1683 functions and 3413 tests. I could not sum the adopt lines with Grep, Read and Glob.
- Counts: I confirmed 797 adopt lines, 525 code files, 272 test files and 17 with untrue coverage. I did not check 241 commits, the 1002 and 650 files (I have no Git; `diffstat.txt` says 1025), 15 live timers, 0.8 seconds and the old ceiling of two seconds. 29 registry layers, 30 catalog layers, 13 tests of `credential-boundary-014` and the ceiling of ten seconds agree with the tests.
- Only Read, Grep and Glob were available, so I ran no mutation and did no Git diff between the base and the delta specs.
- I did not receive the STE lint output. I checked -ing words by hand: only "geocoding" is not in `allowedIng`. I found no passive verb in an instruction.
- I did not report the old text of the carried scenarios.
- I did not report the two sentences in `credential-boundary-014` line 15 (the URL and the word "too"), because the change did not modify them.
