Verdict: FAIL

Tree read: clone `/home/ianblenke/docker/gev-upstream` at `fee3a79ea9ab7ac5ea77f1be3a51caf64cd70f6b` (branch `upstream-sync`). I compared it with the fork's `main` tree at `/home/ianblenke/docker/gods-eye-view`. The gate output `sync-gates-1.out` held only the command line, so I could not check any gate result.

- [ ] F1 major /home/ianblenke/docker/gev-upstream/src/search/reverseGeocodeRoute.test.mjs:37-213 The change deletes one `[credential-boundary-014]` test from `origin/main` without a record. `origin/main` `src/voice/gevActions.test.mjs:3190` had 14 tagged tests. The new file has 13, and `design.md:34` says "the 13 tests moved". The lost test is `a later call for the same coordinate joins the fetch in progress or uses the result in the cache`. `links.json` lost that entry and no gate objected. The behavior still exists in the merged `src/voice/gevActions.js:3823-3848` (a cache plus an in-flight join), and now no test covers it. That file's adopted gap absorbs the loss. Restore a test through the public runner, or name the loss in the "Known limits and later changes" section (for example `sync-dropped-reverse-cache-test`). Correct the "13 tests moved" sentence in `design.md`.

- [ ] F2 major /home/ianblenke/docker/gev-upstream/src/voice/gevActions.test.mjs:3141 The hand resolution dropped the upstream test `ALPR common names toggle only the registered camera layer through the normal voice action`. `resolution-diff.txt:2334-2356` shows it as a `-` line, so it is in `b210ab0` and not in HEAD. It is not in `origin/main` either. The alias code it tests is in HEAD at `src/voice/gevActions.js:226-231`. No test in HEAD uses `flock cameras`, `license plate cameras` or `plate readers`. The `gaps.json` entry for this file has 80 untraced names, which equals the 76 names on `main` plus the 4 other upstream additions, so the ledger absorbed the loss. Restore the test and re-run `adopt` and `ratchet`. The loss is probably an accident at the conflict with the fork's inserted `reverseGeocode` test block. The known limit `sync-hand-merged-files` does not name `src/voice/gevActions.js` at all.

- [ ] F3 major /home/ianblenke/docker/gev-upstream/src/app/layers/osh.test.mjs:19-32 The requirement "Application catalog" says the catalog MUST build the layer "from the production source, with the hosts of the page". Scenario `osh-095` and its test assert only the id, the position, the metadata and `createApplicationOsh().id`. Delete `...createOshPanelHosts()` or `source: createOshSource()` in `src/app/layers/osh.js:13-16` and every test still passes. The layer would then never show its panel. The only tests of the hosts (`src/data/osh.test.mjs:132-210`, `[osh-089]`) go through `src/data/osh.js`. Production no longer loads that module: `constructCatalog.js:161` uses `createApplicationOsh`, and `localLayers.js` is used only by the `osh-033` test (design D1). Add a WHEN, THEN or AND line, or a new scenario, and a test. The test should give a fake `document` before `createApplicationOsh()`, click a system, and assert the panel host shows and the detail host fills.

- [ ] F4 major /home/ianblenke/docker/gev-upstream/src/data/oshRepositoryHygiene.test.mjs:11-28 The scan list `PROVIDER_FILES` for `[osh-034]` (no real address in provider, adapter and layer files) lacks the two new OSH source files `src/layers/osh/hosts.js` and `src/app/layers/osh.js`. This change edits this test (17 to 18 for the new test file) but does not add the two source files. An address pasted into either file would pass every gate. Add both files to the list. Also add `src/app/layers/osh.js` to the `[osh-005]` scan if its file list should cover the wrapper.

- [ ] F5 minor /home/ianblenke/docker/gev-upstream/src/data/layerState.test.mjs:195 The `[osh-033]` test keeps the name `...with the token o` but asserts `'3'`. Two `[credential-boundary-014]` tests, `src/search/reverseGeocodeRoute.test.mjs:126` and `:133` (`a fetch failure gives no place`, `a response that is not JSON gives no place`), now assert `assert.rejects`. The old tests asserted `null`; the new provider throws, and the caller `gevActions.js:3840` catches. The owner forbids a rename, so record both name and assertion mismatches in "Known limits and later changes".

- [ ] F6 minor /home/ianblenke/docker/gev-upstream/server/providers/transitHistory.test.mjs:159-164 The ceiling rose from 2 s to 10 s. `design.md:40` says a quadratic ingestion of 15,000 rows "still takes far longer than that", but no mutation was run. 15,000 squared is 2.25e8 steps, which can take only a few seconds. Run a quadratic mutation and record the measured time, or use a relative check (for example poll 3 against poll 1). The `controller.test.mjs` `within` fix at `:359-366` is sound.

- [ ] F7 minor /home/ianblenke/docker/gev-upstream/docs/CURRENT-STATE.md:972-979 The text says the search box (`src/standalone/placeSearch.js`) and the voice reverse-lookup (`src/voice/gevActions.js`) call the route. It also says the voice reverse-lookup remembers `configured:false`. In HEAD, `placeSearch.js` only re-exports, the calls are in `src/search/defaults.js` and `src/search/http.js`, and `gevActions.js` no longer holds that logic. `src/ui/templates/context.html:246-249` still says "see src/data/osh.js", but production wiring is `src/app/layers/osh.js`. Correct both.

- [ ] F8 minor /home/ianblenke/docker/gev-upstream/openspec/changes/archive/2026-09-25-upstream-sync/proposal.md:33,39 Three statements do not match the ledger.
  - "Gaps that this change closes: none" is false. `history.jsonl` lines 1424-1463 record `closed` for `src/data/radio.js` (511 to 0), `src/annotations/annotationResolver.js` (562 to 0), `src/data/telegeographySubmarineCables.js` (66 to 0) and `src/layers/military/ingestion.js` (47 to 0). They also record `file removed` for three code files and one test file, and many `smaller` entries. `config.yaml` requires Impact to list each gap opened or closed.
  - The list of hand-merged files omits `src/voice/gevActions.js`, `server/providers/places/google.js` and `server/standalone/vite.config.js`. `sync/adopt-summary.txt` shows these are adopted files that both sides changed.
  - The list names `src/app/constructCatalog.js`, `src/search/defaults.js` and `src/search/http.js`, which are not in `both-files.txt`. The counts (16 files, 8 test files) also differ from `both-files.txt` (32) and the adopt summary (8 code plus 10 test files).
  Correct the section.

- [ ] F9 minor /home/ianblenke/docker/gev-upstream/src/data/layerState.js:519 The OSH share-link token changes from `o` to `3`. A share link that the fork made earlier with the OSH layer now turns on the `weather-satellite` layer. No known limit names it. Record it.

- [ ] F10 minor /home/ianblenke/docker/gev-upstream/openspec/specs/credential-boundary/spec.md:96-103 The requirement says the browser "MUST send geocoding requests only to `/api/google/geocode`". The default place search also calls Photon (`src/search/defaults.js:53`) and, as a third step, the keyless route `/api/geocode` (`:57-68`). The `[credential-boundary-013]` tests were edited to expect three calls (`placeSearch.test.mjs:77-89`, `urls.length` 3). Scenario `credential-boundary-013` does not mention the third call. Task 2.7 says "Keep the tests" for tests that changed. Add the third call to the known limits, or write a scenario line for it.

- [ ] F11 minor /home/ianblenke/docker/gev-upstream/src/data/oshProxy.test.mjs:213-218 The `[osh-005]` scan reads `server/providers/common/http.js` as text. That file is now a re-export shell (`common/http.js:1-6`), and the body-reading code lives in `src/sources/httpBody.js`. The scan no longer reaches it. I read `httpBody.js` and found no mutating method or body key, only comments. Add it to the scan or list it as a known limit.

- [ ] F12 minor /home/ianblenke/docker/gev-upstream/openspec/trace/history.jsonl:1451 `src/layers/bikeshare/model.js` opens 16 uncovered branches (0 to 16) with reason `shown by test`, and it is not adopted. Its total branches rose from 2 to 29, so this may be legitimate, but the merged commit changed the file. Confirm that no untested new branch code sits behind this entry, or adopt the file.

Checked and found sound:
- The delta specs equal the live specs. Line positions match the base for every carried scenario, and only `osh-033`, `osh-094`, `credential-boundary-014` and the new `osh-095` differ.
- `ids.json` and `links.json` changed only for `osh-033`, `osh-094`, `osh-095` and `credential-boundary-014`. No `origin/main` test name was renamed. The 13 moved names are identical to their `origin/main` names.
- All 797 adopt lines name `upstream-sync` and have `from` equal to `b210ab0...`. All 837 new history lines name the change, and the 17 `untrue` files match the proposal. No adopt line names a fork-only file (`server/providers/osh*`, `src/layers/osh/`, `src/data/osh*`, `places/geocode.js`, `scripts/spec`).
- The `osh-094` tests expand the template through the same `expandApplicationHtml` that `build/vite.js:15` registers, and they read the style sheet with its imports.
- No POST, PUT, PATCH or DELETE was added. No real address or OSH stream metadata was found in the files the fork wrote or edited. The fixtures are `sys-fixture-*`, `ds-fixture-*` and `osh.example`.
- The `hud.js` banner removal, the `.env.example` OSH lines, the `spec-gates` CI job, `envPrefix`, and `oshProxy()` in `server/providers/local.js` are all kept.

Could not check:
- Running any test or any mutation, and the gate output (empty at read time).
- `make lint` (STE), `check:boundaries`, `format:check`, and CI.
- The Git history, meaning the merge commit, its parents, and whether the upstream remote has `b210ab0` (Rule 21).
- The proposal's counts of uncovered lines, branches and functions.
- `git diff` of the files that the fork edited after the merge and that are not in `both-files.txt`. I read `src/search/http.js`, `defaults.js`, `constructCatalog.js` and `placeSearch.test.mjs` directly.
