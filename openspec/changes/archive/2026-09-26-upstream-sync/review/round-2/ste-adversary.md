Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:42 "Three other hand-merged code files have no adopt line, because they have no gap: `src/app/constructCatalog.js`, `src/search/defaults.js` and `src/search/http.js`." Agreement with the ledger. `openspec/trace/history.jsonl` has an adopt line for each of the three (lines 719, 1045 and 1047). Their gaps are 3 lines, 4 branches and 1 function for `constructCatalog.js`; 2 branches for `defaults.js`; 4 branches and 2 functions for `http.js`. So the sentence is false. It also tells the person who accepts this change on `main` that these files have no gap. `design.md:56` then limits the diff reading to "a file that a person resolved by hand", so that person can skip the files where this change has its hand-written credential-boundary code. The three files are not in the 32 files of `sync/both-files.txt`, so "hand-merged" is also the wrong word for them. Write: "Three more code files have an adopt line, and a person edited them after the merge: `src/app/constructCatalog.js` (3 lines, 4 branches, 1 function), `src/search/defaults.js` (2 branches) and `src/search/http.js` (4 branches, 2 functions). Only the upstream project changed them before the merge." Add these three files to the files in `design.md:56` that the person reads.
- [ ] S2 major openspec/changes/archive/2026-09-26-upstream-sync/design.md:34 "It also remembers, for one coordinate, an answer that has a Google status and gives no place. It never remembers an error answer." (also specs/credential-boundary/spec.md:17 and openspec/specs/credential-boundary/spec.md:108) Two possible meanings. Line 17 says the provider remembers "an answer that has a Google status and gives no place". Line 18 says it does not remember "an answer with an HTTP error status". An HTTP 500 answer with the status `UNKNOWN_ERROR` and no result is in both lines. The test at `src/search/reverseGeocodeRoute.test.mjs:193` pins line 18, and `src/search/http.js:76-78` throws before the code reaches the memory at line 111. "Error answer" in design.md is not defined, and a reader takes an answer with the status `REQUEST_DENIED` as an error answer. Google can send such a status with HTTP 200, and the route passes it on (`server/providers/places/geocode.js:178-185`). Then `http.js:111` remembers it. I could not check what Google sends, so the lead must decide if this memory is intended. Write in spec line 17: "**AND** it remembers an answer that has no HTTP error status, has a Google status and gives no place, so a later call for that coordinate makes no fetch". Write in design.md: "It also remembers, for one coordinate, an answer that has no HTTP error status, has a Google status and gives no place. It does not remember an answer with an HTTP error status or with no Google status."
- [ ] S3 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:38 "The scenario text of `credential-boundary-014` names the provider, and not the voice actions." Articles and nouns. The two sentences before it are about "a Nominatim provider", so "the provider" reads as that provider. Move the sentence to the end of the paragraph at line 36. Write: "The scenario text of `credential-boundary-014` names the HTTP geospatial provider, and not the voice actions."
- [ ] S4 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:48 "`sync-third-geocoder-call`: The default place search calls three services: the server route, Photon and the keyless route `/api/geocode`." (also design.md:38 "a Nominatim provider ... Nominatim") One word, one meaning, and agreement with the code. One thing has two names: "the keyless route `/api/geocode`" in the proposal and "Nominatim" in the design. The code calls it "the local Nominatim route" (`src/search/defaults.js:54`). `createNominatimProvider` is a different object, and the config selects it in place of the three calls. "Calls three services" is true only for a lookup that finds no place. "Two tests of `credential-boundary-013` count three calls" is wrong: `src/search/placeSearch.test.mjs:87` counts three calls in one test, in a loop over two statuses. The test at line 43 answers the third call and does not count it. Write in both files: "the local Nominatim route `/api/geocode`". Write: "A forward lookup that finds no place makes three calls: the server route, Photon and the local Nominatim route. One test of `credential-boundary-013` counts the three calls."
- [ ] S5 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:36 "and smaller counts for 5 files" Numbers. `openspec/trace/history.jsonl:1426-1467` has "smaller" lines for 7 files: `scripts/read-dotenv-value.mjs`, `server/providers/gbfs.js`, `src/bloom.js`, `src/data/lifecycle.js`, `src/layers/bikeshare/model.js`, `src/sources/live/vessels.js` and `src/ui/visualEffects.js`. The 16 "closed" lines, the 3 code files and 1 test file with "file removed", and the three example line counts (511, 562, 66) agree with the ledger. Write "smaller counts for 7 files", or write the rule that gives 5 (for example, "7 files, of which 2 have their own known limit").
- [ ] S6 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:16 "Add the new OSH files to the file lists of two OSH scans. One scan looks for real addresses. The other scan looks for calls that are not GET." Agreement with tasks.md:30. `src/sources/httpBody.js` is a file of the upstream project, not a new OSH file. Write: "Add `src/layers/osh/hosts.js` and `src/app/layers/osh.js` to the file list of the scan for real addresses. Add `src/sources/httpBody.js` to the file list of the scan for calls that are not GET."
- [ ] S7 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:9 "so that the OSH work and the credential boundary work as before." One word, one meaning. "work" is a noun and a verb in one clause, so "the OSH work ... work" can also read as a noun. Write: "Bring the upstream code into the fork. The OSH layers and the credential boundary must work as before." Also design.md:12 "Change only the place where the OSH layers and the geocoders connect to the application." This "only" claim is false for the change: it also edits tests, docs and scenario texts. "the place where they connect" has no definition. Write: "Keep the behavior of the OSH layers and of the geocoders as it was in the fork. Change only how they connect to the application."
- [ ] S8 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:16 "the upstream project owns the structure of the application" Approved words. I am not sure, but "own" as a verb is not an approved STE meaning here (the approved use is "its own"). Write: "the structure of the application comes from the upstream project, and the fork adds its own modules to that structure". Also design.md:18 "and its templates contain the markup": "its" can refer to the page, the shell or the upstream project. Write: "and the component templates of the page contain the markup".
- [ ] S9 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:26 "The scenarios `osh-095` and `osh-096` state the place and the wiring." Words that end in -ing. "wiring" is not in `allowedIng` and is not a technical name. Write: "state the place of the layer, and how the catalog gives it the source and the hosts." Also in design.md:26 "from traffic to directions": these are layer ids. Write "from the layer `traffic` to the layer `directions`". The test `constructCatalog.test.mjs:65-66` agrees.
- [ ] S10 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:34 "for its own life" One word, one meaning. The scenario says "for the life of the provider". Write the same words in both. Also design.md:36 "checked the own cache of the fork in" is not correct English ("own" needs a possessive). Write: "checked the cache that the fork had in `src/voice/gevActions.js`". Also proposal.md:45 "for its own cache and in-flight sharing ... so the test did not apply and does not exist": "sharing" is not in `allowedIng`, and "in-flight" has no definition. "its" can refer to the test or to the fork, and "did not apply" is not clear. Say what "in-flight sharing" means. Write: "The upstream project replaced that cache with a cache for each service, so the test has no code to check. This change does not have the test."
- [ ] S11 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:10 "each gap that the merge brings" Verbs used as nouns, and one word with more than one meaning. "merge" is a noun in design.md:10 and :66 ("the files that the merge brings"), proposal.md:43 ("A later merge of the upstream project") and tasks.md:5 ("the counts of the merge"). "merged files" (proposal.md:17, :34) is a different meaning of "merged" from "the merged commit". Write "the merge commit" (design.md:10, :66), "A later merge commit from the upstream project" (proposal.md:43), "the files that the merge commit brought from the upstream project" (proposal.md:17, :34) and "the counts of the merge commit" (tasks.md:5).
- [ ] S12 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:35 "3414 tests without a scenario ID" One word, one meaning. The spec term is "untraced tests", and design.md:50 uses it. Write "3414 untraced tests". Also proposal.md:44 "a quadratic ingestion of 15000 rows": design.md:44 says "15000 vehicles". Write "15000 vehicles".
- [ ] S13 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:12 "The HTTP geospatial provider and the Google forward geocoder call the server route" One word, one meaning. "the Google forward geocoder" is a new name. `createGoogleGeocoder` serves two routes in `src/search/defaults.js` (lines 42 and 57), so the name has two meanings. Write the function name in inline code. Also design.md:32 "the reverse geocoder" is a second name for the reverse lookup of the HTTP geospatial provider. Also specs/osh/spec.md:155 and the test name at `src/app/layers/osh.test.mjs:46` use "the application layer" and "the elements of the page". The requirement at line 145 says "the OSH systems layer" and "the hosts of the page". Write "the OSH systems layer" and "the hosts of the page" in both places.
- [ ] S14 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:46 "These names stay, because a test name that exists on `origin/main` does not change. Two other moved tests had names that said the opposite of their assertion. The lead renamed these two, and the owner sees this in `review.md`." Agreement, and one word, one meaning. The text gives a rule and then breaks it for two tests without saying so. "the lead" is not a role that any other line of the change defines, and "the owner" and "the person who accepts this change on `main`" may be one person. Write: "The rule is that a test name that exists on `origin/main` does not change. The author of this change broke the rule for two tests, because their names said the opposite of their assertion. The owner decides in `review.md` if these two renames stay." Also proposal.md:50 "The lead wrote two more adopt lines by hand": write "The author of this change wrote". Also state once that the owner is the person who accepts this change on `main`, or use one name.
- [ ] S15 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:3 "Both sides changed 32 files, and a person resolved these files by hand." (also proposal.md:42 and tasks.md:40 "Resolve the conflicts of the 32 files") Agreement. Git merges a file that both sides changed with no conflict when the changes do not overlap, so the text may claim too much. `sync/both-files.txt` has 32 files. `sync/resolution-diff.txt` has 28 files, and it lacks `.gitignore`, `index.html`, `src/standalone/placeSearch.js` and `src/voice/gevActions.js`. I cannot run Git, and I do not know what that file compares. If fewer than 32 files had a conflict, write: "Both sides changed 32 files. A person resolved the conflicts in N of them by hand", and change task 3.2 to say the same.
- [ ] S16 minor openspec/changes/archive/2026-09-26-upstream-sync/specs/osh/spec.md:156 "the fetch of the test answers the same-origin routes of the OSH systems" (also lines 159 and 160) Verbs and articles. "fetch" is a noun here, and a function does not answer a route; it answers a request. "shows" has no object, and "holds" is not the approved meaning "contains" (design.md now writes "contain"). Write: "the test replaces `fetch` with a function that answers the requests to the same-origin OSH routes", "the element `osh-panel` has no `hidden` attribute" and "the element `osh-panel-detail` contains the name of the system". Also line 157 "the test clicks the system on the globe": say which system, for example "the entity of the system with the name System A". The same change applies to the scenario text in `openspec/specs/osh/spec.md` and to the assertion messages that are not in scope.
- [ ] S17 minor openspec/changes/archive/2026-09-26-upstream-sync/tasks.md:41 "Move the OSH panel to the template, and give the OSH layer the token `3`." Instructions. Round-1 finding S24 asked to split this task, and the change did not. Two instructions remain in 3.3 (line 41), 3.4 (line 42: "Write ..., and place the layer in the catalog until ...") and 2.6 (line 30: "Add ... to the address scan ..., and ... to the scan ..."). Split each into two tasks.
- [ ] S18 minor openspec/changes/archive/2026-09-26-upstream-sync/tasks.md:31 "put a real-looking address into `src/layers/osh/hosts.js`" Words that end in -ing. "real-looking" is not in `allowedIng`. Write: "put an address that looks real into `src/layers/osh/hosts.js`".
- [ ] S19 minor openspec/changes/archive/2026-09-26-upstream-sync/tasks.md:14 "rename the element `osh-panel`" Instructions and agreement. An element has an id, not a name. Write "change the id `osh-panel` in `src/ui/templates/context.html`" in mutations 1 and 3, and "change the selector of the rule `.osh-panel[hidden]`" in mutation 4. Also the change added "`aside`" to the scenario `osh-094`, and no mutation of task 2.2 covers it (`src/data/osh.test.mjs:217` needs `<aside`). Add: "Mutation 5: change the tag `aside` of the panel to `div`. The test must fail."
- [ ] S20 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:64 "The files that the change adds or changes" Agreement with tasks 2.1 and 2.2. The list has no `src/data/layerState.test.mjs` and no `src/data/osh.test.mjs`, and `proposal.md:33` uses this list as its list of changed files. Add: "`src/data/layerState.test.mjs` and `src/data/osh.test.mjs`: the tests of `osh-033` and `osh-094`."
- [ ] S21 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:44 "This ceiling catches a gross regression only" Approved words. I am not sure about each word, but "gross" and "regression" are not approved STE words. "catch" is also not an approved word for this meaning. Write: "This ceiling finds only a very large increase of the time." Also proposal.md:44 has the same words in the known limit `sync-ceiling-not-measured`.
- [ ] S22 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:15 "Restore the upstream test of the voice action for the ALPR layer, which the first resolution of the conflicts dropped." Articles and nouns. "which" can refer to the test or to the voice action. "dropped" has the meaning "let fall" in STE, and "the first resolution" is not clear. Write: "Restore the upstream test of the voice action for the ALPR layer. The first version of the conflict resolution removed this test."
- [ ] S23 minor openspec/changes/archive/2026-09-26-upstream-sync/specs/credential-boundary/spec.md:13 "Send a reverse lookup to the server route, and remember a keyless answer" Agreement. The title names one memory, and the scenario now has two. Write: "Send a reverse lookup to the server route, and remember some answers". Also line 17 "for that coordinate": the code keys the memory by the coordinate rounded to four decimals (`src/search/http.js:98`), and no test calls a second coordinate after a no-place answer (`reverseGeocodeTwiceAfter` uses one coordinate). Add a test for a second coordinate that fetches again, or write "for the same coordinate rounded to four decimals". Also the two renamed tests (`reverseGeocodeRoute.test.mjs:126` and `:133`) now say "reject", and no line of the scenario says it. Add: "**AND** a fetch failure, or an answer that is not JSON, makes the reverse lookup reject".
- [ ] S24 minor openspec/changes/archive/2026-09-26-upstream-sync/design.md:28 "The file `src/app/layers/osh.js` then imports only the package entry `src/layers/osh/index.js`" Agreement. This rule, and the rule at design.md:24 ("The layer token must match `^[a-z0-9]$` and must be unique"), are in the design and in no spec. The code agrees (`src/app/layers/osh.js:1-5`, `src/data/layerState.js:641`). The tasks name no mutation for them. Name the upstream test that checks the boundary lists (`src/tooling/packageBoundaries.test.mjs` exists; I did not read it), or record both rules as known limits.
- [ ] S25 minor openspec/changes/archive/2026-09-26-upstream-sync/proposal.md:3 "Together they change 1002 files, and 650 of them are code files." Numbers. `sync/diffstat.txt` says "1025 files changed". I cannot run Git and I do not know which diff it counts. If both count the same set, one number is wrong. If not, say what 1002 counts. Round 1 also raised this, and the lead handling in `skeptics.md` does not answer it. This line did not change since round 1, so treat this finding as open only if the lead cannot explain the number.

Checked and correct:
- The terms "the merged commit", "the merge commit" and "the tree of the merge commit" are used the same way in the four documents. They agree with `gap-ledger` (the "Adoption of merged code" requirement). "the person who accepts this change on `main`" is defined once in design.md:5, and design.md:56 agrees with rule 21 of `AGENTS.md` (must check the upstream remote, must record the result in `review.md`).
- The three conditions of `adopt` in D5 agree with `gap-ledger-089`.
- Numbers that agree with the tree: 797 adopt lines written by the command (799 lines in the ledger, with the 2 hand lines), 525 code files, 272 test files, 17 files with untrue coverage, 16 closed entries, 3 code files and 1 test file removed, 29 registry layers and 30 catalog layers (`local-adsb` is the difference), 13 tests of `credential-boundary-014`, the ceiling of ten seconds, 32 files in `both-files.txt`, the 8 code files and 10 test files with adopt lines, and the two hand adopt lines (44 lines; 184 lines and 102 branches).
- The IDs in tasks 2.1 to 2.9 match the delta scenarios. The names of the 15 tests in `links.json` match the test files. I found no `SHALL`, `SHOULD` or `MAY`, no contraction and no passive verb in an instruction in the new prose.

Not checked:
- I could not run Git or code. The sums (60486 lines, 5866 branches, 1683 functions, 3414 tests), 61 pull requests, 241 commits, 650 code files, 15 live timers, 0.8 seconds and the old ceiling of two seconds.
- No mutation ran, so I did not confirm the mutation claims of tasks 2.1 to 2.6.
- I did not receive the STE lint output. I found the -ing words by hand: "wiring" and "real-looking" are new; "geocoding" is carried text.
- `docs/CURRENT-STATE.md` is outside the texts that I check. Its new words agree with the spec, except "for its own life" (see S10).

Files: /home/ianblenke/docker/gev-upstream/openspec/changes/archive/2026-09-26-upstream-sync/proposal.md, /home/ianblenke/docker/gev-upstream/openspec/changes/archive/2026-09-26-upstream-sync/design.md, /home/ianblenke/docker/gev-upstream/openspec/changes/archive/2026-09-26-upstream-sync/tasks.md, /home/ianblenke/docker/gev-upstream/openspec/changes/archive/2026-09-26-upstream-sync/specs/osh/spec.md, /home/ianblenke/docker/gev-upstream/openspec/changes/archive/2026-09-26-upstream-sync/specs/credential-boundary/spec.md, /home/ianblenke/docker/gev-upstream/openspec/trace/history.jsonl, /home/ianblenke/docker/gev-upstream/src/search/http.js
