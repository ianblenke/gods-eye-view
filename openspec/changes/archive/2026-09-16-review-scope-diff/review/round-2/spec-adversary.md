I read the whole change, the gate code, the tests, the trace files, both round-1 outputs, the agent files, the command file and the full diff against `ba1eccb`.

Round-1 closure check (scope full, round 2): F1 closed (`AGENT_SCOPE` now needs the heading `## Scope of a round`, and test `change-review-027` covers the "## Scope" decoy plus each missing phrase, with `checkAgents(PROJECT_ROOT)` asserted empty). F2 closed (`rounds >= SIGN_OFF_ROUNDS`, scenario `-028`, test asserts `Rounds: 2` stops). F4 closed for minor findings only — see F2 below. F5 closed for the first-round rule (`-030`); the Git check became the named limit `scope-commit-unchecked`. F6 closed (`-029` plus a six-case test). F8, F9, F10 closed. F3 and F7 are only partly closed — see F1 and F6 below. Ledger, trace and STE are clean: eight new IDs in `ids.json` and `links.json`, one tagged test each, no untraced test and no gap entry for `scripts/spec/lib/review.mjs`, no `retired-ids.json`, no new `history.jsonl` line (none is due).

```
Verdict: FAIL
- [ ] F1 major scripts/spec/lib/review.mjs:16 The gate still reads the severity from free prose, so a critical finding can stay open. `MINOR = /\bminor\b/i` matches the word anywhere in the finding text, and the word "critical" is now in no pattern, in no requirement and in no scenario of `openspec/specs/change-review/spec.md`. The text "- [ ] F1 critical scripts/x.mjs:9 The test hides a gap. This is not a minor problem. Accepted by Ian Blenke." passes with "Rounds: 3". Scenario `change-review-028` (delta spec.md:21) says that the gate stops when the finding does not have the severity "minor", so the code does not do the scenario, and the test at src/tooling/spec/review.test.mjs:352 does not find this, because its major example has no word "minor". The path is reachable with an honest agent output: round 1 gives a critical finding, the author does not correct it, round 2 reads only the diff and gives PASS, and the author then accepts the round-1 finding. Read the severity from a fixed position, the second word of the finding text, and compare the letters without case. Add a test for a critical finding with the word "minor" in its text. Round-1 finding F3 asked for this fixed position.
- [ ] F2 major .claude/commands/opsx/review.md:45 The command instructs a procedure that the gate rejects. Line 45 and step 14 at line 48 tell the author to write an open finding for "each open finding after three rounds that is not critical", and `.claude/agents/spec-adversary.md:22` and `.claude/agents/ste-adversary.md:43` give the same rule. The spec requirement (delta spec.md:4) and the gate at scripts/spec/lib/review.mjs:139 accept only the severity "minor". An author who follows step 14 for a major finding gets REVIEW-OPEN-FINDING, and the one way to a green gate is to give that finding the word "minor". A major finding also makes each agent give the verdict FAIL, which REVIEW-AGENT-NOT-PASS stops, so the procedure for a major finding has no end. Write "minor" in the three files, or give the major finding its own rule in the spec.
- [ ] F3 major .claude/commands/opsx/review.md:18 The change adds procedure rules to this file, and no scenario and no test read its content. Step 5 finds the scope, steps 6 and 7 give the scope to the agents, and step 14 gives the round limit. The gate reads the same content of the two agent files (scenario `change-review-027`), but for this file the gate reads only the prose with the STE lint (`openspec/specs/ste-lint/spec.md:25`). An author can remove step 5 or step 14 and each gate stays green. Task 2.3 in tasks.md:16 also has no test task before it, which the rule for tasks in openspec/config.yaml:64 needs. Add a scenario that makes this file name the scope of a round and the limit of three rounds, with a test, or record the gap in the section "Known limits and later changes".
- [ ] F4 minor openspec/changes/archive/2026-09-16-review-scope-diff/specs/change-review/spec.md:4 The number of rounds is self-reported and no gate examines it. The author writes the "Rounds:" line by hand, so the line "Rounds: 3" in the first round opens the acceptance path immediately. The folders `review/round-<n>/` give a count that the gate can read: `establish-spec-governance` has 38 such folders and the line "Rounds: 38". The known limits name the commit and the person, but not this number. Make the gate compare the "Rounds:" value with the number of folders `review/round-<n>/`, or add a known limit.
- [ ] F5 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:32 The known limit `agent-file-freeze` gives the rule and not the gap. It says "The author cannot change a review agent file inside a round", but no gate finds such an edit. The tree hash includes the agent files, so it finds an edit after `review.md` is there. It does not find an edit between two rounds. Round-1 finding F8 asked for this gap. Write: no gate finds an edit of an agent file between two rounds, and only the tree hash finds an edit after `review.md` is there.
- [ ] F6 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:31 The known limit `one-place-by-hand` says "The review agents find such a repeat by hand", and design.md:23 says "The review agents find a repeat of the words of a scenario." No check in `.claude/agents/spec-adversary.md` and no check in `.claude/agents/ste-adversary.md` gives this instruction. The text thus names a control that the agent files do not have. Add the check to the agent files, or write that no gate and no agent check finds such a repeat. Round-1 finding F7 asked for the mechanism; the change corrected only the scope line of the STE agent.
- [ ] F7 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:22 The list of changed files is not complete. The change also changes `src/tooling/spec/gates.test.mjs` and `openspec/changes/archive/2026-09-16-establish-spec-governance/review.md`. The second file is the review record of another change, which the new fields make necessary. Add both files to the Impact section.
```
