I have read the change, the delta spec and the main spec, the two agent files, the review command, the new tests, `scripts/spec/lib/review.mjs` and the round-1 STE output. Here is my review.

```
Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:11 and :33, design.md:27, .claude/agents/spec-adversary.md:22, .claude/agents/ste-adversary.md:43, .claude/commands/opsx/review.md:45 and :48 "each open finding that is not critical" One word, one meaning. The code accepts an open finding only when its text has the word "minor" (`MINOR = /\bminor\b/i` at scripts/spec/lib/review.mjs:16, used at :139), and the test `[change-review-028]` shows that a major finding with "Accepted by Ian Blenke." stops the build. The spec (openspec/specs/change-review/spec.md:116) writes "An open finding with the severity minor". The spec adversary gives three severities, so "not critical" also means major. Write in all seven places: "each open finding with the severity minor".
- [ ] S2 major openspec/specs/change-review/spec.md:116, :129 and :135 (delta :4, :17 and :23) "the text of the finding has the severity "minor"" Approved words. This is the finding S7 of round 1 with the word "critical" changed to "minor". The code tests the word "minor" in all the text of the finding, not a severity field, so "- [ ] F2 major A minor edit hides a gap. Accepted by Ian Blenke." also passes the gate. Write at :129: "the text of the finding has the word "minor", the words "Accepted by " and a name", at :135: "the text of the finding does not have the word "minor"", and at :116: "An open finding with the word "minor" in its text MUST give the name of the person who accepts it."
- [ ] S3 major openspec/changes/archive/2026-09-16-review-scope-diff/design.md:27, .claude/agents/spec-adversary.md:22 and .claude/agents/ste-adversary.md:43 "each open finding that is not critical becomes a known limit" One word, one meaning. This is the finding S10 of round 1, which the author corrected in the proposal only. "known limit" is also the name of the section "Known limits and later changes" of the proposal, which step 11 of .claude/commands/opsx/review.md fills with the minor findings of the spec adversary. The reader cannot tell if the author writes the accepted finding in the proposal or in `review.md`. The proposal now writes at :11 "stays open in `review.md`". Write in all three places: "each open finding with the severity minor stays open in `review.md` with the name of the person who accepts it".
- [ ] S4 major openspec/specs/change-review/spec.md:116 (delta :4) and design.md:19 "the diff against the tree of the round before" One word, one meaning. This is the finding S9 of round 1, which the author corrected in the command file only. In `review.md` the word "tree" is the "Reviewed-Tree:" hash of 64 characters from `make tree` (spec.md:7 and the scenarios `change-review-015` and `change-review-019`), but the "Scope:" line takes a commit of 7 to 40 characters (`SCOPE_VALUE` at scripts/spec/lib/review.mjs:13), and step 5 of the command file writes "the Git commit of the round before". A reader who writes the `make tree` hash in the "Scope:" line makes the gate stop. Write in both places: "the diff against the Git commit of the round before".
- [ ] S5 major openspec/specs/change-review/spec.md:153 and :158 (delta :41 and :46) "That section MUST name the diff rule of a later round and the limit of three rounds" Approved words. The text does not agree with the code twice. First, `checkAgents` tests the three patterns of `AGENT_SCOPE` (scripts/spec/lib/review.mjs:18) against all the prompt, not against that section, so an agent file with the two rules in another section passes. Second, the code needs the words "diff since the round" and "limit of three rounds", not any text that gives the rules. Write at :153: "Each review agent file MUST have a section with the heading "Scope of a round". The file MUST have the words "diff since the round" and the words "limit of three rounds"."
- [ ] S6 major openspec/specs/change-review/spec.md:158 (delta :46) "- **OR** the file does not name the diff rule or the limit of three rounds" OpenSpec words. OpenSpec needs `MUST`, `WHEN`, `THEN` and `AND`. `OR` is not one of them, and this is the only `**OR**` line in `openspec/`. The three lines also read as "(A or B) and C" or as "A or (B and C)", and the third line is a statement, not a condition. Write the scenario with two lines: "- **WHEN** a review agent file has no heading "## Scope of a round", or the file does not have the words "diff since the round" or the words "limit of three rounds"" and "- **THEN** the review gate stops the build".
- [ ] S7 major .claude/commands/opsx/review.md:48 "14. The review has a limit of three rounds. After the third round, ask the person to accept each open finding..." Instructions. Round 1 (S36) asked for a numbered step, but this position is wrong: step 13 writes `review.md` with the line "- [ ] <each open finding after three rounds that is not critical, with "Accepted by <the name of the person>">", and the reader cannot write that line before he asks. Move this step before step 13, as a new step 12.
- [ ] S8 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:5 "makes the review of each later change smaller" Approved words. "small" is a size. The sentence can mean fewer rounds or fewer findings. Write: "gives the review of each later change fewer rounds and gives the review an end".
- [ ] S9 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:12 and :32 "Stop an edit of the files of the review agents inside a round" and "The author cannot change a review agent file inside a round." One word, one meaning. design.md:31 and .claude/commands/opsx/review.md:48 also freeze `.claude/commands/opsx/review.md`, which the proposal does not name. Write at :12: "Stop an edit of the files of the review agents and of the review command inside a round." Write at :32: "The author cannot change a review agent file or the review command inside a round."
- [ ] S10 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:24 "This change does not change the code of the app." One word, one meaning. "change" is the OpenSpec change and also the verb in one sentence. Write: "The code of the app stays the same."
- [ ] S11 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:31 and design.md:23 "find such a repeat by hand" and "find a repeat of the words of a scenario" Verbs. "repeat" is a verb that the text uses as a noun. Write at proposal.md:31: "The review agents find such a repeated rule by hand." Write at design.md:23: "The review agents find the words of a scenario that another text repeats."
- [ ] S12 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:32 "A wrong instruction in an agent file waits for the next change." Verbs. An instruction does not wait. Write: "The author corrects a wrong instruction in an agent file in the next change."
- [ ] S13 minor openspec/changes/archive/2026-09-16-review-scope-diff/design.md:8 "Make the number of findings of a round fall to zero in a few rounds." Approved words. I am not sure that "fall" is approved for a number; in STE it is a movement to a lower position. Write: "Make the number of findings of a round decrease to zero in a few rounds."
- [ ] S14 minor openspec/changes/archive/2026-09-16-review-scope-diff/design.md:9 "Keep the gate strong" Approved words. "strong" is a physical property. Write: "Do not make the gate weaker: a critical finding always stops the build."
- [ ] S15 minor openspec/changes/archive/2026-09-16-review-scope-diff/design.md:13 "The change `simplify-ledger` does this." Verbs. The change is not there yet; the simple present says that it does this now. Write: "The change `simplify-ledger` will do this."
- [ ] S16 minor openspec/changes/archive/2026-09-16-review-scope-diff/design.md:19 "`review.md` gets two new lines" Approved words. "get" is not an approved STE verb. Write: "`review.md` has two new lines".
- [ ] S17 minor openspec/changes/archive/2026-09-16-review-scope-diff/design.md:23 "No gate makes this rule true." One word, one meaning. The sentence before it writes "the words of the rule" for the rule in a scenario, so "this rule" can be that rule or the one-place rule. Write: "No gate stops a text that repeats the words of a scenario."
- [ ] S18 minor openspec/changes/archive/2026-09-16-review-scope-diff/design.md:27 "the name of the person who accepts it" Articles and nouns. "it" can refer to the finding or to the decision. Round 1 (S27) corrected the same words in the two agent files. Write: "the name of the person who accepts the finding".
- [ ] S19 minor .claude/agents/spec-adversary.md:20 and .claude/agents/ste-adversary.md:41 "is the one exception" Approved words. "one" is a number here, but the sentence needs "only". Write: "is the only exception".
- [ ] S20 minor .claude/commands/opsx/review.md:48 "ask the person to accept each open finding" One word, one meaning. This file writes "the user" for the reader of the command (lines 8, 21 and 50), so "the person" is a second word for the same reader. Round 1 (S35) asked for the word of the specs, which made this pair. Write: "ask the user to accept each open finding with the severity minor. Write the name of that person in the finding."
- [ ] S21 minor openspec/specs/change-review/spec.md:143 (delta :31) and the test name "[change-review-030] stops for a first round with another scope than the full change" Approved words. "another ... than" is not a correct form. Write: "Stop for a first round with a scope that is not the full change".
- [ ] S22 minor openspec/specs/change-review/spec.md:148 (delta :36) and the test name "[change-review-031] stops for a rounds line below one" Approved words. "below" is a position, not a number. The gate message writes "a number of 1 or more". Write: "Stop for a rounds line with a number less than 1".
- [ ] S23 minor openspec/specs/change-review/spec.md:133 (delta :21) and the test name "[change-review-028] stops for an open finding that a person accepts before three rounds" Articles and nouns. The WHEN line of the scenario has two conditions, and the test also checks the second one ("F1 major A test hides a gap."), but the title names only the number of rounds. Write: "Stop for an open finding that a person accepts before three rounds or without the word "minor"".
- [ ] S24 minor openspec/changes/archive/2026-09-16-review-scope-diff/tasks.md:16 "the round limit and the sign-off" Verbs. "sign-off" is a noun from the phrasal verb "sign off". Round 1 (S15) corrected the other four places, but not this one. Write: "the round limit and the acceptance by a person".
```

Notes outside the findings. The gate output is clean for STE (`STE: 0 errors, 0 warnings.`) and the only gate error is the expected `REVIEW-MISSING`. The change adds no word to `openspec/ste/words.json` and no `allowedIng` entry, so there is nothing to examine for check 5. Of the 36 findings of round 1, four are not really closed: S7 (now S2), S9 (now S4), S10 (now S3) and S15 (now S24); the other 32 are closed. Two of my round-1 corrections made a new problem: S35 gave the command file two words for its reader (S20), and S36 put step 14 after the step that needs it (S7). I changed no file in the repository.
