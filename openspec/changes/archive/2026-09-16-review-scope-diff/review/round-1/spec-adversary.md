I read the full change (round 1, scope `full`): the four change documents, the delta spec, the diff of `scripts/spec/lib/review.mjs`, both agent files, `.claude/commands/opsx/review.md`, both test files, `ids.json`, `links.json`, the gate output and the archived `review.md` of `establish-spec-governance`.

```
Verdict: FAIL
- [ ] F1 major scripts/spec/lib/review.mjs:17 The check of the agent files finds no real scope rule. The pattern `\b(diff|scope)\b` matches the word "scope" in any sentence. The file `.claude/agents/ste-adversary.md` has the heading "## Scope" at line 16 for a different list of files. That heading alone makes the pattern true. Thus the gate only makes each agent file contain the words "three rounds", and an author can remove the section "Scope of a round" and keep the gate green. Make the gate find the heading "Scope of a round" and a sentence with the diff rule in the prompt. Add a test with an agent file that uses the word "scope" in another sense.
- [ ] F2 major scripts/spec/lib/review.mjs:134 The gate accepts a sign-off in each round. The code reads no round number. A review with the lines "Rounds: 1" and "- [ ] F1 major ... Accepted by <name>" passes. The proposal at line 11 and the design at line 27 give the limit of three rounds. Stop the build when the "Rounds:" value is less than 3 and a finding that is not checked has "Accepted by". Add a scenario for this rule.
- [ ] F3 major scripts/spec/lib/review.mjs:16 The gate reads the severity from free prose. The pattern `\bcritical\b` has no `i` flag, so it does not find "Critical" at the start of a sentence. A finding with no severity word also passes. Thus an author can accept a critical finding and the build does not stop, which scenario `change-review-028` forbids. Read the severity from a fixed position in the finding text and compare the letters without case. Add tests for "Critical" and for a finding with no severity word.
- [ ] F4 major scripts/spec/lib/review.mjs:143 The sign-off cannot work with the scenario `change-review-018`. The gate stops the build when an agent output file does not have the line "Verdict: PASS". An agent gives FAIL for each critical finding and each major finding. A minor finding is already a known limit after step 11 of the review command. Thus the new mechanism gives no new result, or it makes the author write "Verdict: PASS" in `review/spec-adversary.md` for a verdict that the agent did not give. The gate cannot see the difference. Give the sign-off its own rule in the spec: let the agent output keep the verdict FAIL when each open finding has a sign-off, or limit the sign-off to minor findings.
- [ ] F5 major scripts/spec/lib/review.mjs:129 The gate does not make the first round read the whole change. A review with the lines "Rounds: 1" and "Scope: diff 1a2b3c4" passes. The gate also does not examine the commit. A commit that is not in the repository passes, and the commit of the current tree passes with an empty diff. The known limit `review-scope-blind` in proposal.md:29 tells that the first round of each change reads the whole change, so the gate must hold that rule. Stop the build when the "Rounds:" value is 1 and the scope is not "full". Examine the commit with Git. Add a scenario for each rule.
- [ ] F6 major openspec/changes/archive/2026-09-16-review-scope-diff/specs/change-review/spec.md:11 No scenario tells the gate to stop for a scope line that is not correct. The scenario `change-review-025` only tells that a correct line does not stop the build. The error REVIEW-SCOPE lives only in the code and in the test. A later change can remove that error and the assertions of the test, and each scenario stays verified. The design at line 23 tells that a rule lives in a spec scenario. Add the scenario "Stop for a scope line that is not correct".
- [ ] F7 major openspec/changes/archive/2026-09-16-review-scope-diff/design.md:21 The decision "One place for each rule" has no mechanism. No requirement, no gate, no test and no agent rule makes the design and the known limits name a scenario ID. The same decision tells that the STE agent reads only the delta specs of the change, but `.claude/agents/ste-adversary.md:20` still tells that agent to read each new line in `openspec/specs/`. The proposal at line 10 gives this item as a change of this change. One of the three causes in the analysis thus stays open. Change the agent file and add a rule for the one place, or remove the item from the proposal and the design.
- [ ] F8 minor openspec/changes/archive/2026-09-16-review-scope-diff/proposal.md:12 The proposal tells that the change stops an edit of the agent files inside a round. Only the prose in `.claude/commands/opsx/review.md:48` gives this rule. The tree hash finds such an edit after `review.md` is there, because the agent files are diff files outside `openspec/changes/`. It does not find an edit between two rounds. Add this limit to the section "Known limits and later changes".
- [ ] F9 minor scripts/spec/lib/review.mjs:9 The "Rounds:" value has no smallest value. The line "Rounds: 0" passes, but a review with zero rounds is not possible. No scenario gives a value for this line. Stop the build for the value 0.
- [ ] F10 minor openspec/changes/archive/2026-09-16-establish-spec-governance/review.md:7 The file now has two "Rounds:" lines: the new field at line 7 and the prose line at line 11. A reader finds two places for the same number, and a later edit can make them different. Remove the number from the prose line and keep only the sentence about the folders `review/round-<n>/`.
```

Notes for the caller, outside the findings list: the gate output has only the expected REVIEW-MISSING error; the ledger, the trace and the coverage results are clean, `scripts/spec/lib/review.mjs` has no gap entry, and the five new IDs `change-review-024` to `-028` each have one tagged test in `links.json`. The new tests assert the real error codes and messages, so I found no weak test among them.
