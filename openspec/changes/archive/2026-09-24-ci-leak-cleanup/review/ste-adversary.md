Verdict: PASS
- [ ] S33 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:11 "So the Docker image did not show the fault." Articles and nouns. "the fault" can mean either of the two faults, and an image does not show a fault. Write: "So the gates in the Docker image did not show the second fault."
- [ ] S34 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:18,36 "the `flights` run", "The `militaryFlights` run" One word, one meaning. "run" means a CI or gates run elsewhere, and design.md D2 says "test" for the same thing. Write: "the `flights` test" and "The `militaryFlights` test".
- [ ] S35 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:34 "a revert of one fix still passes `make gates`" Verbs. "revert" is a verb that the text uses as a noun. Write: "If you revert one fix, `make gates` in the Docker image still passes."
- [ ] S36 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:35 "because a module request can still start a timer of 50 ms" This states as fact what design.md:28 and the code comment give as "read in the source, not confirmed by a run". Write: "because the source of Vite 6.4.3 shows that a module request can still start a timer of 50 ms. No run confirmed it."
- [ ] S37 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:36 "the mock does not clear the drip timer of the flights layer" A mock makes no real timer, so "the drip timer" has two possible readings (the real timer, or the mock timer). The next bullet explains the mock timer. Write: "the mock replaces the drip timer of the flights layer with a mock timer. This change adds no cleanup to the layer. The layer clears the drip timer only in its own teardown."
- [ ] S38 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:38 (also design.md:44) "stops the drip callback ... from running in `src/data/trackedModelRegime.test.mjs`. A coverage total of that file" Words that end in -ing, and articles and nouns. "running" is not a technical name. "that file" can mean the test file, which has no coverage total, or `enrichment.js`. Write: "the mock does not run the drip callback of `src/layers/flights/enrichment.js` in `src/data/trackedModelRegime.test.mjs`. A coverage total of `src/layers/flights/enrichment.js` can move."
- [ ] S39 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:5 "The three leaks show on the GitHub runner" Words. proposal.md:7 counts four live timers in three files (one, two and one). "three leaks" counts files and reads as a count of timers. Write: "The leaks of the three files show on the GitHub runner".
- [ ] S40 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:16 "Every timer that leaks is a timer of Vite or of the flights layer" The tracer named no timer for `trafficTiming.test.mjs` (proposal.md:35). The sentence claims a source for that timer. Write: "Every timer that the tracer named is a timer of Vite or of the flights layer, and a test starts both."
- [ ] S41 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:24 "With the optimizer stopped, a third timer shows" Verbs. "stopped" is a participle that STE does not approve. Write: "When the optimizer does not run, a third timer shows:".
- [ ] S42 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:28 "A wait does not remove a timer." and "the leak still showed once under load" The new sentence, "The wait stays ... a timer of 50 ms", makes the first one wrong. The 750 ms wait lets a timer of 50 ms fire. "once" does not agree with proposal.md:7 ("on some runs only"). Write: "A wait lets a short timer fire, but it does not stop a long timer." and "the leak still showed on the runner in some runs".
- [ ] S43 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:38 "The acceptance test is the gate `GATES-TEST-LEAK`, run as CI runs it. The fix branch ran `node scripts/spec/gates.mjs ci`" Verbs. "run" is a participle, and a branch does not run a command. Write: "The acceptance test is to run the gate `GATES-TEST-LEAK` the way CI runs it. The runner ran `node scripts/spec/gates.mjs ci` on the fix branch, without the tracer."
- [ ] S44 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:40 "The run reported errors only for the tracer file and the temporary workflow file. ... The same run on the branch without the fixes ... so the run shows the fault and the fix." Articles and nouns. "the temporary workflow file" has no earlier mention, because the round-1 text of task 1.2 that named it is gone. "The run" can mean the gates run, the tracer run or the format check. "The same run" is a different run on another branch, and one run cannot show both the fault and the fix. Write: "The gates run reported errors only for the tracer file and for a temporary workflow file. This change does not contain those two files. The gates on a branch without the fixes listed `GATES-TEST-LEAK` for the three files. So the two runs show the fault and the fix."
- [ ] S45 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:53 "`noDiscovery` can stop a test from finding a dependency problem" Words that end in -ing. "finding" is not a technical name. This is the round-1 wording S19, and it adds an -ing word. Write: "With `noDiscovery`, a test cannot find a dependency problem."
- [ ] S46 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/tasks.md:5 "Run the CI workflow on a temporary branch that holds a timer tracer." Not sure it agrees with the prose. design.md:40 names a temporary workflow file, and D3 says the run was `node scripts/spec/gates.mjs ci`. If a temporary workflow ran, write: "Run a temporary workflow with a timer tracer on a temporary branch."
- [ ] S47 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/tasks.md:32-37 Section 5 has no task for the format check on the runner. proposal.md:25 and design.md:38 give that result as evidence. Write: "5.5 Run `npm run format:check` on the runner with the fixes." "5.6 Confirm that the step prints no error."
- [ ] S48 minor src/data/trafficTiming.test.mjs:340 "The config above now stops the dependency optimizer and the file watcher, which started most of these timers." The tracer named no timer for this file, so "most" has no evidence. "which" can refer to the watcher only. Write: "The config above now stops the dependency optimizer and the file watcher. In `src/tooling/previewServing.test.mjs`, these two started the timers that the tracer named."
- [ ] S49 minor src/data/trafficTiming.test.mjs:165 "No dependency discovery and no file watcher: each arms timers" One word, one meaning. The changed comment at line 340 says "dependency optimizer" for the same thing. Write: "No dependency optimizer and no file watcher: each starts timers that are still live when the test process exits (found on the CI runner)."
- [ ] S50 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:11,35 and design.md:28 (also src/data/trafficTiming.test.mjs:342) "stays live", "The wait stays" Approved words. I am not sure "stay" is an approved STE word. I know "remain" is approved. Write: "remains live" and "The wait remains".

Checked, with no finding:
- **Round-1 majors:** The S1 to S7 corrections agree with `.github/workflows/ci.yml`, with `gates.mjs` (`--test-force-exit`, `GATES-TEST-LEAK`) and with the test files. The two `previewServing` tests each serve a fixture page, and the `trafficTiming` test loads a module with `ssrLoadModule`.
- **Vite claim:** The claim about the 50 ms timer holds in the Vite 6.4.3 source, in `dep-Dm0c1Wj2.js`. `transformRequest` calls `_registerRequestProcessing` (line 35745). That reaches `checkIfCrawlEndAfterTimeout` (lines 48186-48193), which sets the 50 ms `setTimeout`. `close()` only sets a flag through `cancel()` (lines 48102 and 48162). A timer that a request started before the close still fires.
- **Optimizer options:** `noDiscovery: true` with an empty `include` disables the optimizer (line 14726).
- **Flights layer:** `src/layers/flights/enrichment.js:45-49` and `lifecycle.js:311-313` agree with D2 and with the `enrichment-queue-stops` limit.
- **Docker image:** The Dockerfile and `.node-version` use Node 24.21.0, so "same Node version" holds.
- **Spec:** `coverage-gate` spec lines 252-272 cover the live-timer requirement, so "no scenario" holds.
- **Not run:** I ran no test and no mutation, because the code part of the diff is a comment only.
