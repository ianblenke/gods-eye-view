Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:5 "This check is the first step of the Node 24, Node 26 and Windows jobs, so those jobs stop there." Text does not agree with `.github/workflows/ci.yml`. The check is step 5 of the Node jobs (after checkout, setup-node, `npm ci` and `npm run doctor`) and step 4 of the Windows job. The jobs are named "Node 24.14.0", "Node 26.x" and "Windows onboarding". Write: "This check is a step of the jobs "Node 24.14.0", "Node 26.x" and "Windows onboarding". When the check fails, the job stops and the later steps do not run."
- [ ] S2 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:9 "A tracer on the GitHub runner named each timer." Proposal line 30 and design.md:24 say the tracer named no timer for `trafficTiming.test.mjs`. Write: "A tracer on the GitHub runner named the timers of `previewServing.test.mjs` and `trackedModelRegime.test.mjs`."
- [ ] S3 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:22 "Each fix is proven leak-free with the tracer on the GitHub runner, and with the gates in the Docker image." The evidence disagrees across the three files. Proposal line 7 says the Docker image gates passed before the fix, so they cannot prove a fix. Design.md D3 says the plain gates on the runner give the proof. Tasks 2.3 and 4.2 say the tracer runs in the gate image. The sentence is also passive. Write, after you check the facts: "The plain gates on the GitHub runner list no `GATES-TEST-LEAK` for the three files. The tracer in the gate image lists no `Timeout` for the two files where it named a timer."
- [ ] S4 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:16 "Every leak is in test code, or in a library that a test starts." D2 of the same file says the drip timer comes from the flights layer: production code, `src/layers/flights/enrichment.js:46`. The layer is not a library. Write: "Every leak comes from a timer that a test starts, in a library (Vite) or in the flights layer."
- [ ] S5 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:32 (also proposal.md:16) "The two fleet-loader tests ... call the fleet loader of a flights layer" and "The flights layer parks a drip timer in these tests". Text does not agree with the code. The second test uses the military layer, and `src/layers/military` has no enrichment queue and no `setTimeout`. `_enrichDripTimer` exists only under `src/layers/flights`. Only the `flights` test sets a drip timer. Write: "The `flights` fleet-loader test calls the fleet loader of the flights layer. That loader queues a type enrichment and sets one drip timer of about 165 ms. The `militaryFlights` test sets no timer. It gets the same mock because both tests use one test body."
- [ ] S6 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:38 "The fix branch ran on the runner with the plain gates against `HEAD`. The run listed no `GATES-TEST-LEAK` and no formatting error." `scripts/spec/gates.mjs` has no formatting check. "Needs formatting" comes only from `scripts/format.mjs` (`npm run format:check`), a separate CI step, and proposal.md:28 says `make gates` does not run it. The text has two possible meanings. Write, after you check which run gave which result: "The fix branch ran the CI workflow on the runner. The job "Spec gates" listed no `GATES-TEST-LEAK`, and the step `npm run format:check` passed."
- [ ] S7 major openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:49 "the three tests serve fixture pages and provider JSON." Text does not agree with the code. The test in `trafficTiming.test.mjs` serves no page and no JSON. It loads one module with `ssrLoadModule` in middleware mode. Design.md:26 also gives the reason "The page of each fixture has no script" for two tests only. Write: "The two tests in `previewServing.test.mjs` serve a fixture page and provider JSON. The test in `trafficTiming.test.mjs` loads one module with `ssrLoadModule`. None of the three tests checks the optimizer."
- [ ] S8 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:7 "The gates that run in the Docker image showed neither fault" Text has two meanings. The format check is not in `make gates` (proposal.md:28). Write: "The gates in the Docker image did not show the second fault. `make gates` does not run the format check, so it passed while CI failed."
- [ ] S9 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:24 "The change makes the CI jobs pass on `main` again." No task proves it. In the Node jobs and the Windows job, the steps after `npm run format:check` did not run in the last three runs. Write: "The change removes the two faults. It does not change what a job checks."
- [ ] S10 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:42 "Coverage: no code file changes, so no coverage total moves." This is the imprecision that round 2 of `test-teardown-cleanup` found (F2). The mock stops the drip callback at `src/layers/flights/enrichment.js:46-49` from running in this file. `openspec/trace/history.jsonl` line 11 already records that file as unstable (83 to 88 lines). Write: "Coverage: no code file changes. The mock timer stops the drip callback of `src/layers/flights/enrichment.js` from running in `trackedModelRegime.test.mjs`, so a coverage total of that file can move. Task 5.3 measures it." (Not run: I could not measure coverage.)
- [ ] S11 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:28 "A longer wait after `server.close()` is the other fix." The sentence reads as a fix that this change applies, and the next sentence rejects it. Write: "A longer wait after `server.close()` is a fix that this change does not use."
- [ ] S12 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:26 "it gets the same two options" The change sets three settings: `noDiscovery` and `include` under `optimizeDeps`, and `watch` under `server`. Write: "it gets the same three settings."
- [ ] S13 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:14 and design.md:22 "turn off the dependency discovery" and "D1 Turn off what a test does not need in Vite". Verbs. "turn off" is a phrasal verb. Write: "set the options that stop the dependency optimizer and the file watcher in each Vite dev server that the tests start", and "D1 Stop the Vite features that a test does not need".
- [ ] S14 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:14 and design.md:26 "dependency discovery" One word, one meaning. Design.md:24 and :49 say "dependency optimizer" and "optimizer" for the same thing. `noDiscovery` with an empty `include` disables the whole optimizer (`isDepOptimizationDisabled`, vite 6.4.3, dep-Dm0c1Wj2.js:14725). Write "dependency optimizer" in all places.
- [ ] S15 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:29 and design.md:18 "a leaked timer" One word, one meaning. The change uses "live timer" elsewhere, and "leaked" is also a participle. Write: "a timer leaks only when it is live at process exit" and "already checks for a live timer".
- [ ] S16 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:9,29 and design.md:5,7 "has not yet fired", "have fired", "when it has not fired", "has fired", "had already fired" Verbs. These are perfect tenses. Write: "If a timer does not fire before the process exits, the timer stays live. A faster or slower machine changes which timers fire before the exit." / "a timer is a leak only when it is live at process exit" / "A timer that fires before the process exits is not live." / "timers that fired before the exit".
- [ ] S17 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:16 and design.md:32 "parks a drip timer" Approved words. "park" is approved only for aircraft. Write: "sets a drip timer".
- [ ] S18 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:24 "arms a timer of 200 ms ... a timer of 50 ms" Approved words. I am not sure "arm" is approved. Write: "starts a timer of 200 ms ... a timer of 50 ms".
- [ ] S19 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:9 and design.md:49,50 "hid the fault", "`noDiscovery` hides a dependency problem", "A mock timer hides a timer that a test needs" Approved words. I am not sure "hide" is approved. Write: "did not show the fault", "`noDiscovery` can stop a test from finding a dependency problem", "A mock timer can remove a timer that a test needs".
- [ ] S20 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:7 "The runtime list of active resources settled that: it agreed with the gate, and the tracer then skipped destroyed timers." and "to the fork" Approved words and articles. "that" has no clear referent. "the fork" is not defined anywhere. Write: "The first version of the tracer also listed timers that fired before the exit. The list of active resources from the runtime showed which timers were live, and it agreed with the gate. The tracer then skipped destroyed timers." and "to a fork of the repository on GitHub".
- [ ] S21 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:3 "is a leak, whatever it does later" Approved words. "whatever" is not approved. Write: "is a leak. It does not matter what the timer does later."
- [ ] S22 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:34,50 and proposal.md:16 "waits on a real timer", "awaits a real timer", "keeps the real event loop free of this timer", "the real one stays live" One word, one meaning, and ambiguous words. Write: "waits for a real timer" in both places, "With the mock, `setTimeout` makes no real timer", and "without the mock, the timer stays live".
- [ ] S23 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:5,48 "The difference is timing." and "depends on timing" Words that end in -ing. "timing" is not a technical name and is not in `allowedIng`. Write: "The difference is the time at which each timer fires." and "A leak that depends on the time at which a timer fires can come back."
- [ ] S24 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:32 "and these tests do not run it" Articles and nouns. "it" can refer to the timer or to the teardown. Write: "and these tests do not run the teardown."
- [ ] S25 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:3 and proposal.md:9 "The gate `GATES-TEST-LEAK` reads `process.getActiveResourcesInfo()`" and "Each test process exits with a force-exit flag" One word, one meaning. The test guard reads the resources (`scripts/spec/lib/test-guard.mjs:95`), and the gate reports the result (`scripts/spec/gates.mjs:241`). The gate starts each test process with the flag (`gates.mjs:95`). Write: "The test guard reads `process.getActiveResourcesInfo()` when a test process exits, and the gate `GATES-TEST-LEAK` reports each live timer." and "The gate starts each test process with a force-exit flag."
- [ ] S26 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:9,28 "the local run", "the local command" One word, one meaning. Both mean `make gates` in the Docker image. Write: "the gates in the Docker image" and "`make gates`".
- [ ] S27 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:38 "Its only errors were the two temporary diagnostic files" An error is not a file. Tasks.md:5 names "the tracer and a temporary workflow". Write: "The run reported errors only for the tracer file and the temporary workflow file, which this change does not contain."
- [ ] S28 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/tasks.md:4-5,12-13,25-26,32-33 "Run ... " with the sub-bullets "Delete the branch after the run.", "Confirm that the active-resource list has no `Timeout`." Instructions. Each of these four tasks has two sequential instructions. The precedent (`test-teardown-cleanup`, round 1 S19) split them into separate tasks. Write: give "Delete the branch after the run." and each "Confirm ..." its own numbered task.
- [ ] S29 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/tasks.md:34,35 "Run `make gates CHANGE=ci-leak-cleanup`. Read the command output for the verdict." and "Run `npm run format:check` and `npm run check:boundaries`." Instructions. Two sequential instructions in 5.3, and two commands in 5.4. Write: "5.3 Run `make gates CHANGE=ci-leak-cleanup`." "5.4 Read the command output for the verdict." "5.5 Run `npm run format:check`." "5.6 Run `npm run check:boundaries`."
- [ ] S30 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/tasks.md:4 "Run a temporary branch with a timer tracer on the GitHub runner." One does not run a branch. Write: "Run the CI workflow on a temporary branch that holds a timer tracer."
- [ ] S31 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/design.md:34 "The mock resets at the end of each test." The mock resets, but `flightState._enrichDripTimer` keeps the mock timer. Only the layer teardown clears it (`src/layers/flights/lifecycle.js:311`). For the rest of the process, later `flights` tests never set a new drip timer, and the enrichment queue does not drain. The risk paragraph (design.md:50) does not say this. Write: "The mock resets at the end of each test. The layer keeps the mock timer in `_enrichDripTimer` until its teardown, so the enrichment queue does not drain in the later tests of this file. No later test needs a drained queue."
- [ ] S32 minor openspec/changes/archive/2026-09-24-ci-leak-cleanup/proposal.md:30 "The fix removes the same two sources that the other Vite tests had." This is incomplete for `trafficTiming.test.mjs`. In vite 6.4.3 the 50 ms crawl-end timer (`checkIfCrawlEndAfterTimeout`, dep-Dm0c1Wj2.js:48186-48193) also starts on each module request (`transformRequest`, line 35745), even when the optimizer is off. `cancel()` at line 48162 does not clear it. In `previewServing.test.mjs` no module request happens. In `trafficTiming.test.mjs`, `ssrLoadModule` requests modules, so only the existing 750 ms wait removes that timer. I read this in the source and did not run it. Write: "The fix removes the same two sources that the other Vite tests had. The 750 ms wait stays, because a module request can still start a timer of 50 ms."

Not done: I had no shell tool (Read, Grep and Glob only). I could not run `git diff`, `node --test` or the tracer, and I ran no mutation. Every code claim was checked by static reading of the current files. The three test files show no changed test name. The names match `openspec/trace/links.json` and `gaps.json`. I could not confirm "no assertion changed" against the diff. The reasons for `noDiscovery`, `include: []` and `watch: null` hold in vite 6.4.3, at dep-Dm0c1Wj2.js:14725-14726, 38519-38531 and 48041-48045. The mock `setTimeout` reason holds, because `enrichment.js:46` calls the global `setTimeout`.
