No skeptic agent ran in round 4. Both reviewers gave the verdict PASS, so the lead did not start a new round for their minor findings. The lead's decision for each minor finding:

- Spec F3 and F4, and STE S7 to S11, S14 and S15 (minor): corrected in `tasks.md`, `proposal.md` and `design.md` of the archived change. The tasks now say `valid`, name the `[osh-093]` tests, and use the words of `osh-069` and `osh-090`.
- Spec F1 and F2 (minor): named as the known limits `osh-occluder-visible` and `osh-html-test-comments` in the proposal.
- Spec F5 (minor): `review.md` records the run of the real player in a headless browser.
- STE S1 to S6, S12, S13 and S16 (minor): kept. Each needs a change of the scenario text, of a test name or of a comment in a test file. That change means a new ratchet and a new review round for a wording point. The code and the tests are right in each case, and the change is on record here for the person who accepts the minors.
