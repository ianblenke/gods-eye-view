Verdict: PASS

Findings:
- [ ] S1 (minor) "element of the list" has two meanings: `openspec/changes/archive/2026-09-25-osh-camera-video/specs/osh/spec.md:211` (same line in `openspec/specs/osh/spec.md:849`) says "the world overlay places a label or a card clear of an element of the list when it can". The list holds selectors. An element of the list is a selector, and a label cannot be clear of a selector. In the same spec, "element" also means a page element (osh-094). The code and design D79 make the meaning clear, but a reader of the spec alone must guess. Write: "the world overlay places a label or a card clear of each element that matches a selector of the list, when it can".
- [ ] S2 (minor) The test name says "card", but the test paints a label: `src/overlays/worldOverlay.test.mjs:1532`. `selectedEntry()` gives the variant `label` by default (`:414`), and the test passes no variant. The code has `label` and `card` as two variants. Line 211 of the spec says "a label or a card", and the test covers one variant only. Write the name: "[osh-093] the world overlay places a label clear of the panel of the OSH layer when it can". Change "the card" to "the label" in the two assertion messages (`:1546`, `:1553`). Change "steps clear of it" to "is clear of it". Alternatively add a second case with `variant: 'card'`.
- [ ] S3 (minor) An id is called a datastream: `specs/osh/spec.md:83` (synced `:721`) says "or the id is a ninth datastream". An id is not a datastream. Line 85 and `osh-068` say "for a ninth datastream". Write: "and either no root answers or the request is for a ninth datastream".
- [ ] S4 (minor) `osh-080` still reads as two scenarios in one. `specs/osh/spec.md:115` says "after that the hub keeps no group ...". "that" can mean the key message or the group, and the sentence has no clear noun. Write lines 114 and 115 as: "the hub starts the group again at each key message" and "when a group has more than 2097152 bytes, the hub empties it and keeps no group until the next key message". The title at `:109`, "Replay the current message group to a late client", now names only the first trigger. The WHEN also has the close and the retry. Write: "Replay the message group to a late client and open the socket again after a close". This needs the same change in the synced spec. Check the trace files as text, and do not round-trip the JSON.
- [ ] S5 (minor) `differs` in `specs/osh/spec.md:169` (synced `:807`): "an SPS that differs from the SPS of the last configuration". I think `differ` is not an approved STE verb, and `different` is. Round 3 proposed "is different from". Write: "it configures the decoder again when a key message has a PPS and an SPS that is different from the SPS of the last configuration". This has 25 words, which is the limit. Check with the lint.
- [ ] S6 (minor) `osh-087` line 191 (`specs/osh/spec.md:191`, synced `:829`) reads as if it conflicts with line 190. Line 190 shows the id when the datastream has no name. Line 191 shows the title `Video` "when the name that the layer gives it is empty". The layer gives the view `name || id` (`src/layers/osh/index.js:291`). So the given name is empty only when the name and the id are both empty. "gives it" can also mean the datastream. Write: "the view shows the title `Video` when the datastream has no name and no id".
- [ ] S7 (minor) `tasks.md:92`: "Cover the ignored delta messages: a queue of more than eight chunks starts them, and the next key message ends them." "them" means messages. A queue does not start messages, and a key message does not end messages. "ignored" is a passive form. Write two lines. First: "Cover how the player ignores each delta message when the decoder queue holds more than eight chunks." Second: "Cover how the player decodes again from the next key message."
- [ ] S8 (minor) The word "delay" has no clear noun in `tasks.md:47`, and V25 uses a new term, `retry delay`. `osh-069` says "the delay before each new socket", and `osh-080` says "the delays of `osh-069`". Line 47 write: "Cover `[osh-080]`: each video client gets `down` when the socket closes, and `open` when the new socket opens, after the delays of `osh-069`." `tasks.md:81` write: "V25: use one second, and not two seconds, for the delay before the second new socket of the video entry."
- [ ] S9 (minor) The old words "good message" and "bad message" remain in `tasks.md:43`: "a good message gives an event `frame`. Each kind of bad message gives no event." The spec and the test names now say `valid` (round-3 S11), so two words name one thing. The bad kinds also include the text message and the message of more than 2097152 bytes, which `valid` does not cover. Write: "Cover `[osh-079]`: a message that the hub relays gives an event `frame`. Each other kind of message gives no event."
- [ ] S10 (minor) The unwritten bytes belong to a client in two places (round-3 S8, not fully corrected). `tasks.md:48` says "the response of a client with more than 8388608 unwritten bytes". `tasks.md:72` says "the unwritten bytes of a client". Write line 48: "Cover `[osh-090]`: the provider destroys a response that has more than 8388608 unwritten bytes. The other clients keep their responses." Write line 72: "V16: remove the check of the unwritten bytes of a response."
- [ ] S11 (minor) Task 6.8 (`tasks.md:122`) says "the `[osh-093]` test of the occluder selector". There are now two `[osh-093]` tests, and no task covers the second. Write: "Write the `[osh-093]` tests in `src/overlays/worldOverlay.test.mjs`." Add two sub-lines: "One test checks the occluder selector." and "One test checks that the world overlay places a label clear of the panel."
- [ ] S12 (minor) The name of the `[osh-080]` test says more than the test checks: `src/data/oshLive.test.mjs:2222`. "after each delay" implies all delays, but the test checks only the delays of one second and two seconds. Write: "[osh-080] each video client gets the event down when the socket closes, and the event open when the new socket opens after one second and after two seconds".
- [ ] S13 (minor) The name of the first `[osh-094]` test says less than the test checks: `src/data/osh.test.mjs:211`. The test now also checks the class `osh-panel` (`:216`). Write: "[osh-094] index.html has one element osh-panel with the class osh-panel and the attribute hidden, and one video host and one detail host inside that element".
- [ ] S14 (minor) `proposal.md:33` (round-3 S12 is only partly corrected). "No file of this change touches it." "it" can mean the ratchet history, and the change does write to that history. "the final count equals `main`" compares a count with a branch. Write: "No file of this change touches `src/data/labelArbiter.js`. The history of `teardown-guard` shows the same flips, and the final count is 52, the count of `main`."
- [ ] S15 (minor) The lists of what the tests check are now incomplete. `design.md:88` (D79) says "Tests check the ids of the elements in `index.html`, which elements are inside `osh-panel`, the rule for `hidden` in `style.css`, and the occluder selector". It omits the attribute `hidden` and the class `osh-panel`. `proposal.md:57` omits the class. Write D79: "Tests check the ids of the elements in `index.html`, the attribute `hidden`, the class `osh-panel`, and which elements are inside `osh-panel`. Tests also check the rule for `hidden` in `style.css` and the occluder selector." Write `proposal.md:57`: "Tests check the ids, the attribute `hidden`, the class `osh-panel`, which elements are inside `osh-panel`, the rule for `hidden`, and the occluder selector."
- [ ] S16 (minor) Three comments in the new tests use vague words.
  - `src/data/oshLive.test.mjs:2179`, "each read keeps the last characters of the one before". Write: "A read can end inside the text `event: frame`. So the test adds the last 11 characters of each read to the next read."
  - `src/data/osh.test.mjs:226`, "so the comments go first". Write: "so the test removes the comments first."
  - `src/overlays/worldOverlay.test.mjs:1533`, "The panel is the shipped one". Write: "The stacking of the panel comes from the rule of `.osh-panel` in style.css, so the test reads that rule."

What I checked:
- **Scope.** I read the whole round-4 diff, and the current delta spec, tasks, proposal and design. `openspec/specs/osh/spec.md` has the same text as the delta at the changed lines (grep of nine lines).
- **Round-3 findings.**
  - Corrected: S1 in meaning (`worldOverlay.js:63-74` says the occluder list is a placement preference, so the new text agrees with the code), S7, S9, and S13.
  - S3 corrected in the WHEN. `osh-092` also gets S3 above.
  - S2 partly corrected (P6 is good; S7 above).
  - S4 corrected in the lines, with the leftovers in S4 above.
  - S5 corrected except S5 above.
  - S6 corrected in meaning (S6 above).
  - S8 partly corrected (S10 above).
  - S10 corrected for five names, with S12 and S13 above.
  - S11 corrected in the spec and the test names (S9 above).
  - S12 partly corrected (S14 and S15 above).
- **Other round-3 STE points.** The `binary` word in D74 is now in the design. D77 and `osh-084` now agree on the PPS. The test names of `[osh-079]`, `[osh-083]`, `[osh-090]`, `[osh-091]` and `[osh-084]` are clear and agree with the spec.
- **Code.**
  - `src/layers/osh/index.js:291`, for the view name.
  - `src/layers/osh/videoPlayer.js:174`, for the `Video` title.
  - The `variant` values in `src/overlays/worldOverlay.js`.
  - The `selectedEntry` helper in the test file.
  - `osh-069` and `osh-068` in the synced spec.
  - `openspec/ste/words.json` has no entry for the words that I questioned.
- **Standing constraints.**
  - The changed lines name no real server, host, stream, system or place. They have no non-GET request and no `SHALL`, `SHOULD` or `MAY`.
  - The word `send` appears only in spec text.
  - The renamed tests are all new in this change.
  - I made no network call.
- **Note for the lead (not an STE finding).** `openspec/changes/archive/2026-09-25-osh-camera-video/review.md` does not exist yet. `round-3/skeptics.md` says that `review.md` records the headless-browser run, and `proposal.md` states the run. Task 8.8 is open, so write that record when you write `review.md`.
