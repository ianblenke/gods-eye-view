Verdict: FAIL
- [ ] F1 major scripts/spec/lib/test-guard.mjs:18 The gate loads the real guard file as the first `--require` preload of each test process (`GUARD_PRELOAD`, gates.mjs:101). This file is also in the inventory. Thus the guard gets coverage from each test process, and a test does not need to load it. I copied `test-guard.mjs` and `trace-reporter.mjs` into `review-spec-r17/exp1/`. Then I did a run in the same way as the gate, with one test file that does not import the guard. The guard checked the file as true (`"checked":["scripts/spec/lib/test-guard.mjs"]`), and lcov gave 220 of 281 lines, 33 of 48 branches and 17 of 24 functions as covered. Then I added a new branch `if (env.NODE_TEST_CONTEXT) { guard.untested = true; }` before `process.on('exit', …)`. The preload alone covered lines 275–277. Today `testGuard.test.mjs` alone gives 100%, so the counts in this change are correct. But a later change can add code on the preload path of the guard with no test, and the gate shows 100% with no stop. No known limit names this case. Correction: make `GUARD_PRELOAD` load a copy of the guard from a folder outside the root, for example the result folder, so that the preload adds no coverage to `scripts/spec/lib/test-guard.mjs`. Add a gates.test.mjs test that asserts that a run with a test that does not load the guard gives no coverage entry for the guard file. Or add a known limit: "The guard is the preload of each test process, so it gets coverage without a test. The spec adversary checks each change to test-guard.mjs."
- [ ] F2 major .github/workflows/ci.yml:80 The CI job runs `scripts/spec/gates.mjs` and `scripts/spec/lib/*` from the pull request itself. A pull request can change `review.mjs` so that `checkReviewFolder` returns no errors, change `ci.mjs` so that no archived change is necessary, and change `coverage.mjs` or `ledger.mjs` so that a coverage gap does not stop. Then the CI job passes with no archived change and no review. No gate compares the gate code with the base. The `review-attestation` limit only says that the author can write the agent output. It does not say that the author can change the gate code that CI runs. Correction: in CI, run the gate code of the merge base when the base has `scripts/spec/`, or make a person approve each change to `scripts/spec/`, `.github/workflows/` and `openspec/trace/` (for example with a CODEOWNERS rule and branch protection). Or add a known limit: "CI runs the gate code of the pull request. A change to scripts/spec/ can turn off a gate. A person checks each change to the gate code."
- [ ] F3 major scripts/spec/lib/spec-lint.mjs:296 No gate stops a requirement that has no scenario. `lintSpecs` checks MUST, the origin line and the WHEN and THEN lines of each scenario that is there. It does not check that a requirement has a scenario. I tested the real `loadSpecs` and `lintSpecs` (`review-spec-r17/exp3/`) with a main spec in which `### Requirement: Stream check` has "The radio MUST stop a stream that has no audio for ten seconds." and no scenario. The result was no spec errors and no lint errors. `openspec validate` gave "Requirement must have at least one scenario" for the same file, but the gates and CI do not run it. An author can also remove each scenario of a requirement from `openspec/specs/`, retire the IDs and remove the tests, and keep the requirement text. Thus a requirement with no test passes the gates. No known limit names this case. Correction: add a rule such as `SPEC-LINT-NO-SCENARIO` for each requirement that is not removed and has no scenario. Add a scenario to spec-lint and a specLint.test.mjs test for it. Or run `openspec validate --strict` in the gates.
- [ ] F4 minor scripts/spec/lib/review.mjs:64 `computeTreeHash` skips each diff file in `openspec/changes/`. It does not skip only `review.md` and `review/` of the checked change. change-review-015 (openspec/specs/change-review/spec.md:55) says "the hash of the change files and the diff files". design.md:159 says "each file in the diff against the base". Neither one names this exception. The gates run each tracked test file and measure each tracked code file in `openspec/changes/**`. So a test file or a code file in another archived change folder, or in the `review/` folder of the change, can change after the review, and the tree hash does not change. I found no path that lets a gap pass in this way, because each effect on coverage or trace also changes a file in `openspec/trace/`. Correction: put each diff file outside the checked change folder into the hash, or stop for code files and test files in `openspec/changes/`. Or add the exception to change-review-015 and to design.md:159.
- [ ] F5 minor Makefile:22 Check 3 of .claude/agents/spec-adversary.md:31 tells me to report each new or changed file with code that has no code extension. This change has three such files: `Makefile`, `Dockerfile` and `.github/workflows/ci.yml`. ci-gates-006 and ci-gates-008 check the lines that do the gate work in `ci.yml` and `Dockerfile`. In `Makefile`, ci-gates-007 checks only the `gates` target. No scenario or test checks the targets `test` (:22), `gates-init` (:28), `ratchet` (:31), `stability` (:34), `lint` (:37) and `tree` (:40), which AGENTS.md:23–28 tells authors to use. For example, if the `ratchet` target lost `$(CHANGE_ARG)`, no test would fail. Also, check 3 says "report", but the known limit `inventory-extensions` (proposal.md:71) says that the spec adversary "checks" each such file. If each such file is a finding, a change that edits `Makefile` can never get PASS. Correction: extend the ci-gates-007 scenario and the ciFiles.test.mjs test to assert each gate target and its `$(CHANGE_ARG)` and `$(BASE_ARG)` options. Change check 3 so that it reports such a file only when its code has no scenario or no test, so that it agrees with proposal.md:71.

Round-16 finding:
- **Recorded as a known limit:** F1. proposal.md:71 has the new limit `inventory-extensions`. It names a shell script without `.sh`, an SVG file with a script and a file that `require` loads. It also says that a diff with only such files needs no archived change. proposal.md:58 (`script-coverage`) now says "a new or longer `.sh` file". Check 3 in .claude/agents/spec-adversary.md:31 has the new sentence.

Other checks:
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected before review.md is written. There are no warnings.
- **Ledger and history:** All 468 ledger keys are files in the merge base `d5b09ef`, and each entry has the origin `pre-spec`. The 6 history lines name `establish-spec-governance`. The base has no ledger, so for this change `compareWithBase` and `compareRegistryWithBase` return no errors. Each range is within its width limit.
- **Trace:** `openspec/trace/retired-ids.json` does not exist. No test or code file changed since round 16.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r17/`. I ran F1 and F3 on the local Node v26.8.2. F3 uses only JS logic. F1 depends on how the preload and node:test coverage work, and I did not run it on 24.21.0.