Verdict: FAIL
- [ ] S90 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:52 "the content hash of each inventory file" One word, one meaning: "inventory file" has two meanings. It means a code file in the inventory at design.md:52, :53 ("the URL of an inventory file"), :54 ("each inventory file that it checked"), :59 ("each inventory file again") and :187 ("only for inventory files"), and in gov-tests-r4.txt:53 ("checks only the inventory files inside the root"). It means the file `inventory.json` at design.md:39 ("the hash of the inventory file"), :59 ("when the inventory file changed"), specs/coverage-gate/spec.md:87, :175, :177, :180, and gov-tests-r4.txt:77 and :78. design.md:59 uses both meanings in two sentences next to each other. The specs already use "a file in the inventory" for the first meaning. Write: "the content hash of each file in the inventory" (and the same at :53, :54, :59 first part, :187, and gov-tests-r4.txt:53 "checks only the files in the inventory inside the root"). For the second meaning write "`inventory.json`": design.md:39 "the hash of `inventory.json`"; design.md:59 "It also stops when `inventory.json` changed."; coverage-gate/spec.md:87 and :177 "the hash of `inventory.json`"; :175 "Stop a process with a changed `inventory.json` file"; :180 "Stop for an `inventory.json` file that changes during the run"; gov-tests-r4.txt:77 "stops a process with an inventory.json file that is not the file of the gate"; gov-tests-r4.txt:78 "stops for an inventory.json file that changes during the run". Fix the openspec/specs copy too (line + 4).
- [ ] S91 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:39 "It then sets `NODE_OPTIONS` to `--import` with the test guard" One word, one meaning: design.md:45 says that the guard is "the first `--require` preload of `NODE_OPTIONS`", and `scripts/spec/lib/test-guard.mjs:18` uses `--require`. The text uses two option names for the same preload. Write: "It then sets `NODE_OPTIONS` to `--require` with the test guard".
- [ ] S92 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:48 "also wraps named imports" Verbs: this uses the verb "import" as a noun. S71 removed the same problem from the coverage-gate spec, and design.md:46 and proposal.md:61 use "import" as a verb. If you keep "named import" as a technical name, say so in the text. Write: "also wraps the methods that a module imports by name".
- [ ] S93 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:47 "An in-process `node:inspector` session" Approved words: "in-process" is a compound word that is not an approved STE word. I am not sure if it is a technical name. Write: "A `node:inspector` session in the same process".
- [ ] S94 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:61 "The only exception is a node:test runner that runs its tests in child processes." Approved words: I am not sure that "exception" is an approved STE word. The approved word is "except". Write: "The gate does not stop for a node:test runner that runs its tests in child processes."
- [ ] S95 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:79 "The test changed when the base file does not contain this text." Verbs: the sentence gives a rule, but it mixes the simple past ("changed") with the simple present ("does not contain"). specs/spec-trace/spec.md:221 and :225 say "use ... as a changed test" for this. Write: "The gate uses the test as a changed test when the base file does not contain this text."
- [ ] S96 openspec/changes/archive/2026-09-14-establish-spec-governance/proposal.md:62 "The ledger stores counts, not the location of each branch. A change can lose the coverage of some branches and show the same number of new covered branches." Articles: an article is possible before "counts". Approved words: in STE, "show" means "make visible". In this change, "branches that a new test shows" means branches that V8 now counts, but here "show" means "add coverage to". I am not sure that "lose" is an approved STE verb. Write: "The ledger stores the branch counts, not the location of each branch. A change can remove the coverage of some branches and add coverage to the same number of other branches."
- [ ] S97 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/coverage-gate/spec.md:192 "the guard writes an error without a file to the guard folder" Approved words: "without a file" does not give the correct meaning. The error has a file, but its file field is empty (`file: ''` in `scripts/spec/lib/test-guard.mjs`). Write: "the guard writes an error that names no file to the guard folder". Also gov-tests-r4.txt:81 "stops for a guard error without a file" → "stops for a guard error that names no file". Fix the openspec/specs copy too (line + 4).
- [ ] S98 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/gap-ledger/spec.md:59 "Stop for a ledger entry without total counts" Articles: an article is possible here. Write: "Stop for a ledger entry without the total counts". Also gov-tests-r4.txt:145 "stops for a ledger entry without total counts" → "stops for a ledger entry without the total counts". Fix the openspec/specs copy too (line + 4).
- [ ] S99 gov-tests-r4.txt:220 "uses the next test when the scan cannot find the line that closes a test call" Verbs, and one word, one meaning: this uses the verb "scan" as a noun. It also adds a second name for the gate, which specs/spec-trace/spec.md:232 calls "the gate". Write: "uses the next test when the gate cannot find the line that closes a test call".
- [ ] S100 gov-tests-r4.txt:213 "does not use a test as changed for a comment after the line that closes the test" One word, one meaning: the spec (spec-trace/spec.md:220, :232), design.md:79 and gov-tests-r4.txt:215 and :220 all say "the line that closes the test call". Write: "does not use a test as changed for a comment after the line that closes the test call".
- [ ] S101 .claude/agents/spec-adversary.md:37 "Read the section "Known limits" in the proposal." One word, one meaning: the section in proposal.md:51 and design.md:14 has the name "Known limits and later changes". Write: "Read the section "Known limits and later changes" in the proposal."

Notes for the caller:
- **Round-3 check:** all 23 findings (S67–S89) are corrected everywhere they apply.
  - S67 is at design.md:53.
  - S68 and S69 are at proposal.md:60, design.md:41, spec-trace/spec.md:63, :150 and :151, and in the test names.
  - S72 is at coverage-gate/spec.md:116 and design.md:59.
  - For S71, the text "the guard import" is not in the specs now.
  - The rewritten texts for S74, S75, S76 and S79 have no STE problems.
- **Merged specs:** openspec/specs/*/spec.md is the same as the change delta specs at line + 4, and I checked the Purpose lines. The STE lint shows 0 warnings, so there are no STE-PASSIVE or STE-ING warnings to examine. `openspec/ste/words.json` has no change in the working tree.
- **S91 is also a content error:** the design names the wrong preload option. The spec adversary can check it too.
- I did not read .env, and I made no changes to files in the repository.