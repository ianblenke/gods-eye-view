Verdict: FAIL
- [ ] F1 major scripts/spec/gates.mjs:145 Round-1 F8 is not fully corrected. The result folder is `mkdtempSync(path.join(tmpdir(), 'gev-spec-results-'))`. Its path is on the command line of the `node --test` parent process: `--test-reporter-destination=<dir>/tests-main.jsonl` (gates.mjs:77). A test can find the path with `readFileSync('/proc/' + process.ppid + '/cmdline')` or with `readdirSync(tmpdir())`. `COVERAGE-EXTRA-RESULT` (gates.mjs:149-152) checks only for extra file names. It does not check the lines inside the named files. I reproduced this on Node 24.21.0:
  - The test `[demo-001] real test` found `/tmp/gev-spec-results-Z28bWh/tests-main.jsonl` in both ways.
  - The test appended `'\n'.repeat(100000)` and then a record with `fullName` `[demo-001] real test` and `tags:["demo-002"]`. The padding stops the reporter from writing over the line.
  - `evaluateTrace` on that file gave `errors: []`. It also marked `demo-002` as verified by `src/forage.test.mjs › [demo-001] real test`. The forged record uses the assertion count of the real test, so the attack does not need the `guard-results` limit.
  
  This contradicts coverage-gate spec.md:92 ("The test processes do not get the name of the result folder") and design.md:41. design.md:41 also gives the folder as `.gev-cache/spec-results/`, but the code uses the OS temp folder. The coverage-gate-030 test (gates.test.mjs:495) writes the extra file from the gate spawn function, not from a test. Correct it in one of these ways:
  - Write the reporter output to a destination that test processes cannot open, for example a pipe or a file descriptor that only the gate reads.
  - Stop for a record whose `tags` are not the tags in its own name, or whose `fullName` does not end with its `name`, or that has the same file and full name as another record. Add a scenario with a test that does the forgery from inside a test.
  
  If you do neither, remove the claim from the requirement and from the design, and add it to Known limits.
- [ ] F2 major scripts/spec/lib/test-guard.mjs:113 A test can hide coverage from the guard in a child process. `withGuardEnv` builds `{ ...guardEnv, ...options.env, NODE_OPTIONS }`, so the child's own `GEV_SPEC_OUT` wins. It does not add the guard when `NODE_OPTIONS` only contains the `GUARD_IMPORT` text. `installGuard` returns null for an empty `GEV_SPEC_OUT` (:159). coverage-gate-025 makes this required behavior: "keeps its own guard folder" (spec.md:154). I reproduced this on Node 24.21.0 with the real guard, the lcov reporter, `parseLcov`, `untrueFiles` and `measureCoverage`. The fixture `src/target.js` has 9 lines and 3 branches, and no test calls it. The test started two child processes:
  - Child 1 ran `import(target)` with the gate's `GEV_SPEC_OUT`, `GEV_SPEC_ROOT` and `NODE_OPTIONS`, and with `NODE_V8_COVERAGE: '/tmp/throwaway'`. The guard wrote `checked:["src/target.js"]` to the gate folder, and the coverage of this child went to the throwaway folder.
  - Child 2 ran `spawnSync(process.execPath, ['-e', "new Function(fake + '//# sourceURL=' + url)()"], { env: { ...process.env, GEV_SPEC_OUT: '' } })`. It ran with no guard, and its coverage went to the gate run.
  
  The gate data gave `"untrue":false,"complete":true,"lines":{"total":9,"uncovered":0},"branches":{"total":3,"uncovered":0}` with 0 violations. A control test that calls one branch gives 4 lines and 1 branch not covered. There is a second path: a test can rewrite `.gev-cache/spec/inventory.json` in `GEV_SPEC_OUT`. Each later guard reads that file when it starts (:165), and the gate checks only the inventory files after the run (gates.mjs:158-162). design.md:52 ("A test cannot change the file on disk to hide this") and design.md:55 overstate the protection. The `guard-results` limit (proposal.md:60) names only the same process. Correct it in these ways:
  - In each child process that collects coverage, always set the gate's `GEV_SPEC_OUT`, `GEV_SPEC_ROOT` and guard import.
  - Parse `NODE_OPTIONS` instead of a substring test, and stop for an empty `GEV_SPEC_OUT`.
  - Keep the inventory hashes where test processes cannot write.
  
  If you do not, add these paths to Known limits.
- [ ] F3 major scripts/spec/lib/ledger.mjs:340 This round's range shift makes allowed gaps larger. When one metric is out of range, the ratchet moves every metric with `gap[metric] + (entry[metric] - entry.low[metric])`. So a metric that is still in its range gets a higher high count. I reproduced this with the real `src/data/labelArbiter.js` entry (lines 18, branches 50 to 52) and a run with lines 10 and branches 52:
  - `ratchetLedger` wrote `branches: 54, low.branches: 52`. The history has only a `lines` line, so nothing records the larger branch count.
  - `compareLedger` gave no errors.
  - `compareWithBase` with `testChanged: true` gave `LEDGER-UNSTABLE-NO-HISTORY` and `LEDGER-UNSTABLE-TEST-CHANGE`.
  
  So a backfill that closes lines in one of the four unstable files cannot pass the check unless someone edits gaps.json by hand. The same shift occurs for a changed file with counts inside the range. gap-ledger-045 (spec.md:121-124) describes only a count below the low count. Move only the metrics that are below their low count, and do not raise a high count. Record each changed metric in the history. Add a scenario and a test for a mixed case.
- [ ] F4 major scripts/spec/lib/registry.mjs:121 Round-1 F6 is only partly corrected. `idsInFiles` collects every ID string in the full content of each changed test file (gates.mjs:283). `compareRegistryWithBase` (:113) and `updateRegistry` (:78) then accept a new hash when any part of such a file changed, including a comment or a different test. For example, ledger.test.mjs names most gap-ledger IDs. One unrelated edit in that file lets an author change the meaning of those scenarios without a change to their tests. This contradicts spec-trace spec.md:169: "a test that names the ID MUST also change." Compare the source of each test whose name contains the ID with the base version, or require a changed line inside such a test. Add a scenario for a changed file where only another test changed.
- [ ] F5 minor scripts/spec/lib/specs.mjs:225 Round-1 F11 is not complete. A requirement under `## RENAMED Requirements` does not go into `requirements`, and its scenarios are not orphans. But `closeScenario` still adds each of its scenarios to the trace. I reproduced it: `## RENAMED Requirements` / `### Requirement: Anything` / `no must here` / ``#### Scenario: Loose `demo-001` `` / `- **AND** x` gave scenarios `[["demo-001","unknown"]]`, 0 requirements, 0 orphans, and `lintSpecs` gave `[]`. This contradicts spec-lint spec.md:8. Stop for a scenario under a renamed requirement, or lint it. Add a scenario.
- [ ] F6 minor scripts/spec/lib/ledger.mjs:219 The risk of the unstable-range exception is larger than proposal.md:58 says ("The limit of 5 or 2% on each range and the rule against test file changes make this risk small"). There are three problems:
  - Any hand-written history line with `file` and `reason: "unstable"` is accepted. The line does not have to name the change or a date, and the tests use only `{file, reason}` (ledger.test.mjs:421).
  - `testChanged` looks only at `.test.mjs` names (gates.mjs:380). A change to `ALLOCATION_TEST_FILES` (scripts/run-unit-tests.mjs:6-9) moves a test file into a run without coverage (gates.mjs:89) with no test file change. A change to a test helper module does the same.
  - The limit of 5 or 2% is measured against the base entry, so each new change can add it again and a range can grow with no end.
  
  Require new history lines to name the change under check. Treat a change to the allocation list as a test change. Measure the limit against the first recorded range. If you do not, state these risks in the limit.
- [ ] F7 minor scripts/spec/lib/ste.mjs:241 The lint skips every folder named `review` and every file named `review.md` at any depth in `openspec/`, for example `openspec/specs/review/spec.md` or all of `openspec/changes/review/`. ste-lint-018 (spec.md:70-72) and design.md:136 exclude only the review files of a change folder. Skip only `<change folder>/review.md` and `<change folder>/review/`, and add a test.
- [ ] F8 minor src/tooling/spec/ledger.test.mjs:253 The gap-ledger-047 THEN line covers gaps.json, ids.json and links.json, but the tagged test asserts the key order of gaps.json only. The ids.json and links.json key order is tested under spec-trace-030 (registry.test.mjs:133). Also, task 4.47 (tasks.md:137), the test task for this scenario, comes after task 2.32 (tasks.md:57), which writes the code for ids.json and links.json. Assert all three files in the tagged test, and move the test task before 2.32 or split the scenario.
- [ ] F9 minor scripts/spec/lib/ledger.mjs:234 No scenario describes some stop rules, and their tests carry tags for other scenarios:
  - `LEDGER-UNSTABLE-CHANGED` (:234-235) is asserted only in the test of gap-ledger-037, which is a "does not stop" scenario (ledger.test.mjs:426-427).
  - `GATES-NO-LEDGER` (gates.mjs:290, 335) is asserted only under spec-trace-002 and gap-ledger-036.
  - `REVIEW-AGENT` for a missing agent file, name, description or tools (review.mjs:183-190) is asserted under change-review-017, whose WHEN line names only another tool. An agent file without `tools` gets all tools, so this rule needs its own scenario.
  
  Add a scenario for each rule and tag the tests with it.