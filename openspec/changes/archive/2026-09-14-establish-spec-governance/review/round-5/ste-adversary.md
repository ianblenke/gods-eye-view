Verdict: FAIL
- [ ] S102 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:49 "The guard does not load into a worker thread" One word, one meaning: "load into" has two meanings in the change, and the texts do not agree. specs/coverage-gate/spec.md:159 says "the test guard loads in a worker thread", and :161 says that the guard in the worker "gives the gate values to the child processes that the worker starts". design.md:61 says the same thing. In `scripts/spec/lib/test-guard.mjs`, the guard loads in a worker and wraps `node:child_process`. It stops before the inspector session only because `mainThread` is false. proposal.md:61 "The test guard cannot load into a worker thread" has the same problem. The texts also use two words for one thing: "worker thread" and "worker" (proposal.md:61 "a file that only a worker loads", "A worker can still add coverage"; gov-tests-r5.txt:83 "a child process of a worker"). coverage-gate/spec.md:159 uses "loads in", but :150 uses "load into". Write: design.md:49 "In a worker thread, the guard does not check the sources, and an inspector session does not start." proposal.md:61 "The test guard does not check the sources in a worker thread. The gate gives 0% coverage to a file that only a worker thread loads. A worker thread can still add coverage to a file that a test also imports." coverage-gate/spec.md:159 "the test guard loads into a worker thread of a gate run". gov-tests-r5.txt:83 "gives the gate values to a child process of a worker thread or of a test runner without an inspector". Fix the openspec/specs copy too (line + 4).
- [ ] S103 openspec/changes/archive/2026-09-14-establish-spec-governance/proposal.md:62 "The change can be only a new comment." One word, one meaning: S1 and S67 made this rule: use the noun "change" only for an OpenSpec change. The same section uses that meaning at :53 ("Each one has a later change"). The first sentence at :62 also does not say that the scenario text changes. Write: "When the text of a scenario changes, the ratchet command writes a new hash if the text of a test with its ID also changes. The only new text in that test can be a comment." Also specs/spec-trace/spec.md:228 "without a change to its text" → "and the text of the test does not change". Also .claude/agents/spec-adversary.md:55 "the problem and the change that the author must make" → "the problem and what the author must do to correct it". Fix the openspec/specs copy too (line + 4).
- [ ] S104 .claude/commands/opsx/review.md:22 "put the change back before the archive" Verbs: "put back" is a phrasal verb. One word, one meaning: in this sentence, the noun "archive" means the archive command. In `openspec/changes/archive` and AGENTS.md:43 ("the archive folder"), it means the folder. Instructions: :24 "Run `git checkout -- openspec/specs`, and remove each capability folder" gives two instructions in one sentence, and "Put `openspec/specs/` back" is also a phrasal verb. Write: :22 "If an agent gives the verdict FAIL, move the change to its location before the archive command:". :24 "Make the content of `openspec/specs/` the same as before the archive command. Run `git checkout -- openspec/specs`. Remove each capability folder that the archive command added."
- [ ] S105 openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:61 "so the guard keeps the guard values in the `process.env` of the runner" One word, one meaning: this paragraph has two words for the same values, "guard values" and, one sentence later, "gate values". In a gate run, the runner has the values that the gate set, and design.md:57, specs/coverage-gate/spec.md:161, :166 and :178 call them "the gate values". The "so" in the sentence is also not clear. The reason is that the runner does not use the wrapped `spawn`. Write: "Such a runner gets the `spawn` function before the preloads load, so it does not use the function that the guard wraps. Thus the guard keeps the gate values in the `process.env` of the runner." Also design.md:168 "the guard values of child processes" → "the gate values of child processes". Also gov-tests-r5.txt:86 "keeps the guard values in the environment of a test runner" → "keeps the gate values in the environment of a test runner". Also gov-tests-r5.txt:87 "a preload that removes the guard values" → "a preload that removes the gate values". At coverage-gate/spec.md:181 and gov-tests-r5.txt:75 ("Keep the guard values of a child process"), the values come from the test, not from the gate. Write: "Keep the values of `GEV_SPEC_OUT`, `GEV_SPEC_ROOT` and `GEV_SPEC_INVENTORY` from the test in a child process with its own coverage folder" (test name: "keeps the values from the test in a child process with its own coverage folder"). Fix the openspec/specs copy too (line + 4).
- [ ] S106 gov-tests-r5.txt:87 "from a nested test runner" Approved words: I am not sure that "nested" is an approved STE word. I think it is not approved. Write: "stops for untrue coverage from a test runner that a test starts, with a preload that removes the gate values". Also gov-tests-r5.txt:172 "records nested tests and their lines from a real node:test run" → "records the subtests and their lines from a real node:test run".
- [ ] S107 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/coverage-gate/spec.md:165 "a preload of the runner removed `GEV_SPEC_OUT` from its environment" Articles and clarity: "its" can refer to the preload, the runner or the child process at :164. Also, :164 "the coverage folder of the gate run" is a second name for the folder that :177 and design.md:57 call "the coverage folder of the test process". In :166, "the test guard as the first preload and the gate values" can mean that the gate values are also a preload. Write: :164 "a node:test runner without an inspector starts a child process that writes coverage to the coverage folder of the runner". :165 "a preload of the runner removed `GEV_SPEC_OUT` from the environment of the runner". :166 "the child process gets the gate values and gets the test guard as the first preload". Fix the openspec/specs copy too (line + 4).
- [ ] S108 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/coverage-gate/spec.md:158 "Do not check sources in a worker thread" Articles: an article is possible before "sources". S96 and S98 applied this rule to plural nouns. Write: "Do not check the sources in a worker thread". Also :160 "does not check sources, does not count assertions" → "does not check the sources, does not count the assertions". Also gov-tests-r5.txt:84 "does not check sources in a worker thread" → "does not check the sources in a worker thread". Fix the openspec/specs copy too (line + 4).
- [ ] S109 openspec/changes/archive/2026-09-14-establish-spec-governance/specs/spec-trace/spec.md:219 "the base file contains the text of a test with a tag, without the spaces at the end of each line" One word, one meaning: "the base file" is a second name for "a test file of the base" at :224 and :229 and design.md:79. It also gives a different meaning, because the gate looks in each test file of the base, not only in the same file. Clarity: "without the spaces at the end of each line" can refer to "a tag". :224 has the same phrase. Write: :219 "**AND** a test file of the base contains the text of a test with a tag, and the gate does not compare the spaces at the end of each line". :224 "**WHEN** no test file of the base contains the text of a test with a tag, and the gate does not compare the spaces at the end of each line". Fix the openspec/specs copy too (line + 4).

Notes for the caller:
- **Round-4 check:** all 12 findings (S90–S101) are corrected everywhere they apply.
  - S90: design.md:39, :52–:54, :59, :187, coverage-gate/spec.md:87, :185, :190, and gov-tests-r5.txt:53, :76 and :77.
  - S91–S95: design.md:39, :48, :47, :61 and :79.
  - S96: proposal.md:63.
  - S97: coverage-gate/spec.md:202 and gov-tests-r5.txt:80.
  - S98: gap-ledger/spec.md:59 and gov-tests-r5.txt:149.
  - S99 and S100: gov-tests-r5.txt:225 and :217.
  - S101: spec-adversary.md:37.
  - The rewritten texts have no new STE problems. The one exception is design.md:61, which is in S105.
- **Merged specs:** openspec/specs/*/spec.md is the same as the change delta specs at line + 4. The only differences are the header lines. The Purpose lines have no findings. The gate output shows 0 STE warnings, so there are no STE-PASSIVE or STE-ING warnings to examine.
- **S102 is also a content error:** design.md:49 and proposal.md:61 say that the guard does not load into a worker thread, but test-guard.mjs and coverage-gate-039 show that it does. The spec adversary can check this too.
- I did not read .env, and I made no changes to files in the repository.