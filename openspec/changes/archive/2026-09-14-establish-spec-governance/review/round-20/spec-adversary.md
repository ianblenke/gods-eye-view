Verdict: FAIL
- [ ] F1 major scripts/spec/lib/specs.mjs:8 The spec parser still misses some requirements and scenarios that OpenSpec 1.3.1 reads. The gates read a requirement only from a `### Requirement:` heading and a scenario only from a `#### Scenario:` heading (specs.mjs:5, :8). At any other heading, the gates end the scenario but keep the current requirement (specs.mjs:143–146). OpenSpec does not use these words. `MarkdownParser.parseRequirements` (/usr/lib/openspec/dist/core/parsers/markdown-parser.js:149) reads each child heading of the `Requirements` section as a requirement. `parseScenarios` (:183) reads each child heading of a requirement as a scenario. `findSection` (:137) finds a `Requirements` heading at any level. I ran the real `loadSpecs` and `lintSpecs` on copies (`review-spec-r20/expA`, `expB`, `expE`). Each main spec had a correct requirement `Play` with scenario `radio-001`. (a) After it I put `### Stream check` with "The radio MUST stop a stream that has no audio for ten seconds." and no scenario. The gates found no spec errors and no lint errors, and they put the MUST text into `Play`. `openspec show` gave 2 requirements, and `openspec validate --specs --strict` gave "Requirement must have at least one scenario". (b) After `radio-001` I put `#### Scenario - Stop a silent stream `radio-002`` with WHEN and THEN lines. The gates found no errors and did not record `radio-002`. `openspec show` gave 2 scenarios for `Play`, and validate gave no scenario error. (c) Under `## Purpose` I put `### Requirements` and `#### Requirement: Stream check` with MUST text and no scenario. The gates found no errors. OpenSpec read `Stream check` as the only requirement and gave "Requirement must have at least one scenario". Thus a requirement with no test, or a scenario ID with no test, passes the gates. This is the same result as round-19 F1, by a different path. No known limit names spec Markdown. Correction: in a main spec, stop the build for a heading with the title `Requirements` at a level other than `##`. Also stop for each `###` heading under `## Requirements` that is not a requirement heading. Also stop for each heading of level 4 to 6 that comes after a requirement heading and before its first `#### Scenario:` heading, and that is not a scenario heading. Add spec-lint scenarios and specLint.test.mjs tests for cases (a), (b) and (c). As an alternative, run `openspec validate --specs --strict` in the gates. Then compare the number of requirements and scenarios in each spec from `openspec show --json` with the numbers from `loadSpecs`, and stop the build when they are different.
- [ ] F2 major scripts/spec/lib/specs.mjs:64 `parseSpecFile` splits the text only at `\n`. `trimEnd` (specs.mjs:65) removes the `\r` of a `\r\n` ending, but a line that ends with only `\r` stays joined to the next line. OpenSpec changes each `\r\n` and each `\r` to `\n` before it reads the file (markdown-parser.js:12, spec-structure.js:6). I ran the real `loadSpecs` and `lintSpecs` on a copy (`review-spec-r20/expD`). The main spec had the requirement `Play` with scenario `radio-001`. After it came the lines `### Requirement: Stream check`, "The radio MUST stop a stream that has no audio for ten seconds." and `Origin: spec-first`, each one ending with `\r` only. The gates found no spec errors and no lint errors, and they read only `Play`. `openspec show` gave 2 requirements, and `openspec validate --specs --strict` gave "Requirement must have at least one scenario". A scenario heading after a `\r` is also not read, so its ID needs no test. No known limit names this case. Correction: before the split in `parseSpecFile`, change each `\r\n` and each `\r` to `\n`, as OpenSpec does. Add a spec-lint scenario and a specLint.test.mjs test. The test must have a requirement with no scenario and a scenario ID after lines that end with only `\r`. It must assert `SPEC-LINT-NO-SCENARIO` and the recorded scenario ID.

Round-19 finding:
- **F1, corrected:** Round-19 F1 is corrected. `parseSpecFile` now stores the character and the length of the opening fence (specs.mjs:73–75). It ends the fence only at a line that has only the same character, repeated at least as many times (specs.mjs:13, :68–69). This is the same rule as `isClosingFence` in OpenSpec 1.3.1. The scenario is `spec-lint-015` (openspec/specs/spec-lint/spec.md:60). Its test (specLint.test.mjs:102) covers a three-backtick line in a four-backtick fence, a `~~~` line in a three-backtick fence, a short tilde line with text, and a four-backtick line with text. It asserts that the hidden requirement is not read. It also asserts that the headings after the real end fence are read, and that `SPEC-LINT-NO-SCENARIO` is at line 19. The main spec and the delta spec are the same. Test task 1.14 comes before code task 1.15 in tasks.md. The ID is in `ids.json` and in `links.json`.

Other checks:
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected before review.md is written. There are no warnings.
- **Tests:** `specLint.test.mjs` and `specs.test.mjs` pass on copies in the scratchpad (27 tests).
- **Not reported:** A `#####` heading under a scenario does not start a new scenario in OpenSpec either (`expC`). The two parsers agree, so I do not report it.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r20/`. F1 and F2 depend only on JS parser logic, so the Node version does not change them.