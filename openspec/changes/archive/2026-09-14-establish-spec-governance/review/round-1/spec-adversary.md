Verdict: FAIL
- [ ] F1 critical openspec/trace/history.jsonl:3 Three history lines name another change: `"change":"remove-classification-banner"` for `src/hud.js` (lines 3-5). The other trace files also hold data from that change. `openspec/trace/gaps.json` records `src/hud.js` with sha `6da3908f…` and 231 lines, which is the edited file; `origin/main` and HEAD have sha `b678c5a4…`. `openspec/trace/ids.json` has 3 entries with `"change":"remove-classification-banner"`, and `links.json` has `hud-markings-001..003`. The reviewed tree also contains that change: `computeTreeHash` (scripts/spec/lib/review.mjs:63-66) hashes each diff file, so it includes `src/hud.js`, `style.css` and `src/hudMarkings.test.mjs`. So this tree cannot merge as one change or as two. If you commit this change alone, the gates stop with hash, `TRACE-ID-DROPPED` and `TRACE-LINKS-STALE` errors. If you commit both, `ci` stops with `CI-ACTIVE-CHANGE` now, and with `CI-TWO-CHANGES` (scripts/spec/lib/ci.mjs:25-27) after you archive the other change. The given gate output comes from `check`, not `ci`, so it does not show this. Split the work: build the ledger, history, registry and links of establish-spec-governance on a tree without the banner change, and review that tree. Then ratchet remove-classification-banner in its own pull request.
- [ ] F2 critical scripts/spec/lib/ledger.mjs:227-248 `compareWithBase` compares only `lines` with the base. It never compares `sha`, `branches` or `functions`. An author can edit a file so that it has more uncovered branches or functions on lines that tests already cover. The author then sets `sha`, `branches` and `functions` in gaps.json by hand. `compareLedger` sees an unchanged hash, so it skips the branch check at ledger.mjs:141-150, and the base check has no error. I ran it: `compareLedger` gave `{"errors":[],"stale":[]}` and `compareWithBase` gave `[]` for an entry changed from branches 3, functions 1, sha old to branches 7, functions 3, sha new. This contradicts proposal.md:10: "so a person cannot edit them to hide a gap". It also contradicts design.md:20, "Stop the known ways to hide a gap: ledger edits". Compare the entry with the base entry: when `entry.sha !== base.sha`, stop for more branches or functions than the base. Stop for a hash change unless `sameAsBase` is false and the history after the base has a `content changed` line. Add scenarios and tests for this.
- [ ] F3 critical scripts/spec/lib/ledger.mjs:229-237 The unstable-range exception lets coverage drop when an author deletes or weakens tests. `stabilityLedger` (ledger.mjs:395-420) takes its samples from `.gev-cache/spec-samples.jsonl` (gates.mjs:273-274). That file is an untracked cache that the author controls, and it keeps runs from before the tests were removed. The only guard is `sameAsBase(file)`, and it is true when only the tests changed. The base check then accepts any range that has an `unstable` history line, and anyone can append that line by hand. I ran it: an unchanged file went from 5 to 40 lines, with `low` and one appended `{"reason":"unstable"}` line, and both compare functions returned no errors. design.md:93 only says "a code change cannot use a range to hide a new gap". The limit `deterministic-tests` does not state this hole. This change also raised four entries above their earlier counts with this command: `ais-store.js` 40→44, `annotationResolver.js` 562→567, `labelArbiter.js` branches 50→52 and `gevActions.js` 1302→1304. Proposal Impact says "Gaps that this change opens: none". Make the gate reject a range when a test file that loads the file changed against the base. Also require the samples to come from runs of the base tree, or record the unstable files in a reviewed allow list. List the four widened entries in Impact.
- [ ] F4 critical scripts/spec/lib/test-guard.mjs:53 The guard compares the parsed source with the file on disk at parse time. `measureCoverage` reads the file again after the run (scripts/spec/lib/coverage.mjs:84-86). A test can write a short version of a code file, padded to the same byte length, import it, and then restore the file. I ran this on node:24.21.0 with the guard: the `src/target.js` fixture has 4 branches and was only partly exercised. It got `LF:12 LH:12 BRF:2 BRH:2 FNF:1 FNH:1` and the guard reported `"violations":[]`, so the gate would record the file as complete. Put the content hash of each inventory file in `inventory.json` before the run, and compare the parsed source with that snapshot. Stop for a hash change after the run. Add a scenario and a test for this.
- [ ] F5 critical scripts/spec/lib/test-guard.mjs:166-173 The inspector `Session` sees only the main thread. Code in a `worker_threads` Worker is not checked. I ran this on node:24.21.0: `new Worker("new Function('return 1;\\n//# sourceURL=file:///exp/src/other.js')()", {eval:true})` gave `src/other.js` `LH:6 LF:6 BRH:2 BRF:2 FNH:1 FNF:1`, and the guard wrote `"violations":[]`. The file was never imported. The limit `child-process-guard` covers processes only. Install the guard in workers, for example by wrapping `Worker` to add the guard to `execArgv`, or stop for any worker coverage of inventory files. Add a scenario and a test, or list the limit.
- [ ] F6 major scripts/spec/gates.mjs:359-362 `ids.json` is checked only against the current specs (registry.mjs:46-62). Nothing compares it with the base. So spec-trace-032 is enforced only when the author runs `ratchet`. An author can write the new hash into ids.json by hand and change scenario text with no test change; `checkRegistry` returned `[]`. An author can also delete a scenario from `openspec/specs` together with its ids.json entry. Then `TRACE-ID-DROPPED` never fires, the ID is not retired, and a later change can reuse the ID with a new meaning. Compare ids.json with the base in the check: an entry that is removed and not in retired-ids.json is an error. A changed hash needs a changed test file that names the ID, the same rule as the ratchet. Add scenarios for both.
- [ ] F7 critical src/tooling/spec/testGuard.test.mjs:135 Tags on tests that do not assert the THEN line of the named scenario. coverage-gate-015 says the test runs "do not get these values from the gate environment". These tests assert other things: `[coverage-gate-015] gives the guard to a child process that collects coverage` (:135), `keeps the promisified result of the real execFile` (:169), `does nothing without GEV_SPEC_OUT` (:189) and `[coverage-gate-015 spec-trace-027] writes the guard results of a child process at exit` (:175, which asserts only `Array.isArray`). More tests have the same problem:
  - specs.test.mjs:181 `[spec-trace-006] reads no retired IDs… from an empty project`
  - specLint.test.mjs:275 `[spec-lint-005] does not check removed requirements`
  - ste.test.mjs:61 `[ste-lint-001] reads no Markdown files from a project without openspec`
  - inventory.test.mjs:233 `[coverage-gate-010] stops when Git cannot list the files`
  - gates.test.mjs:383 `[coverage-gate-003 coverage-gate-022] stops when the test runner … cannot start and reads the command line`
  - git.test.mjs:280 `[gap-ledger-026 gap-ledger-030] reads the merge base…`, which does not assert that the comparison is skipped
  - ledger.test.mjs:253 `[gap-ledger-010] writes the ledger with sorted keys`

  The tags hide code that no scenario describes: the child-process guard (`withGuardEnv`, `wrapChildProcess`), `GATES-TEST-RUN`, usage and exit status 2, the `tree` command, the ratchet range shift (ledger.mjs:316-320) and origin `unstable` for new stability entries (ledger.mjs:415). Write scenarios for this behavior and tag the tests with them. Otherwise, remove the tags and record the tests as untraced.
- [ ] F8 major scripts/spec/gates.mjs:145-146 The gate reads each `^tests-.*\.jsonl$` and `^guard-\d+\.jsonl$` file in `.gev-cache/spec`. Each test process gets that folder in `GEV_SPEC_OUT` (gates.mjs:100). A test or the code under test can write `tests-x.jsonl` with passing tagged records, or `guard-1.jsonl` with assertion counts. That makes scenarios verified and avoids `TRACE-NO-ASSERTION`, and no gate finds it. Known limits does not list this. Read only the files the gate named: `tests-main.jsonl`, `tests-allocation-<n>.jsonl` and guard files from known child PIDs. Or write the outputs to a folder that the test processes do not know. Add a scenario.
- [ ] F9 major scripts/spec/lib/coverage.mjs:89-96 Shell files and HTML inline scripts always get 0% coverage. `scripts/dev-*.sh` (4 files) and `tools/cesium-render.html` are permanent gaps. The gate stops for a new shell script, for a longer one and for new inline script code (`LEDGER-NEW-COVERAGE-GAP` or `LEDGER-LARGER-GAP`), and no change can ever close these gaps. Known limits lists only `browser-coverage`, and no follow-up change covers `.sh` or inline HTML. Add this limit and a follow-up to the proposal, or change the rule for these file types.
- [ ] F10 minor scripts/spec/lib/test-guard.mjs:84-96 `wrapAssertModules` wraps only properties of the module. A direct `assert(value)` call, for both `node:assert` and `node:assert/strict`, and `t.assert.ok()` are not counted. I ran this on node:24.21.0: `"assertions":[]`. Such a traced test gets `TRACE-NO-ASSERTION`, but spec-trace-027 says "calls no function of node:assert". The count also includes `assert.ok(true)` and assert calls inside the code under test, and Known limits does not say that the count cannot tell a real check from a trivial one. Count the default export and `t.assert`, and add that limit to Known limits.
- [ ] F11 minor scripts/spec/lib/specs.mjs:101 A scenario that is not under a `### Requirement:` heading is not added to `requirement.scenarios`. So `lintSpecs` (spec-lint.mjs:24) never checks its WHEN and THEN lines. Its origin stays `unknown` with no error, and the trace gate still uses the scenario. Stop for a scenario outside a requirement, and add a scenario.
- [ ] F12 minor scripts/spec/lib/ci.mjs:23 `touched` includes only code files, test files and `openspec/`. A pull request can change `.claude/agents/*.md`, `AGENTS.md`, `.claude/commands/opsx/review.md`, `.github/workflows/ci.yml`, `Makefile`, `Dockerfile` or `.node-version` with no archived change and no review. Someone can weaken the review agent prompts this way, because `checkAgents` checks only the tools. Add these process files to `touched`, or list this limit.
- [ ] F13 minor openspec/changes/archive/2026-09-13-establish-spec-governance/tasks.md:24 Task 1.22 "Write `scripts/spec/lib/registry.mjs`" comes before the test tasks 2.21 `spec-trace-029` and 2.22 `spec-trace-030`, and `checkLinks`/`writeLinks` live in registry.mjs. That breaks the rule that each test task comes before its code task. Also, every task in the archived tasks.md is still `- [ ]`, so the record does not show what was done. Move the links code task after 2.22, and check the completed tasks.
- [ ] F14 minor openspec/changes/archive/2026-09-13-establish-spec-governance/design.md:91 The design names three unstable files ("a third unstable file"), but gaps.json and history.jsonl:7 have a fourth, `src/annotations/annotationResolver.js`. proposal.md:33-34 omits files that the diff adds: `.claude/commands/opsx/{apply,archive,explore,propose}.md`, `.claude/skills/*/SKILL.md`, `openspec/config.yaml`, and `compose.yaml`/`.dockerignore` from commit 5d470ae, which is not in origin/main. It also calls `Dockerfile` and `Makefile` changed files, but they are new against the base. design.md:59 says the guard does "hook blocks", but the guard only detects hooks. Correct the design and the Impact list.
- [ ] F15 minor scripts/spec/gates.mjs:336 The history `commit` field is `headCommit(root)` at ratchet time. AGENTS.md step 3 runs the ratchet before the commit in step 6, so each line records the parent commit (`5d470aee…` on all 7 lines), not the commit that closes the gap. Record the change name only and let git blame supply the commit, or state in gap-ledger-010 what the field means.
- [ ] F16 minor src/tooling/spec/testGuard.test.mjs:112 `assert.ok(guard.results().assertions.length >= before)` cannot fail. ledger.test.mjs:340 (gap-ledger-034) does not assert the THEN line "tells you to run the ratchet command". gates.test.mjs:237-251 (spec-trace-032) and gates.test.mjs:461-469 (gap-ledger-036) do not assert "does not change the registry" or "does not change the ledger" on disk. Remove the constant assertion. Assert the message, and compare the ids.json and gaps.json files before and after the command.
- [ ] F17 minor scripts/spec/lib/review.mjs:83 `changeFolder` returns the first archived folder with a matching name, sorted by date, so the oldest one. If a new archived change reuses an old change name, `check --change` compares the old `review.md` with the tree hash. `checkArchivedReviews` then skips the old folder and checks the new review without a tree hash. Select the folder that the diff adds, as `planCi` does, or stop for a reused change name. Add a scenario.
