Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-23-ledger-waiver/design.md:188 "remove the loaded condition from the waived count of `compareLedger`. The test fails." This text has two possible meanings, and with one of them it does not agree with the code. `scripts/spec/lib/ledger.mjs:324` has two loaded conditions: `!entry.loaded || !gap.loaded`. Case 3 of the `gap-ledger-082` test (`src/tooling/spec/ledger.test.mjs`, "Case 3") has an unloaded entry and an unloaded file. If you remove only one of the two conditions, the other condition still gives 0, so the test passes. The same problem is at design.md:187 "the count condition" and at tasks.md:23 "remove one condition at a time. One case must fail for each". `ledger.mjs:246` has `Number.isInteger(line.count) && line.count > 0`, and no case has a count of 0 or less, so the removal of `line.count > 0` alone fails no case. Write: "remove the two loaded conditions, `!entry.loaded` and `!gap.loaded`, together" and "remove `Number.isInteger(line.count)`". Or add a case for each part of each condition.
- [ ] S2 major openspec/changes/archive/2026-09-23-ledger-waiver/proposal.md:44 "So a shell file or an inline HTML script, which no test can load, has no waiver." This text does not agree with the code. proposal.md:9 says "A waiver is a history line". The `faults` array of the `waive` command (`scripts/spec/gates.mjs:369-377`) does not refuse a file that no test loads, so the command writes the waiver line for such a file. proposal.md:47 uses "has no waiver" next to "The waive command refuses", so a reader can think that the command refuses this file too. Write: "So a shell file or an inline HTML script, which no test can load, gets no waived count. The waive command does not refuse such a file."
- [ ] S3 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:130 "the content hash of the counts" Articles and nouns. No earlier text in the paragraph names "the counts". "Of the counts" can also mean a hash that the code calculates from the counts, but the change always uses a content hash of file content (proposal.md:9). Write: "One or more waivers name the file and its content hash. For `compareWithBase`, this is the hash of the ledger entry."
- [ ] S4 minor openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:292 "Allow a ledger entry that the base does not have, by the waived count" Articles and nouns. Round-3 S16 is only partly corrected. The title still has "by", which has no clear meaning for an entry. The new test name uses different words. Write: "Allow the waived count for a ledger entry that the base does not have".
- [ ] S5 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:59 "which adds the entry of a waived file" One word, one meaning. Round-3 S12 is corrected at design.md:190 and tasks.md:38, but not here. The change waives a count, not a file. Write: "which adds the entry of a file with no entry and a waived count".
- [ ] S6 minor openspec/changes/archive/2026-09-23-ledger-waiver/proposal.md:13 "or for a waiver line whose count is not a positive whole number" Approved words, and one word, one meaning. I think that "whose" is not an approved STE word, but I am not sure. The sentence uses "give none to" a file and "give none for" a line. spec.md:134 uses other words for the same rule. Write: "Also give none to a file that no test loads. A waiver line with a count that is not a positive whole number gives no waived count."
- [ ] S7 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:193 "or write the base commit" Articles and nouns. This text does not say where the command writes the commit. It can also mean "make a commit on the base". tasks.md:69 gives the full words. Write: "or write the base commit in place of the head commit in the waiver line".

Round-3 findings S1, S2, S3, S6 to S11, S13, S15 and S17 are corrected. S14 was refuted and has no change. S4 is corrected, but its new words cause S3 above. S5 is corrected at design.md:132 and design.md:136, but design.md:130 has S3 above. S12 is only partly corrected (S5 above), and S16 is only partly corrected (S4 above). The two `gap-ledger-087` test names have no findings.
