Verdict: FAIL
- [ ] F1 critical src/tooling/spec/gates.test.mjs:812 The test for `gap-ledger-079` asserts the waiver line's fields, that the ratchet command then passes, and that `check` passes, but it never asserts the scenario's last AND: "the command runs no test and does not change `openspec/trace/gaps.json`." No assertion in the test (lines 812-851) reads `openspec/trace/gaps.json` before and after the `waive` call at line 826 to confirm it is unchanged, and nothing confirms the `waive` command ran no test. Add an assertion that `openspec/trace/gaps.json` has the same content immediately before and after the `waive` call.
