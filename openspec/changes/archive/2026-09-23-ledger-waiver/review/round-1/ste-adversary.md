Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-22-ledger-waiver/specs/gap-ledger/spec.md:249 "the gate compares the counts of the file with its entry with a waived count of 0" Approved words / one clear meaning. The two "with" phrases stack on "compares," so the sentence reads two ways: the gate compares the file's counts with its entry, using a waived count of 0; or the entry itself has a waived count of 0. Write: "the gate compares the counts of the file with the counts of its entry, using a waived count of 0."
- [ ] S2 major src/tooling/spec/ledger.test.mjs:816 "adds the entry for a waived file with no entry, and records the rise" Disagreement with the change's own vocabulary. Nowhere else does the change call a file "waived" — only a count or metric is waived (see the added requirement "Coverage waiver" and the sibling test at ledger.test.mjs:808, "a file with no entry and an unwaived count"). Write: "adds the entry for a file with no entry and a waived count, and records the rise".
- [ ] S3 minor openspec/changes/archive/2026-09-22-ledger-waiver/proposal.md:5 "The two gates close on the change from two sides." Approved words. "Close on" is figurative, not a literal STE meaning. Write: "The two gates both stop the change, one from each side."
- [ ] S4 minor openspec/changes/archive/2026-09-22-ledger-waiver/proposal.md:33 "hits the exact defect it fixes" (also design.md:123 "hit the exact defect they fix") Approved words. STE approves "hit" only for a physical blow. Write: "has the exact defect it fixes" (and "have the exact defect they fix" at design.md:123).
- [ ] S5 minor openspec/changes/archive/2026-09-22-ledger-waiver/proposal.md:33 "A restructuring closed six of the seven phantom branches" (also design.md:125 "A restructuring of the seven `if` blocks... closed six of the seven") -ing word used as a noun. Write: "The seven `if` blocks were made into one array and one `find` call, and that closed six of the seven." / at design.md:125: "Restructuring the seven `if` blocks into one array and one `find` call closed six of the seven" → better: "The team put the seven `if` blocks into one array and one `find` call. That closed six of the seven."
- [ ] S6 minor openspec/changes/archive/2026-09-22-ledger-waiver/design.md:127 "Writing more tests cannot close a defect in the counting." Two -ing words used as nouns. Write: "More tests cannot close a defect in how the gate counts."
- [ ] S7 minor openspec/changes/archive/2026-09-22-ledger-waiver/design.md:129 "the loop over existing entries" -ing word used as an adjective. Write: "the loop over the entries it already has".
- [ ] S8 minor openspec/changes/archive/2026-09-22-ledger-waiver/design.md:131 "because it is undefined" Voice. An active form is possible. Write: "because the entry does not exist".
- [ ] S9 minor openspec/changes/archive/2026-09-22-ledger-waiver/tasks.md:57 "Reddens on the missing `reason` field." -ing word used as an adjective. Write: "Reddens because the `reason` field is not there."
- [ ] S10 minor openspec/changes/archive/2026-09-22-ledger-waiver/tasks.md:65 "...one `find` call, keeping `gap-ledger-080` green." An -ing clause added to an instruction. Write: "Restructure the seven checks of `waive` into one `faults` array and one `find` call. Keep the tests of `gap-ledger-080` green."
- [ ] S11 minor openspec/changes/archive/2026-09-22-ledger-waiver/tasks.md:87 "its one remaining phantom branch" -ing word used as an adjective. Write: "the one phantom branch it still has".
- [ ] S12 minor openspec/changes/archive/2026-09-22-ledger-waiver/tasks.md:88 "a reason naming the marker evidence" -ing phrase in place of a relative clause. Write: "a reason that names the marker evidence".
- [ ] S13 minor openspec/changes/archive/2026-09-22-ledger-waiver/specs/gap-ledger/spec.md:275 "the history gets one line for each not-covered metric" Voice. The actor (the ratchet command) is named in the line just above and can be the subject. Write: "the ratchet command adds one line to the history for each not-covered metric".
- [ ] S14 minor src/tooling/spec/gates.test.mjs:812 "records a coverage waiver, allows the rise in ratchet and passes the check" Articles and nouns. No article before "ratchet". Write: "records a coverage waiver, allows the rise in the ratchet command and passes the check".
- [ ] S15 minor src/tooling/spec/ledger.test.mjs:808 "a file with no entry and an unwaived count" (also src/tooling/spec/ledger.test.mjs:844, same wording) Approved words. "Unwaived" is a negative-prefix word not used elsewhere in the change. Write: "a file with no entry and a count with no waiver" (both test names).
