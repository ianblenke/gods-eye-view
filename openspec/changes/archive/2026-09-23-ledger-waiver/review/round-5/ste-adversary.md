Verdict: PASS
- [ ] S1 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:187 "Also remove the loaded condition of `compareWithBase`." Instructions. "Also" can make a reader remove the loaded condition in the same mutation as one of the four `waiversOf` conditions. "The test fails for each" and tasks.md:23 ("one condition at a time") show that each is a separate mutation, so the text has one clear meaning. Put it in a separate bullet. Write: "- `gap-ledger-084`: remove the loaded condition of `compareWithBase`. The test fails."
- [ ] S2 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:193 "Also write the base commit in place of the head commit." Instructions. This has the same problem as S1. tasks.md:70 gives this as a separate mutation (Mutation 3). Write: "- `gap-ledger-079`: write the base commit in place of the head commit. The gate test fails."
- [ ] S3 minor openspec/changes/archive/2026-09-23-ledger-waiver/proposal.md:46 "Its waiver applies to each check with that change name, also the CI check of the archived change." Articles and nouns. The part "also the CI check of the archived change" has no verb. The text agrees with the code: `planCi` (`scripts/spec/lib/ci.mjs:30`) gives the name of the archived change, and `gates.mjs:451` then uses it in `waiversOf`. Write: "Its waiver applies to each check with that change name. This includes the CI check of the archived change."

Round-4 findings:
- **S1 is corrected.** In the `gap-ledger-082` test, Case 4 fails when you remove only `!gap.loaded`, and Case 5 fails when you remove only `!entry.loaded` (`scripts/spec/lib/ledger.mjs:324`). In the `gap-ledger-084` test, the case with a count of -1 fails when you remove `line.count > 0`, and so does the case with a count of 0 (`ledger.mjs:246`). design.md:187-188 and tasks.md:16 now name each part. tasks.md:23 now agrees with the test.
- **S2 is corrected** at proposal.md:44. The `faults` array of the `waive` command (`scripts/spec/gates.mjs:369-377`) has no loaded condition, so the new text agrees with the code.
- **S3, S4, S5 and S7 are corrected.** The new title of `gap-ledger-087` agrees with its test name (`src/tooling/spec/ledger.test.mjs:942`), with the scenario title of `gap-ledger-086`, and with `openspec/specs/gap-ledger/spec.md:373`.
- **S6 was refuted, and the diff does not change it.**
