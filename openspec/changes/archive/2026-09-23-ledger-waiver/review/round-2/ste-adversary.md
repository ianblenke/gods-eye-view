Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-23-ledger-waiver/design.md:13 "the seven changed or added scenarios" Wrong count. The change modifies five scenarios and adds nine (`gap-ledger-079` to `gap-ledger-087`). This disagrees with the delta spec, proposal.md:27 and design.md:11. Write: "the five changed scenarios and the requirement "Coverage waiver" with its nine scenarios".
- [ ] S2 major src/tooling/spec/ledger.test.mjs:835 "stops the build for a file with no entry and a count with no waiver" (also ledger.test.mjs:871 "stops for a ledger entry the base does not have and a count with no waiver") Name does not match the code. Both tests give a waiver with the count 1 for a not-covered count of 2, so the count has a waiver that is too small. The text came from round-1 S15, which I proposed. Write: "stops the build for a file with no entry and a count above the waived count" and "stops the build for a ledger entry the base does not have and a count above the waived count".
- [ ] S3 major openspec/changes/archive/2026-09-23-ledger-waiver/design.md:133 "replace the `fullyWaived` check with `true`. The two tests of the compareLedger case redden." (also tasks.md:35 "The two `compareLedger` cases must redden.") Two meanings, and neither agrees with the code. If you set `fullyWaived = true`, only the test at ledger.test.mjs:835 reddens. If you write `if (true)`, the tests at :827 and :843 redden and :835 stays green. No single mutation reddens both `compareLedger` tests. design.md:139 has the same two meanings ("with `false`"). Write: "Replace the value of `fullyWaived` with `true`. The stop case reddens. Replace the condition `!fullyWaived` with `true`. The allowed case and the ratchet case redden."
- [ ] S4 minor openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:256 "using a waived count of 0" Words that end in -ing (STE-ING). This text came from round-1 S1, which I proposed. Write: "THEN the gate compares the counts of the file with the counts of its entry, and the waived count is 0".
- [ ] S5 minor openspec/changes/archive/2026-09-23-ledger-waiver/proposal.md:41 "The gates of this change hit the same defect" Approved words. This is the same misuse of "hit" as round-1 S4. Write: "The gates of this change have the same defect".
- [ ] S6 minor openspec/changes/archive/2026-09-23-ledger-waiver/proposal.md:17 "the file's not-covered counts are each at or below the sum of its waivers for that metric" Articles and nouns. No earlier word in the sentence gives "that metric". Write: "the not-covered count of each metric of the file is at or below the sum of its waivers for that metric".
- [ ] S7 minor openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:288 "the comparison with the base commit does not stop the build" One word, one meaning. The other scenarios, including `gap-ledger-083`, name the same actor "the gate". Write: "THEN the gate does not stop the build for that file".
- [ ] S8 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:131 "after the loop over the entries it already has" / "one history line per not-covered metric" Articles and nouns. "It" points back to `ratchetLedger`, but the text means the ledger (round-1 S7 text, which I proposed). I think "per" is not an approved word, but I am not sure. Write: "after the loop over the entries of the ledger" and "one history line for each not-covered metric".
- [ ] S9 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:133 "Scenario `gap-ledger-086` covers this." (also design.md:139 "Scenario `gap-ledger-087`") Articles and nouns. No article. Write: "The scenario `gap-ledger-086` covers this." and "The scenario `gap-ledger-087` covers this."
- [ ] S10 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:129 "the same shape as the changed-file one" Articles and nouns. "One" does not clearly point to a noun. Write: "with the same shape as the waived count for a changed file".
- [ ] S11 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:137 "caught what it missed" Approved words. "Caught" and "missed" are figurative, and "it" can be the run or the design. Write: "found the rule that the first version did not examine".
- [ ] S12 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:123 "`gap-ledger-080` triggers every one of the seven" Approved words. I think "trigger" is not an approved verb, but I am not sure. Write: "`gap-ledger-080` causes each of the seven faults".
- [ ] S13 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:104 "So the omission is deliberate in effect" Approved words. "Deliberate" is not approved, and "in effect" is an idiom. Write: "So the omission is correct".
- [ ] S14 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:34 "waived and under-waived" (also tasks.md:37) Approved words. "Under-waived" is not a word. Write: "with a waiver for the full count and with a waiver that is too small".
- [ ] S15 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:57 "Reddens ... Confirmed." (also :58 "Reddens with a broken history-continuity check. Confirmed.", :59, :66 "Confirmed by mutation that this closes") Voice. These are fragments with no subject, one is in the passive voice, and "broken" is figurative. Write: "The test reddens because the `reason` field is not there.", "The test reddens with `LEDGER-HISTORY-CHANGED`.", "A mutation showed that this closes six of the seven phantom branches."
- [ ] S16 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:61 "Spot-checked the unknown-metric guard; reddens. The other six follow the identical pattern." Approved words and verbs. There is no subject, and "spot-check" is not an approved verb. Use "same" in place of "identical". Write: "We ran the mutation only for the unknown-metric check, and the test reddened. The other six checks have the same shape."
- [ ] S17 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:88 "Correct the stale 7-branch waiver ... for the one phantom branch it still has" Articles and nouns. "It" can be the waiver or `gates.mjs` (round-1 S11 text, which I proposed). I think "stale" is not approved, but I am not sure. Write: "Replace the old 7-branch waiver for `gates.mjs` in the history with a waiver for the one phantom branch that `gates.mjs` still has."
- [ ] S18 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:10 "The seven checks of the command" (also design.md:123, tasks.md:61, tasks.md:65) One word, one meaning. "Check" also means the gates check command (design.md:82, design.md:189, and the `gap-ledger-079` name "passes the check"). Keep "check" for the gates command. Write: "The seven fault conditions of the command".

Round-1 findings S1 to S15 are all corrected in the text. S2, S4, S8 and S17 above are problems in the replacement text that I proposed in round 1.
