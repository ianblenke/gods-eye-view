Verdict: FAIL
Tree read: branch `ledger-waiver` at 0ca2a8c plus the working tree, which includes the untracked archive folder. Round 1: F1 is corrected (gates.test.mjs:826-830). STE findings S1 to S15 are corrected in the text.
- [ ] F1 critical src/tooling/spec/ledger.test.mjs:784 Case 4 of `gap-ledger-084` cannot fail for the rule it claims to check: that a file with the base content gets a waived count of 0. Its `LEDGER-MORE-THAN-BASE` comes from the covered-count rule for an unchanged file at scripts/spec/lib/ledger.mjs:437, and that rule does not read the waived count. If you delete `if (unchanged) return 0;` at ledger.mjs:403, every test still passes. design.md:182 says this mutation makes a test fail, and that is false. Add a lines case: an unchanged file with entry lines 7, base lines 5 and a lines waiver of 2 for its hash, and expect `LEDGER-LARGER-THAN-BASE`. Then run the mutation and report the test that fails.
- [ ] F2 major scripts/spec/lib/ledger.mjs:298 The two paths for a file with no entry give a waived count to a file with the base content. These are `waivedNew` in `compareLedger` and `newWaived` at line 390 in `compareWithBase`. The requirement says "The gates MUST give no waived count … to a file with the base content", and `gap-ledger-084` says the same in its second AND. Example: a change deletes the tests of a base file that is at 100%. The author appends a waiver line to history.jsonl by hand. The gates cannot tell that line from one the `waive` command wrote. `compareLedger` then passes, the ratchet adds the entry, and `compareWithBase` passes. So the 087 path gives a waiver where the base-entry path, with its covered-count rule, gives none. Also, `every(...)` passes an entry with all counts at 0 and no waiver at all, but 087 says "one or more waivers". Such a hand-added entry for an unchanged file then gets the tolerance. Give 0 when `sameAsBase(file)` is true, require at least one matching waiver line, and add a test for each case.
- [ ] F3 major openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:7 The change did not modify `gap-ledger-003` (line 7), `gap-ledger-021` (line 101) or `gap-ledger-048` (line 119), and each still says the gate stops the build with no condition. `gap-ledger-086`, `gap-ledger-087` and `gap-ledger-083` say it does not stop, and the code follows those three. Example: in the `gap-ledger-083` test, the covered count is 95 against 97, so `gap-ledger-048` says stop, and the code passes. The proposal names only five modified scenarios. Modify 003, 021 and 048 with the waived-count condition, and add the changed tests and the task lines for them.
- [ ] F4 major scripts/spec/lib/ledger.mjs:297 `compareLedger` does not record a fully waived file with no entry as not current. A change can pass `make gates CHANGE=x` with no ratchet run, and the ledger then does not show the gap. The requirement text of "Ratchet rule" says the gates MUST stop for that. After the merge, a check with no change name has no waivers. `LEDGER-NEW-COVERAGE-GAP` then stops main and each later change. The `gap-ledger-086` test at ledger.test.mjs:832 asserts `[]`, so the test locks in the defect. Push a stale entry for that file, as `gap-ledger-081` does, and change the THEN line of 086 and its test.
- [ ] F5 major src/tooling/spec/ledger.test.mjs:721 The tests of 082, 083, 086 and 087 do not check the WHEN conditions of the paths for a file with no entry. They also do not check the lines rule of `compareWithBase`. Each of these mutations passes the tests:
  - Remove `item.sha === gap.sha` (ledger.mjs:300) or `w.sha === entry.sha` (:392). These are bound 2 of D5.
  - Remove the file filter.
  - Remove `gap.loaded &&` (:302) or `entry.loaded &&` (:394).
  - Remove `+ waivedLines` (:409).
  
  The THEN line of 086 says "one history line for each not-covered metric", but the test has only one metric. Add a negative case for each condition and a case with two metrics. Run each mutation and report the test that fails.
- [ ] F6 minor src/tooling/spec/gates.test.mjs:835 The `gap-ledger-079` test uses `find` to get the waiver line, so a command that appends the line two times still passes, and the waived count doubles. The THEN line says "one line". The test checks the commit only with a regex, not against HEAD. The test also does not check the waivers that gates.mjs:489 passes to `compareLedger` in the check, because the check runs after the ratchet. Assert that the command adds exactly one line, compare the commit with `git rev-parse HEAD`, and add a check before the ratchet that expects `LEDGER-STALE`.
- [ ] F7 minor scripts/spec/lib/ledger.mjs:520 `!codeFiles.has(file)` and `!gap.loaded` in the second loop of `ratchetLedger` are dead code. `compareLedger` already stops an unloaded gap with no entry before the loop runs, and `current.coverage` comes from the inventory. This is the same kind of guard as the `if (!gapped)` guard that D10 removed. No scenario describes them. Remove them.
- [ ] F8 minor openspec/trace/history.jsonl:590 The `ledger-waiver` line records the untraced count of `src/cockpitMarkup.test.mjs` as smaller, from 38 to 33. The change does not touch that file, and gaps.json:7121 still has 38 names for it. The proposal says that the change closes no gap, and it names only the drift of `labelArbiter.js`. Remove the line, because it is after the base, or name it in the proposal.
- [ ] F9 minor openspec/trace/gaps.json:1069 The change adds a new ledger entry for `scripts/spec/gates.mjs` (branches 1, origin `ledger-waiver`) with the waiver at history.jsonl:586. The proposal names it in Impact and in D10. The marker evidence is outside the gates, so the reviewer must accept this entry by name.
- [ ] F10 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:61 Task 4.3 checked the mutation of only one of the seven faults, and AGENTS.md rule 13 requires each one. Task 6.2 (the mutation report) and tasks 7.1 to 7.4 are open in the archived change. design.md:179 names the `gap-ledger-081` test for the lines mutation, but that test has no lines case. Run each mutation and record the test that fails in review.md.
- [ ] F11 minor gate output The gate output has `ERROR REVIEW-MISSING openspec/changes/ledger-waiver/review.md`. The run was before the archive, so no gate run has measured the archived tree. Run `make gates CHANGE=ledger-waiver` again after the archive and review.md, and report its output.
- [ ] F12 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:7 The changed-file lists in design.md and in proposal.md:31 do not name `src/tooling/spec/testGuard.test.mjs`, which the change edits at lines 442-443. Add it to both lists.
