Verdict: PASS
Tree read: branch `ledger-waiver` at 0ca2a8c plus the working tree, which includes the untracked archive folder. I read the code and did not run it, so each mutation result below comes from reading the code.

Round-2 status: F1, F2, F3, F4, F5, F7, F8 and F12 are corrected. F6 is partly corrected: the one-line assertion and the check before the ratchet are there, but the commit check is not. F9, F10 and F11 are still open, and F13 to F15 below carry them.

For F1, removing `if (unchanged) return 0;` makes case 5 at ledger.test.mjs:829 fail. Every other mutation in design.md:180-192 makes the test that the design names fail.

- [ ] F1 minor scripts/spec/lib/ledger.mjs:245 `waiversOf` does not check the fields of a waiver line. Suppose a person writes a line by hand with `"count":"5"`. Then `sum + item.count` at :326 and :409 gives the string "05". The sum at :260 becomes `5 + 0 + "05"`, which is "505", so the gate allows 505 lines and not 10. The same happens at :269, :412 and :436. The known limit `waiver-by-hand` says that the reviewer reads the line, but the reviewer reads 5. Keep only lines with a known metric, a string sha and `Number.isInteger(count) && count > 0`. Add a test with a string count.
- [ ] F2 minor scripts/spec/lib/ledger.mjs:322 The entry paths give a waived count to a file that no test loads (:322-327, and :405-410 in `compareWithBase`). `waiversCover` (:197) refuses such a file, and D10 makes "the record is loaded" a condition. A file that no test loads has no coverage report that can be wrong: coverage.mjs:131 counts each physical line. So an author can add untested lines to a pre-spec file that no test loads, and then waive them. Give 0 when the gap or the entry is not loaded, and add a test. Or name this case in the known limits.
- [ ] F3 minor scripts/spec/gates.mjs:365 The waive command checks only that the count is a positive whole number. For the metric `lines`, a count above the number of named lines cannot be true, for example `--lines 380 --count 40`. The gates do not compare the count with the lines or with the measured rise. D5 says this in words, but the section "Known limits and later changes" does not name it. Stop the command for a lines count above `lines.length`, or add a known limit `waiver-count-not-checked`.
- [ ] F4 minor scripts/spec/lib/ledger.mjs:239 Three guards in `waiversOf` have no test that fails when you remove them:
  - The `typeof` checks. No caller passes text that is not a string: gates.mjs:449-450 pass strings, and `compareWithBase` calls `history.startsWith` at :388.
  - `!history.startsWith(baseHistory)`. No test has a waiver line in a rewritten history.
  - `!change`. Without this guard, a check with no change name matches a waiver line with no `change` field, because `undefined === undefined`. No test has such a line.
  
  This is the same kind of guard as round-2 F7. Remove the `typeof` checks. Add a case for each of the other two guards.
- [ ] F5 minor scripts/spec/gates.mjs:360 `active` uses `changeFolder`, and review.mjs:112-113 also returns an archived folder. So `waive --change <archived change>` records a waiver. But `gap-ledger-080` says that a change that is not active is a fault, and no scenario allows an archived change the way `gap-ledger-077` does for the ratchet. No test covers this case. State the archived case in `gap-ledger-079` or `gap-ledger-080` and add a test, or refuse an archived folder.
- [ ] F6 minor openspec/trace/history.jsonl:586 The waiver reason says that the marker fired 8 times, "plus more from other GATES-WAIVE tests". No other test gives GATES-WAIVE: the 8 are the eight runs of the `gap-ledger-080` test (gates.test.mjs:878-913), which are the seven faults and the case without `--lines`. The reason is the only evidence of the waiver (D1, D4), and after the merge the line is part of the base history. Correct the reason before the merge.
- [ ] F7 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:152 The text says "Only five scenario bodies change" and "for each of the five". The change modifies eight scenarios (proposal.md:15, design.md:120). Write "eight".
- [ ] F8 minor src/tooling/spec/gates.test.mjs:846 This is the rest of round-2 F6. The test checks the `commit` field only with a regex, so a command that writes the base commit or any 40-digit hex value passes. The THEN line names the commit, and no rule reads it. Compare the field with `git(root, 'rev-parse', 'HEAD')`.
- [ ] F9 minor openspec/trace/gaps.json:1069 This is round-2 F9. The change adds a new ledger entry for `scripts/spec/gates.mjs`: branches 1, origin `ledger-waiver`, with the waiver at history.jsonl:586-587. The marker evidence is outside the gates, so the user must accept this entry by name in review.md.
- [ ] F10 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:90 This is round-2 F10. Tasks 6.1, 6.2 and 7.1 to 7.4 are open in the archived change, and the gates do not check that tasks are complete. Record each mutation of design.md:180-192, with the test that failed, in review.md. Then check each box only when its work is complete.
- [ ] F11 minor gate output This is round-2 F11. The output has `ERROR REVIEW-MISSING openspec/changes/archive/2026-09-23-ledger-waiver/review.md`. The run came before the lead changed gaps.json:2659 and history.jsonl, so no gate run has measured the final tree. The excerpt also leaves out the `Coverage:` and `STE:` lines. Run `make gates CHANGE=ledger-waiver` after review.md, and report the full output.
