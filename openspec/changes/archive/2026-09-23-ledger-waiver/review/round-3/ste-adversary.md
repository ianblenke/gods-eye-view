Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-23-ledger-waiver/design.md:152 "Only five scenario bodies change, so the trace gate needs a changed test for each of the five" This text does not agree with the other prose of the change. design.md:11, design.md:14, design.md:120, proposal.md:15, proposal.md:27 and tasks 2.6 to 2.10 and 2.13 to 2.15 give eight changed scenarios (003, 004, 013, 017, 021, 022, 040 and 048). Write: "Only eight scenario bodies change, so the trace gate needs a changed test for each of the eight and for no other carried scenario."
- [ ] S2 major openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:44 "make the new-entry rule of `compareWithBase` always false" (also tasks.md:40 "make the rule always false") This text has two possible meanings. design.md:138 calls the stop rule for an entry that the base does not have "its own rule". With that meaning, "always false" makes the gate never stop. design.md:190 gives the opposite mutation: the call of `waiversCover` becomes `false`, so the gate always stops. Write at tasks.md:44 and tasks.md:40: "replace the call of `waiversCover` in `compareWithBase` with `false`".
- [ ] S3 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:124 "has seven early-return checks" (also design.md:136 "a check for a loaded code file") One word, one meaning. Round-2 S18 is corrected at design.md:10 and tasks.md:71, but not here. "Check" is also the gates command. The present tense "has" gives old code: gates.mjs:369-379 now has one `faults` array. Write: "first had seven fault conditions, each with an early return" and "a condition for a loaded code file".
- [ ] S4 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:130 "It is true only when three conditions are true. The record is loaded" Voice (STE-PASSIVE), and the count. The active voice is possible. The text and ledger.mjs:197-201 give four conditions. Write: "It is true only when four conditions are true. A test loads the file. The file does not have the base content. One or more waivers name the file and its content hash. The not-covered count of each metric is at or below the sum of the waivers for that metric."
- [ ] S5 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:130 "the content hash of the record" (also design.md:132 "Without that record", design.md:136 "a `complete` record") One word, one meaning. "Record" is a verb in the change. As a noun, it means the measured counts here, the mark "not current" at :132, and the waiver line at :77 and :198. Write: "the content hash of the file", "Without the `LEDGER-STALE` error" and "drops a file at 100%".
- [ ] S6 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:97 "the untraced loop is right" Approved words. STE approves "right" only for the direction. Write: "the untraced loop is correct".
- [ ] S7 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:105 "An `unchanged` exception would let that case pass and no other case." Verbs. "Would" is not an approved tense. Write: "An `unchanged` exception can let only that case pass."
- [ ] S8 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:107 "The circularity is the ratchet." Approved words. "Circularity" is not approved, and no earlier sentence tells about a circle. Write: "The ratchet command cannot add a name."
- [ ] S9 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:109 "No instrument stands between the author and the tag. A name has no trace because no test name carries a scenario ID." Approved words. "Stands between" and "carries" are figurative. The words "no test name" say that no test in the project has an ID. Write: "The author writes the tag, and no instrument can make the tag wrong. An untraced name has no trace because the name has no scenario ID."
- [ ] S10 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:138 "unconditionally" (also design.md:101 and design.md:140 "stays unconditional") Approved words. I think that these words are not approved, but I am not sure. Write: "with no condition" and "The rule for untraced names has no condition."
- [ ] S11 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:186 "the base prefix cut" Verbs. "Cut" is a verb that the text uses as a noun. Write: "the `slice` of the base history in `waiversOf`".
- [ ] S12 minor openspec/changes/archive/2026-09-23-ledger-waiver/design.md:188 "remove the stale record for a waived file with no entry" (also tasks.md:36, and tasks.md:56 "a waived file") One word, one meaning. The change waives a count, not a file (round-1 S2). Write: "do not record a file with no entry and a waived count as not current" and, at tasks.md:56, "which adds the entry of a file with no entry and a waived count".
- [ ] S13 minor openspec/changes/archive/2026-09-23-ledger-waiver/tasks.md:35 "too small a waiver" Articles and nouns. tasks.md:29 writes "a waiver that is too small". Write: "with a full waiver, a waiver that is too small and the added entry".
- [ ] S14 minor openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:284 "THEN the gate does not stop the build for that count" One word, one meaning. The WHEN lines give each metric of the file, so "that count" does not point to one count. `gap-ledger-087` writes "for that file". Write: "THEN the gate does not stop the build for that file".
- [ ] S15 minor openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:290 "an entry for a loaded file that the base ledger does not have" Articles and nouns. The clause attaches to "file", but the base ledger has entries, not files. Use the words of `gap-ledger-021`. Write: "WHEN the ledger has an entry for a loaded file" and "AND the base ledger has no entry for that file".
- [ ] S16 minor openspec/changes/archive/2026-09-23-ledger-waiver/specs/gap-ledger/spec.md:289 "a ledger entry the base does not have, by the waived count" (also the two `gap-ledger-087` test names) Articles and nouns. The word "that" is not there, but the title and test name of `gap-ledger-021` use it. I think that STE does not let you omit "that", but I am not sure. For an entry, the meaning of "by" is not clear. Write: "allows a ledger entry that the base does not have, with the waived count" and "stops the build for a ledger entry that the base does not have and a count above the waived count".
- [ ] S17 minor openspec/changes/archive/2026-09-23-ledger-waiver/proposal.md:33 "The change waives the last one on itself." Approved words. "On itself" is figurative, and the change waives a branch of a file. Write: "The change writes a waiver for the last one in `scripts/spec/gates.mjs`."

Round-2 findings S1, S2 and S4 to S17 are corrected. S3 is corrected at design.md:190, but tasks.md:40 and tasks.md:44 now have two meanings (S2 above). S18 is only partly corrected (S3 above). The S1 count is correct at design.md:11 and design.md:14, but design.md:152 still has the round-2 count (S1 above).
