Verdict: FAIL
- [ ] F1 critical src/tooling/spec/testGuard.test.mjs:26 The code does not do the AND line of `coverage-gate-046`, and the tagged test does not assert it. The AND line says: "each test that starts no test run with the guard runs as usual". But `GUARDED_RUN` skips all 22 guarded tests in this file on Node 24.14.0. I removed the skip in a copy and ran the file with the Node 24.14.0 binary. Only 3 of the 22 tests failed: "[spec-trace-027] counts the assertions of each test", `coverage-gate-028` and `coverage-gate-036`. The other 19 tests pass on Node 24.14.0, and many of them start no test run. Examples are "[coverage-gate-018] reads the relative file of a script URL" (line 114), `coverage-gate-027` (line 362) and `coverage-gate-040` "keeps the gate values in the environment of a test runner" (line 367). The 28 skips in `gates.test.mjs` are correct: all 28 tests fail on Node 24.14.0 without the skip. The test for `coverage-gate-046` (line 402) asserts only the result of `missingTestContext({})` and `missingTestContext({ getTestContext() {} })`. It also asserts `GUARDED_RUN.skip === missingTestContext()`, which is always true because the constant is made from that call. No assertion shows which tests node:test skips, so a tag or a skip that is not correct does not make the test fail. Remove `GUARDED_RUN` from the 19 tests that pass on Node 24.14.0. Then add an assertion that connects the skip to the tests that start a guarded run, or change the THEN and AND lines to text that the test can assert.
- [ ] F2 minor scripts/spec/lib/ledger.mjs:191 For the covered branches and functions, the band uses the not-covered count (`band(entry[metric])`). The requirement "Count band for unchanged files" gives the band of "a covered count". Now no ledger entry gives a larger code band than the spec band. But a file with more than 250 not-covered items, and more not-covered items than covered items, gets a wider band than the spec allows. The test for `gap-ledger-066` cannot find the difference: 88 against 97 gives a band of 5 in both calculations. When I changed the code to `band(before)`, all 63 tests in `ledger.test.mjs` passed. Use one base in the code and in the spec, and add a test in which the two bands are different.
- [ ] F3 minor src/tooling/spec/ledger.test.mjs:757 The tests for `gap-ledger-065` to `gap-ledger-067` do not show "no band" for a file that no test loads or for a file with untrue coverage. The 067 cases for these files have the same counts in the entry and in the gap. Some of my code changes did not make a test fail. I removed `entry.loaded && gap.loaded` or `!entry.untrue && !gap.untrue` at ledger.mjs:227, and `entry.loaded` or the hash check at ledger.mjs:234. After each of these changes, all tests in `ledger.test.mjs` passed. Without `entry.loaded`, an entry with `loaded: false` for a file that a test now loads is not stale. The ledger then keeps the count of the physical lines as the limit. The code is correct now, and `gate-code` names changes to the gate code. Add cases with a count difference in the band for:
  - an entry that no test loaded, for a file that a test now loads
  - an untrue entry for a file that is now true and loaded
  - an entry that no test loaded, for a file that is now at 100%
- [ ] F4 minor scripts/spec/lib/ledger.mjs:390 `ratchetLedger` calls `compareLedger` without `sameAsBase`. Thus a count in the band of an unchanged file stops the ratchet command, but the check does not stop for it. Local runs also give different counts: the history of this rebuild changed `annotationResolver.js` from 567 to 562 lines. Thus a later ratchet run can stop a correct change. Also the known limit `unfound-instability` (proposal.md:57) is now not correct: for these unchanged files, the check no longer stops with `LEDGER-LARGER-GAP`, `LEDGER-LOST-COVERAGE` or `LEDGER-STALE` while the counts are in the band. Only the ratchet command stops. Correct the limit text. You can also let the ratchet command keep the old entry for a count in the band of an unchanged file. It must never record a larger count.
- [ ] F5 minor openspec/changes/archive/2026-09-15-establish-spec-governance/proposal.md:65 The limit `unchanged-band` does not state two things:
  - The band adds to the range. For an unchanged file with a range, the check allows the high count plus the band of the high count. For example, `ais-store.js` has the base count 40 and the range 40 to 44, and the check allows 49 lines. `range-slack` and `unchanged-band` each name one part, but they do not name the sum.
  - A test change is not the only cause. A change to another code file can also remove coverage in the band from an unchanged file, for example a change to a module that the unchanged file imports. The instruction "checks each removed or changed test" does not find this.
  State the sum and the second cause in the limit. Tell the spec adversary to also check changed code that unchanged files with ledger entries use.
- [ ] F6 minor Makefile:8 The target can hide a failure of the copy steps:
  - `$(GATES_BACK); exit $$status` does not use the status of the copy back. Thus `make ratchet` can give status 0 when `openspec/trace/` in the project did not change.
  - Makefile:6: `/bin/sh` in the image is dash, so the pipeline gives only the status of the last `tar`. I ran `false | tar … -cf - | tar -xf -` in the image, and the status was 0 with an empty copy. Thus a `git ls-files` failure (for example "dubious ownership" for a host user that is not uid 1000) makes an empty copy. The gates then stop with a module error, not with a copy error.
  - `--ignore-failed-read` skips a tracked file that the container cannot read, and it shows no message. The gates then measure a tree without that file.
  Stop for each failed copy step: write the file list first and check its status, and use `$(GATES_BACK) || exit 2`. Skip only files that do not exist, not files that the container cannot read.
- [ ] F7 minor Makefile:6-7 The copy steps and the container do not agree fully with the spec and with CI:
  - `GATES_BACK` adds `.gev-cache/` of the copy to the project folder, but it does not remove old files. Now `.gev-cache/spec/` has 2,243 files from many runs, with guard files from 01:21 to 09:44. A reviewer who reads guard results there reads results from different runs.
  - `GATES_COPY` also copies the ignored file `.gev-cache/spec-samples.jsonl` into the copy. Scenario `ci-gates-007` says the copy has only files that Git tracks or does not ignore, and the test regex does not assert this step.
  - The image sets `NODE_ENV=development`, `HOST` and `PORT`, and the CI job does not set them. Now no count is different, but the design text "as in CI" is not fully correct.
  Remove `.gev-cache/spec/` in the project before the copy back. Add the samples step to `ci-gates-007` and to its test. Remove these three values in the container command.
- [ ] F8 minor scripts/spec/gates.mjs:71 `localEnvFiles` checks only dotenv names in the root folder. When you run `node scripts/spec/gates.mjs` directly, other ignored local files can still change the counts. An example is `pinokio/ENVIRONMENT`, which `server/standalone/key-setup.js:100` reads when Pinokio manages the launcher. Also, a `.env` symlink to a file that does not exist makes `statSync` throw, and the gates stop with status 2 and no gate code. `make gates` does not copy these files, so only a direct run can have this problem. Name this in the design, or check the ignored files that the key setup reads.

Notes:
- **Gate output.** It has one error, `REVIEW-TREE`. This error is expected because the review for this round is not written yet. `MaxListenersExceededWarning` is a Node warning, not a gate warning. The trace, coverage, ledger and STE gates have 0 errors.
- **Pinned runtime.** Node 24.21.0 in `gods-eye-view:local` has the function `getTestContext`, so `missingTestContext()` returns false and no test skips. A tagged test that skips gives `TRACE-SKIPPED-TAG` (trace.mjs:67).
- **Node 24.14.0 results with the skips:**
  - `gates.test.mjs`, `ledger.test.mjs` and `ciFiles.test.mjs`: 77 pass, 28 skip, 0 fail.
  - `testGuard.test.mjs`: 2 pass, 22 skip, 0 fail.
  - The test for `coverage-gate-045` passes on Node 24.14.0 with the real `node:test` module.
- **Ledger against the CI lcov data** (`ci-artifact/lcov.info`, run 34925825877):
  - With the band and `sameAsBase` from the diff against `3ca81fb`: 0 errors and 0 stale entries.
  - With no band: 0 errors other than `LEDGER-STALE`.
  - 7 files have other counts. 2 are in their ranges (`ais-store.js`, `labelArbiter.js`). 5 are only stale, and all are in the band: `spriteOrder.js`, `trackedReadout.js`, `keySetupCore.mjs`, `flights/queries.js` (total branches ±1 each) and `flights/enrichment.js` (8 fewer not-covered lines, 4 more covered branches, 3 more total branches).
  - `key-setup.js` (51 lines, 25 branches) and `vite.config.js` agree exactly with CI.
  - The CI log of the same run gives "5 entries do not match (first: src/data/spriteOrder.js)". This agrees with my result.
  - I had only one CI lcov file, so I could not check the counts that other CI runs gave for files such as `manager.js`.
- **Ledger against the latest local lcov data.** The counts agree exactly, except for the 2 files with ranges, which are in their ranges. Because of the band, the gate line "Ledger: 0 entries do not match" does not show differences for unchanged files, so I did this check separately.
- **Ledger against the base and against `2069f91`.** The base has no ledger. Against `2069f91`, only `key-setup.js` and the new entry `vite.config.js` changed. The cause is the removed `.env` effect. The 7 history lines name `establish-spec-governance`.
- **Band and unchanged files.** I found no way that the band hides a gap in a changed file, a new file or a new gap. A new file or a renamed file has no base content, so the check uses no band for it. A gap larger than the band stops the build. A ledger entry cannot increase above the base (`LEDGER-LARGER-THAN-BASE`, `LEDGER-MORE-THAN-BASE`), and the ratchet command does not record a larger count. Thus the band does not add up across changes.
- **Other checks.**
  - The IDs, the links and the order of the tasks (tests before code for 045-047 and 065-067) are correct. The change uses no retired ID.
  - The test for `coverage-gate-047` asserts that no test run starts, and it asserts the message.
  - The test for `ci-gates-006` asserts the new AND line.
  - The proposal and the design numbers (120 of 580 files, 3,533 tests, 289 with a scenario ID, 107 files that no test loads, 25 untrue) agree with the gate output.

I did not change a file in the repository, I ran only read-only git commands, and I did not read `.env`. Each Docker run mounted the repository read-only. I wrote experiment files only in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r29/`: `cmp.mjs`, `mut.sh`, `changed.txt`, and the folders `copy/` and `noskip/`. The `git status --short` hash does not agree with `status-before-r29.md5`. I think that file uses a different command, because no command of this review wrote to the repository.
