Verdict: FAIL
- [ ] F1 minor scripts/spec/lib/specs.mjs:263 The gates do not see a requirement removal in a form that OpenSpec 1.3.1 accepts, so spec-trace-052 (openspec/specs/spec-trace/spec.md:273) does not work for it. The gate parser reads a removed requirement only from a `### Requirement:` heading (specs.mjs:10, :112–129). OpenSpec `parseRemovedNames` (dist/core/parsers/requirement-blocks.js) also reads the list form ``- `### Requirement: Play` `` and the heading form `###Requirement: Play`, with no space. The OpenSpec validator says: "REMOVED may use bullet list syntax". I ran the real `loadSpecs`, `checkArchivedChange` and `lintSpecs` on copies (`review-spec-r22/exp1/root` and `root-b`). The main spec `radio` had `Play` (`radio-001`) and `Stop` (`radio-002`). The archived change `2026-09-14-drop-play` had `## REMOVED Requirements` with one of the two forms. The main spec still had `Play`. For both forms, there were no parse errors, no archive errors and no lint errors. With the same folder as an active change, `openspec validate drop-play --strict` said "valid", and `openspec archive drop-play --yes` said "1 removed" and removed `Play` from the main spec. Thus an archived change can show a removal that the main spec does not have. This is round-21 F2(b) by a different path. No known limit names it. A RENAMED section with `- FROM:` / `- TO:` lines has the same result (`root-c`): OpenSpec renames `Play` to `Listen`, and the gates read nothing. Correction: in `checkArchivedChange`, read the removed names with the same rules as OpenSpec `parseRemovedNames`, or call `parseDeltaSpec` of the pinned OpenSpec package. Then stop the build for each removed name that the main spec still has. Add cases with the list form and the no-space form to the spec-trace-052 test in specs.test.mjs:321. If renames must also be checked, add a scenario for RENAMED pairs.
- [ ] F2 minor scripts/spec/lib/specs.mjs:38 The scenario hash and the requirement hash ignore the spaces at the start of each line (`line.trim()` at specs.mjs:38, :81 and :179). Requirement spec-trace-047 (openspec/specs/spec-trace/spec.md:188) says that the hash changes when the text of a requirement changes. The only exception it gives is empty lines. The design (design.md:83) gives no exception. I ran the real `parseSpecFile` on a copy (`review-spec-r22/exp2`). (a) The requirement `Stop` had a fence with `if silent_for(stream) >= 10:` / `    if not stream.selected:` / `        pass` / `    stop(stream)`. I added four spaces before `stop(stream)`, so the radio stops only a stream that the user did not select. (b) A nested list item `  - The user did not select the stream.` became a top-level item. In both cases the scenario hash of `radio-001` and the requirement hash did not change, and there were no errors. Thus an author can change the meaning of a requirement and change no test. `TRACE-ID-CHANGED`, `TRACE-ID-BASE-CHANGED` and `SPEC-DELTA-NOT-APPLIED` do not stop the build. The known limit `scenario-text-change` is only about a changed test. Correction: keep the spaces at the start of each line in the hash, and remove only the spaces at the end (`trimEnd`). Add a spec-trace-047 test case where only the indentation of a fence line and of a list line changes. The test must assert that the hash changes. If you keep the current rule, add an AND line to spec-trace-047 that says that the spaces at the start of a line do not change the hash, and add this limit to "Known limits and later changes".

Notes:
- **Round-21 F1, corrected:** `keepFenceLine` (specs.mjs:79–82) adds each non-empty fence line of a requirement that has no open scenario to the requirement text. The spec-trace-047 test (specs.test.mjs:237) asserts that fence text changes the hash, and it asserts the exact requirement hash.
- **Round-21 F2, corrected, except F1 above:** `checkArchivedChange` (specs.mjs:260–270) compares the name, the hash and the scenario IDs, and it stops for a heading-form removed requirement that the main spec still has. gates.mjs:211 runs `lintSpecs` on the archived requirements and orphans. The spec-trace-051, spec-trace-052 and spec-lint-022 tests assert the codes, the files, the lines and the messages.
- **Round-21 F3, corrected:** gates.mjs:210–211 reads `openspec/changes/archive/<folder>/tasks.md`. The test in gates.test.mjs:386 asserts `SPEC-LINT-NO-TASK` with that path.
- **Round-21 note, corrected:** `AGENT_VERDICT` is now `^[ \t]*Verdict:.*$`. review.test.mjs:180 asserts that output with empty lines before the verdict passes.
- **Order and tasks:** Test tasks 1.21–1.22 come before code task 1.23, and 7.22–7.23 come before 7.24.
- **Tests:** specs.test.mjs, specLint.test.mjs and review.test.mjs pass on a copy with Node 24.21.0 (59 tests).
- **Ledger:** `gaps.json` and `history.jsonl` did not change since round 21.
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected. `MaxListenersExceededWarning` is a Node warning, not a gate warning.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r22/`.
