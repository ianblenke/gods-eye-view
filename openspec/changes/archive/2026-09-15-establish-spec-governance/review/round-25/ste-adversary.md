Verdict: FAIL
- [ ] S165 major openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:87 "The ratchet command writes a new scenario hash only for an ID in the tag of a changed test." One word, one meaning. This sentence has two possible meanings, and one of them does not agree with the code. "a new scenario hash" can be a new hash for a scenario that is already in the registry. It can also be the hash of a new scenario. With the second meaning, the sentence is not correct. `updateRegistry` in `scripts/spec/lib/registry.mjs:73-74` writes the hash of each ID that is not in the registry, and it does not look for a changed test. `compareRegistryWithBase` (registry.mjs:108) also checks only the IDs of the base registry. The S163 text kept the condition "When the hash of a scenario changes", but the correction removed it. Write: "When the hash of a registered scenario changes, the ratchet command writes the new hash only if a changed test has a tag with the ID of that scenario. For a new scenario ID, the ratchet command writes the hash without this condition."
- [ ] S166 major .claude/commands/opsx/review.md:22 "If the corrections change only the proposal, the design, `tasks.md` or files in `.claude/`, do not move the change. Make the corrections and start again at step 3. For each other FAIL, move the change" Articles and nouns. This text has two possible meanings.
  - **Meaning 1:** "the corrections" are all the corrections of the two agent outputs together, so you make one decision for the round.
  - **Meaning 2:** "For each other FAIL" tells you to decide for each FAIL verdict. Then, if the STE adversary gives a FAIL with only prose corrections and the spec adversary gives a FAIL with a code correction, the step gives two instructions that cannot both be done. One tells you to keep the change in the archive and start again at step 3. The other tells you to move the change and start again at step 1.
  - **Which corrections:** The text also does not say whether "the corrections" includes the corrections of minor findings. The other path says "Correct each critical finding and each major finding. You can also correct minor findings." The in-place path says only "Make the corrections".

  Write: "9. If one or the two agents give the verdict FAIL, examine all the corrections that you will make. If these corrections change only the proposal, the design, `tasks.md` or files in `.claude/`, do not move the change. Correct each critical finding and each major finding. You can also correct minor findings. Then start again at step 3. If one or more corrections change other files, move the change to its location before the archive command:"
- [ ] S167 minor .claude/commands/opsx/review.md:22 "look at each correction" Approved words and Verbs. "look at" is a phrasal verb. I am not sure that LOOK is an approved STE verb. Write: "examine each correction" (see S166 for the full text).
- [ ] S168 minor .claude/commands/opsx/review.md:26 "The two agents can give the verdict PASS and also give minor findings." Instructions. The step starts with a description of something that is possible, not with a condition or an instruction. The condition for step 10 is clear only because step 9 handles each FAIL. Write: "10. If the two agents give the verdict PASS, do not start a new review round for their minor findings."
- [ ] S169 minor openspec/changes/archive/2026-09-14-establish-spec-governance/design.md:175 "A correction to code, tests or specs needs a new review round." One word, one meaning. The paragraph lets the author change only the proposal and the design after a PASS. But this sentence names only code, tests and specs. It does not tell what happens to a correction in `tasks.md` or in `.claude/` after a PASS. The meaning is still clear from the sentence before it. Write: "Each other correction needs a new review round."
- [ ] S170 minor openspec/changes/archive/2026-09-14-establish-spec-governance/tasks.md:292 "Correct each critical finding and each major finding, and record a decision for each minor finding." Instructions. The task gives two instructions, and you do not do the two actions at the same time. Write two tasks: "Correct each critical finding and each major finding." and "Record a decision for each minor finding in `review.md`."

Notes for the caller:
- **Gate output:** It says "STE: 0 errors, 0 warnings.", so I had no STE-PASSIVE or STE-ING warnings to examine. REVIEW-MISSING is expected and is not an STE problem. The traced test names in `gov-tests-r25.txt` are byte-for-byte the same as in `gov-tests-r24.txt`.
- **How I found the changed prose:** I compared the files with the round-24 copy in `scratchpad/review-spec-r24/repo`. Only the items in your list changed. The specs, AGENTS.md, `ste-adversary.md`, the scripts and the tests have no changes.
- **Round-24 findings:**
  - **S159:** Corrected. design.md:175 now agrees with step 10.
  - **S160:** Corrected. `spec-adversary.md:62` now says "record it as a known limit".
  - **S161:** Corrected. Step 10 now has a separate instruction for the minor findings of each agent. S168 is a remaining minor point.
  - **S162:** Corrected. design.md:85 now uses "contains" and gives the reason.
  - **S163:** The text was rewritten, but not with the given text. The new text brings in the major finding S165.
  - **S164:** Corrected. `spec-adversary.md:54` is now in the active voice.
- **Round-24 spec F1 and F2:** The new `review-attestation` sentences (proposal.md:61) and the two new tree hash sentences (design.md:179) agree with each other, with step 10 and with the code. `review.mjs:130` checks only the `Verdict: PASS` line, and it does not compare severities or finding IDs.
- **Not reported (order):** Task 9.8, where you correct, comes before task 9.9, where you save the agent output. `review.md` does it the other way: step 8 saves and step 9 corrects. This order is old and earlier rounds accepted it, so I did not make a finding. If you make the S170 change, you can also put 9.9 before the correction tasks.
- **Safety:** I did not read `.env`. I did not change any repository file, and I wrote no notes.
