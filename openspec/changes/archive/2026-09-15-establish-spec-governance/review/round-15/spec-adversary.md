Verdict: FAIL
- [ ] F1 major scripts/spec/lib/coverage.mjs:5 The round-14 F1 correction is not complete. A browser runs the code of a script element in two more cases, but the gate counts no lines for them. Case 1: an attribute with an unquoted value that starts after a space, for example `<script data-x= src=1>`. In HTML, `src=1` is the value of `data-x`, so the element has no `src` attribute. `SRC_ATTRIBUTE` (coverage.mjs:7) finds ` src=` and skips the element. Case 2: an end tag with more text after the name, for example `</script x>` or `</script/>`. A browser closes the element at this tag. `SCRIPT_ELEMENT` accepts only `</script\s*>`, so the element does not match and its code is not counted. I tested the real `inlineScriptLines` and `measureCoverage` (`review-spec-r15/exp.mjs`, `review-spec-r15/exp2.mjs`). Each case has two lines of code. Each case gave a count of 0 and `complete: true, lines {total: 0, uncovered: 0}`. A complete file needs no ledger entry, so the gates do not stop. No known limit names this case. Correction: read the attributes and the end tag in the same way as the HTML tokenizer does. A value that starts after `=` and spaces belongs to the attribute before it. The end tag `</script` can be followed by a space, `/` or `>`. Add both cases to the coverage-gate-041 or coverage-gate-013 test. Or add a known limit: the gate finds HTML code with text patterns, not with an HTML parser, and the spec adversary checks each changed HTML file.
- [ ] F2 major scripts/spec/lib/coverage.mjs:8 The round-14 F2 correction is not complete. `HANDLER_ATTRIBUTE` finds an event handler attribute only after a space. A browser also reads an attribute after `/`. Thus `<svg/onload="fetch(1); go()"></svg>` runs code, but the count is 0. `SCRIPT_URL` (coverage.mjs:9) finds only the plain text `javascript:`. A browser decodes character references in attribute values and removes tabs and line breaks from URLs. Thus `href="java&#115;cript:go()"` and `href="java<TAB>script:go()"` also run code, and each count is 0. The same applies to `<iframe srcdoc="&lt;script&gt;go()&lt;/script&gt;">`. I tested each case (`review-spec-r15/exp.mjs`). Each file was `complete: true` with `lines {total: 0}`, so the gates do not stop. No known limit names these cases. Correction: find handler attributes after a space or `/`. Decode character references and remove tabs and line breaks from attribute values before you look for `javascript:`. Count `srcdoc` attributes as code. Add these cases to the coverage-gate-042 test. Or name them in the known limit that F1 gives.
- [ ] F3 major scripts/spec/lib/coverage.mjs:11 The round-14 F3 correction finds only three forms: `from '…'`, `import('…')` and `require('…')`, with the full path on one line and the path ending with `.test.mjs`. A code file can load a test file in many other ways, and `findTestImports` gives no error. I tested the real function (`review-spec-r15/exp.mjs`) with these forms, and it found none of them:
  - a side-effect import, `import './helper.test.mjs';`
  - a dynamic import with the path on the next line, `import(\n  './helper.test.mjs'\n)`
  - a template literal
  - a query suffix, `'./helper.test.mjs?v=1'`
  - `new URL('./helper.test.mjs', import.meta.url)` followed by `import(u)`
  - `createRequire`
  - a Vite alias in `vite.config.js`
  - a `package.json` `imports` map with `import … from '#helper'`
  - `<script type="module" src="/src/helper.test.mjs">` in `index.html`

  Only `export { h } from './helper.test.mjs'` gave an error. The coverage run excludes `**/*.test.mjs` (scripts/spec/gates.mjs:71). Thus the code in the test file is not measured, and the gates do not stop. No known limit names this case. Correction: find the loaded file at run time, not in the text. For example, add a module resolve hook in the test guard that writes an error when a file in the inventory resolves a file with a name that ends with `.test.mjs`. Also stop for an HTML `src` attribute and a `package.json` `imports` or `exports` value that names a test file. Add the side-effect import and the multi-line import to the coverage-gate-043 test. Or add this case to "Known limits and later changes".
- [ ] F4 minor src/tooling/spec/coverage.test.mjs:122 The coverage-gate-043 THEN line says that the coverage gate stops the build. The AND line says that the gate shows the file and the line. The test calls only `findTestImports`. No test in src/tooling/spec/gates.test.mjs makes a check run with a code file that imports a test file. If you remove scripts/spec/gates.mjs:149, each test still passes. The coverage-gate-008 and coverage-gate-017 tests do assert the `ERROR` line in the check output (gates.test.mjs:310–322). Correction: add a code file that imports a test file to the fixture of the gates.test.mjs:310 test. Add `coverage-gate-043` to its tag, and assert `/ERROR COVERAGE-TEST-IMPORT src\/…:1/` in the check output.
- [ ] F5 minor openspec/changes/archive/2026-09-14-establish-spec-governance/proposal.md:69 The round-14 F4 correction is not complete. The known limit `uncalled-code` names only a function that no test calls. The gate also counts a file that no test loads by its physical lines (coverage.mjs:147), and it counts each event handler attribute as one line, whatever its length (coverage.mjs:74). Thus a change can add code on the current lines of a file that no test loads, or add lines inside a current handler attribute. For example, `onclick="a();\nb();\nc();\nd();"` gave a count of 1 (`review-spec-r15/exp.mjs`). No count changes. The spec adversary check in the limit covers each changed code file with a ledger entry, so this case does not pass that check. Correction: add a sentence to `uncalled-code`. For example: "The gate counts a file that no test loads by its physical lines and counts each event handler attribute as one line. Thus a change can add code on the current lines of such a file or inside a current attribute, and no count changes."

Round-14 findings:
- **Partly corrected:** F1. `<script data-src="x">` and `<script class="src=1">` are now counted, and the coverage-gate-041 test asserts this (coverage.test.mjs:108–113). The cases in the new F1 are still open.
- **Partly corrected:** F2. A handler attribute after a space and a plain `javascript:` URL now give a not-covered line, and the coverage-gate-042 test asserts this. The coverage-gate-012 WHEN line has the new text. The cases in the new F2 are still open.
- **Partly corrected:** F3. `findTestImports` exists, gates.mjs:149 calls it, and the three forms in its test give an error. The other forms (new F3) and the gate-level test (new F4) are still open.
- **Partly corrected:** F4. The `uncalled-code` limit is new, but it does not name files that no test loads (new F5).

Other checks:
- **Gate output:** the only error is `REVIEW-MISSING`, which is expected. There are no warnings.
- **Specs:** the archived delta specs have the same requirements and scenarios as `openspec/specs/`. Only the headers are different.
- **Tasks:** tasks.md 3.17–3.19 put the tests for coverage-gate-041, -042 and -043 before task 3.20, the code task.
- **Trace files:** `ids.json` and `links.json` have the three new IDs. I did not find an ID from `retired-ids.json`.
- **Ledger and history:** they did not change since round 14.

I did not change any file in the repository. My test files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r15/`.