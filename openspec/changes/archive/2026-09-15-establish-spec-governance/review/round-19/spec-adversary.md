Verdict: FAIL
- [ ] F1 major scripts/spec/lib/specs.mjs:66 The spec parser still does not find some requirements and scenarios that OpenSpec finds. The parser switches the fence state at each line that matches `FENCE` (specs.mjs:12, `^\s*(```|~~~)`). It does not check that a closing fence uses the same character as the opening fence. It does not check that the closing fence is at least as long, or that it has no text after the marker. OpenSpec 1.3.1 checks all three (`/usr/lib/openspec/dist/core/parsers/spec-structure.js:83`). Thus a line such as ```` ``` ```` inside a fence that starts with ```` ```` ````, or `~~~` inside a fence that starts with ```` ``` ````, ends the fence for the gates only. The real closing fence then starts a new fence for the gates, and the gates do not read the text after it. I ran the real `loadSpecs` and `lintSpecs` on copies (`review-spec-r19/expC`, `expE`, `expF`). Each main spec had a correct requirement with a scenario, then a four-backtick fence that contains a line ```` ``` ````, or a three-backtick fence that contains `~~~`. (a) After the fence I put `### Requirement: Stream check` with "The radio MUST stop a stream that has no audio for ten seconds." and no scenario. The gates found no spec errors and no lint errors. `openspec validate --strict` gave "Requirement must have at least one scenario". (b) After the fence I put `#### Scenario: Stop a silent stream `radio-002`` with WHEN and THEN lines. The gates found no errors and did not record `radio-002`. `openspec show` gave two scenarios for the requirement. Thus a requirement with no test, or a scenario ID with no test, passes the gates. This is the same result as round-18 F1, by a different path. The known limit `pattern-checks` names only HTML code and the code that loads a test file. No known limit names spec Markdown. Correction: use the OpenSpec fence rules in `parseSpecFile`. Record the character and the length of the opening fence. End the fence only at a line that has only the same character, repeated at least as many times. Add a spec-lint or spec-trace scenario for each of these cases, and a test in specs.test.mjs or specLint.test.mjs with a `~~~` line in a ```` ``` ```` fence and a ```` ``` ```` line in a ```` ```` ```` fence, followed by a requirement with no scenario. As an alternative, run `openspec validate --specs --strict` in the gates and stop the build for each error that it gives.

Round-18 findings:
- **F1, corrected:** `parseSpecFile` adds `SPEC-DELTA-HEADER` (specs.mjs:77–79) and `SPEC-REQUIREMENT-OUTSIDE` (specs.mjs:85–87) for main specs. `REQUIREMENT_HEADING` and `SCENARIO_HEADING` have the `i` flag (specs.mjs:5, :8). The scenarios are `spec-lint-012`, `spec-lint-013` and `spec-lint-014` (openspec/specs/spec-lint/spec.md:48–58). The tests at specLint.test.mjs:71, :86 and :94 assert the codes, the lines and the message. They also show that a delta spec gives no error, that `## requirements` in lower case is correct, and that a lower-case requirement with no scenario gives `SPEC-LINT-NO-SCENARIO`. The main spec and the delta spec are the same. In tasks.md, test tasks 1.11–1.13 come before code task 1.14. F1 of this round gives a remaining path by a different syntax.
- **F2, corrected:** change-review-019 (openspec/specs/change-review/spec.md:59) and the comment at review.mjs:45 now say "the diff files outside `openspec/changes/`".

Other checks:
- **Gate output:** The only error is `REVIEW-MISSING`, which is expected before review.md is written. There are no warnings.
- **Ledger and history:** The 6 history lines name `establish-spec-governance`. `openspec/trace/retired-ids.json` does not exist.
- **Tests:** `specLint.test.mjs` and `specs.test.mjs` pass on copies in the scratchpad (26 tests).
- **Other heading forms:** I also tried a heading with a space before `###`, a line such as ```` ```text``` ```` and a fence line with four spaces before it. OpenSpec also does not read the requirement after these lines, so the gates and OpenSpec agree, and I do not report them.

I did not change any file in the repository. My experiment files are in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r19/`. F1 uses only JS parser logic, so the Node version has no effect on it.