Verdict: FAIL
- [ ] S209 major openspec/specs/gap-ledger/spec.md:345 (delta :341) gap-ledger-013 "AND the file is not a band file with its counts in the band". Articles and nouns / one word, one meaning. This is the same problem as S196(b) and S198, in a new line. The requirement defines the band as an amount for each count, so "in the band" has two meanings. It can mean "on both sides of the entry count" or "not past the limit that stops the build". The two readings give different results. I ran `ratchetLedger` on a band file with the entry 10/50/1 (1000 branches) and the current gap 13/10/1. The line count is +3, which is in its band. The covered branches are +40, which is outside the band but is a gain. The command does not stop: it writes branches 10 and adds a `smaller` history line. With the "both sides" reading, the scenario says that the command stops. Write: "AND the file is not a band file, or its not-covered line count is above the entry count plus the band of the entry count, or a covered count is below the covered count in the entry minus the band of that count". The gap-ledger-068 WHEN part, "no count of the file is outside its band", has the same words. There both readings agree with the code, so that part is minor.
  Can wait: yes. The code, the tests, gap-ledger-065 and gap-ledger-066 agree, and the wrong reading needs a mixed case.
- [ ] S210 major openspec/specs/gap-ledger/spec.md:63 (delta :59) gap-ledger-054 "the covered count is below the entry covered count minus its band". Articles and nouns: two meanings. "its" can refer to "the covered count" (the current count) or to "the entry covered count". This is the same problem as S199. I ran the code with an entry covered count of 251 and a current count of 245. The code does not stop, because it uses the band of the entry count (6). With the other reading, the gate stops (band 5). Write: "the covered count is below the covered count in the entry minus the band of that entry count".
  Can wait: yes. gap-ledger-065, the JSDoc of `compareLedger` and the code all use the entry count, and the two readings differ only at the edge of the band.
- [ ] S211 major openspec/specs/gap-ledger/spec.md:136 (delta :132) title "Write the smaller counts of a band file and keep a loss in the band", and the test name src/tooling/spec/ledger.test.mjs:781 "writes each smaller count of a band file, and keeps a larger not-covered line count or a smaller covered count in the band". These do not agree with the code, and they have two meanings. (a) The command does not write each smaller count. With the entry 10/50/1 (1000 branches) and the current gap 10/45/1 (990 branches), the not-covered count is smaller (45). But the covered count is smaller too (950 to 945), so the command keeps 50 and writes no history line. The THEN lines of 068 say this correctly. (b) "keeps a larger not-covered line count" can mean that the ledger keeps the larger current count. The test asserts the opposite: the entry stays at 5 for a current count of 9. "keep a loss" has the same problem. Write the title: "Write the smaller line count of a band file and keep the entry counts for a loss in the band". Write the test name: "writes the smaller not-covered line count of a band file and the counts with a covered count that is not smaller, and keeps the entry counts for a loss in the band".
  Can wait: yes. The THEN lines, the JSDoc of `bandFileCounts` and the test assertions are correct. Only the title and the name are wrong.
- [ ] S212 major openspec/changes/archive/2026-09-15-establish-spec-governance/design.md:130 "For a band file, the ratchet command writes the smaller not-covered line count." This does not agree with the code for a band file with a range. I ran the code with the entry 44 (low 40) and the current count 42. `sameGap` is true, so the command keeps 44 and writes no history line. gap-ledger-068 and the JSDoc of `bandFileCounts` say "with no range". Write: "For a band file with no range, the ratchet command writes the smaller not-covered line count."
  Can wait: yes. This is design prose only, and the spec (068 "with no range", 033) is correct.
- [ ] S213 minor src/tooling/spec/gates.test.mjs:408 "uses the band of a band file in the check command and in the ratchet command". There are two problems. Articles and nouns: a file has no band, because the spec defines a band for each count. One word, one meaning: the other gates test names say "the check" (for example "stops the check for ..."), but this name says "the check command". Write: "uses the count bands of a band file in the check and in the ratchet command".
  Can wait: yes. The meaning is clear.
- [ ] S214 minor openspec/specs/coverage-gate/spec.md:250 (delta :246) "AND on a Node version with the function, the skip option of each test is false". Only the guarded tests have a skip option (`GUARDED_RUN`). The other tests have no options, so "each test" is not fully correct. Write: "AND on a Node version with the function, the skip option of each test that needs the guard is false".
  Can wait: yes. The line before gives the meaning for the other tests.
- [ ] S215 minor openspec/changes/archive/2026-09-15-establish-spec-governance/proposal.md:65 "The ratchet command keeps a smaller covered count out of the ledger." This is a phrasal verb (Verbs). Also, "The ledger keeps the old count of a smaller gap until the next ratchet run" is not fully correct. The run also keeps the entry for a smaller not-covered branch count when the covered count is smaller (see S211a). Write: "The ratchet command does not write a smaller covered count to the ledger, also for a smaller gap."
  Can wait: yes. The two sentences together give the correct behavior.
- [ ] S216 minor scripts/spec/gates.mjs:330 "The file ${name} changes the coverage of the tests." and Makefile:3 "near to CI". The S206 correction changed the Makefile and the JSDoc to "can change", but the message still says "changes". "near to" in the sense of "similar to" is not an approved meaning of "near". Write: "The file ${name} can change the coverage of the tests." and "..., which is almost the same as CI".
  Can wait: yes. These are message and comment words only.
- [ ] S217 minor openspec/specs/gap-ledger/spec.md:4 "A band file can differ from its entry by a small band, and it can have a smaller gap." Articles and nouns. The Purpose still uses "band file" before its definition, which is the first half of S202. The Ratchet rule half is corrected. Write: "A file with the base content and the entry hash can differ from its entry by a small band, and it can have a smaller gap."
  Can wait: yes. The definition is in the same spec.

Notes:
- **Round 30 findings:**
  - **S196:** corrected. With the entry 400/50/1 and the current gap 300/51/1, the ratchet command now writes 300 lines and keeps the branch count in the entry. This agrees with gap-ledger-068.
  - **S197:** corrected for 010 and 028, because the code changed. The 028 case (10/50/1 to 10/53/1, totals 1003) now writes 53 with the reason `shown by test`. For 013, the new AND line has a new problem (S209).
  - **S198:** corrected in 004. For 054, the text is better, but the pronoun "its" gives S210.
  - **S199, S200, S201, S203, S204, S205, S207:** corrected.
  - **S202:** partly corrected (see S217).
  - **S206:** corrected in the Makefile and in the JSDoc of `localEnvFiles`, but the gate message still says "changes" (see S216).
  - **S208:** corrected by the code. The Makefile now uses `sort -zu` and `comm`, and no copy step hides a failure. The ci-gates-006 text agrees now.
- **Facts checked, no finding:**
  - "32 tests" agrees with 3 + 29 `GUARDED_RUN` tests, and the coverage-gate-046 test asserts the list.
  - "3,535", "291 of 3,535", "120 of the 580", "107" and "25" agree with gate-output-r31.
  - The coverage-gate-047 AND line agrees with the message and the test.
  - Check 6 in spec-adversary.md is correct.
  - The delta specs and the main specs have the same requirement text.
  - The gate output has 0 STE warnings, so there were no STE-PASSIVE or STE-ING warnings to examine.
- **Safety:** I did not read `.env`, did not change a repository file, and ran only read-only git commands. My only scratch file is `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-ste-r31/band013.mjs`. It reproduces S209, S210, S211 and S212.
