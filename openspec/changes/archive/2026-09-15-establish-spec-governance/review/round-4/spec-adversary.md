Verdict: FAIL
- [ ] F1 major scripts/spec/lib/test-guard.mjs:220 The guard returns before `wrapChildProcess` (:238) in two cases: in a node:test runner (:220) and in a worker thread (:204). In both cases, `node:child_process` does not add the gate values. Thus the round-3 F3 path is open again, and coverage-gate-036 and coverage-gate-025 are not true. I tested this on the local Node with a copy of the guard. The copy had the correct inventory hash, `NODE_V8_COVERAGE` was the gate coverage folder and `NODE_OPTIONS` was the guard:
  - A test used the guarded `spawnSync` to start `node --require ./strip.cjs --test fake.test.mjs`. `strip.cjs` is `delete process.env.GEV_SPEC_OUT;`. `fake.test.mjs` runs `new Function` with a `sourceURL` of an inventory file. The runner process wrote no guard file and no coverage file. The test process of the runner wrote a coverage file with the fake script. It did not write a guard file. With an empty preload in place of `strip.cjs`, the same test process wrote `COVERAGE-FAKE`.
  - A worker thread started `node -e <fake>` with `env: { NODE_V8_COVERAGE: <gate coverage folder> }`. The child wrote a coverage file with the fake script and no guard file. The same call on the main thread wrote `COVERAGE-FAKE`.

  The gate counts this coverage for each file that a guard checked in another process. The `guard-results` limit says only that "A process that a test starts without `node:child_process` does not get the guard". These two processes use `node:child_process`. Wrap `node:child_process` with the gate values before the inspector check and before the worker check. Add a scenario and a test for a nested `node --test` runner with a preload that removes `GEV_SPEC_OUT`, and for a child process that a worker starts. If you do not do this, add the two paths to Known limits.
- [ ] F2 critical src/tooling/spec/testGuard.test.mjs:307 Two tags are on tests that do not assert the THEN line of their scenario:
  - `[coverage-gate-024] does nothing in a worker thread` asserts only that `installGuard` returns null in a worker. The THEN line of coverage-gate-024 is "the gate records the file as not loaded and as untrue". Also, no scenario describes the worker check at test-guard.mjs:204, and coverage-gate-027 names only `GEV_SPEC_OUT`.
  - src/tooling/spec/git.test.mjs:47 `[spec-trace-045] finds the old name of each renamed file` asserts only the map from `renamedFiles`. The THEN line is "the gate compares the tests in the file with the base file that has the old name". The test that asserts this result is gates.test.mjs:262-265, but its tag names spec-trace-032, spec-trace-036 and spec-trace-037, which is already three IDs.

  Add a scenario for the guard in a worker thread and use its ID on the test at testGuard.test.mjs:307. Put the spec-trace-045 tag on a test that runs the gate with a renamed test file and asserts the result, for example a new test made from gates.test.mjs:262-265.
- [ ] F3 minor scripts/spec/lib/ledger.mjs:133 gap-ledger-055 says that the gate stops for a ledger entry of a loaded file without a total branch count or a total function count. `sameTotals` returns true for each entry with a range (`entry.low`). Also, `coveredCount` returns null without totals (:21), so `LEDGER-LOST-COVERAGE` does not occur. I tested a copy of `ledger.mjs`: a range entry without `totals` and a run in the range gave `{"errors":[],"stale":[]}`. The same entry with branches above the high count and fewer covered branches gave only `LEDGER-STALE`. Stop for a loaded entry without totals, also for an entry with a range. Add this case to the gap-ledger-055 test (ledger.test.mjs:152).
- [ ] F4 minor scripts/spec/lib/registry.mjs:205 A test counts as changed when the base of its own file does not contain its text. So the gate accepts two changes that do not change what a test checks. I tested a copy of `registry.mjs`:
  - A test that moved without changes from `src/a.test.mjs` to `src/b.test.mjs` gave `['demo-001']`. `git diff -M` does not find a test that moves between two files.
  - One space added at the end of an assertion line gave `['demo-001']`. A new comment in the test body does the same.

  Thus an author can give an ID a new scenario text with no real change to the test. Known limits do not name this. Compare the text of the test with all base test files, not only the base of its own file. Also add a Known limits item that says that any text change in a tagged test permits a new scenario text, and that the spec adversary must check each such ID.
- [ ] F5 minor .claude/commands/opsx/review.md:22 Step 9 says "remove the specs that the archive command added". That is correct only for new capabilities. For a change with `MODIFIED` or `REMOVED` requirements, the archive command changes spec files that were already in `openspec/specs/`. If you remove those files, you lose the old specs. If you keep them, the delta goes into the specs two times. Step 9 also does not say to remove the date prefix from the folder name when you move the change back. Tell the author to put `openspec/specs/` back to its content before the archive command, for example with `git checkout -- openspec/specs` and the removal of the new capability folders. Also tell the author to use the folder name `openspec/changes/<name>`.

Round-3 findings:
- **Corrected:** F1. The ledger records `totals`. `compareCoverageEntry` stops for fewer covered branches or functions in an unchanged file (gap-ledger-054). `compareWithBase` stops when the covered count is smaller or when a total is missing (gap-ledger-048). F3 in this round is a remaining case for range entries.
- **Corrected:** F2. `checkFailedRuns` (gates.mjs:131) and coverage-gate-035 are there, and the `guard-results` limit names changes to the entries of other tests.
- **Corrected for the reported path:** F3. The guard is the first `--require` preload, and coverage-gate-036 tests `NODE_OPTIONS` and the command line. I confirmed that Node runs `NODE_OPTIONS` preloads first. F1 in this round is a new path.
- **Corrected:** F4. `testCallEnd` ends the text at the closing line, and the gate uses the old name of a renamed file. F4 in this round is the remaining path.
- **Corrected:** F5 for this change, with the problem in F5 of this round.
- **Corrected:** F6 and F7. The history lines have `[low, high]` for each metric, and the gap-ledger-047 THEN line names the sorted maps.

The two later corrections:
- **(a) runner without an inspector:** On the local Node, a `--test` runner with process isolation had no inspector and wrote no coverage file, even with `NODE_V8_COVERAGE` set. A runner with `--test-isolation=none` had an inspector. So the exception does not let the runner itself add coverage. The problem is F1: the runner does not pass the guard values to its child processes.
- **(b) worker threads:** The preload runs in the worker and does nothing, which keeps coverage-gate-024. But the worker does not wrap its own `node:child_process` (F1), and the tag on the worker test is wrong (F2).

Gate output: the only error is `REVIEW-MISSING`, which you expected. There are no warnings. `history.jsonl` has four `unstable` lines. Each line names establish-spec-governance and agrees with `gaps.json` and the proposal table. All 197 IDs in `ids.json` name this change, and no entry in `links.json` is empty.

I did not change files in the repository. I ran the experiments in `/tmp/claude-1000/-home-ianblenke-docker-gods-eye-view/bdb4f5c7-182e-45ee-82b1-3e3b1892873d/scratchpad/review-spec-r4/` with copies of `test-guard.mjs`, `ledger.mjs` and `registry.mjs` on the local Node v26.8.2, not 24.21.0. F3 and F4 use only JS logic. F1 depends on how preloads work in worker threads and in the test runner. Node 24 also has this behavior, but I did not run F1 on 24.21.0.