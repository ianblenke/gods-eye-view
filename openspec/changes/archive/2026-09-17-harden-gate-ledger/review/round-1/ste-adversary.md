```
Verdict: FAIL
- [ ] S1 major proposal.md:9 "one group of words for the tests that need the guard" One word, one meaning. This bullet uses the words that the change removes, so the reader cannot see which tests. Write: "for the tests that need the guard to count assertions".
- [ ] S2 major openspec/changes/archive/2026-09-16-establish-spec-governance/proposal.md:74 "`range-totals`" The change records three limits as closed, but `simplify-ledger` proposal:34 writes "It closes four known limits" and its design:46 writes that the new rule does not stop the comparison of the covered counts. The lines 65, 71 and 72 (`deterministic-tests`, `range-slack`, `unchanged-band`) also give the ranges and the band in the simple present. Record `range-totals` as closed, and give the other three lines the simple past.
- [ ] S3 major openspec/changes/archive/2026-09-16-establish-spec-governance/proposal.md:58 "These limits stay open after this change." Three limits below this line now start with "Closed by". Write: "These limits were open after this change. Each closed limit names the change that closed it."
- [ ] S4 major openspec/changes/archive/2026-09-16-establish-spec-governance/proposal.md:63 "The samples do not record the test files of their run." and "A later change can add the test files to each sample." The entry starts with "Closed by", but these sentences give the samples in the simple present and give a later change. Write the sentences in the simple past and remove the last sentence.
- [ ] S5 major proposal.md:3 "because it removed the ranges and the count band" For `sample-identity` and `ci-samples`, the design table and the new limit lines give the kept samples. Write: "because it removed the ranges, the count band and the kept samples".
- [ ] S6 minor openspec/changes/archive/2026-09-16-establish-spec-governance/proposal.md:63,64,73 "Closed by the change `simplify-ledger`, which removed the kept samples." Voice. The sentence has no subject and uses the passive. Write: "The change `simplify-ledger` closed this limit. It removed the kept samples."
- [ ] S7 minor proposal.md:26,27 "This change writes the proposal" and "The requirement "Ledger file" writes that" One word, one meaning. "write" is also "put data in a file". Write: "This change changes the proposal" and "The requirement "Ledger file" says that".
- [ ] S8 minor design.md:41 "The requirement changes the body of one scenario" The requirement changes nothing. Write: "This change changes the body of one scenario of the requirement".
- [ ] S9 minor design.md:33 "The change gives the second line the words of the first line." Articles and nouns. "the second line" can be line 2 of the scenario. Write: "The change gives the last **AND** line the words of the **THEN** line."
- [ ] S10 minor design.md:29 "This change corrects it." Articles and nouns. "it" can be the finding or the scenario. Write: "This change corrects the scenario."
- [ ] S11 minor src/tooling/spec/testGuard.test.mjs:442 "each test with assertion counts" One word, one meaning. This is a third group of words for the tests of the scenario. Write: "each test that needs the guard to count assertions".
```
