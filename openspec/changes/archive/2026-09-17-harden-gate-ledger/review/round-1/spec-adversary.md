```
Verdict: FAIL
- [ ] F1 major proposal.md:21 The ratchet of this change makes a ledger entry larger: `openspec/trace/gaps.json` gives `src/data/labelArbiter.js` 52 not-covered branches in place of 50 (totals 405 to 407), and two `history.jsonl` lines name this change with the reason "shown by test". The covered count stays 355, so no coverage is lost now. But `compareCoverageEntry` (scripts/spec/lib/ledger.mjs:204-208) compares only the not-covered counts for a changed file, so the next change that edits `src/data/labelArbiter.js` can lose 2 covered branches with no error. The Impact says "Gaps that this change opens: none", and no known limit names this effect of a run with counts that are not the same. Line 19 also does not list `openspec/specs/coverage-gate/spec.md` or the three files in `openspec/trace/`. Correct the Impact, or record a known limit for the banked count.
- [ ] F2 major openspec/changes/archive/2026-09-16-establish-spec-governance/proposal.md:65 The change corrects three known limits, but four more in the same list still describe mechanisms that `simplify-ledger` removed: `deterministic-tests`, `range-slack`, `unchanged-band` and `range-totals`. Lines 71 and 72 also give a smaller slack than the gate gives now: the tolerance is 8, or 4% of the total, for each loaded file with the tolerance conditions. This is the fault that the Why of this change names. Correct these four entries, or record the decision in "Known limits and later changes".
- [ ] F3 minor src/tooling/spec/testGuard.test.mjs:444 The new assertion cannot fail for the current file. `GUARDED_RUN.skip` is `missingTestContext()` (line 26), and line 405 asserts that `missingTestContext()` is equal to `missingTestContext(await import('node:test'))`. The assertion only stops a later edit of line 26. Assert the skip option that a test object receives, or accept that the line adds no check today.
- [ ] F4 minor src/tooling/spec/testGuard.test.mjs:406 The THEN line says "each test that needs the guard to count assertions", but the test finds those tests with the regular expression `^test\('([^']*)', GUARDED_RUN, ` in two files only. A test that counts assertions in another gate test file, or with an inline skip option, is not in the lists and gets no check. Add a check that ties the set of tests to the tests that need the assertion counts.
- [ ] F5 minor design.md:28 The table records S220 as closed, but the fault that S220 names is still there. `openspec/specs/gap-ledger/spec.md:61-62` has "the covered branch count or the covered function count" in one line and the singular "its covered count" in the next line. `simplify-ledger` removed the name `gap-ledger-066` only. Correct the scenario, or record S220 as open.
- [ ] F6 minor openspec/changes/archive/2026-09-16-establish-spec-governance/proposal.md:63 The new first sentence of `sample-identity` says "Closed", but the five sentences after it stay in the simple present and the last one gives work to a later change: "A later change can add the test files to each sample." The samples are not in the project. Write the entry in the simple past, and remove the later change.
- [ ] F7 minor openspec/changes/archive/2026-09-16-establish-spec-governance/review.md:18 The review record still gives `harden-gate-ledger` "a tool that makes samples from the results of a CI run". The samples are not in the project, so this change cannot do it. The change does not record this item as closed and does not name it in its known limits. Record it with the three known limits.
```
