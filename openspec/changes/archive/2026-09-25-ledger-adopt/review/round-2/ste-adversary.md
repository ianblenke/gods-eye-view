Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-25-ledger-adopt/design.md:23 "The merged tree has 423 code files with a gap" Numbers. `upstream-gates-1.out` line 3 says "Coverage: 976 files, 235 complete". So 741 code files in the merged tree have a gap, and `coverage.mjs:168` defines complete as 0 not covered in all three metrics. 423 is 301 + 121 + 1 (`LEDGER-NO-BASELINE`), the code files that the errors name (656 minus 233 test files). The conclusion "at least 423 calls" is right, but the premise is false. Write: "The errors name 423 code files with a gap that the ledger does not allow, so the command needs at least 423 calls".
- [ ] S2 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:67 "removes that stop for its file (`gap-ledger-098`). The file has other content than the base commit." Rule stated as a fact, and a verb used as a noun. The second sentence reads as a fact about the file, but `gap-ledger-098` (spec.md:171) states it as a condition. "stop" is a verb used as a noun. Write: "A valid adopt line with the mark for untrue coverage makes the gate not stop the build for its file when the content of the file is not equal to its content in the base commit (`gap-ledger-098`)."
- [ ] S3 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:141 "Allow more untraced tests than the base entry up to the adopted count" Test names, and two meanings. The round-1 correction changed the rule to "the number of untraced tests of the entry is not above the adopted count" (spec.md:145, design.md:61, code `total`). The title and the test name `ledger.test.mjs:1138` still read as "the rise above the base entry is up to the adopted count", which is the rule that the lead removed. The body is exact, so this is minor. The same title is at live spec:440, and the tag is on `ledger.test.mjs:1138`. Write the title and the test name: "Allow an entry with more untraced tests than the base entry when its number of untraced tests is not above the adopted count". Also spec.md:144 (live spec:443) "or with a higher count" is elliptical. Write: "the entry has a test name that the base entry does not have, or a test name with a higher count than in the base entry".
- [ ] S4 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:157 "names a commit that is not a merged commit" Spec against code and design. By its letter, `gap-ledger-096` (and 097 at line 162) also applies to a line that `gap-ledger-095` rejects. Take a line with `lines: -1` and a commit that is not a merged commit. 095 says "the gate shows no error for such a line", and 096 says the gate stops with `LEDGER-ADOPT-FROM`. The code agrees with 095: `adoptsOf` filters first (ledger.mjs:269-279), `checkAdopts` sees only the valid-count lines, and test `ledger.test.mjs:1038-1060` shows this. design.md:39-43 gives this order, but the spec does not. Add to 096 and 097: "AND the line has no fault that `gap-ledger-095` names". Two related points:
  - design.md:43 lists "the wrong kind, the wrong change name or a wrong count" but not "no file or no commit", which 095 names at spec.md:152.
  - "the old errors" (spec.md:155, design.md:43) has no clear referent. Write: "the gate shows the errors of the gap as if the line was not there".
- [ ] S5 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:153 "a line with a count of untraced tests that is not a whole number" One word, one meaning. 089, 092 and 094 say "the number of untraced tests". "count" names the not-covered values here. Write: "a line whose number of untraced tests is not a whole number of 0 or more gives no adopted count". The live spec has the same line at 453.
- [ ] S6 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:39 "It must also have a commit and a file that the merge commit brought." Two meanings for one phrase, and not the rule. The first sentence already says a valid line "has a file and a commit", which means the fields are there. This sentence means the commit is a merged commit and the merged commit changed the file (spec.md:157, 162). "brought" is looser than "changed since its merge base". The two bullets below state the exact rules. Write: "It must also pass the two checks below."
- [ ] S7 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:51 "The adopted count limits the entry against a later change of the ledger." Round-1 S4 fault in other words. "limits ... against" reads as "protects against", and design.md:53 says that a later edit can raise a count up to the adopted count with no waiver. Delete the sentence, because design.md:53 states the rule.
- [ ] S8 minor src/tooling/spec/gates.test.mjs:1163 "a commit or a file that the merge commit did not bring" Test names, one word for one meaning. The spec titles were changed to "a commit that is not a merged commit" (096) and "a file that the merged commit did not change" (097). The test name still says "merge commit" (another term) and "bring" (another verb). Write: "stops the check for an adopt line with a commit that is not a merged commit or a file that the merged commit did not change". Change the same string in `src/tooling/spec/testGuard.test.mjs:449`, and run the ratchet command again for `links.json`. Also `src/tooling/spec/git.test.mjs:91` "after the base and not the first parents or the older merge commits": "the base" alone is not a defined term, and "older" than what is not clear. Write: "finds the merged commits after the base commit and not the first parents or the merge commits before the base commit".
- [ ] S9 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:49 "The reviewer of the merge reads" Verbs used as nouns. Round-1 S11 is not complete. "merge" is still a noun in proposal.md:49 ("the merge", "the merge resolution"), proposal.md:51 ("a merge of the upstream branch and a merge of a local branch"), design.md:3 ("The merge is one Git merge commit") and design.md:33 ("The merge resolution"). Write:
  - proposal.md:49: "The reviewer of the merge commit reads ... A gap that a person added when that person resolved the conflicts is inside that count."
  - proposal.md:51: "a merge commit of the upstream branch and a merge commit of a local branch"
  - design.md:3: "The change makes one Git merge commit."
  - design.md:33: "A person can add a gap to such a file when that person resolves the conflicts, and the command adopts it with the rest."
- [ ] S10 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:49 "gets its whole measured gap adopted" Voice. "gets ... adopted" is a passive form, and the active form is possible. Round-1 S24 gave the active text, and the correction did not use it. Write: "The command adopts the whole measured gap of a file that upstream changed and that a person merged by hand." Same for proposal.md:51 "the gaps of that branch get valid lines". Write: "the command writes valid adopt lines for the gaps of that branch". Also in proposal.md:51 "adopt from it" has an unclear "it". Write: "adopt from the merge commit of that branch". "forbids" is not an approved word as far as I know, and I am not sure. Write: "Rule 21 of `AGENTS.md` says that a person does not do this".
- [ ] S11 minor AGENTS.md:34 "Do not use it for code of this project. The reviewer of the change compares the merged commit with the upstream remote." One word, one meaning, and undefined terms.
  - After the merge, the merged code is also code of this project, so the phrase has two meanings. The design uses "code that the project wrote" (design.md:17) and "the code of the fork" (design.md:31), and proposal.md:14 says "A person did not write that code in this project". Use one form.
  - "the change" does not say which change.
  - "the merged commit" stands next to "a merge commit" in the same rule, and `AGENTS.md` does not define the difference.
  - Write: "Do not use it for code that a person wrote in this project. The reviewer of a change that uses `adopt` compares the second parent of the merge commit with the upstream remote."
- [ ] S12 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:55 "a file with other content than the base commit" Two forms for one condition (round-1 S14 is not complete). The spec uses "the content of the file is not equal to its content in the base commit". The design still uses "other content than" at lines 12, 55 and 67. Line 84 (R1) has "its content is equal to the merged commit", which compares content with a commit. The term "the base content" is at design.md:80 and proposal.md:57, and "find if" (design.md:80, proposal.md:57) should be "find whether". Write:
  - design.md:12: "16 of these files have content that is not equal to its content in the merged commit".
  - design.md:55: "only to a file whose content is not equal to its content in the base commit".
  - design.md:84: "Adopt a file only when its content is equal to its content in the merged commit."
  - design.md:80 and proposal.md:57: "to find whether the content of a file is equal to its content in the base commit".
- [ ] S13 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:78 "Each of these runs takes about 15 minutes, and the merge commit needs them only one time." Verbs used as nouns, and the wrong subject. "runs" is a verb used as a noun, and a commit does not need a run. Write: "Each of these commands takes about 15 minutes. The change `upstream-sync` runs each command one time." Same fault in proposal.md:43 "between runs" and "records that move". Write: "V8 counts the branches of `src/data/labelArbiter.js` in a different way each time that the tests run, and the ratchet command records the new count." Also the known limit `banked-branch-count` is not in this proposal. Add the place, as other changes do: "in the archived change `2026-09-17-harden-gate-ledger`".
- [ ] S14 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:53 "A later edit in the same change can raise a count up to it with no waiver, also after an edit makes the entry smaller." Verbs used as nouns. "edit" is a noun here (also proposal.md:55, twice). Write: "Later in the same change, a person can edit the ledger and raise a count up to the adopted count with no waiver. This is also true after the person made the entry smaller." Keep the limit name `adopt-later-edit` as it is.
- [ ] S15 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:59 "An adopt line cannot keep the names, because the merge commit brings about 1800 untraced tests in 233 test files." One reason in three forms, and the reason does not support "cannot". A line is for one file, about 8 tests for each file. design.md:85 (R2) gives "a reviewer cannot read them", and proposal.md:54 gives "too long to read" for "the names of all tests" (all tests of the file, or of the merge?). Write one reason in the three places: "An adopt line does not keep the names, because the history file then holds about 1800 tests with their names, and a reviewer cannot read them." Also:
  - design.md:61 "If the sum is above it": "it" can be the entry or the adopted count, and "sum" is a second word for "the number of untraced tests of the entry" (spec.md:145). Write: "If that number is above the adopted count, the gate keeps the old error for each name that is new or that has a higher count than in the base entry."
  - design.md:85 "about 1800 names" and design.md:59 "1800 untraced tests" name one value with two words. A name can have a count above 1.
- [ ] S16 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:25 "compare the list with the Git diff of the merge commit" Text against the other prose. design.md:31 and the rule of `adopt-hand-merged-files` (proposal.md:49) use the Git diff between the merge base and the merged commit, or between the merged commit and HEAD. The Git diff of the merge commit is another diff, and it also has the changes of the fork. Write: "The reviewer can compare the list with the Git diff between the merge base and the merged commit."
- [ ] S17 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:29 "The file has a gap, which is a code file with a not-covered count above 0 or a test file with untraced tests." Articles and nouns. "which" refers to "a gap", so the text says that a gap is a file. Write: "The file has a gap: it is a code file with a not-covered count above 0, or a test file with untraced tests."
- [ ] S18 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:87 "Add the adopted lines to the base ledger in memory" One word, one meaning. The term is "adopt line" everywhere else, and the term of R5 (design.md:88) "The adopted exception" is a third name. Write for R4: "Add the adopt lines to the base ledger in memory". Write for R5: "The exception of the command `adopt` has a commit and a file list, so it is narrower."
- [ ] S19 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:3 "The gates stop the merged tree with 823 errors." Words and articles. The gates stop the build, and design.md:5 says so. A tree does not stop. Write: "The gates give 823 errors for the merged tree and stop the build." Three more points:
  - "Upstream has no scenario IDs" (also proposal.md:26 and 49) uses "upstream" as a noun with no article. Write: "The upstream project has no scenario IDs".
  - proposal.md:12 "they give no way to allow them": "them" is the files. Write: "no way to allow the gaps of these files".
  - "shapes" (proposal.md:3, design.md:5) uses "shape" with a meaning that is not the STE meaning (the form of an object). I am not sure. Consider "kinds".
- [ ] S20 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:18 "of a merge commit after the base commit" One definition in two forms. spec.md:89 and `git.mjs:46` say "between the base commit and HEAD". Use "of a merge commit between the base commit and HEAD" in both.
- [ ] S21 minor openspec/changes/archive/2026-09-25-ledger-adopt/tasks.md:24 "Mutation 4" Instructions and tasks. The numbers are out of order: 3.1 has 1, 4, 2, 3, and 5.2 (lines 76-80) has 1, 2, 4, 3 with the line "The test also compares" in the middle. 5.3 (lines 83-84) has "Mutation 2" and then a "Mutation" with no number. Renumber in order, and put each description line before the mutations. Also tasks.md:83 "write the ledger file in the code of a fault" reads as an error code. Write: "write the ledger file in the part of the code that handles a fault".

1 major (S1) and 20 minor. Only S1 makes the verdict FAIL. All paths above are relative to /home/ianblenke/docker/gev-adopt.

**Checked and found correct**
- **Error counts:** I counted 823 `ERROR` lines, 301 `LEDGER-NEW-COVERAGE-GAP` and 158 `LEDGER-LARGER-GAP` in `upstream-gates-1.out`.
  - 184 `LEDGER-NEW-UNTRACED` and 170 `LEDGER-NEW-UNTRACED-NAME` errors, which give 813 with the two counts above.
  - The other 10 are 4 `COVERAGE-FAKE`, 2 `TRACE-FAILED-TEST` for one test, and one each of `GATES-TEST-LEAK`, `LEDGER-NO-BASELINE`, `LEDGER-STALE` and `TRACE-LINKS-STALE`.
- **File counts:** the 170 name errors are in 49 test files. Then 184 + 49 = 233, 301 + 121 + 1 = 423, 423 + 233 = 656, and 656 − 16 = 640.
- **Untraced tests:** about 1800 agrees with 5069 untraced tests in `upstream-gates-1.out` minus the 3240 that round 1 read from `adopt-gates-1.out`. I did not read `adopt-gates-1.out` again.
- **Round-1 corrections that hold:**
  - "admit", "would" and "ceiling" are gone from the documents.
  - The four merged-commit conditions of 089, 095, 097 and 098 are stated as the code reads them.
  - The tasks with two instructions are split.
  - "base side" and "work branch" are now "fork".
  - "merged commit" and "merge commit" are used as two terms in most places.
- **Spec against code (I read `ledger.mjs`, `gates.mjs` and `git.mjs`):**
  - 089: the `null` counts of a test file, the origin and the date of an entry that exists, and the fact that `adopt` writes no registry or links file.
  - 090: the five faults come before any test run.
  - 092: the untrue-coverage condition (`adoptedCovers`).
  - 094: the sum of the counts of all names (`total`).
  - 022 and 040: the waived count and the adopted count are shown "each when it is above 0".
  - The `LEDGER-ADOPT-FROM` and `LEDGER-ADOPT-FILE` split (`checkAdopts`).
- **Delta and live spec:** the changed hunks of the two files are the same text for 022, 040, 089 to 092, 094 to 098.
- **Style rules:** no `SHALL`, `SHOULD` or `MAY`. The only `-ing` words are `nothing` and file paths. `openspec/ste/words.json` has `bring` and `ceiling` in `allowedIng`, and this change adds no word there. No passive verb in an instruction (S10 is a description). Each task starts with an imperative verb.
- **Tests:** the only changed `test(` lines have tags 089 to 099, which are new IDs, and `links.json` and the list in `testGuard.test.mjs` hold the new names. The tests of 021, 022, 032 and 040 keep their names.
- **Known limits:** each limit that the design names is in the proposal.

**Could not check**
- **Code run:** I ran no code, so I have no STE lint result for this commit. `adopt-gates-2.out` is from before the archive, and `adopt-gates-3.out` holds only the command line.
- **Git and `origin/main`:** I have no Git access. I could not confirm "no rename of a test on `origin/main`" against `origin/main`. I confirmed it only from the diff and from the new tags.
- **Numbers:** 61 pull requests, 1002 files, 650 code files, the 16 hand-merged files, "8 of these files" and "about 15 minutes". Also the count of 121 distinct code files: I counted the 158 errors and the 49 test files, and I relied on round 1 for the 121.
