Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:146 "an adopt line with a count that is not a whole number of 0 or more gives no adopted count" Spec against code and design. `adoptsOf` (scripts/spec/lib/ledger.mjs:269-279) and design.md:34 accept `null` for lines, branches and functions. Every adopt line of a test file has `null` there (test ledger.test.mjs:1036). `null` is not a whole number, so the literal text rejects each test-file line, and 092:125 and 094 could never apply. The scenario also omits the other conditions of `adoptsOf`: a line with no file, no commit, or an untraced count that is not a whole number (`null` is not accepted there). Write: "an adopt line with a count that is not `null` and not a whole number of 0 or more gives no adopted count. A line with no file, no commit, or a count of untraced tests that is not a whole number of 0 or more gives no adopted count". Also add to 089 (line 101): "the line has `null` for each not-covered count of a test file". The same text is in the live spec at lines 442-446.
- [ ] S2 major openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:119-126 "Allow a ledger entry that the base does not have for an adopted file" Spec against code. The code stops a new entry with untrue coverage when no valid adopt line has the mark (`adoptedCovers`, ledger.mjs:337; test ledger.test.mjs:1160-1163). The conditions of 092 do not say this. By its letter, 092 allows the entry, and 021 (line 10) then does not stop it. Design D6 (design.md:60) states the rule but cites 032, and 032 names an entry that the base has. Add to 092: "AND the entry does not record untrue coverage, or the conditions of `gap-ledger-098` are true for the file". The live spec is at lines 418-425.
- [ ] S3 major openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:160-164 "Allow untrue coverage of an adopted file" Design rule that the spec does not state. design.md:48 says the adopted rules apply only to a file with other content than the base commit. The code (`adoptedFor`, ledger.mjs:491-494) and the test (ledger.test.mjs:1159) give a base-content file no allowance for untrue coverage. 092, 093 and 094 have this condition. 098 does not, so by its letter 098 allows untrue coverage for a base-content file. Add: "AND the content of the file is not equal to its content in the base commit".
- [ ] S4 major openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:46 "A later edit to an adopted file in the same change cannot raise a count above the adopted count. Such an edit can also lower the entry, and then the adopted count has no more effect." The text states a protection under "Known limits", and the last clause reads two ways. The limit is the reverse: a later edit can raise the entry, and a hand edit of the ledger then passes, up to the adopted count with no waiver. When a later edit lowers the entry, the adopted count stays a limit (`adoptedCounts`, ledger.mjs:317-330), and a later rise can reach it again. design.md:46 "The ceiling protects the entry against a later edit of the ledger by hand" describes the same rule in other words and also states the protection. Write one rule in both places: "A later edit in the same change can raise a count up to the adopted count with no waiver. The adopted count stays a limit for the whole change, also after an edit makes the entry smaller."
- [ ] S5 major openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:44 "The origin of the entry is the name of the change that adopted it." Proposal against spec. Scenario 089 (spec.md:97-98) says an entry that the ledger already has keeps its origin and its date. Only a new entry gets the change name. Write: "The origin of a new entry is the name of the change that adopted it. An entry that the ledger already had keeps its origin."
- [ ] S6 major openspec/changes/archive/2026-09-25-ledger-adopt/design.md:5 "They stop the build with 823 errors, and the errors have four shapes. 301 ... 158 ... 184 ... 170" Numbers. The four counts add to 813, not 823 (upstream-gates-1.out has 823 `ERROR` lines). The other 10 errors have other codes: 4 COVERAGE-FAKE, 2 TRACE-FAILED-TEST, and one each of GATES-TEST-LEAK, LEDGER-NO-BASELINE, LEDGER-STALE and TRACE-LINKS-STALE. Write: "813 of them have four shapes ... The other 10 have other codes." The same applies to proposal.md:3, where the errors are listed with no sum. Also design.md:7 "the 656 files that these errors name": the four shapes name 655 files by my count (301 + 121 + 184 + 49). The 656th is `scripts/pinokio-update.mjs` (LEDGER-NO-BASELINE). Say which errors the 656 counts.
- [ ] S7 major openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:3 "158 code files with a larger gap" Numbers. 158 is the number of LEDGER-LARGER-GAP errors. The error fires once for each metric, and the errors name 121 code files (upstream-gates-1.out). Same in design.md:5 "158 code files have a larger gap than their entry". Write: "121 code files with a larger gap (158 errors, one for each metric)".
- [ ] S8 major openspec/changes/archive/2026-09-25-ledger-adopt/design.md:52 "the merge brings about 5000 untraced tests" Numbers. upstream-gates-1.out shows 5069 untraced tests in the whole merged tree. adopt-gates-1.out shows 3240 in the fork tree of this change. The merge brings about 1800 untraced tests, not 5000. proposal.md:45 "the line would have thousands of names" reads two ways. An adopt line is for one file, and 1800 tests across 233 test files do not give one line thousands of names. design.md:76 (R2, "300 kilobytes") depends on the same number. Correct the number, and write "the history file would grow by about N kilobytes" only with a measured N.
- [ ] S9 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:5 "would hide", "would be worse" Verbs. The text uses "would", which is not an allowed tense. Write: "Hundreds of waivers by hand hide the gaps under reasons that nobody reads. A weaker gate does not solve the problem." Also proposal.md:45 "would have" (see S8).
- [ ] S10 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:5 "admit" Approved words. "admit" is not an approved STE word. Write "allow", and in design.md:11 "Allow the gaps of merged code ...". In design.md:11 I am not sure about "bounded" and "exception". Consider "an exception with an exact limit that a reviewer can see".
- [ ] S11 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:149 "a commit that the merge did not bring" Verbs used as nouns. "the merge" and "a merge" are verbs used as nouns. The same appears in proposal.md:37, 41 and 43, design.md:7, 20, 34, 52, 71, 75 and 76, and in the test names git.test.mjs:91 "the older merges" and gates.test.mjs:1157 "that the merge did not bring". Write "the merge commit" and "the older merge commits".
- [ ] S12 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:3 "The text calls the second parent the merged commit." Two words for one thing. Here "the merged commit" is the second parent of the one merge commit. In spec.md:87 it is any non-first parent of any merge commit after the base. Other names for it are "the commit `from`" (design.md:32), "merged parent" (proposal.md:43), "the commit of the option" (spec.md:99, 116) and "the commit of the line" (spec.md:156). The line stores the full hash from `resolveCommit` (gates.mjs:396, 483), not the text of the option. Use "the merged commit" everywhere, and in spec.md:99 write "the full hash of the merged commit".
- [ ] S13 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:3 "Its first parent is the fork, and its second parent is the last upstream commit." A commit is not a fork, and "last" can mean "final" or "newest". Write: "Its first parent is the newest commit of the fork, and its second parent is the newest commit of the upstream branch."
- [ ] S14 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:94 "has other content than the base commit, and that commit changed the file" Two forms for one condition, and an unclear "that commit". The same fact has three forms: "other content than the base commit" (spec.md:94; design.md:24, 48; proposal.md), "the file has the base content" (spec.md:116), and "its content in the base commit" (spec.md:122, 129, 136). design.md:75 "its content is equal to the merged commit" compares content with a commit. "that commit" refers to the nearest noun, the base commit. Write: "the content of a file is not equal to its content in the base commit, and the merged commit changed the file since its merge base with the base commit".
- [ ] S15 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:95 "a code file with a not-covered count" Articles and nouns. Every code file has counts, and some are 0. The same wording is in spec.md:115 and design.md:24. Write "a code file with a not-covered count above 0".
- [ ] S16 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:13 "This covers an entry that the base does not have" One word, one meaning. In this spec "cover" means that a test runs a line. Write "This applies to". design.md:67 "each file that the adopt command did not cover" should be "did not adopt".
- [ ] S17 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:54 "It counts a name with a higher count than in the base entry for the difference." Two possible readings, and it disagrees with the spec. Write: "For a name with a higher count than in the base entry, it adds the difference to the sum." Also spec.md:141 "stops the build for each of these tests" should be "each of these test names". The code and design.md:54 give one error for each name (ledger.mjs:553-555; test ledger.test.mjs:1145). A rise of 5 tests over 3 names gives 3 errors.
- [ ] S18 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:153-158 "the gate stops the build with the code `LEDGER-ADOPT-FILE`" The conditions do not exclude a line with a commit that is not a merged commit. For that line the code gives only LEDGER-ADOPT-FROM and asks for the changed files only for a merged commit (`checkAdopts`, ledger.mjs:299-303; test ledger.test.mjs:1072). By its letter, 097 applies to that line too. Add: "AND the commit is a merged commit". In design.md:36-37, "If not," is unclear. Repeat the condition: "If the commit is not ..., the gate stops with `LEDGER-ADOPT-FROM`".
- [ ] S19 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:19 "the gate shows the file, the two counts, the waived count and the adopted count" Spec against code. The code shows the adopted count only when it is above 0 (`adoptedNote`, ledger.mjs:495). Test ledger.test.mjs:353 asserts the short message. The waived count works the same way (older text). The same applies to line 27. Write: "the waived count and the adopted count, each when it is above 0".
- [ ] S20 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:40 "The checks of the commit and the file are the only checks that a person cannot pass by a text edit." The word "only" has no bound, and "pass a check" reads two ways. The stale check of the entry against the measurement also resists a text edit. Write: "The gate checks the commit and the file of an adopt line with Git. It does not compare the counts of the line with a measurement. The known limit `adopt-by-hand` names this."
- [ ] S21 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:44 "It is a ceiling." Two words for one thing. "ceiling" (design.md:44, 46) and "adopted count" (everywhere else) name the same value. Use "adopted count", or say "limit" once. "ceiling" is not an approved STE word as far as I know.
- [ ] S22 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:105-111 "a fault is a change that is not active, no option `--from`" A fault is not a change, and "a fault is no option" is not a sentence. "a ledger file that is not there" is not a fault in the options. Write: "WHEN you run the ledger command `adopt` AND the change is not active, or the option `--from` is not there, or Git cannot find the commit, or the commit is not a merged commit, or the ledger file is not there THEN the command stops and shows the fault".
- [ ] S23 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:147 "the gate shows no error for such a line alone" The word "alone" is unclear. Write: "the gate shows no error for such a line, and the old errors show the gap". spec.md:102 "changes no entry of another file" is also unclear, because the command changes the entry of each file that it adopts. Write: "changes no entry of a file that does not have these conditions".
- [ ] S24 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:41 "takes its whole measured gap" The verb has no clear subject. Write: "The command adopts the whole measured gap of a file that upstream changed and that a person merged by hand." Also "merged by hand" (proposal.md:41) and "resolved by hand" (design.md:7, 75) are two phrases for one thing.
- [ ] S25 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:18 "For 656 files that is more than 1000 calls" I could not check the number 1000. 233 of the 656 files are test files (184 + 49), and `waive` cannot handle them (proposal.md:5). Base the count on the code files (422 by my count) with up to 3 metrics each, and give a measured number.
- [ ] S26 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:33 "The ratchet run also changes `openspec/trace/ids.json`, `openspec/trace/links.json` and `openspec/trace/history.jsonl`." The list is not complete. The diffstat (adopt-diffstat.txt) also shows `openspec/trace/gaps.json` changed. "run" is a verb used as a noun (also design.md:71 "The two runs", proposal.md:3 "A run of the gates"). Write: "When you run the ratchet command, it also changes ... and `openspec/trace/gaps.json`".
- [ ] S27 minor openspec/changes/archive/2026-09-25-ledger-adopt/tasks.md:9 "with the tags `gap-ledger-090`" There is one tag, so write "the tag". Other task issues:
  - tasks.md:14 "the base side" and tasks.md:69 and 72 "the work branch" are two names for one thing (design.md uses "the fork").
  - tasks.md:63 (4.4) "Update ... and write ...", tasks.md:102 (8.2) "Run `make lint`, and correct each STE error" and tasks.md:20 "run the named mutation, and report the test" each give two instructions that do not happen at the same time. Split them.
  - tasks.md:41 "remove the adopted count from the line rule" uses "line" in a third meaning, next to "adopt line" and "history line". Write "the rule for the lines metric".
- [ ] S28 minor src/tooling/spec/gates.test.mjs:1078 "adopts the gaps of the merged files, and the ratchet command and the check pass" Test names. The name has a second subject, and "pass" can read as a second verb of "adopts". Write: "adopts the gaps of the merged files and gives no error in the ratchet command and in the check". src/tooling/spec/ledger.test.mjs:1104 "and no more" is elliptical. Write "and not above it".

Checked and found correct:
- The delta and the live spec agree on the requirement "Adoption of merged code" and on the four changed scenarios.
- The other ten carried scenarios are all present in the delta (4 changed + 10 kept = 14).
- `SHALL`, `SHOULD` and `MAY` do not appear anywhere in the change.
- The change adds no `-ing` word except `nothing` and `ceiling`, and `openspec/ste/words.json` did not change, so there are no new `allowedIng` words.
- The change adds no passive verb in an instruction.
- The 4 changed tests (021, 022, 032, 040) keep their names, and the names match the scenario titles.
- The new test names match the links file (adopt-new-links.txt) and the names in `testGuard.test.mjs:447-449`.
- The numbers 823, 301, 184 and 170 match upstream-gates-1.out. The 25 untrue entries in the ledger are all `pre-spec` (design.md:58).
- The requirement has scenarios 089 to 099, and each has a test.

Could not check (I have no code or Git access):
- The numbers 61 pull requests, 1002 files, 650 code files, 16 hand-merged files, 640, "8 of these files", "about 30 minutes" and "300 kilobytes".
- Whether a test that exists on `origin/main` was renamed. I compared only the four changed tests with their scenario titles.
- The text of the ten carried scenarios on `origin/main`.
- The STE lint result. I read adopt-gates-1.out. It has no STE warning for this change and one REVIEW-MISSING error, as expected.

Format note: I wrote the severity as the second word (`S1 major`), without parentheses. The review gate reads the second word as the severity (scripts/spec/lib/review.mjs:15-17), and `(major)` would not match `minor` there.
