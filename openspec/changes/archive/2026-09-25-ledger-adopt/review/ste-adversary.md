Verdict: PASS

The two round-3 major findings are corrected, and I found no new major finding. I found ten minor findings in the new and changed text. I read the four change documents, the live spec, rule 21 of `AGENTS.md`, and the code diff. I read `adoptsOf` in `scripts/spec/lib/ledger.mjs` and its test, to check the text against the code.

- [ ] S1 minor AGENTS.md:34 "compares the second parent of the merge commit in that change with the upstream remote. That person checks that the upstream remote has that commit, and records the result in `review.md`." Rule 21 has three faults. Proposal.md:51 repeats the same sentences.
  - "that commit" can mean the second parent or the merge commit. The nearest noun "commit" is the merge commit, and the upstream remote cannot have that commit. One meaning is clear only from the domain.
  - "compares ... with the upstream remote" and "checks that the upstream remote has" name one check with two verbs. "compare" also has another meaning in design.md:25, where it means a list against a diff.
  - The person who merges has a duty, so the text needs "must". The present tense reads as a fact.
  - Write in AGENTS.md: "The person who merges a change that uses `adopt` must check the merge commit of that change. The upstream remote must have the second parent of that merge commit. That person must also record the result in `review.md`." One sentence with "in that change" has 26 words, which is over the limit of 25. I did not run the lint.
  - Write in proposal.md:51: "The person who merges a change that uses `adopt` must check that the upstream remote has the merged commit of that change. That person must record the result in `review.md`."
- [ ] S2 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:23 "It says that a person or an agent uses `adopt` only for code ... It also says that the person who merges the change checks the merged commit against the upstream remote" Voice and one word, one meaning. A rule is an instruction, so the text needs "must". This is the same fault as round-3 S8, fixed at line 51 but not here. The check has a third wording here ("checks ... against"). Design.md:52 has a fourth wording ("records the check of the merged commit"), and no earlier text in D3 says what this check is.
  - Write in proposal.md:23: "It says that a person or an agent must use `adopt` only for code that a merge commit brought from the upstream project. It also says that the person who merges the change must check that the upstream remote has the merged commit, and must record the result in `review.md`."
  - Write in design.md:52: "The person who merges the change reads the lines, and records in `review.md` that the upstream remote has the merged commit."
- [ ] S3 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:51 "adopt from the merge commit of that branch" "Merge commit" and "merged commit" are two terms. The option `--from` needs a merged commit, which is a parent of a merge commit (proposal.md:18). A merge commit itself gives `LEDGER-ADOPT-FROM`. The person must use the tip of the local branch. Write: "A person can merge a local branch and adopt from the tip of that branch. This tip is a merged commit." In the first sentence, write "a merged commit of the upstream branch and a merged commit of a local branch". Keep the name `adopt-own-merge-commit`.
- [ ] S4 minor openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:50 "The gate checks the commit and the file of the line with Git." This is an incomplete correction of round-3 S1. An adopt line has two commits, the head commit and the merged commit, and the gate does not check the head commit. Design.md:52 has the corrected text and this line does not. Write: "The gate checks the commit that the line names and its file with Git." Also in design.md:52, "its file" can refer to the commit. Write in both places: "The gate checks with Git the merged commit that the line names, and the file that the line names."
- [ ] S5 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:37 "the head commit, the file and the full hash of the merged commit" The correction of round-3 S1 made scenario 095 and D3 use the field `from`. No prose says that `from` holds the merged commit. The bullets in design.md:41-42 say only "the commit", and it can mean the head commit. Only the tasks (5.2) and the code show the link. Write in design.md:37: "the head commit, the file and, in the field `from`, the full hash of the merged commit". Write in design.md:41-42: "If the commit in the field `from` is not a merged commit". Write in spec.md:104, and in the live spec at line 403: "the line also has the full hash of the merged commit in the field `from`".
- [ ] S6 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:23 "tells the reviewer nothing" One word, one meaning. Round-3 S9 asked for one role name. Line 25 of the same section now says "the person who merges the change", and line 23 still says "the reviewer". Write: "tells the person who merges the change nothing".
- [ ] S7 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:25 "one decision for all the files that the merged commit changed. It records that decision as one history line for each file." The text is wider than D2, scenario 089 and scenario 091. A file that the merged commit changed and that has no gap gets no line. My round-3 S6 text caused this. Write: "The command `adopt` makes one decision for all the files that it adopts (D2). It records that decision as one history line for each of these files."
- [ ] S8 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:68 "the gate keeps the old error" The word "old" has no reference, and the round-3 review counted "old errors" as removed. The next sentence gives the error. Write: "If this number is above the adopted count, the gate stops the build for each name that is new or that has a higher count than in the base entry."
- [ ] S9 minor openspec/changes/archive/2026-09-25-ledger-adopt/design.md:12 "16 of these files differ from the same file in the merged commit" I am not sure that "differ" is an approved STE verb. An earlier review of this project (`openspec/changes/archive/2026-09-25-osh-camera-video/review/ste-adversary.md:8`) says that it is not. The change uses "is not equal to" for content everywhere else. Write: "16 of these files have content that is not equal to the content of the same file in the merged commit, because a person merged them by hand."
- [ ] S10 minor openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:144 "or a name with a higher count" The comparison is missing: "higher" than what? The same fault is in the live spec at line 443. D5 says "a higher count than in the base entry". Also "name" and "test name" are two forms for one thing. Write: "or a test name with a higher count than in the base entry".

**Round-3 findings that hold**
- **S1 (major):** Scenario 095 (delta line 152, live line 451) and D3 (design.md:39, 48) say that a line whose field `file` or `from` is not a string gives no adopted count. `adoptsOf` (`ledger.mjs:274-275`) checks exactly this, and `ledger.test.mjs:1050-1051` tests it. The text of the full hash is gone.
- **S2 (major):** Design.md:66 and proposal.md:54 now give the reason with "because", and the contradiction is gone. R2 (design.md:92) agrees with them.
- **S3 to S7, S11, S14, S15:** The corrections are in place. Scenarios 094 to 097 are the same in the delta and the live spec. Task 3.3 has Mutation 2, and task 3.5 has the corrected Mutation 3.
- **Terms:** "adopt line", "adopted count" and "types" (against the field `kind`) are consistent. "Merge commit" and "merged commit" are consistent, except in S3.
- **Other checks:** The new text has no `SHALL`, `SHOULD` or `MAY`, no passive verb in an instruction, and no new word that ends in -ing.
- **Test names:** No test name changed in round 4. The added lines in `gates.test.mjs` are inside an existing test body. The finding S13 from round 3 (the test names) stays open and is unchanged.

**Could not check**
- **Lint and code:** I ran no code and no `make lint`. I counted the words of sentences by hand, including the sentences in S1.
- **Git:** I have no Git access, so I did not compare test names with `origin/main`. I relied on the diff and on your statement that no test name changed.
- **Approved words:** I am not sure about "differ" (S9) and about "reject" in 096 and 097. "Reject" also appears in the requirement text, and earlier rounds accepted it.
- **Out of scope:** The JSDoc of `adoptsOf` (`ledger.mjs:260-261`) still says "no file or commit". It is code, so I give no finding, but it does not agree with the new scenario 095.

Paths without a prefix are relative to `/home/ianblenke/docker/gev-adopt/`. The live spec is `openspec/specs/gap-ledger/spec.md`.
