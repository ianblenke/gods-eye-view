Verdict: FAIL
- [ ] S1 major /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:152 "a line with no file, or with no full hash of a commit, gives no adopted count" Articles and nouns, and text against code. Two problems.
  - "a commit" has two meanings. An adopt line has the head commit (`commit`) and the merged commit (`from`). The new line 156 says the gate does not check the head commit. `ledger.test.mjs:1053` accepts a line with no head commit. So "no full hash of a commit" can reject a line that line 156 and the test accept.
  - The code does not check a full hash. `ledger.mjs:274-275` checks only that `file` and `from` are strings. A line with `from` = `lost` passes `adoptsOf` and gets `LEDGER-ADOPT-FROM` from `checkAdopts` (`ledger.test.mjs:1064-1077`). Under "no full hash", scenario 095 says such a line gives no error. The code gives an error.
  - Write in 095: "a line whose field `file` or field `from` is not a string gives no adopted count". If the intent is a full hash, the code and the test must check it.
  - The same text is in the live spec at `openspec/specs/gap-ledger/spec.md:451`.
  - Also design.md:39 "when it has a file and a commit": write "when its fields `file` and `from` are strings". design.md:43 "no full hash of a commit": write "a field `file` or `from` that is not a string". design.md:46 and proposal.md:50 "checks the commit and the file of a line": write "checks the commit that the line names and its file".
- [ ] S2 major /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:60 "An adopt line does not keep the names. The history file then holds about 1800 tests with their names, and a reviewer cannot read them." Two meanings. The correction of round-2 S15 dropped the word "because". The text now says that the line does not keep the names, and then that the history file holds the names. This is a contradiction. The reason for the rule is only clear from R2 (design.md:86), where the option gives the names.
  - Same fault in proposal.md:54 (`adopt-untraced-total`). Write in both places: "An adopt line does not keep the names, because the names of about 1800 untraced tests are too many for a reviewer to read."
  - Keep R2 as it is.
- [ ] S3 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:144 "the entry has a new test name, or a higher count of a test name, than the base entry" Grammar. "a new test name ... than" is not correct. Write: "the entry has a test name that the base entry does not have, or a test name with a higher count than in the base entry". Same in the live spec at line 443.
- [ ] S4 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/specs/gap-ledger/spec.md:160 "the line has no fault that `gap-ledger-095` names" One word, one meaning. Scenario 095 names no fault. "fault" also names an option or state fault in `gap-ledger-090`. The requirement text (line 89) uses "reject". Write in 096 (line 160) and 097 (line 166): "the scenario `gap-ledger-095` does not reject the line". Also line 155 "as if the line was not there" is an unreal condition. Write: "the gate shows the same errors as it shows with no such line". The live spec has the same lines at 459, 465 and 454.
- [ ] S5 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:39 "It must also pass the two checks below." Instructions and structure. Four bullets follow, and only the first two are checks. Bullets 3 and 4 give lines that the gate does not stop for. Write: "It must also pass these two checks:" with the two bullets. Then move bullets 3 and 4 into a paragraph after the list.
- [ ] S6 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:25 "one decision for the whole merge commit" One word, one meaning. The command takes the merged commit (`--from`), and it adopts only the files that the merged commit changed since the merge base. So the merge commit is the wrong term. This is round-2 S16 in part. Write: "one decision for all the files that the merged commit changed". Same fault in proposal.md:45 "runs it on the merge commit". Write: "runs it with the merged commit".
- [ ] S7 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:62 "It allows the entry when that number is at or below the adopted count." Articles and nouns. The previous sentence gives no number. The result of "adds" is the number of untraced tests of the entry in scenario 094. Write: "`compareWithBase` adds the counts of all names of the entry, and this gives the number of untraced tests of the entry. The gate allows the entry when this number is at or below the adopted count." Also design.md:86 "the gate then still stops for a sum above it": "sum" is a second word, and "it" is not clear. Write: "the gate still stops for an entry whose number of untraced tests is above the adopted count".
- [ ] S8 minor /home/ianblenke/docker/gev-adopt/AGENTS.md:34 "compares the second parent of the merge commit with the upstream remote" Two meanings. The person who merges the change also makes a merge commit, so "the merge commit" can be either. The text does not say what the comparison must show. Write: "compares the second parent of the merge commit in that change with the upstream remote, and checks that the upstream remote has that commit". Change proposal.md:51 in the same way. Also proposal.md:51 "Rule 21 of `AGENTS.md` says that a person does not do this": a rule is an instruction, not a fact. Write: "says that a person must not do this".
- [ ] S9 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:49 "The reviewer of the merge commit reads the Git diff" One word, one meaning. One role has four names.
  - design.md:46 and proposal.md:50 "the reviewer".
  - proposal.md:49 "the reviewer of the merge commit".
  - proposal.md:51 and rule 21 "the person who merges a change that uses `adopt`".
  - proposal.md:23 "an agent" against "a person" (proposal.md:51).
  - Say who does each check, or use one name. If the person who merges is the reviewer, write that once in the design. Say in design D3 that this person records the result in `review.md`.
- [ ] S10 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/proposal.md:49 "a file that upstream changed" Articles and nouns. This is round-2 S19 in part. "upstream" is a noun with no article. Write: "a file that the upstream project changed". Same in proposal.md:26 "a file that upstream did not change".
- [ ] S11 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:5 "have four kinds" One word, two meanings. This came from the change of "shapes" to "kinds". "kind" also names the field `kind` of a history line (design.md:37, design.md:43, proposal.md:19). Use another word for the errors, for example "types". I am not sure that "kind" or "type" is the approved STE word. Same in proposal.md:3 and proposal.md:10.
- [ ] S12 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:17 "code that the project wrote" One word, one meaning. Round-2 S11 said to use one form. Rule 21 and proposal.md:14 now say "a person wrote in this project". design.md:17 and design.md:31 "the code of the fork" still use two other forms. Write: "code that a person wrote in this project". Also tasks.md:41 "a file with the base content": round-2 S12 removed this term. Write: "remove the condition that the content of the file is not equal to its content in the base commit".
- [ ] S13 minor /home/ianblenke/docker/gev-adopt/src/tooling/spec/gates.test.mjs:1163 "when the merged commit did not change its file" Test names. "its" can mean the merged commit or the adopt line. Write the words of the spec titles: "stops the check for an adopt line with a commit that is not a merged commit, or with a file that the merged commit did not change". Also `ledger.test.mjs:1063` "a wrong commit or a wrong file" is a different form for the same conditions of `gap-ledger-096` and `gap-ledger-097`. Write: "separates the valid adopt lines from the lines with a commit that is not a merged commit or a file that the merged commit did not change". Change the string in `testGuard.test.mjs:449`, then run the ratchet command for `links.json`. Both tags are new in this change.
- [ ] S14 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/tasks.md:32 "One valid line has no head commit and no date." Instructions and tasks. Task 3.3 has a new test case, but no mutation kills it. The mutation "remove one condition at a time" cannot fail it. Add: "Mutation 2: require the head commit, or the date. The test must fail." Number the first mutation "Mutation 1".
- [ ] S15 minor /home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/design.md:12 "16 of these files have content that is not equal to its content in the merged commit" Grammar. "its" does not agree with "files". Write: "For 16 of these files, the content is not equal to the content of the same file in the merged commit, because a person merged them by hand." R1 (design.md:85) is correct.

Only S1 and S2 make the verdict FAIL. Each other finding is minor. Paths outside the change folder are absolute. The other paths are in `/home/ianblenke/docker/gev-adopt/openspec/changes/archive/2026-09-25-ledger-adopt/`, except the live spec at `/home/ianblenke/docker/gev-adopt/openspec/specs/gap-ledger/spec.md`.

**Round-2 corrections that hold**
- **S1 (423):** design.md:23 now says the errors name 423 code files.
- **Titles and names:** the title and test name of `gap-ledger-094` say the rule of the total. The test has a name that keeps its base count.
- **Other corrections:** the word "number" in 095 (S5), the deleted sentence of S7, the "content is not equal" form and the ordered mutations of the tasks.
- **Removed terms:** "forbids", "gets ... adopted", "merge resolution", "old errors", "ceiling" and "admit" are gone from the documents.
- **Delta and live spec:** the changed hunks of 094 to 097 are the same in both files.
- **Word list:** `openspec/ste/words.json` is not in the diff. The documents have no `SHALL`, `SHOULD` or `MAY`, no passive verb in an instruction, and no new word that ends in -ing.
- **Test names:** the changed names have tags 089 to 099, which are new IDs, so no test of `origin/main` is renamed. `links.json` and `testGuard.test.mjs` hold the new names.
- **Numbers:** 301 + 158 + 184 + 170 = 813, 813 + 10 = 823, and the 10 other errors are 4 + 2 + 1 + 1 + 1 + 1. Also 656 − 16 = 640. The numbers agree in the proposal and the design. The scenario and the test agree for the total rule of 094 (3 + 2 + 1 + 4 = 10, base 1 + 4 = 5).
- **`banked-branch-count`:** it exists in the archived change `2026-09-17-harden-gate-ledger`, and the text of proposal.md:43 agrees with it.

**Could not check**
- **Code run:** I ran no code and got no STE lint output for this round. I found the passive verbs and words that end in -ing by reading the text.
- **Git and `origin/main`:** I have no Git access, so I could not compare the test names with `origin/main`. I judged them from the new tags.
- **Numbers from measurements:** I did not read the gate output again. I did not check 233 and 49 test files, the 121 distinct code files, "about 1800 tests", "about 15 minutes", 61, 1002, 650, the 16 and 8 hand-merged files, or the 25 entries with untrue coverage. I relied on rounds 1 and 2 for these.
