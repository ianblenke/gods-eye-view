Verdict: FAIL

Findings:
- [ ] S1 (major) "double the limit" disagrees with the task: design.md:28 says "The mutations that remove the check, double the limit or skip the destroy fail the test". tasks.md:10 (B2) raises the limit to 1677721600, which is 200 times 8388608. A doubled limit is 16777216. The provider then destroys the response at message 8, and the test still passes, because the loop allows 40 messages. So the design text says the wrong thing about the mutation. Write: "The test fails with each of these mutations: remove the check, raise the limit to 1677721600, or keep the check and remove the destroy."
- [ ] S2 (major) `[osh-090]` names five tests: tasks.md:8 "`[osh-090]` fails each time". Five tests carry the tag `[osh-090]` (src/data/oshLive.test.mjs lines 2077, 2101, 2114, 2128, 2147). A reader can see another test fail and mark the task done while the edited test still passes. Write: "The test `[osh-090]` with real HTTP clients fails each time."
- [ ] S3 (major) "stops after 40 messages" is false when the provider works: proposal.md:15, design.md:24 and tasks.md:6. The loop is `while (written < 40 && !closed.includes('lagging'))`. It stops at the destroy, which is the fifth message (design.md:24 says so one sentence earlier). It stops at 40 only when the provider does not destroy. The text has two readings. Write: "The test stops when the provider destroys the response. It stops after 40 messages when the provider does not destroy it, and then the test fails."
- [ ] S4 (major) The text does not say which cause is likely, and it names the wrong proof: proposal.md:9 "So the cause is likely, and not proved. The fix does not depend on it." Also proposal.md:25 "the run of the fixed test on the CI runner is the proof", and design.md:35.
  - Paragraph 3 never names "the cause". The word "So" gives the wrong reason. A failure that does not repeat makes the cause unproved. It does not make the cause likely.
  - A green run of the fixed test shows that the fix works. It does not prove the cause.
  - The failed assertion `closed.includes('steady')` does prove one fact: the response of the client that reads had more than 8388608 unwritten bytes at a check.
  - Write for proposal.md:9: "The failed assertion proves that the response of the client that reads had more than 8388608 unwritten bytes at a check. The reason is not proved. The likely reason is the slow runner that the text above describes. The old test did not fail in the Docker image of `make gates`, on Node 26 or on one CPU core, so a local run cannot prove the reason. The fix does not need the reason."
  - Write for proposal.md:25: "`osh-buffer-test-cause-unproved`: the reason for the failure is not proved. A green CI run of the fixed test shows that the fix works. It does not prove the reason."
- [ ] S5 (minor) Tense and verb use: proposal.md:3 "failed in two jobs ... Both jobs fail one test ... The job "Node 26.x" passes". The text mixes the simple past and the simple present. "Fail" takes an object here, but in STE only the job or the test can fail. Write: "In both jobs, the test `[osh-090]` with real HTTP clients failed. The job "Node 26.x" passed."
- [ ] S6 (minor) "too" can mean "also" or "as well": proposal.md:3 "the provider destroyed the response of the client that reads too". Write: "the provider destroyed the response of the client that reads, and not only the response of the client that does not read." I am not sure that "too" is an approved STE word.
- [ ] S7 (minor) Wrong actor: proposal.md:5 "The test sends messages of 2000000 bytes to two real HTTP clients." The test emits each message on a fake upstream socket, and the provider relays it to the clients. "One client never reads" and "the client that does not read" are two phrases for one client. Write: "The test gives the upstream socket messages of 2000000 bytes. The provider relays each message to two real HTTP clients. One client does not read. The test expects that the provider destroys only the response of that client." This also removes "loses its response", a second verb for "destroys".
- [ ] S8 (minor) Paragraph 3 must be read twice: proposal.md:7.
  - "A slow runner can let it fall behind" is a phrasal verb, and "it" can mean the client or the loop.
  - "pass the limit" uses "pass" in a second meaning. The text uses "pass" for a job that succeeds in the same section.
  - "Then its own response lasts for more messages" does not say who does what.
  - "many messages" has no size. The buffers of the kernel hold some megabytes, which is a few messages.
  - Write: "On a slow runner, the client that reads can read more slowly than the test writes. Then its response has more than 8388608 unwritten bytes. The client that does not read can keep some messages in the buffers of the kernel. Then the provider destroys its response later, after more messages. The client that reads has more time to read too slowly."
- [ ] S9 (minor) "the image" has no definition: proposal.md:9. Write "the Docker image that `make gates` uses". Also use the simple past for the past runs: "did not repeat".
- [ ] S10 (minor) Two words for one thing, and a wrong tense: proposal.md:13 "That client never holds a backlog". The spec, the design (design.md:20) and the code use "unwritten bytes". The same file, line 13, has "has got that message", which is the present perfect. Write: "The test waits after each message until the client that reads receives it. That client never has more than one message of unwritten bytes at a check." The test comment at src/data/oshLive.test.mjs:2206 has the same words, "a backlog of its own".
- [ ] S11 (minor) Unclear grammar and a client that "is" a response: proposal.md:14 "The test makes the client that does not read with a corked response, `res.cork()`, on the real HTTP server." A test does not make a client here. design.md:22 has "The client that does not read is a corked response". A client is not a response. Write for proposal.md:14: "The server of the test calls `res.cork()` on the response of the client that does not read." Write for design.md:22: "D2 The response of the client that does not read is corked".
- [ ] S12 (minor) "as it does" and "the same" skip a condition: proposal.md:14 "`writableLength` grows as it does for a client that does not read". Also proposal.md:26 "The provider sees the same `writableLength`", and the comment at src/data/oshLive.test.mjs:2163.
  - A real client that does not read fills the buffers of the kernel first. Its `writableLength` grows only after that. This is the difference that the change removes on purpose.
  - Write: "grows at once, and a real client that does not read grows it only after the buffers of the kernel are full." Also use one name for the store. Proposal.md:14 says "in memory". Design.md:24 says "in the buffer of the socket". Use the second name in both.
- [ ] S13 (minor) Verbs used as nouns: design.md:3 "The test ... checks that check on real responses". The route "reads `res.writableLength` at each write". Write: "The test `[osh-090]` with real HTTP clients runs this rule on real responses. The route reads `res.writableLength` before each call of `res.write()`."
- [ ] S14 (minor) The list of checks is incomplete, and "the open socket" has two meanings: design.md:10 "Keep its three checks: the destroyed response, the events of the client that reads, and the open socket."
  - The test has one more check, `closed.includes('steady')` is false. This is the assertion that failed in CI.
  - In these documents "socket" also means the raw `net` socket (proposal.md:15) and the socket of the response (design.md:24). Here it means the upstream socket.
  - Write: "Keep its checks: the response of the client that does not read is destroyed, the response of the client that reads stays open, that client keeps its events, and the upstream socket stays open."
- [ ] S15 (minor) A reader can take a non-goal as a goal: design.md:13-14 "Change the provider code or the scenario `osh-090`." and "Test the buffers of the kernel." Write: "Do not change the provider code or the scenario `osh-090`." and "Do not test the buffers of the kernel."
- [ ] S16 (minor) D1 has no numbers and no moment: design.md:20 "holds at most one message of unwritten bytes, which is far below the limit, on any machine". "which" can refer to two nouns.
  - The wait ends when the first bytes of the message arrive. The test counts the text `event: frame`, which starts the message. So "receives that message" (and "gets that message" in proposal.md:13) says more than the test checks.
  - "At most one message" is true at the check, before the write. Just after a write it can be two messages, about 5333336 bytes.
  - Write: "The test waits with `until()` until the client that reads receives the start of each message. So at each check, that client has at most one message of unwritten bytes, about 2666700 bytes. The limit is 8388608 bytes."
- [ ] S17 (minor) The count "the fifth message" needs a fact that the text omits: design.md:24 "The provider destroys the response at the fifth message." The provider relays each message as base64 text (server/providers/osh/live.js:194). Each write is about 2666700 bytes, not 2000000. With 2000000 bytes, four writes are 8000000 bytes, which is below the limit, and a reader calculates the sixth message. The text also uses three names for one thing: "message", "write" and (in the spec) "event `frame`". Write: "The provider writes each message as one event `frame` with base64 text, about 2666700 bytes. At the fifth message the response has 10666800 bytes of unwritten data, more than 8388608, so the provider destroys it."
- [ ] S18 (minor) Unclear words in the gate section: design.md:28 and design.md:30. "The mutations ... fail the test" uses "fail" with an object (also see S1). "the lead ran them" gives a role that a later reader may not know, and no evidence. "The CI run after the push is the acceptance test" gives "test" a second meaning and does not say which push. Write: "The CI run of the push of this change is the acceptance. All jobs must pass." Write for the other sentence: "The test fails with each of these mutations, and the author ran them (task 1.3)." The same problem is in proposal.md:25, "the machine of the lead". Write "the local machine".
- [ ] S19 (minor) Task 1.1 has two instructions, and its sub-lines are descriptions, not instructions: tasks.md:3-6 "Change the test ... Keep its name." then three sentences that start with "The test ...". Write: "Change the test `[osh-090]` with real HTTP clients in `src/data/oshLive.test.mjs`, and keep its name." Sub-lines: "Make the test wait after each message until the client that reads receives it." "Cork the response of the client that does not read." "Stop the test at the destroy, or after 40 messages. Remove the import of `node:net`." The last sub-line has two actions, so give each action its own sub-line.
- [ ] S20 (minor) Task 1.2 has no expected result: tasks.md:7 "Run the file `src/data/oshLive.test.mjs` on one CPU core, six times." Add: "Each run must pass."
- [ ] S21 (minor) The mutation list is not exact: tasks.md:9-11.
  - B1 gives the file `server/providers/osh.js`. B2 gives no file. The limit is `OSH_LIVE_MAX_CLIENT_BUFFER_BYTES` in `server/providers/osh/live.js`.
  - B3 "check the bytes and do not destroy the response" is unclear. The design calls it "skip the destroy".
  - No task removes the mutations after the run. The proposal says "No code file changes".
  - Write: "B2: set `OSH_LIVE_MAX_CLIENT_BUFFER_BYTES` in `server/providers/osh/live.js` to 1677721600." "B3: keep the check of `writableLength` in `server/providers/osh.js` and remove the call of `res.destroy()`." Add a task: "Remove each mutation after its run."
- [ ] S22 (minor) "changes" is a noun or a verb: proposal.md:16 "No test name changes. No code file changes." and design.md:28 "no code file changes". Write: "The change renames no test and changes no code file. It has no spec delta."
- [ ] S23 (minor) Test comments (rank below the documents): src/data/oshLive.test.mjs:2164 "The result does not depend on the size of the buffers of the kernel or on the speed of a reader." "Reader" has a second meaning in the provider, where `reader` is the schema reader. "The result" does not say of what. Write: "The count of messages before the destroy does not depend on the buffers of the kernel or on the speed of the client that reads." Line 2163 has the condition problem of S12.

Checked:
- I read proposal.md, design.md and tasks.md in full, and the diff of `src/data/oshLive.test.mjs` with the added comments.
- I compared the prose with `server/providers/osh.js:716`, `server/providers/osh/live.js:194` (base64 relay), the scenario `osh-090` in `openspec/specs/osh/spec.md:764` and the test body at `src/data/oshLive.test.mjs:2147-2214`.
- I checked the arithmetic of the fifth message, of the doubled limit (message 8, so the test passes) and of the 1677721600 limit.
- I checked the CI job names in `.github/workflows/ci.yml` ("Spec gates", "Node 24.14.0", "Node 26.x"). They match.
- I checked `openspec/ste/words.json`. The list treats "get" as the preferred word, so I did not report it. `shall`, `should` and `may` do not occur. There is no spec delta, so there are no `MUST`, `WHEN` or `THEN` lines to check.
- I checked one word for one meaning for: client, response, unwritten bytes, destroy, message, write, socket, cork, reader, test, pass, fail.
- I checked voice and -ing words. I found no passive instruction and no non-technical -ing word.
- I checked the standing constraints. No real server, host, stream or place name appears in the prose. No test name changes: the title at line 2147 is the same as the entry in `openspec/trace/links.json`. The test files and documents use no word that breaks the `send` rule in a scanned provider file. The change edits no provider file.
- I made no network call and modified no file.
