Verdict: FAIL
- [ ] F1 major src/data/oshRepositoryHygiene.test.mjs:140 The hygiene scan reads only `https?://` addresses, so a real `ws://` or `wss://` address in any OSH code or test file passes. This change adds the `wss://` scheme at src/data/oshLive.test.mjs:346 and builds ws URLs in server/providers/osh/ids.js:201. `BARE_HOST_TOKEN` finds only `host.tld:port` in the provider files, and never in the test files. Constraints 2 and 3 are open on this scheme. Change the regex to `(?:https?|wss?)`. Add a self-check test like the bare-host one, with a real-looking `wss://` host built from parts.
- [ ] F2 major openspec/specs/osh/spec.md:424 Scenario osh-039 still says "the four files that `osh-005` discovers". The delta changes osh-005 to five files, and the changed `[osh-039]` test (src/data/oshProxy.test.mjs:1038-1055) pins five files and scans live.js. The spec text is now wrong, and the delta has no MODIFIED entry for the requirement "Query construction". The `[osh-021]` body also changed: its option allowlist now has `liveHub` (oshProxy.test.mjs:900-928). tasks.md 2.1 names only `[osh-005]`, so neither change has a task. Modify "Query construction" with every carried scenario ID and a real test diff. Add task lines for `[osh-039]` and `[osh-021]`.
- [ ] F3 major server/providers/osh/live.js:114 An oversize frame that is not JSON is untested. A real video frame is not JSON. Every oversize test frame is a valid JSON observation with a `result` (oshLive.test.mjs:475-501 and the loopback test at :1131). A mutation that runs the size check after `JSON.parse`, or only for frames with a `result`, passes every test. Video frames would then be dropped as non-JSON, and the stream would never send `unsupported`. Add oversize tests for a binary frame that is not JSON, a text frame that is not JSON, and a JSON frame with no `result`. Each must close the socket and send `unsupported`.
- [ ] F4 major server/providers/osh/live.js:67 Two timer clean-ups have no failing test. This is traced by hand. (a) Remove `entry.retryTimer` from the list cleared in `drop()`. No test leaves the last client while a retry wait of more than 2 s is pending. After the idle drop, the retry then runs `connect()` on a dropped entry. That entry is not counted by the limit of 8, and its socket never closes. A later `down` then calls `entries.delete(id)`, which can delete a new live entry of the same id. (b) Remove `clearTimeout` and the null assignment for `stableTimer` in `down()` (live.js:80-81). The reset test (oshLive.test.mjs:772-795) closes the socket at 29.999 s, after the next delay was chosen and 1 ms before the stale timer fires. A stale timer then resets the delay for a socket that stayed open for less than 30 s. Add a test for each. For (a): fail 3 times, leave, advance 60 s, and assert the socket count and `pending() === 0`. For (b): open, close after 10 s, fail again at 30 s, and assert the delay is 2 s.
- [ ] F5 critical server/providers/osh/live.js:210 `hub.close()` has no scenario and no asserting test. Production never calls it: server/providers/osh.js has no caller. Only `t.after(() => hub.close())` in oshLive.test.mjs and oshProxy.test.mjs:257 calls it. Its coverage comes from cleanup hooks. A mutation `close() {}` leaves every assertion true. Either remove it and clean up in the tests through `leave()`, or wire it to the server close event. If you keep it, add a scenario and a test that asserts it ends each client, clears each timer and closes each socket.
- [ ] F6 major src/layers/osh/index.js:232 The layer opens up to 8 `EventSource` connections. The app is served over HTTP/1.1 (no `https` or `http2` setting in server/standalone/vite.config.js), where a browser allows about 6 connections to one origin. A selected system with 6 or more datastreams fills the pool. The poll's `getObservation` fetches (index.js:282) then wait behind streams that never end, and so do the fetches of every other same-origin `/api` layer. `_shownStates` (index.js:293) is set only after the poll loop, so the detail may never draw. The fake `EventSource` in the tests cannot show this, and the known limits do not name it. Cap the browser streams at 3 or fewer, or carry all datastreams of a selection in one stream. If the owner accepts it, name it in the known limits with the number.
- [ ] F7 minor server/providers/osh/live.js:75 The requirement text ("MUST send no frame to the server") and osh-065 ("zero frames") have no bounded exception. `socket.close()` (live.js:75, reached from :167 and :110) sends a Close frame. The loopback test asserts `[8]` at oshLive.test.mjs:1128, and the known limit `osh-live-one-way` does not say it. Constraint 1 asks that the provider never write to the socket. Say "no data frame; the Close frame is the only frame" in the requirement, in osh-065, and in `osh-live-one-way`.
- [ ] F8 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:4 The requirement "GET only" still says "through one function". Sockets now open through `oshOpenStream()`, so there are two functions. The text stayed the same on purpose, to avoid a rehash of the scenarios. Name this in the known limits, or reword the text in a later change.
- [ ] F9 minor server/providers/osh.js:67 Some behavior has no scenario line. The response head (`text/event-stream`, `no-store`, `X-Accel-Buffering`) is asserted at oshLive.test.mjs:609. The reset of the delay after 30 s open (live.js:97) has a test at :772. The `res.destroyed` early return (osh.js:691) has a test at :625. In the browser source, `error` maps to down and the connection `open` without data is ignored (source.js:104-109). Add AND lines to osh-065, osh-069 and osh-071. These requirements are new in this change, so free to edit.
- [ ] F10 minor src/data/oshProxy.test.mjs:190 The `send` scan `/\bsend\s*\(/` misses `socket.send?.(x)` and `socket['send'](x)`. The `[osh-065]` route test asserts `socket.sent` equals `[]` (oshLive.test.mjs:350) with a socket that never opens. Only the loopback test proves "no frame", and only on the listen path. `[osh-064]` at oshLive.test.mjs:323 is a source-text scan tagged to a behavior scenario. Extend the scan to `\bsend\b`. Keep the loopback test as the runtime proof.
- [ ] F11 minor server/providers/osh/live.js:114 Three limits are not named. (1) The frame is fully received before the size check, so a large frame is held in memory first. (2) `refusedUntil` (live.js:54) is never pruned. (3) The result of `res.write` (osh.js:700) is ignored, so a stalled client can grow memory. Name them in the known limits.
- [ ] F12 minor server/providers/osh/live.js:181 A 503 (`live_busy`, `live_unsupported`, `no_key`) makes an `EventSource` fail for good. The layer only gets `error`, which it treats as down, and it never retries (source.js:109). One selection of 8 streams fills the hub, and its entries stay 2 s after the client leaves. A second selection in that time gets `live_busy` for every stream and polls. Name this in the known limits.
- [ ] F13 minor src/layers/osh/source.js:100 `typeof observation === 'object'` accepts a JSON array as an observation. The test at source.test.mjs:293 has no `[]` case. Reject arrays and add the case.
- [ ] F14 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:3 The prose has -ing nouns and adjectives that config.yaml forbids: "moving" (design.md:3), "polling" (:12), "Buffering" (:28), "sending" (:48), "growing" (tasks.md:55). The gate shows only the total of 309 STE warnings, so I cannot tell how many come from this change. Check the count against main.

Notes:
- Tree read: branch osh-live-observations at 20de3374c89000425e3c20a8329cc3712c191f24, plus the working tree. This session had no shell tool. I ran no test and no mutation, and I did not read `git diff`. F3 and F4 are traced by hand and need a mutation run to confirm.
- Constraint 1: no `send` call exists, and the handshake is a GET. The Close frame is F7.
- Constraint 2: I found no non-fixture id and no real host in the changed files.
- Constraint 3: F1 covers the ws/wss gap. Loopback servers bind 127.0.0.1.
- Constraint 4: names of `[osh-005]`, `[osh-021]`, `[osh-039]` and `[osh-034]` agree with links.json. I could not compare them with main.
- Constraint 5: no credential reaches an event, a response header or the `EventSource` URL.
- REVIEW-MISSING is the only gate error, and it is expected before this round.
