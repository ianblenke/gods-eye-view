Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:52 "the provider sends zero frames" Two meanings, and the text does not agree with the code. `oshLive.test.mjs:1128` and `:1144` assert that the client opcodes are `[8]` after the socket closes. `socket.close()` in `live.js` (`drop`, `refuse`) writes one close frame, and the runtime answers a ping with a pong. So "zero frames", "sends no frame" and "sends nothing" are false. The same text is at spec.md:19 and :30, proposal.md:12, design.md:13 and :38, tasks.md:26, and in the test name `oshLive.test.mjs:1083` "sees a GET handshake and no frame". Standing constraint 1 holds for `send`, because no file calls it, but the provider does write a control frame. Write: "the provider sends no message frame, and the only frame it sends is the close frame".
- [ ] S2 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:44 "encoded as the URL rules say" Two meanings. The URL rules allow `/` and `+` in a query. The code uses the form serializer, and the tests pin `application%2Fom%2Bjson` (`oshIds.test.mjs:313`, `oshLive.test.mjs:346` and `:1119`). Write: "with the value written as `application%2Fom%2Bjson`".
- [ ] S3 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:69 "eight upstream sockets are open" The text does not agree with the code. `live.js:183-185` counts entries. A datastream holds its place from `join` until `drop`, also while its socket connects, waits to retry, or waits the two idle seconds. `oshLive.test.mjs:638` reaches `live_busy` with sockets that never emit `open`. "a new client for another datastream" does not say "another than what". Write: "WHEN the provider serves eight datastreams, or sixteen clients listen to one datastream, THEN a client for a ninth datastream, or a seventeenth client for one datastream, gets `503`".
- [ ] S4 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:77 "after a delay of 1, 2, 4, 8, 16 and then 30 seconds" Two meanings. The list can end after 30 seconds, or 30 seconds can repeat. The code repeats it (`live.js:89`) and `oshLive.test.mjs:756` tests 30, 30, 30. The reset after 30 seconds open (`live.js:97-101`, design.md:56, test at :772, mutation V19) is not in the spec. Write: "after 1, 2, 4, 8 and 16 seconds, and then after each 30 seconds. A socket that stays open for 30 seconds resets the delay to 1 second".
- [ ] S5 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:90 "MUST open a live stream for each datastream of the selected system" The requirement does not agree with `osh-072` at :95 ("at most eight") or with `index.js:14` and `:232`. proposal.md:16 has the same omission. Write: "MUST open a live stream for each of at most eight datastreams of the selected system".
- [ ] S6 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:106 "as `osh-022` says" Wrong reference. `osh-022` is the observation route. The 15-second poll interval is in `osh-030`. Write: "as `osh-030` says".
- [ ] S7 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:4 "through one function" The requirement text does not agree with the code or with the changed scenario. `get.js` now has two functions that open an upstream connection: `oshGet()` (`get.js:55`) and `oshOpenStream()` (`get.js:122`). The `osh-005` title ("Keep one upstream call site") and its steps count files, not functions. proposal.md:18 keeps the requirement text. Write: "through one file". This forces the requirement rehash. `osh-004` and `osh-006` then need a task line and a test diff.
- [ ] S8 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:95 "opens one live stream" Two meanings for "open". `osh-072` uses it for "create the stream". `osh-073` and `osh-074` (:102, :105, :107) use "is open" for "the event `open` arrived, and no `down` or `unsupported` came since" (`index.js:235-244`, `state.open`). Read with the first meaning, `osh-074` never polls a stream that the layer opened. proposal.md:16 and design.md:66-68 have the same clash. The event name `open` is a third use. Write: "the layer starts one live stream" for the create step, and add "A stream is open from the event `open` until the event `down` or `unsupported`".
- [ ] S9 major openspec/changes/archive/2026-09-24-osh-live-observations/design.md:79 "The event `down` and the poll cover it" The text does not agree with the code. `live.js` has no idle timer on the upstream socket. `down` comes only after a close or an error (`live.js:152-159`). `index.js:280` skips the poll while the stream is open and the layer holds an observation. A silent upstream socket leaves the last observation in the detail, with no poll and no `down`. Write: state the limit as it is, and add a limit name such as `osh-live-silent-upstream` to proposal.md "Known limits".
- [ ] S10 major openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:3 "a position that is up to 15 seconds old" The proposal and design.md:3 ("up to 30 seconds old") disagree. The poll interval is 15 s (`index.js:10`) and the observation route cache is 15 s (`observations.js:7`). Write: "up to 30 seconds old".
- [ ] S11 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:75 "after each open ... after each close or failure" Verbs. "open" and "close" are used as nouns. The same misuse is at design.md:54, tasks.md:30 ("the reset after 30 seconds open"), and in the test names `oshLive.test.mjs:685`, `:625` ("before the join") and `oshLayer.test.mjs:2888` ("a live move"). Write: "after each time the upstream socket opens" and "after each time it closes or fails"; "the delay resets when a socket stays open for 30 seconds"; "while the route reads the schema"; "a live observation moves the entity across the horizon".
- [ ] S12 minor openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:40 "The reader of a later client is not used." Voice. Write: "The hub does not use the reader of a later client". The same problem is in spec.md:94 "a system is selected and its datastreams are known" (write: "the user selects a system and the layer reads its datastreams"), tasks.md:72 "is polled" (write: "the layer polls it"), tasks.md:20 "are fixed", test name `oshLive.test.mjs:816` "is not opened again" (write: "the hub does not open a new socket after a socket closes while no client listens"), and design.md:78-81 "Accepted." four times (write: "We accept this risk.").
- [ ] S13 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:12 "polling as the fallback" Words that end in -ing. None of these is a technical name, and none is in `allowedIng`: "polling" (design.md:12, :64, :85; tasks.md:74, :86), "growing" (tasks.md:55), "moving" (design.md:3), "existing" (design.md:7), "sending" (design.md:48). "fallback" is also not an STE word. Write: "the layer polls a datastream whose stream is not open", "the delays of `osh-069`", "A system that moves", "Three rules that exist", "when the hub sends the event". Do not add these words to `allowedIng`.
- [ ] S14 minor openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:14 "bounds their number" Approved words. "bound" as a verb is not approved (also design.md:15 and the spec.md:68 title). Write: "limits". Other words that I believe are not approved: "oversize" (tasks.md:27 and :49; test names `oshLive.test.mjs:503` and `:525`), which is a second term for "more than 65536 bytes" and "too large"; write "too large". "reconnect" (spec.md:73 title): write "open a new upstream socket". "gains" (design.md:64): write "has". "Extend the layer" (proposal.md:16): write "Change the layer". "also when" (spec.md:45): write "and when the root has them". I am not sure about "relay" (proposal.md:7, :14; design.md:5). Keep it only if you treat it as a technical verb.
- [ ] S15 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:57 "gives no event" One word, several meanings. "give" means "produce" here, "provide" in design.md:26, and "pass" in design.md:42 ("The hub gives the constructor as an injected option"). It also appears in the test names `oshLive.test.mjs:374` ("gives the headers"), `:475` ("gives the event unsupported") and `:832`. Write: "produces no event", "The hub takes the constructor as an option", "passes the headers to the constructor", "sends the event `unsupported`".
- [ ] S16 minor openspec/changes/archive/2026-09-24-osh-live-observations/tasks.md:24 "each frame that the client sends" One word, two meanings. Here "client" is the provider's WebSocket. Everywhere else it is a browser connection to the live route. Write: "each frame that the provider sends". In spec.md:57, "the stream stays open" can mean the upstream socket or the client responses (`oshLive.test.mjs:428` checks both). Write: "the upstream socket and the client responses stay open".
- [ ] S17 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:46 "throws for another host, path, query, user name, password or fragment" The list is shorter than the code. `assertLiveUrl()` also checks the scheme and the port (`ids.js:223-224`), and `oshIds.test.mjs:326` tests both. tasks.md:21 has the same short list. Write: "another scheme, host, port, path, query, user name, password or fragment".
- [ ] S18 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:87 "it gives the observation, open, down and unsupported events to its callbacks" The spec is silent on two behaviors that tests pin. The `error` event of the `EventSource` calls the down callback (`source.js:109`; `source.test.mjs:319`). The `open` event of the connection, with no data, gives nothing (`source.js:104-106`; `source.test.mjs:310`). Write: "AND the `error` event of the `EventSource` also gives `down`, and the `open` event of the connection gives nothing".
- [ ] S19 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:69 "logs a fixed text with the error code" The code logs the close code of the socket, `code N`, or 0 when there is none (`live.js:153`). When the constructor throws, it logs a fixed text with no code (`live.js:143`). Write: "with the close code of the socket, when there is one".
- [ ] S20 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:36 "The limits are eight upstream sockets and sixteen clients for each datastream." Articles and nouns. "for each datastream" can bind both limits. proposal.md:29 has the same sentence. Write: "At most eight upstream sockets in all, and at most sixteen clients for each datastream".
- [ ] S21 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:32 "A client joins an entry, and it leaves when its connection closes." "it" can refer to the client or to the entry. Write: "and the client leaves when its connection closes". In design.md:68, "the stream then keeps it current" has the same problem; write "keeps that observation current". In design.md:40, "It takes the constructor" does not say which one; write "the WebSocket constructor".
- [ ] S22 minor openspec/changes/archive/2026-09-24-osh-live-observations/tasks.md:37 "Run each mutation below on the server. Report one line for each mutation" Instructions. Each of these tasks gives two or more instructions in sequence: 2.1, 2.4b, 2.9, 3.3 and 3.5, and the sub-lines at :61 and :90 ("Stop. Add a test ..., and then continue."). The "Report" has no destination. Task 1.4 says a mutation ledger sits under each test task, but tasks.md holds no result line. Write: one instruction for each task, and "Write one line for each mutation in this file: the id and the test that failed".
- [ ] S23 minor src/data/oshLive.test.mjs:323 "the fixed pair from ids.js" Test names. Some do not name the object. Write: ":323 'liveUrl() and assertLiveUrl() from ids.js'". Write: ":332 'with binaryType set to arraybuffer'" (the spec says `binaryType`, not "the arraybuffer type"). Write: ":638 'a client for a ninth datastream gets live_busy, and the hub opens no socket'" (a datastream cannot get an answer, and the client does not open the socket). In src/data/oshLayer.test.mjs:2908, "and the detail shows it" can refer to the entity or the observation; write "shows the observation". In :3016, "does not draw the datastreams of the old one" is unclear. The test proves that a live observation that comes before the first poll of a new selection does not redraw the old detail. Write that.
- [ ] S24 minor src/data/oshRepositoryHygiene.test.mjs:140 "https?:\/\/" Standing constraint 3. `assertOnlyFixtureAddress` matches only `http` and `https` addresses. This change adds `ws` and `wss` addresses, so a real `wss://` host in a test file passes the scan. It is not a violation today, because the only such address is `osh.example`. Write: `/(?:https?|wss?):\/\/[^\s'"`)]+/g`.
- [ ] S25 minor openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:14 "sends a heartbeat" Two possible meanings. Line 12 says "the provider sends no frame". The heartbeat goes to the browser clients, not to the upstream server. Write: "sends a heartbeat comment to each client". Also, in proposal.md:3 "The detail changes in steps" is unclear (write: "The detail changes only at each poll"). In proposal.md:5, "this held the change for a day" is unclear (write: "this delayed the change by one day").

Note: this session had no shell. I did not run `git diff` or `node --test`, and I did not run mutations. I read the change files, the code files and the changed tests directly. Base-versus-delta comparison of `osh-004` to `osh-006` uses the 2026-09-18 archive text. The other constraints hold: 2 (synthetic fixtures), 4 (existing names match the trace) and 5 (credentials).
