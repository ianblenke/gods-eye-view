Verdict: FAIL
- [ ] S26 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:89 "a frame that is not JSON or has no `result` gives no event, and the upstream socket and the client responses stay open" Two meanings, and line 89 disagrees with line 90. A frame of more than 65536 bytes that is not JSON matches both lines. Line 89 says the socket stays open, and line 90 says it closes. design.md:54 bounds the rule ("of 65536 bytes or less"), but the spec does not. The same gap is at openspec/specs/osh/spec.md:621, in tasks.md:31 ("A frame that is not JSON, or has no `result`, gives no event") and in the test name oshLive.test.mjs:428. Write: "AND a frame of 65536 bytes or less that is not JSON, or has no `result`, gives no event" and "AND the upstream socket and the client responses stay open after that frame". Only the scenario osh-066 changes. Edit both spec copies in the same way.
- [ ] S27 major openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:83 "its only frame is the close frame of a socket that it closes" The text does not agree with the runtime. This is round-1 S1, and the correction keeps the "only" that the round-1 skeptic rejected. The skeptic ran a loopback server on Node v26.8.2. The client sent opcode 10 (pong) after a server ping, and opcode 8 after `close()`. The fixture in oshLive.test.mjs never sends a ping, so the `[8]` assertions at :1238 and :1254 cannot prove "only". The same claim is at openspec/specs/osh/spec.md:615, design.md:46 ("That is the only frame that the provider sends"), proposal.md:39 and the comments in get.js and live.js. In design.md:46, "the runtime sends the close frame" and "the only frame that the provider sends" also disagree, because a pong frame is a runtime frame too. Write: "the provider sends no message frame, and the runtime sends only control frames: the close frame when the provider closes a socket, and a pong frame for each ping of the server".
- [ ] S28 major openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:46 "A second selection within two seconds of a full hub gets `live_busy` for each stream" The text does not agree with the limit of three streams, and it has two meanings. The condition comes from the text for eight streams. Now one selection takes at most three of the eight places, so a second selection cannot meet a full hub with that text. "within two seconds of a full hub" has no clear meaning. "fail for good" is an idiom. Write: "an answer `503` makes the `EventSource` stop, and it does not try again. The layer treats it as `down`, polls the datastream, and starts no new stream for the same selection. When eight datastreams hold a place, each new datastream gets `live_busy`. A datastream keeps its place for two seconds after its last client leaves, so quick selections can fill the hub."
- [ ] S29 minor openspec/changes/archive/2026-09-24-osh-live-observations/tasks.md:35 "The delays 1, 2, 4, 8, 16 and 30 seconds, and the reset after 30 seconds open" Round-1 S4 and S11 are not applied here. The list can end at 30, but the code repeats 30 seconds. "30 seconds open" uses "open" as a noun. The same phrase is in the V19 line (tasks.md:63) and in the test name oshLive.test.mjs:839 "a socket that closes before 30 seconds open". Write: "The delays 1, 2, 4, 8 and 16 seconds, then 30 seconds each time, and the reset when a socket stays open for 30 seconds". V19: "do not reset the delay when a socket stays open for 30 seconds". Test name: "a socket that closes less than 30 seconds after it opens does not reset the delay later".
- [ ] S30 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:112 "opens a new socket after 1, 2, 4, 8 and 16 seconds, and then every 30 seconds" "the delay" at line 113 has no antecedent. "every 30 seconds" can read as a timer that fires with no failure. "after" does not say after what. The round-1 skeptic said the same about my S4 fix. The same text is at openspec/specs/osh/spec.md:644-645. Write: "AND while a client listens, the delay before each new socket is 1, 2, 4, 8 and 16 seconds, and then 30 seconds each time" and "AND a socket that stays open for 30 seconds resets that delay to 1 second".
- [ ] S31 minor openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:31 "eight upstream sockets in all" One counted unit has two names. The spec counts datastreams (osh-068). design.md:36 says a datastream holds its place while "its socket ... waits to try again", and then no socket exists. The same text is at design.md:36, design.md:84 and tasks.md:60 ("the limit of eight sockets"). In spec.md:105-106, "a datastream counts" has no object. Write: "eight datastreams in all", and "a datastream counts toward the limit of eight from its first client until two seconds after its last client leaves".
- [ ] S32 minor openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:44 "The event `down` comes only after a close or an error." Verbs. "close" is a noun here (round-1 S11). The same text is at design.md:85. "silent" (twice) is not, I think, an approved word. I am not sure. "A socket ... leaves the last observation in the detail" gives the socket an action that it does not do. Write: "The event `down` comes only when the socket closes or fails", "a socket that sends no data for a long time", "So the detail keeps the last observation, with the age that the hub sent".
- [ ] S33 minor openspec/changes/archive/2026-09-24-osh-live-observations/proposal.md:45 "the result of a write to a client" Verbs. "write" is a noun. Write: "The provider does not check the result of `write()` for a client". "shrinks" is an approved word only if I remember correctly. I am not sure. Write: "The map of refused datastreams keeps every entry".
- [ ] S34 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:38 "A restart of the server in the same process then leaves no timer and no socket" Verbs. "restart" is a noun. "as the AIS provider does" uses a pro-verb. "The hub closes with the HTTP server" is unclear. Write: "The hub closes when the HTTP server closes, and the AIS provider does the same. When the server starts again in the same process, no timer and no socket of the old server stay."
- [ ] S35 minor openspec/changes/archive/2026-09-24-osh-live-observations/design.md:68 "keeps the poll of today" "of today" refers to a date. After the merge it has no meaning. The same text is at design.md:92 and tasks.md:93. Write: "keeps the poll that the layer runs without this change". In design.md:70, "A browser keeps about six connections" is unclear. Write: "A browser allows about six connections".
- [ ] S36 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:127 "the hub opens no new upstream socket after that" Two words for one thing. This is the only scenario that says "the hub". The other scenarios say "the provider", and osh-075 uses both. "each upstream socket that is open" is narrower than the code. `drop()` also closes a socket that still connects. "after that" is unclear. Write: "AND the provider closes each upstream socket that it holds" and "AND no timer of the provider opens a new upstream socket after that". The same text is at openspec/specs/osh/spec.md:659-663.
- [ ] S37 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:10 "`oshOpenStream()` passes the URL and an option object with only `headers`" The WHEN clause of osh-004 covers only the three routes. It does not say that the provider opens a WebSocket. proposal.md:19 says the change "names the WebSocket", and the line names `oshOpenStream()` only. Write in the WHEN: ", and the provider opens an upstream WebSocket". Write in proposal.md:19: "Change the scenario `osh-004` to check the option object of `oshOpenStream()`".
- [ ] S38 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:124 "the `open` event of the connection calls no callback" Two events have the name `open`. The line before uses "the event `open`" for the route. Write: "the event `open` that the browser raises with no data calls no callback".
- [ ] S39 minor src/data/oshLayer.test.mjs:2660 "the layer opens one live stream for each of the first three datastreams" The name is in the diff. It still uses "opens" and "open" (round-1 S8 item 7 is not applied). The spec says "starts" and "closes every stream". The same wording is at :2682, :2700, :2735, :2757 and :2775. Write: "the layer starts one live stream for each of the first three datastreams of the selected system", "a new selection closes every live stream and starts the streams of the new system", "a click on empty space closes every live stream". Also :3016 "does not redraw the old detail": "redraw" is not an approved word. Write: "does not change the detail of the old selection".
- [ ] S40 minor src/data/oshLive.test.mjs:820 "a retry that waits when the last client leaves is cleared with the entry" Voice, and verbs. "is cleared" is passive. "retry" is a noun. The spec says "waits to try again". Write: "the hub clears the wait for a new socket when the entry drops after the last client leaves". Also src/data/oshGet.test.mjs:35 "opens one WebSocket from the URL": the test builds two sockets and does not name the function. Write: "oshOpenStream() gives the WebSocket constructor the URL and an option object that holds only headers, so the handshake is a GET with no body".
- [ ] S41 minor openspec/changes/archive/2026-09-24-osh-live-observations/specs/osh/spec.md:90 "whatever the frame holds" I am not sure that "whatever" is an approved word. The same text is at design.md:52, tasks.md:32 and the test name src/data/oshLive.test.mjs:503. Write: "with any content".
- [ ] S42 minor openspec/changes/archive/2026-09-24-osh-live-observations/tasks.md:38 "Make the address scan ... read `ws` and `wss` addresses, and add a test for it" Instructions. Each of tasks 2.4c (:38), 2.8b (:43) and 2.9b (:78) gives two instructions in sequence. This is round-1 S22 in new lines. Split each into two tasks.

Note: this session had no shell, so I did not run a test or a mutation. S27 uses the loopback run of the round-1 skeptic. The five standing constraints hold for the diff: no `send` call, and the hygiene test builds its fake hosts from parts. Every renamed test is new in this change, and no credential reaches the browser. The carried scenarios of "Query construction" are the same as in `main`, except osh-039. The lint reports no -ing word and no `SHALL`, `SHOULD` or `MAY`.
