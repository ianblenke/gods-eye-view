Round-2 findings: F1 closed. F2 partly closed (see F2 below). F3 closed as name-based; the test is now a runtime Proxy on the option bag, residual in F3 below. F4 closed. F5 closed, residual in F6 below. F6 not closed and worse (see F4 below). All five STE majors closed. The shortened `osh-005` test name still agrees with its scenario on every line except the adapter count (F1).

```
Verdict: PASS

- [ ] F1 minor specs/osh/spec.md:31 The osh-005 WHEN says "the three OSH adapters under `src/data/`", but the test asserts a four-file list and `src/data/osh.js` is layer wiring, not an adapter. The WHEN also still names a fixed list, so the discovery guard that the round-2 correction added has no scenario line. Correction: say the test discovers `server/providers/osh/*.js` and `src/data/osh*.js` and fails when either set changes, and give the count as four.
- [ ] F2 minor src/data/oshProxy.test.mjs:145 The new count guard reaches only two paths. A module the provider imports from anywhere else is neither scanned nor counted, so a fetch in it escapes osh-005, and osh-004 does not catch it either. Correction: build the scanned set from the transitive imports of server/providers/osh.js, or name the path limit in the proposal.
- [ ] F3 minor src/data/oshProxy.test.mjs:556 The no-seam test observes only the own property names that oshProxy() reads on its first argument. A seam re-added as a second parameter, as a mutable module export, as a key inside env, or as an option on the exported factories passes it unchanged. Correction: also assert the arity and that the route's identifiers resolve to the ids.js imports.
- [ ] F4 minor proposal.md:46 The round-2 text says the ledger moved src/data/labelArbiter.js "from 50 to 52 branches ... then back to 50" and that "the final ledger entry equals its base value". It does not. gaps.json banks it at branches 52, totals 407, two above the base 50 / 405. Correction: state the banked count and why the count tolerance absorbs it.
- [ ] F5 minor specs/osh/spec.md:91 The osh-014 WHEN is "a systems or datastreams page carries `links` with a `rel:'next'` entry", but four of its tagged tests use a page with no next link. The status, page-cap and stop lines hold for every page. Correction: widen the WHEN to a systems or datastreams page walk.
- [ ] F6 minor src/data/oshRepositoryHygiene.test.mjs:64 BARE_HOST_TOKEN lists 13 TLDs. A bare real host under `.ai`, `.app`, `.cloud`, `.uk`, `.us` or `.de`, and any host:port whose host is not dotted or an IPv4 literal, still passes. The two lookaheads for `osh.example` and `attacker.example` are dead, since `example` is not in the list. Correction: refuse any dotted token whose last label is not `example`, and drop the two lookaheads.
```
