**Round 7, spec adversary — osh-fusion.** I read the scope diff, `get.js`, both spec copies, the proposal, the design, `review.md`, `tasks.md`, the four round-5/round-6 outputs, `links.json`, and `scripts/spec/lib/review.mjs`. No network call, no repository change.

**Your F1 — do the two new assertions prove the table?** Yes. Node parses `https://user@osh.example/api/systems?limit=100` as username `user`, password `""`, and `https://:pass@…` as `""`/`pass` (I ran it); both share origin and pathname with `current` and carry only `limit`, so each one reaches `get.js:106` and nothing else refuses it. The only credentialed URLs in the whole test file are `:221`, `:230`, `:234` and the walk case at `:378`, which sets both halves and stays refused by either half alone. So keeping one half fails exactly one test — the unit case — and deleting the clause fails two. Rows 4, 5 and 3 of the table (1, 1, 2) are right, and your mutation run matches what the code allows.

**Reviewed-Tree.** `gates tree --change osh-fusion` gives `2de538bc68da…`, identical to line 9. The hash excludes `review.md` and `review/`, so the header edits cannot stale it. **STE lint on this tree is 0 errors, 0 warnings** — the 6 warnings in your quoted output are gone. F2 and F6 are corrected in `get.js:116`, `design.md:60`, `review.md:86` and the capability purpose. `links.json` holds all 14 `osh-036` tests under the renamed names.

Verdict: PASS
- [ ] F1 minor `review.md:113` "The last two rows of the table record each half." The two new rows are the fourth and the fifth; the last two are the allowlist and the rebuild. Write "the fourth and fifth rows", or move the rows to the end of the table.
- [ ] F2 minor `review.md:133` "In each the code was right … every later finding was about the proof or the words", with the `;` as the one exception. Round-5 F3, six lines above at `:87`, is a second exception: before that fix a credentialed next link made Node throw and the walk lost every page it had. Write "two exceptions" and name it.
- [ ] F3 minor `review.md:13,15` The running tally stops at round 5. Round 6 holds 6 + 29 = 35 findings, the joint-highest of the seven rounds. Give that number too, or drop the tally.
- [ ] F4 minor `proposal.md:72` `osh-page-link-rewrite` is true on both examples I re-checked (`a%20b`→`a+b`, `10;offset=10`→`10%3Boffset%3D10`), but "signs the bytes of its query string" is narrower than the class. A server that keeps an opaque cursor and compares it verbatim breaks the same way with no signature. Write "reads the bytes of its query string".
- [ ] F5 minor `review.md:7` `Rounds: 6` with `Scope: diff 4de06fe` is honest today: round 6 read exactly that diff, and `scripts/spec/lib/review.mjs:13` says the field is the scope of the last round. It stops being honest when you write `Rounds: 7`, because round 7 read only the diff since round 6. Either commit the round-6 tree and point Scope at that commit, or keep `Rounds: 6` and record round 7 in prose as a confirming round. `tasks.md:78` has the same shape: 9.3 is ticked before the round-7 result is recorded.
- [ ] F6 minor `review/ste-adversary.md:6` REVIEW-AGENT-NOT-PASS is still true of the tree: the stored output holds `Verdict: FAIL`. Storing the round-7 outputs clears it. It is the only open gate error; REVIEW-SCOPE passes now.

No critical and no major finding. One note: `review/round-5/ste-adversary.md` has no closing note; the other three files do.
