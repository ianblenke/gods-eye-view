```
Verdict: FAIL

- [ ] F1 critical src/data/oshLayer.test.mjs:106 The test tagged osh-029 is named "does not change the entities" but never reads the entity list, and no test in the change populates entities and then fails a fetch. update() could call removeAll() on error and every test still passes. Correction: update once with records, then make getSystems throw, then assert the entity count is unchanged.
- [ ] F2 critical src/data/oshLayer.test.mjs:88 Scenario osh-029 names `stale` in the getStats() THEN line, but no test asserts getStats().stale and no fake source returns stale:true. src/layers/osh/index.js:249 could read `_stale = false` and the suite passes. Correction: add a source with stale:true and assert the flag.
- [ ] F3 major server/providers/osh/get.js:96 oshPages() discards the HTTP status of each page, so an upstream 401, 403, 404 or 500 with a non-list body becomes an empty list. The route answers 200 with count 0 and stale:false, and the list cache overwrites a good snapshot for five minutes. The requirement "List cache" and scenario osh-018 are not met for the most likely failure. The probe only checks `systems`, so a 403 on `datastreams` shows as zero datastreams. Correction: throw for a status outside 200-299, as server/providers/osh/observations.js:23 does, and add a test.
- [ ] F4 major src/data/oshProxy.test.mjs:125 Scenario osh-005 says "each file under server/providers/osh/", but the test reads a hardcoded list of five paths. A new file in that folder is not scanned, and neither is any module the provider imports: src/data/oshSystems.js, src/data/oshDatastreams.js, src/data/oshObservations.js and server/providers/common/http.js. A fetch in one of those is an upstream call site that osh-005 misses, and osh-004 misses it too, because a call to globalThis.fetch is never recorded by the injected fetchImpl. Correction: read the folder with readdirSync, assert the discovered file count, and add the imported osh modules to the scan.
- [ ] F5 major src/data/oshProxy.test.mjs:133 The same scan is weaker than the THEN line. BAD_METHOD_TOKEN is upper case only, so `method: 'delete'` passes, and fetch upper-cases it. The body check is the literal /body:/, so `body :` or `'body':` passes. Nothing asserts that get.js holds exactly one fetch call, so a second call site inside get.js passes every assertion. Correction: match the method token case-insensitively, match `body\s*:` and a quoted key, and assert exactly one match of the call token in get.js.
- [ ] F6 major server/providers/osh.js:233 The WHEN of osh-022 is a client request to /api/osh/observations, but no test asserts the 200 body of that route. The two tests that reach this line assert only the fetch options and the absence of secrets, so `datastream`, `fetchedAt`, `stale` and `ttlMs` are unverified, and "an empty item list gives observation:null" is checked only in the cache unit. Correction: add a route test that asserts the whole 200 payload and the null observation.
- [ ] F7 major src/data/oshRepositoryHygiene.test.mjs:33 The scan finds only files whose base name starts with "osh", so src/layers/osh/source.test.mjs and src/layers/osh/detail.test.mjs are not checked for a real address, although they are OSH test files and hold OSH payload shapes. Correction: match the path, not the base name.
- [ ] F8 minor src/data/oshRepositoryHygiene.test.mjs:51 The address match needs a scheme, so a bare host name or a `host:port` pair, which is the likely shape of a real OSH address, is not caught. Correction: also refuse a token that resolves as a host with a port, or a dotted host that is not a *.example name.
- [ ] F9 minor server/providers/osh.js:83 `buildObservationUrl` and `assertObservationSafety` are injectable options that design.md D13 does not name, and the injection at oshProxy.test.mjs:490 is the only way to reach the guard-failure branch at osh.js:227, which the real guard cannot reach with the current pattern. A caller can also replace the URL builder and defeat the whole id boundary. Correction: make both module-private, or record the seam in design.md D13 and prove the branch another way.
- [ ] F10 minor proposal.md:43 "Gaps that this change closes: none" contradicts the ledger: openspec/trace/history.jsonl records src/data/localLayers.js lines 23 to 0 with reason "closed", and gaps.json drops its entry. Correction: name that closed gap in Impact.
- [ ] F11 minor openspec/trace/history.jsonl:489 This change writes two opposite entries for src/data/labelArbiter.js, a file it does not touch: branches 50 to 52 "shown by test" and 52 to 50 "smaller", with the totals moving 405 to 407 and back. No test in the change explains either. Correction: rerun the ratchet on a clean tree and keep only a stable entry, or record the instability as a known limit.
- [ ] F12 minor src/layers/osh/detail.js:56 writeOshDetail() is exported from src/layers/osh/index.js:4 but production never calls it. src/layers/osh/index.js:42 repeats the same two lines inside writeDetail(). The function reaches 100% coverage only through detail.test.mjs. Correction: call writeOshDetail() from the layer, or remove it and its export.
- [ ] F13 minor src/data/oshObservationsProxy.test.mjs:106 Scenario osh-023 says the cache "drops the oldest past that cap", but the test asserts only size(). A cache that dropped the newest id would pass. Correction: assert that the first id is gone and the last id is present.
- [ ] F14 minor src/data/oshGet.test.mjs:168 Scenario osh-015 says an oversized declared body is "never read or parsed", but the test asserts only the rejection. Correction: give the response a body stream whose read the test can observe, and assert it stayed unread.
```

Notes on the two safety properties, which hold apart from the findings above.

GET only: oshGet() at server/providers/osh/get.js:45 is the one call site; the probe (base.js:66), the page walk (get.js:96) and the observation fetch (observations.js:22) all pass through it; every call carries method GET, no body and redirect 'manual', and a 3xx throws at get.js:51 before the body is read, so the Basic header cannot reach a second host. The browser method guard at osh.js:138 runs before any other work. The keyless path answers at osh.js:180 before resolveRoot(), and osh-001 and osh-002 count zero calls. The proof of a single call site is the weak part: see F4 and F5.

Datastream id: readDatastreamId() (ids.js:21) reads the parsed query only, refuses an absent, empty or repeated value, and applies a pattern with no ':', '/', '?', '#', '%', '.', '@' or space, so the value cannot carry a scheme, an authority, a dot segment, a separator or a fragment. encodeURIComponent() is the identity on that set, url.search is assigned whole after the id (ids.js:38), and assertObservationUrl() (ids.js:51) re-checks origin, pathname, search and hash. The guard compares against the raw id while the builder encodes, so it fails closed if a later edit widens the pattern, including for '..', which URL normalization makes visible. osh.js:213 runs the id check before resolveRoot() and osh.js:226 runs the guard before observationsCache.get(), so a refused id makes zero upstream calls. The one bypass is the injectable builder in F9.

No real OSH data: the three fixtures, the provider files and the test files hold only *.example hosts and sys-fixture-/ds-fixture-/obs-fixture- ids, and .env.example adds the three keys commented and empty. The two gaps in the proof are F7 and F8.
