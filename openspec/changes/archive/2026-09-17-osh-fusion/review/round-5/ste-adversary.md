```
Verdict: PASS
Findings: 12 minor

- [ ] S1 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:55 "Next link stays the same request" Approved words. A link is not a request, so the heading says something the body does not. Write: "D10a A next link is a later page of the same request".
- [ ] S2 minor openspec/changes/archive/2026-09-17-osh-fusion/specs/osh/spec.md:84 (and openspec/specs/osh/spec.md:88) "MUST stay on the resolved root's origin ... A next link MUST stay a later page of the same request, never a different one" One word, one meaning. The same sentence pair uses "stay" for "remain on" and for "remain equal to". "a different one" also needs its noun. Write: "A next link MUST name a later page of the same request, never a different request".
- [ ] S3 minor openspec/changes/archive/2026-09-17-osh-fusion/specs/osh/spec.md:105 (and openspec/specs/osh/spec.md:109) "a query key such as `_method`, that could ask the server to treat the request as another method, refuses the link the same way" Voice. A key does not refuse; the provider does. The clause about the server is also a statement this project cannot verify. Write: "**AND** the provider refuses a link with a query key such as `_method` the same way, because that key is outside the list".
- [ ] S4 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:18 "a fixed pagination allowlist" One word, one meaning. design.md:56 calls the same list "a fixed query-key allowlist", and `f` is a format key, not a pagination key. Use one name in both files. Write: "a fixed query-key allowlist".
- [ ] S5 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:56 "proves a next link is only a later page" Approved words. The function does a check, it does not give proof. Write: "makes sure that a next link is only a later page".
- [ ] S6 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:56 "It checks the origin, the path, a fixed query-key allowlist ... and an empty fragment" Articles and nouns. It does not check an allowlist or a fragment, it checks the query keys against the allowlist and checks that the fragment is empty. Write: "It checks the origin and the path, it checks each query key against a fixed list ..., and it checks that the fragment is empty".
- [ ] S7 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:58 "A server that pages in an unfamiliar way is not a failure to report — it is a link this provider does not follow" One word, one meaning and Verbs. "page" is a noun everywhere else in this change, and here it is a verb; "it" has the server as its subject, but a server is not a link. Write: "A server that gives its next pages in an unfamiliar way is not a failure to report. Its next link is a link this provider does not follow".
- [ ] S8 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:58 "The property enforced is \"the same request\"" Voice. Write: "The check enforces one property: the same request".
- [ ] S9 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:58 "so a key nobody has named yet still fails closed" Verbs. "has named" is not a simple tense, "fails closed" is not an approved use of "fail", and a key does not fail. Write: "so the check also refuses a key that no person named before today".
- [ ] S10 minor openspec/changes/archive/2026-09-17-osh-fusion/tasks.md:23 "the same-request page link check" Articles and nouns. The group has more than three nouns. Write: "the check for a page link of the same request".
- [ ] S11 minor src/data/oshGet.test.mjs "isSamePageWalk() accepts a candidate that differs only in an allowed pagination key" Test names. The test also accepts a candidate that differs in four keys, so the singular says less than the test does; "pagination" repeats S4. Write: "accepts a candidate that differs only in the allowed query keys".
- [ ] S12 minor src/data/oshGet.test.mjs "... including a method override" and "... without throwing" Words that end in -ing. Neither word is a technical name. Write: "such as a method override" and "and does not throw an error".
```
