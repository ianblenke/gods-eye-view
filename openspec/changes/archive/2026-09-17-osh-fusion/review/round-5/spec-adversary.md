```
Verdict: FAIL
- [ ] F1 major server/providers/osh/get.js:103 No test fails when the origin clause is deleted. I removed `if (candidate.origin !== root.origin) return false;` and all 18 tests in src/data/oshGet.test.mjs still pass: the osh-014 foreign-origin test uses `https://attacker.example/systems`, which the new pathname clause rejects on path grounds (`/systems` vs `/api/systems`). The clause runs, so branch coverage stays green, but nothing asserts it. Without it a next link `//attacker.example/api/systems?limit=1` is followed and the Basic `Authorization` header goes to the attacker. Add an osh-036 case: `isSamePageWalk(new URL('https://attacker.example/api/systems?limit=1'), root, new URL('https://osh.example/api/systems?limit=1'))` must be false, plus an oshPages case with a same-path foreign-origin href.
- [ ] F2 major server/providers/osh/get.js:106 A literal `;` defeats the allowlist. `new URL('systems?limit=1;_method=DELETE')` yields keys `['limit']`, so isSamePageWalk returns true and `oshGet` sends the raw string `?limit=1;_method=DELETE` upstream. `?f=json;_method=DELETE` passes the same way. OSH runs on Jetty, whose UrlEncoded has historically split a query on `;` as well as `&`, so the server can see `_method=DELETE` — the exact payload osh-036 names. This falsifies design.md:58 ("a key nobody has named yet still fails closed"). Refuse a candidate whose `search` contains `;`, or rebuild the query from `searchParams` so only `&`-joined allowlisted pairs survive, and add the `;` case to osh-036.
- [ ] F3 major server/providers/osh/get.js:103 A userinfo field passes the guard. `URL.origin` drops credentials, so `https://user:pass@osh.example/api/systems?limit=1` matches origin and path and is accepted as "the same request", although it carries different credentials. `oshGet` then calls `fetchImpl(String(url))`; Node's Request constructor throws `TypeError: Request cannot be constructed from a URL that includes credentials` (verified), so an attacker-chosen next link makes `oshPages` reject and lose every page already gathered, instead of stopping cleanly. Add `if (candidate.username !== '' || candidate.password !== '') return false;` and a test for it.
- [ ] F4 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:70 The silent stop is named only in design.md D10a, not in "Known limits and later changes". A server that pages with any other query key (an echoed filter such as `bbox` or `datetime`, or a trailing-slash path — `systems/?limit=1` fails the pathname clause) drops the later pages with no error and no log. Add an entry such as `osh-page-link-allowlist` to that section.
- [ ] F5 minor src/data/oshGet.test.mjs:206 Two new STE warnings come from this round's test names: STE-ING "including" (line 206) and STE-ING "throwing" (line 221). Rename, or record them with the other accepted warnings.
- [ ] F6 minor src/data/oshGet.test.mjs:186 Four of the five tests linked to osh-036 call `isSamePageWalk()` directly and never make the scenario's WHEN ("a page names a next link"). Only the walk test at line 221 does, and it exercises only the query clause. The path and fragment clauses are never proven to stop a real walk. Add one oshPages case per clause, or note the unit-level proof in the scenario.
```

Notes, for the author:
- The allowlist itself is otherwise sound. `f` carries no danger on its own — its value is never a separator (`%26`, `%3B` and a tab all stay inside the value or split into a caught key), the pathname stays pinned, and the method never changes. Duplicate keys, an empty key (`?=evil` → key `''`), a case-different key (`LIMIT`), a dot segment (`/api/systems/../systems` normalises back to the same path, correctly accepted), an encoded separator (`%2F`, `syste%6ds`) and a protocol-relative href all behave as the scenario says.
- The "keeps the earlier items, no throw" behaviour is real and the test at `src/data/oshGet.test.mjs:221` pins it; dropping the query clause fails it.
- The only gate error is the expected REVIEW-TREE one.
