I have everything I need. Here is my round-6 report.

---

```
Verdict: FAIL
Findings: 1 major, 27 minor

- [ ] S1 major server/providers/osh/get.js:115 "The origin and the path come from `current`, never from the candidate" The sentence does not agree with the code. `buildNextPageUrl()` builds `new URL(current.pathname, root)` (get.js:132), so the path comes from `current` and the origin comes from `root`. The same untrue sentence is in two more places: design.md:60 "builds the request from parts it already trusts: the current page's origin and path", and review.md:84 "now builds the next URL from the origin and the path of the current page". The behaviour is correct; only the three sentences are wrong. Write, in get.js:115: "The origin comes from `root` and the path from `current`, never from the candidate". In design.md:60: "`buildNextPageUrl()` then builds the request from parts it already trusts: the resolved root's origin and the current page's path." In review.md:84: "now builds the next URL from the resolved root's origin and the path of the current page".
- [ ] S2 minor openspec/specs/osh/spec.md:4 "with the newest observation of a selected system" The Purpose disagrees with `osh-030` (spec.md:225), which reads "the newest observation of each datastream of that system". The Purpose before this change said the same. Write: "with the newest observation of each datastream of the selected system".
- [ ] S3 minor openspec/specs/osh/spec.md:4 "Read the server through same-origin routes, so the browser never gets the credentials" Articles and nouns, and One word, one meaning. The sentence does not say who reads, and "gets" is a second word for "receives" (proposal.md:16, "The browser never receives them"). Write: "Let the browser read the server only through same-origin routes, so it never receives the credentials."
- [ ] S4 minor openspec/specs/osh/spec.md:105 (and changes/.../specs/osh/spec.md:101) "even a protocol-relative link whose path is the same" The clause hangs on "follows it only when", so a reader can read it as a second link that the provider follows. Write: "even when the link is protocol-relative with the same path" (23 words, inside the lint limit).
- [ ] S5 minor openspec/specs/osh/spec.md:112 (and :108) "the request the provider sends for an accepted link carries a query rebuilt from the candidate's own allowlisted values" Voice, and the allowlist holds keys, not values (`OSH_PAGE_LINK_QUERY_KEYS`, get.js:80). Write: "**AND** the request for an accepted link carries a query that the provider builds from the values of the candidate's allowed keys".
- [ ] S6 minor openspec/specs/osh/spec.md:113 (and :109) "that rebuild keeps a separator such as `;` inside one value as plain text, never a second key" Verbs. "rebuild" is a verb that the line uses as a noun, and "never a second key" has no preposition. Write: "**AND** the query that the provider builds keeps a separator such as `;` inside one value as text, and never as a second key".
- [ ] S7 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:18 "only when it stays the same request" A link is not a request. This is round-5 S1, which the author corrected in the D10a heading and in the requirement but not here; "stay" also means "remain on" at spec.md:104. The line is already 25 words, so split it. Write: "Follow a next-page link only when it names a later page of the same request. The link must keep the path, and carry no query key outside a fixed query-key allowlist."
- [ ] S8 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:71 "carries no user name and no password" One word, one meaning. Every other place writes "username": design.md:56, spec.md:107, review.md:85, the code and the test names. Write: "carries no username and no password".
- [ ] S9 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:71 "A server that pages with another key" Verbs. "page" is a noun everywhere else in this change. This is round-5 S7, which the author removed from design.md:58 and then wrote again here. Write: "A server that gives its next pages with another key".
- [ ] S10 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:71 "So does a server whose path ends in a slash." Articles and nouns. A server has no path; its next link has one. Write: "A server whose next link ends the path with a slash also stops the walk."
- [ ] S11 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:58 "keeps out an unanticipated key" Verbs and Approved words. "keep out" is a phrasal verb and "unanticipated" is not an approved word. Round 5 read this paragraph and missed it. The paragraph already has six sentences, the lint limit, so keep it to one sentence. Write: "The fixed list, not a denylist of dangerous keys such as `_method`, stops an unknown key."
- [ ] S12 minor server/providers/osh/get.js:92 "no userinfo" One word, one meaning. This is a third word for the same thing, after "username and password" (design.md:56, spec.md:107) and "credentials" (spec.md:4). Write: "no username and no password".
- [ ] S13 minor server/providers/osh/get.js:93 "This is not a redirect or a resource check" Articles and nouns. The first item has no noun, so the sentence says the function is not a redirect. Write: "This is not a redirect check or a resource check".
- [ ] S14 minor server/providers/osh/get.js:96 "cannot ride along" Verbs. The phrase is an idiom, not an approved verb. Write: "cannot become part of the request".
- [ ] S15 minor server/providers/osh/get.js:118 "Rebuilding, rather than reusing the candidate's URL, makes sure a separator the guard does not know about ... re-encodes it as one opaque value." Words that end in -ing, one sentence of about 50 words, and "the guard" is a second name for the check (the design and the proposal say "the check"). Write: "The rebuild does not use the candidate's URL. So a separator that the check does not know about cannot hide a second key inside the value of an allowed key. Some servers read `;` as a second query separator. The rebuilt query writes that value as one text string."
- [ ] S16 minor server/providers/osh/get.js:139 "following a link only when it names the same page walk" Articles and nouns. A link names a page, not a walk. Write: "follows a link only when the link names a later page of the same request (see `buildNextPageUrl`)".
- [ ] S17 minor server/providers/osh/get.js:141 "so the items gathered so far are still returned" Voice. Write: "so the function still gives the items it collected".
- [ ] S18 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:81 "whether a GET request can trigger a delete in OSH" Verbs. "trigger" is a noun in STE, and "a delete" is a verb that the sentence uses as a noun. Write: "whether a GET request can cause a deletion in OSH".

Note on the tail: my list runs to S29, not S28. Ask for S19 to S29 and you have all of it.
- [ ] S19 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:81 "The agent mutation-tested `isSamePageWalk()`" Verbs. The line makes a verb from a noun. Write: "The agent did a mutation test on `isSamePageWalk()`".
- [ ] S20 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:83 "the branch count stayed green" Approved words. A count is not a colour, and "green" has no approved meaning here. Write: "the branch count stayed complete".
- [ ] S21 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:83 "Two cases now pin it" One word, one meaning. This project uses "pinned" for a value that does not change (spec.md:36, ci-gates/spec.md:4, design.md:103); here "pin" means "test". Write: "Two cases now test it".
- [ ] S22 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:84 "A `;` defeated the allowlist." Approved words. "defeat" is not an approved verb. Write: "The allowlist did not stop a `;`."
- [ ] S23 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:84 "OSH runs on Jetty, which has split a query on `;` as well." Verbs. "has split" is not a simple tense, and the sentence does not say when. Write: "OSH runs on Jetty. Earlier versions of Jetty also split a query on `;`."
- [ ] S24 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:88 "never made the scenario's WHEN" Approved words. "make" with this meaning is not clear: it can read as "never wrote the WHEN". Write: "never did the action of the scenario's WHEN".
- [ ] S25 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:105 "the query rebuild of `buildNextPageUrl()`" Verbs. The other rows of the table are noun phrases; "rebuild" is a verb. Write: "the query that `buildNextPageUrl()` builds again".
- [ ] S26 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:107 "A clause with no failing test is present, not enforced." Words that end in -ing, and Voice. "failing" is not a technical name, and this is the rule of F5. Write: "A clause with no test that fails is in the code, but nothing shows that it operates."
- [ ] S27 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:92 "The corrections include: one name, ... and two test names without a word that ends in -ing." One sentence of about 60 words. review.md:84 has one of 32 words. The lint does not read this file, so no warning shows them. Write each item as its own sentence, or as a short list.
- [ ] S28 minor src/data/oshGet.test.mjs:244 "a next link that fails the same-page-walk check stops the walk, keeps the earlier items, and does not throw an error" Test names and Articles and nouns. "same-page-walk check" is a group of four nouns. This is round-5 S10, which the author corrected in tasks.md:23. Write: "a next link that the check refuses stops the walk, keeps the earlier items, and does not throw an error".
- [ ] S29 minor src/data/oshGet.test.mjs:380 "oshPages() follows an accepted next link with the rebuilt, safely-encoded query" Test names. Two participles act as adjectives, and "safely" does not say what is safe. Write: "oshPages() follows an accepted next link with the query that the provider built again".

Two notes on this batch. S27 is the one finding where I give no exact replacement text: review.md:92 is your own summary line and only you know which items you want to keep. S28 and S29 change test names, so `openspec/trace/links.json` needs the new names.

Verdict for the record: FAIL on S1 alone. With S1 corrected in all three places, and S2 to S29 corrected or accepted by name, this change has nothing open from me.
```

The author asked for this note. The agent's own header says "Findings: 1 major, 27 minor". The list holds 28 minor findings, S2 to S29. The agent found the error itself and gave the correct range before the last batch.
