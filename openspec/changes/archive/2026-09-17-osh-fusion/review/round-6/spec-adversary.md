I read the diff, `get.js`, both spec copies, the proposal, the design, `review.md`, the round-5 outputs, `tasks.md`, and the callers of `oshPages` in `server/providers/osh.js` and `server/providers/osh/base.js`. No network call, no write, no command that touches the repository.

**On the lead's question 1 — is the rebuild complete?** Yes. I traced every byte that reaches `fetchImpl`. `oshPages` calls `oshGet(fetchImpl, url, …)` with `url` that is either `firstUrl` (built in `server/providers/osh.js:109` / `:118` from `root`) or the return of `buildNextPageUrl()`. `buildNextPageUrl` (`server/providers/osh/get.js:128`) takes the scheme, host and port from `root`, the path from `current`, and the query keys from the six literal strings of `OSH_PAGE_LINK_QUERY_KEYS` — a key reaches the wire only after `includes()` matched it against one of those literals. The only server-controlled bytes left are the **values** of those six keys, and `URLSearchParams.toString()` percent-encodes `&`, `=`, `;`, `#`, `/` and `?` out of them, so a value cannot become a second key or leave the query. The path is pinned to `firstUrl`'s path for the life of the walk, so page 20 cannot be on a different resource than page 1. The other two `oshGet` call sites (`base.js:64` probe, `observations.js:22`) build their URLs locally too, so the round-5 claim "every URL this provider sends is one it built" holds.

I also checked `+` round-tripping, since `firstUrl` carries `f=application/geo+json`: `URLSearchParams` decodes `+` to a space and re-encodes a space to `+`, so page 1 and page 2 decode identically under any one parser. Not a defect.

**Question 2 — spec against code.** The nine THEN/AND lines of `osh-036` each map to a clause: origin → `get.js:103`, path → `:104`, userinfo → `:105`, fragment → `:106`, key allowlist → `:107-109`, silent stop → `oshPages` `:176`, rebuild → `:130-134`. None claims more than the code does. One test asserts less than its line (F1 below).

**Question 5 — `review.md` numbers.** The mutation table is right: origin 3 (tests at `src/data/oshGet.test.mjs:207`, `:285`, `:300`), path 2 (`:200`, `:320`), userinfo 2 (`:216`, `:360`), fragment 2 (`:238`, `:340`), allowlist 2 (`:229`, `:244`), rebuild 2 (`:264`, `:380`). The scenario count 36 is right — `grep -c '^#### Scenario:'` gives 36 in both spec copies, `osh-001` through `osh-036` with no gap. 27 tests in the file is right (13 committed plus the 14 new). Rounds 1, 3 and 4 add up. Round 2 does not.

```
Verdict: PASS
- [ ] F1 minor src/data/oshGet.test.mjs:216 The spec line openspec/specs/osh/spec.md:107 says the provider refuses a link that carries "a username or a password", but both tests of that clause (`:216` and the walk case at `:360`) use a URL with a username AND a password. `get.js:105` is `candidate.username !== '' || candidate.password !== ''`; deleting either half leaves the other half catching both tests, so neither half is asserted on its own. The mutation table's "username and password" row is honest about treating the line as one clause, so no number there is wrong. Add `https://user@osh.example/api/systems?limit=1` (username, empty password) to the case at `:216`. Consequence is nil today — `buildNextPageUrl()` rebuilds the origin from `root`, so credentials never reach the wire either way; the clause only decides whether the walk stops.
- [ ] F2 minor openspec/changes/archive/2026-09-17-osh-fusion/design.md:60 and server/providers/osh/get.js:116 Both say the rebuild takes "the current page's origin and path" / "The origin and the path come from `current`". `get.js:132` is `new URL(current.pathname, root)` — the path comes from `current`, the origin comes from `root`. The two are equal for both callers, because `firstUrl` is built from `root` at `server/providers/osh.js:109` and `:118`, so nothing is wrong on the wire. But `oshPages` is exported, and a future caller passing an off-origin `firstUrl` would fetch page 1 at that origin and page 2 silently at `root`'s. Write "the resolved root's origin and the current page's path" in both places, or take the origin from `current` so the prose is the code.
- [ ] F3 minor openspec/changes/archive/2026-09-17-osh-fusion/proposal.md:71 The known limit `osh-page-link-allowlist` is true on every sentence I checked — the trailing-slash case and the echoed-`bbox` case both stop the walk as written — but it names only the stop, not the rewrite. `buildNextPageUrl()` re-encodes each accepted value, so a next link the walk *accepts* can still reach the server as different bytes: `?limit=10;offset=10` from a server that means `;` as a separator arrives as one `limit` with the value `10%3Boffset%3D10`, and a cursor whose exact bytes are signed no longer matches its signature. Both are silent and neither is in the gates. Add a sentence: the walk also re-encodes the value of an accepted key, so a server that separates on `;` or signs its query string gets a request it did not send.
- [ ] F4 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:13 "The number of findings decreased in each round: 35 in round 1, 22 in round 2, 13 in round 3 and 1 in round 4." The round-5 section this diff adds makes the sentence false: round 5 is 6 spec plus 12 STE, 18 findings, up from 1. The round-2 figure is wrong independently, as above. Rewrite the sentence with 24 for round 2 and with round 5 named as the round that rose.
- [ ] F5 minor openspec/changes/archive/2026-09-17-osh-fusion/review.md:7 The header says `Rounds: 4` and `Scope: full` while the body now records round 5 and this round, both diff-scoped, and `Verdict: PASS` stands above a round-5 section marked FAIL. The expected REVIEW-TREE error already tells you to rewrite the header; these three fields belong in that same edit.
- [ ] F6 minor openspec/specs/osh/spec.md:4 The new Purpose says "the newest observation of a selected system". A system has no observation in this capability: `osh-022` serves the newest observation of one datastream, and `osh-030` has the layer read the newest observation of *each datastream* of the selected system. The sentence the diff replaced said "each datastream of the selected system" and matched. Write "with the newest observation of each datastream of the selected system". The other two sentences check out — `/api/osh` is installed on the app's own dev and preview servers (`osh-035`), and `osh-008` keeps the three configured values out of every response and log line.

No critical and no major finding. I tried the completeness attack you asked for and could not break it: even with a clause of `isSamePageWalk()` missing, `buildNextPageUrl()` rebuilds the scheme, host, port, path and key set from values this code owns, so the attack surface is one opaque, percent-encoded value per allowlisted key on a pinned GET path. The three round-5 defects are closed, not merely re-described.
```

The author asked for this note, on the round-2 count.

The round-2 figure: `review.md:13` says "22 in round 2". The correct number is 24. I counted the finding lines in the two round-2 outputs: `review/round-2/spec-adversary.md` holds 7 (F1 to F7, every one minor) and `review/round-2/ste-adversary.md` holds 17 (S1 to S5 major, S6 to S17 minor). 7 + 17 = 24. `review.md` contradicts itself on this: line 53 says "F1 to F7" and line 57 says "S1 to S17", which is 24, not 22. I checked the other three the same way and they hold: round 1 is 14 + 21 = 35, round 3 is 6 + 7 = 13, round 4 is 0 + 1 = 1 (the round-4 STE file records its one major in prose, not as a `- [ ]` line, so a grep for finding lines returns 0 there; the finding is real and the figure 1 is right).
