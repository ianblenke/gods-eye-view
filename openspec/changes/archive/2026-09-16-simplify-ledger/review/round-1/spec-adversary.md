I reviewed the full change: the delta specs, the merged main specs, `ledger.mjs`, `specs.mjs`, `trace.mjs`, `gates.mjs`, the five test files, the trace files, the Makefile/AGENTS.md, the gate output, and the diff against `47e355e` (including the old band/range code at `47e355e:scripts/spec/lib/ledger.mjs` for a strength comparison).

```
Verdict: FAIL
- [ ] F1 critical src/tooling/spec/specs.test.mjs:363 The only test tagged `[spec-trace-054]` asserts `loadSpecs().removedIds`. It asserts neither THEN line: the trace gate does not stop the build for such a scenario, and the gate counts it with the scenarios that wait for a test. No test in the repository calls `evaluateTrace` with a non-empty `removedIds`, so the new branch at scripts/spec/lib/trace.mjs:97-98 has no test. The coverage gate does not see the hole, because the line runs in every trace test and only the `specs.removedIds ?` guard is a branch. Add a test that calls `evaluateTrace` with a main scenario ID in `specs.removedIds` and no passing test, and assert that the result has no `TRACE-UNVERIFIED` error for that ID and that `report.counts.pending` counts it.
- [ ] F2 minor scripts/spec/lib/ledger.mjs:223 The JSDoc of `compareLedger` still describes the band: "A band file is a loaded code file ... above the entry by at most the band of the entry count." The change removes the band. A later author reads the wrong rule. Write the tolerance rule of the requirement "Count tolerance" in its place.
- [ ] F3 minor scripts/spec/lib/ledger.mjs:300 `compareWithBase` still takes `outsideChanged`, and scripts/spec/gates.mjs:418 still gives it. No line of the body reads it, and its JSDoc at :296 names the removed `unstable` history line. The parameter comes from the removed range rules and no scenario describes it. Remove the parameter, the argument at gates.mjs:418, the JSDoc line and the argument in src/tooling/spec/ledger.test.mjs:154.
- [ ] F4 minor openspec/specs/gap-ledger/spec.md:7 The requirement "Ledger file" still says that an entry records "the total branches and functions". The new requirement "Total counts of the ledger" at :228 needs the total lines as well, and `LEDGER-NO-TOTALS` stops the build without it. The two requirements of one spec disagree. Name the total lines in the requirement "Ledger file".
- [ ] F5 minor openspec/changes/archive/2026-09-16-simplify-ledger/design.md:13 The goal "Keep the gate as strong as the three mechanisms together" does not hold for the line counts. The old band was `max(5, ceil(count * 0.02))` of the entry count; the new line tolerance is `min(8, 4% of the total lines)`. For 206 of the 353 loaded entries the new tolerance is larger, and the sum over the ledger goes from 1965 to 2039 not-covered lines. The covered branch counts get stronger (1802 to 532), so the change is not weaker in sum, but the goal as written is wrong. Correct the goal, or record the larger line tolerance with `tolerance-slack` in the proposal.
- [ ] F6 minor src/tooling/spec/gates.test.mjs:271 The `[gap-ledger-077]` test asserts the log line and `links.json`. The THEN line says that the command writes the ledger, the ID registry and the scenario links. Add an assertion on `openspec/trace/gaps.json` and on the entry for `demo-001` in `openspec/trace/ids.json`.
- [ ] F7 minor src/tooling/spec/ledger.test.mjs:515 The `[gap-ledger-074]` test covers the other content, the changed hash and the untrue coverage. It does not cover the first half of the AND line, "a file that no test loads". Other tests cover it under other IDs. Add the unloaded case to this test.
```

Notes on the points I was asked to weigh, beyond the findings: the covered-count comparison is made again (`compareCoverageEntry` at `ledger.mjs:210-217` with the tolerance, and `compareWithBase` at `:334-343` against the base), so the old limit `range-totals` does not come back, and `gap-ledger-072` tests it at both the error edge and the pass edge. The anti-drift rule holds.
