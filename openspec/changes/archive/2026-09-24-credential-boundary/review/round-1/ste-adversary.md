Verdict: FAIL
- [ ] S1 major openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:22 "every file under `build/`, and every non-test file under `src/`" (also :96, and openspec/specs/credential-boundary/spec.md:25,99) Does not agree with the code. `jsFilesUnder()` (src/tooling/bundleCredentials.test.mjs:167) reads only `.js` files. So the 004 and 015 scans do not read `src/keySetupCore.mjs`, `src/keySetupHardening.mjs`, `src/overlays/worldOverlayAllocation.worker.mjs`, or the `.json` and `.css` files. Write: "every `.js` file under `build/`, and every non-test `.js` file under `src/`". Or change the test so that it reads every file.
- [ ] S2 major openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:8 "builds a fixture page through the real browser config" (also openspec/specs/credential-boundary/spec.md:11, proposal.md:11 "A test built through the real browser config", design.md:96 "It builds through `createBrowserViteConfig()`") Does not agree with the code. The test sets `configFile: false` and gives `build()` only the `envPrefix` and `define` of the real config (bundleCredentials.test.mjs:88-101). The plugins and the other settings do not apply. Also, the test is not built; the fixture is. Write: "builds a fixture page with the `envPrefix` and `define` of the real browser config".
- [ ] S3 major openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:64-65 "a bad-input request answers `400`" (also design.md:41 "Neither mode, or both, answers `400`.") Does not agree with the code or with scenario 007. geocode.js:69-74 checks the key before it checks the input. With no key, a malformed request answers `200` with `configured:false`, as 007 states. So 007 and 010 give two different results for the same request. Write: "**WHEN** a Google key is set and a request names neither mode, ...". In D1, write: "When a key is set, a request with neither mode or both modes answers `400`."
- [ ] S4 major openspec/changes/archive/2026-09-23-credential-boundary/tasks.md:45-46 "swap the key resolver for a browser-key-only one. `008`'s table must fail" (also spec.md:54 "under each row of the server-key/browser-key selection table") Two possible meanings, and does not agree with the code. The 008 table test (src/googleGeocodeProxy.test.mjs:87-106) gives the route its own resolver, `String(server).trim() || browser`, and has 3 of the 5 rows. A change to the default resolver of geocode.js does not make it fail. A change to the resolver that google.js:255-258 passes does not make it fail either. Only the untagged test at src/googleServerKey.test.mjs:70 runs the real resolver on this route with all 5 rows. Write: "under each of the five rows of the selection table, with the key resolver of `googlePlacesContextProxy()`", and tag that test with 008. Or write "under three rows, with a resolver that the test gives" and remove Mutation 1.
- [ ] S5 major openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:55 "Five test files gain a new tagged assertion" (also :43-44 "and their test files") Does not agree with the code or with design.md:188-194. `src/keySetupCore.test.mjs` also gets the tagged tests of 005 and 006. `coordinates.js` has no test file of its own. The list does not name the new file `src/tooling/bundleCredentials.test.mjs`, and the changed-file list does not name `.gitignore`. Write: "Six test files gain ...: ..., `gevActions.test.mjs` and `keySetupCore.test.mjs`." Also write: "New files: `geocode.js`, `coordinates.js`, `src/googleGeocodeProxy.test.mjs` and `src/tooling/bundleCredentials.test.mjs`." Add `.gitignore` to the changed files.
- [ ] S6 major openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:30 "caps every list" (also tasks.md:50 "an uncapped list") Does not agree with the code. `projectGeocodeResults()` does not limit the `types` list of each address component (src/data/placeProviderPayloads.js:131). Write: "It keeps at most 12 results, 20 address components and 8 result types". Or add that limit to the code.
- [ ] S7 major openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:74-75 "The browser stops sending the key to Google in its own requests" Does not agree with design.md:86-88 or with the code. The browser still sends the browser key to Google for the photoreal Map Tiles (`window.__GOOGLE_MAPS_API_KEY__`, and the 'google-direct' route in src/mapStartup.js). Write: "The browser stops sending the key in its geocoding requests to Google".
- [ ] S8 major openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:87 "a `configured:false` answer counts as no verdict" (also design.md:78-79, and the test name at src/search/placeSearch.test.mjs:43 "contributes no verdict, not a negative one") Two possible meanings. The code gives `{place:null, answered:true}` (src/search/google.js:49), which is the same shape as a `ZERO_RESULTS` miss (google.js:55). "No verdict" can also mean `answered:false`. "A negative one" names `answered:false`, which the test comment calls "inconclusive". Write: "a `configured:false` answer gives `{place:null, answered:true}`, so the combined answer does not become inconclusive, and the Photon fallback runs next". For the test name, write: "a configured:false answer does not make the combined answer inconclusive".
- [ ] S9 major openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:4 "only three inputs to the browser ... Each comes from an explicit input." (also openspec/specs/credential-boundary/spec.md:4,7) One word, one meaning. "input" first means a value in the bundle, then a parameter of `createBrowserViteConfig()`. The AIS knobs do not come from a parameter. Vite reads them from the environment through `envPrefix` (build/vite.js:32). Write: "The browser build MUST expose only these values to the browser: the Google Maps browser key and the Cesium ion token, from the parameters of `createBrowserViteConfig()`, and the AIS live settings, from environment names that start with `VITE_AIS_LIVE_`."
- [ ] S10 minor openspec/changes/archive/2026-09-23-credential-boundary/design.md:127 "Its ledger entry allows 221 not-covered branches of 798." Verbs. openspec/trace/gaps.json now gives 222 of 845 for this file. Write: "On `main`, its ledger entry allowed 221 not-covered branches of 798."
- [ ] S11 minor openspec/changes/archive/2026-09-23-credential-boundary/tasks.md:10 "with D1-D6" design.md has D1 to D8. Write: "with D1-D8".
- [ ] S12 minor openspec/changes/archive/2026-09-23-credential-boundary/tasks.md:1 "## 1. Gitignore" / "0.1" Section numbers and task numbers do not agree: "## 4. Build" holds tasks 3.1 to 3.4. Write: give each task the number of its section.
- [ ] S13 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:10-11 "are needed" Voice. The active voice is possible in these descriptions: proposal.md:17-18 "it was meant for", :20-21 and :39 "is documented", :28 "when no key is set", :65 "It is designed"; design.md:65 "is shared by"; spec.md:12 "are both set on the environment", :27 "is given as", :92 "is remembered". The same applies to these test names: keySetupCore.test.mjs:398 "is documented", googleGeocodeProxy.test.mjs:296 "is read as an empty query", gevActions.test.mjs:3220 "is remembered for the page life". Write, for example: "the reverse-lookup remembers a `configured:false` answer for the life of the page", "the route reads a request with no URL as an empty query", "the test sets a ... sentinel and a ... sentinel on the environment before the build".
- [ ] S14 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:19 "reaching the bundle" Words that end in -ing. Also: design.md:7 "Sending", :21 and :32 "reaching", :55 "importing", :70 "working", :211 "narrowing"; spec.md:40 "ending", :67 "leaking". Write: "that gets to the bundle", "When the browser sends it", "and the browser gets it by design", "so that neither one imports the other", "keep their function", "The narrower `VITE_` prefix", "for a name that ends in", "with no key in the answer".
- [ ] S15 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:18 "knobs", "outlive" Approved words. Also: "knob(s)" in design.md:12,20,71,211,214 and spec.md:4; "mints"/"server-minted" (proposal.md:65, design.md:32); "brokers" (design.md:4); "honest" (design.md:22); "genuinely", "loudly" (design.md:220-221); "regardless of whether" (proposal.md:75); "re-asks" (tasks.md:78); "preserve", "discover", "construct" (viteBuild.test.mjs:8,50); "established" (gevActions.test.mjs:3236); "in-flight" (:3179); "sparse", and "defaults" used as a verb (googleGeocodeProxy.test.mjs:270,280, gevActions.test.mjs:3204); "unparsable" (:3281). Write: "settings", "stay after", "makes", "sends", "correct", (delete), "when the server key exists and when it does not", "sends a second request", "keep", "read", "make", "the same place shape as before this change", "in progress", "a result with missing fields gets a default value for each field", "that is not JSON". I am not sure about "malformed" (spec.md:63,78 and test names 010, 012) and "trouble" (spec.md:67). STE can prefer "incorrect" and "problem".
- [ ] S16 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:63 "a 1 MB response cap" Approved meaning. In STE, "cap" is a cover. Here it means "maximum": proposal.md:30; design.md:47; tasks.md:50-51; spec.md:68,71,75; test names googleGeocodeProxy.test.mjs:193,239. Write: "a maximum response size of 1 MB", "larger than the maximum size", "keeps at most 12 results".
- [ ] S17 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:69 "passes its error through" Phrasal verbs and idioms. Also: googleGeocodeProxy.test.mjs:161 "passes through its error"; tasks.md:50 "passes ... through"; placeSearch.test.mjs:32 "falls back to Photon"; googleGeocodeProxy.test.mjs:87 "wins over ... falls back"; design.md:108 "turn its test red"; design.md:219 "cuts the other way". Write: "sends its error to the client", "the search then uses Photon", "the server key has priority over the browser key, and a blank server key gives the browser key", "that must make its test fail".
- [ ] S18 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:19 "both names define to `undefined`" One word, one meaning. "define" is a verb here. Elsewhere it is a noun: spec.md:16 "define list"; tasks.md:27, :32, :34; the test names keySetupCore.test.mjs:368, googleServerKey.test.mjs:119, viteBuild.test.mjs:8. Write: "both names have the value `undefined`". Write the Vite option as `define` in code format.
- [ ] S19 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:26 "out of the global" An adjective used as a noun. Also bundleCredentials.test.mjs:146 "out of the global, named on failure", where "named on failure" has no subject. Write: "out of `window.__GOOGLE_MAPS_API_KEY__`". For the test name, write: "the server key stays out of the bundle and out of main.js, and a failure names it".
- [ ] S20 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:53 "Forward an address lookup" (also :58 "Reverse a coordinate lookup") One word, one meaning. Elsewhere, "forward" and "reverse" are adjectives ("forward lookup", "reverse mode"). "Reverse a lookup" can also mean "undo a lookup". Write: "Send a forward lookup to Google with the resolved key", "Send a reverse lookup to Google with the resolved key".
- [ ] S21 minor src/voice/gevActions.test.mjs:3276 "resolves to no place" (also :3281) One word, one meaning. In the other texts, "resolve" means "select a key" (proposal.md:26, design.md:43, spec.md:53,58). Write: "a fetch failure gives no place", "a response that is not JSON gives no place".
- [ ] S22 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:70 "a thrown fetch answers `502`" Subjects. Also: spec.md:71 "an over-cap body answers with an error, not a thrown exception"; googleGeocodeProxy.test.mjs:181, :193 "not a throw", :222 "a set rate limiter answers 429"; gevActions.test.mjs:3179 "a cached or in-flight coordinate makes no second fetch". The route answers, not the fetch, the body or the limiter. A fetch is not thrown. "throw" is a verb used as a noun, and "set" has many meanings. Write: "when the fetch throws, the route answers `502`"; "when the body is larger than the maximum size, the route answers with an error and does not throw"; "when the rate limiter refuses the request, the route answers 429 with Retry-After"; "a second call for a coordinate in the cache or in progress makes no fetch".
- [ ] S23 minor src/keySetupCore.test.mjs:368 "browser build define names" A group of more than three nouns. Also: tasks.md:79-80 "second-call fetch-count assertion"; tasks.md:84 "Google geocode host string"; tasks.md:15 "registry-read scan test", although the test scans reads on the server, not reads of the registry. Write: "the names in the `define` of the browser build", "the assertion on the fetch count of the second call", "the Google geocoding host name, as a string", "the scan test of the credential names that the server reads".
- [ ] S24 minor src/googleGeocodeProxy.test.mjs:54 "forward mode sends" Articles. Also :108 "reverse mode sends", :239 "caps lists", and viteBuild.test.mjs:50 "build helper does not". Write: "the forward mode", "the reverse mode", "the lists", "the build helper".
- [ ] S25 minor openspec/changes/archive/2026-09-23-credential-boundary/tasks.md:32-33 "Tag the existing define tests ... Add the `Object.keys` equality assertion" Instructions. A task must give one instruction. Also :60-61, :63-64, :72-73, :110-111 and :113. tasks.md:4 "Its own commit, ahead of every other change here." has no verb. Mutation lines such as :28 "`001` must name it, then revert" put a rule and an instruction in one sentence. Write one instruction per task, for example: "Make this commit before all other commits of the change." and "Revert the edit after the check."
- [ ] S26 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:52-53 "one of their branches" References. "their" can mean the tests or the functions. Also: design.md:108 "Each test task below", but design.md has no tasks; design.md:221 "It fails loudly if the prefix match exposes it", where the two "it" refer to different things; design.md:157 "stopped their names", which is not clear. Write: "one branch of these functions", "Each test task in tasks.md", "The test then fails if the prefix exposes that name", "the LEDGER-NOT-IN-BASE check failed on their names".
- [ ] S27 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:45 "when no key is set" Two words for the same thing: "set" here and "configured" in :49. Also "bad-input" and "malformed input" (spec.md:63-65); "probe value" (proposal.md:12) and "sentinel value" (spec.md:8); "non-ok" (spec.md:68, googleGeocodeProxy.test.mjs:161,173) and "non-OK" (gevActions.test.mjs:3269); "the page's life" (spec.md:92) and "the page life" (gevActions.test.mjs:3220). Write: use one term for each.
- [ ] S28 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:60 "the two new route files" Does not agree with design.md:53-55: `coordinates.js` is a shared module, not a route file. Write: "the two new files".
- [ ] S29 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:13 "could ship" Verbs. Use the simple tenses. Also design.md:102 "would pass", :203 "would fail" and :217-218 "would pass". Write: "can ship", "passes", "fails".
- [ ] S30 minor src/voice/gevActions.test.mjs:3269 "a non-OK status or empty results gives no place" The test gives one answer that has both a `ZERO_RESULTS` status and an empty list. Also, "results gives" does not agree. Write: "a ZERO_RESULTS answer with no results gives no place".
- [ ] S31 minor src/tooling/previewServing.test.mjs:29 "stop unknown APIs" The servers do not stop an API. They answer 404 for an unknown API path. Write: "answer 404 for an unknown API path".
- [ ] S32 minor openspec/changes/archive/2026-09-23-credential-boundary/specs/credential-boundary/spec.md:64 "an address past 256 characters" Approved words. Write: "an address of more than 256 characters".
- [ ] S33 minor openspec/changes/archive/2026-09-23-credential-boundary/design.md:59 "the other project functions" One word, one meaning. "project" can also mean the software project. Write: "the other `project...()` functions".
