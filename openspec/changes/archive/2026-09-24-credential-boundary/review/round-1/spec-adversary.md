Verdict: FAIL
Tree: /home/ianblenke/docker/gev-cred, branch credential-boundary, HEAD 90ffba96 plus the working tree
- [ ] F1 critical src/googleGeocodeProxy.test.mjs:87 The [credential-boundary-008] key-table test gives the route the stub resolver `() => (String(server).trim() || browser)`. Thus the test calculates its own expected key, and the route only forwards that key. No tagged test runs `googleServerApiKey()` or the resolver that server/providers/places/google.js:256 passes. No test runs the default `resolveApiKey = googleServerApiKey` at server/providers/places/geocode.js:60. Mutation 1 of task 4.1 cannot make this test fail. The only real check of the table is in the untraced test "both Places routes select the intended key and keep it out of responses" (src/googleServerKey.test.mjs:70). This change edited that test and kept its old untraced name. Run all five table rows through the environment and `googlePlacesContextProxy()` in a tagged 008 test. Add a test for the default resolver of geocode.js.
- [ ] F2 major src/googleGeocodeProxy.test.mjs:194 The over-cap body is 2 MB of `x`, which is not JSON. If an author removes or raises the 1 MB cap, JSON.parse fails, and the test still gets an error. Send a valid JSON body that is larger than 1 MB. Assert the error text 'Upstream response too large' and `Cache-Control: no-store`.
- [ ] F3 major server/providers/places/google.js:257 No test shows that the geocode route uses the same rate limit as the Places routes. The 429 test injects `rateLimiter: () => false`. If an author changes this line to `rateLimiter: null`, each test passes. Add a tagged 011 test through `googlePlacesContextProxy()` with `GEV_RATELIMIT_GOOGLE_PER_MIN=1`. Send one Places request, then one geocode request from the same client. The geocode request must get 429.
- [ ] F4 major src/tooling/bundleCredentials.test.mjs:88 The fixture build copies only `envPrefix` and `define` from `createBrowserViteConfig()`. It drops `plugins` and all other fields. The WHEN of 001 and D5 say "through the real browser config". A plugin or a config field that exposes an environment value passes 001, 002 and 016. Give `build()` the full config object, and replace only `root`, `outDir`, `configFile` and `publicDir`.
- [ ] F5 major src/tooling/bundleCredentials.test.mjs:173 `jsFilesUnder()` reads only `.js` files. The WHEN of 004 names every non-test file under src/ and every file under build/. The browser loads src/keySetupCore.mjs (index.html and src/keySetup.js import it), src/keySetupHardening.mjs and the JSON data under src/. The scan does not read these files. The 015 scan uses the same helper. Read each file that is not a test file, or change the scenario text and record the limit.
- [ ] F6 major src/tooling/previewServing.test.mjs:29 The change put the tag [credential-boundary-007] on an old untraced test and added one row (line 103). That row asserts only status 200 and a JSON content type. On the real dev and preview servers, the test does not assert the THEN body `{configured:false, error:null, status:null, results:[]}` or `Cache-Control: no-store`. The untraced ledger count of this file went from 2 to 1 because of this edit. Assert the body and the header for the geocode row on both servers.
- [ ] F7 major src/voice/gevActions.js:2945 `reverseGeocode()` caches null for each answer whose status is not OK. The new route sends such a JSON answer when the upstream fetch fails (502), when the limiter refuses (429) and when a read is over the cap. Before this change, a failed fetch threw and the result was not cached. Now a short outage turns off each coordinate that it touches until the page reloads. No scenario states this, and no 014 test sends a 502 or a 429 answer. Do not cache when `response.ok` is false or `status` is null. Add a scenario line and a test.
- [ ] F8 major src/googleGeocodeProxy.test.mjs:155 The 405 test asserts only the status. It has no fetch stub, and it does not assert the THEN part "with zero upstream calls". Stub fetch, assert zero calls and assert the body. The 405 body also says `configured:false` when a key is set (geocode.js:65). State that body in 010, or send `configured:true`.
- [ ] F9 minor server/providers/places/geocode.js:71 The keyless answer comes before the input checks. Thus a keyless server answers 200 to a request that names neither mode. The WHEN of 010 has no key condition and gives 400 for that request, so 010 and 007 disagree. Add "with a Google key set" to the WHEN of 010.
- [ ] F10 minor server/providers/places/geocode.js:102 No scenario states the 5 s upstream timeout, the refusal of a blank address (line 38) or the error for a body that is not JSON (line 109). The timeout test has the tag 011, but 011 does not name a timeout. Add these results to 010 and 011.
- [ ] F11 minor src/data/placeProviderPayloads.js:131 The projection does not cap `address_components[].types`. It also passes `geometry.location`, `bounds` and `viewport` with all their fields. The title of 012 says "capped, named fields", but each test input already has the exact shape. Cap the nested lists and keep only the named nested fields. Add a test with extra nested fields.
- [ ] F12 minor server/providers/places/geocode.js:112 The error text in a response comes from upstream text: `error_message`, and JSON.parse messages that quote part of the body. No code removes the key from this text. The 011 test uses only the fixed text 'Denied'. Remove the key from each error text, and test an upstream message that contains the key.
- [ ] F13 minor src/search/placeSearch.test.mjs:19 The change edited these untraced tests and kept their old untraced names: this test, src/locations.test.mjs:57, src/annotations/annotationEngine.test.mjs:424, src/annotations/annotationResolver.test.mjs:202-577 and src/voice/gevActions.test.mjs:3078 and 3115 (for googleServerKey.test.mjs:70, see F1). The "refusal failures" case now sends HTTP 200 with REQUEST_DENIED. Thus no test sends a route answer that is not ok (403, 429 or 502) through the `response.ok === false` arm at src/search/google.js:44. Tag each edited test, or record that each edit only changes the transport. Add a test for a route answer that is not ok.
- [ ] F14 minor src/keySetupCore.test.mjs:407 The 006 scan finds only `process.env.NAME` and `env.NAME`. It does not find a read such as `process.env['NAME']` or `const { NAME } = process.env`, or any read under pinokio/. The test has no positive control, for example a check that the scan finds OPENAI_API_KEY. The known-limits section does not name this gap. Add a positive control and record the limit.
- [ ] F15 minor src/tooling/bundleCredentials.test.mjs:118 Test 001 compares the found set with a set that it makes from the registry `clientExposed` flags. It does not use the three literal names of the THEN line (AGENTS rule 14). src/googleGeocodeProxy.test.mjs:291 calculates its expected status in the same way. Compare with literal values.
- [ ] F16 minor openspec/changes/archive/2026-09-23-credential-boundary/proposal.md:41 The Impact section does not list the gaps that the change closes or makes smaller in openspec/trace. These are: src/standalone/placeSearch.js closed, the smaller entries of gevActions.js and application.js, and the smaller untraced counts of four test files. The list of test files with new tagged assertions does not include keySetupCore.test.mjs. Design Risk 5 (the test probes only one AIS knob) is not in the known-limits section. Correct the Impact section and move Risk 5 into the known limits.
- [ ] F17 minor openspec/changes/archive/2026-09-23-credential-boundary/tasks.md:108 Tasks 8.1 to 8.5 are not checked, but the ratchet ran (history.jsonl lines 588-612) and the change is archived. No record shows that task 8.4 ran (each mutation again on the final tree). F1 shows that mutation 1 of task 4.1 cannot make its test fail. Do each task, record the result, and then check the box.
- [ ] F18 minor pinokio/_ENVIRONMENT:12 The text "Browser: 3D tiles only." is not true for a setup with one key. With one key, the server uses the browser key for Places, Street View and geocoding, as the known limit states. The text at .env.example:42 says that the rate limit applies only to /api/google/nearby-places. It now also applies to text search and geocoding. Correct both texts.
- [ ] F19 minor src/voice/gevActions.test.mjs:3147 The 014 URL test does not set `window.__GOOGLE_MAPS_API_KEY__`. Code that adds the global key when it exists thus passes. Set the global key in the test, and assert that the URL has no key parameter.
- [ ] F20 minor openspec/changes/archive/2026-09-23-credential-boundary/design.md:52 The gate output has ERROR REVIEW-MISSING and 294 STE warnings. The error closes when review.md is written. The config forbids -ing forms as nouns or adjectives, but this change uses them, for example "cost-bearing" at design.md:52 and "Sending it from the browser" at design.md:8. Run `make lint` and correct each warning in the files of this change.

The one-branch claim for the waiver of src/voice/gevActions.js is correct. I compared gev-main.lcov and gev-cb2.lcov record by record. There are 221 and 222 records with no hits, and the only new record is BRDA:2974. The raw V8 data shows exactly one zero-count range (1 character) in the async function of `reverseGeocode`, and marker.txt gives 8 runs of the finally block and 2 runs of the catch block. The raw V8 capture came from an earlier version of the test, but gev-cb2.lcov shows the current tests.
