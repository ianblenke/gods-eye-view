Verdict: FAIL
- [ ] S34 major openspec/changes/archive/2026-09-24-credential-boundary/design.md:127 "The scans of `credential-boundary-004` and `credential-boundary-015` read each file under `build/`." Does not agree with the code. The 015 scan (src/tooling/bundleCredentials.test.mjs:207-216) reads `src/` and `server/`, not `build/`. Only 004 reads `build/` (:195). Write: "The scan of `credential-boundary-004` reads `index.html`, each file under `build/`, and each file under `src/` that is not a test file. The scan of `credential-boundary-015` reads each file under `src/` that is not a test file, and each such file under `server/`."
- [ ] S35 major openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:40 "the test reads the files under `server/`, `scripts/` and `tools/`" (also openspec/specs/credential-boundary/spec.md:43; design.md:240; proposal.md:92) Does not agree with the code. `scriptFilesUnder()` (src/keySetupCore.test.mjs:388-396) reads only `.js` and `.mjs` files. `scripts/dev-secure.sh` and `scripts/opensky-import-client.sh` read credential names, and the scan does not read them. Write: "the `.js` and `.mjs` files under ...". In `credential-scan-forms`, add: "It does not read shell scripts."
- [ ] S36 major openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:85 "`geometry` keeps only the `lat` and `lng` values of `location`, `bounds` and `viewport`" (also openspec/specs/credential-boundary/spec.md:88) Two possible meanings, and does not agree with D2 (design.md:73-75) or the code (src/data/placeProviderPayloads.js:118-129). `bounds` and `viewport` keep `northeast` and `southwest`, each with `lat` and `lng`. A point or box without number coordinates gives null. The spec does not say this. Write: "`geometry` keeps `location` as `{lat,lng}`, and `bounds` and `viewport` as `{northeast, southwest}` boxes of such points. A value that is not a finite number gives null."
- [ ] S37 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:66-71 "a fault is neither mode, both modes" (also openspec/specs/credential-boundary/spec.md:69-74) Articles and nouns. "a fault is neither mode" can mean "the fault is not a mode", and "also" has no clear job. The last AND ("with or without a key") does not agree with the WHEN ("the server has a Google key"). The `?lat=30` case (src/googleGeocodeProxy.test.mjs:190) is not named. Write: "**WHEN** the server has a Google key, and a request names neither mode or both modes" and "**AND** the request has a bad `bounds`, a missing or out-of-range coordinate, a blank address, or an address of more than 256 characters". Put the 405 case in its own WHEN.
- [ ] S38 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:75 "with the key removed" (also design.md:55-57 "For a read that is too large, too late or not JSON") Voice, verbs and agreement. "with the key removed" is passive, and "read" is a verb used as a noun. When Google gives no `error_message`, the route sends the fixed text "Google Geocoding request failed" (src/googleGeocodeProxy.test.mjs:246-258). The text omits this. Write: "an HTTP error status from Google gives the same status and the Google error text, or a fixed text when Google gives none. The route removes the key from the text." and "For an answer that is too large, too late or not JSON".
- [ ] S39 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:49-53 "a request with neither mode or both modes answers `400`" (also design.md:53 "A request with a key sends one GET"; spec.md:71; spec.md:75 "an HTTP error status ... gives"; src/voice/gevActions.test.mjs:3317, 3329, 3341, 3353, 3364 "a later call fetches again"; :3247 "a configured answer gives"; src/googleGeocodeProxy.test.mjs:215 "a non-ok upstream status gives") Subjects. The route answers and sends. A request, a status, an answer or a call does not. Write: "the route answers `400` for a request with neither mode or both modes", "for a request with a key, the route sends one GET to Google", "after an HTTP 502 answer with a null Google status, a later call makes a new fetch".
- [ ] S40 minor src/googleGeocodeProxy.test.mjs:215 "a non-ok upstream status" (also :246; :227 "upstream error message"; :442 "incorrect input"; src/tooling/previewServing.test.mjs:157 "geocode route"; src/googleGeocodeRateLimit.test.mjs:24 "geocode request"; spec.md:48 "upstream request" and :54 "upstream calls"; spec.md:48-54 "response" and "answer") One word, one meaning. Prose says "HTTP error status", "error text", "bad input", "geocoding route" and "upstream call". Write: "an HTTP error status", "the upstream error text", "a bad input", "the geocoding route", "a geocoding request". Choose "answer" or "response", and "call" or "request", and use it everywhere.
- [ ] S41 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:93-95 "so the result stays open to a later search" (also design.md:205 "an open answer"; tasks.md:89 "closes the search"; src/search/placeSearch.test.mjs:43, :71 "inconclusive") One word, one meaning, and agreement. Four terms name `answered:false`. The D4 sentence is true only when Photon also finds no place. A Photon place gives `answered:true` (src/search/placeSearch.js:41; placeSearch.test.mjs:81-89). Write: "An HTTP error answer of the route gives `{place:null, answered:false}`. When Photon also finds no place, the combined answer is `answered:false`, and the search does not keep it." Use `answered:false` in D8, in task 6.1, and in the two test names.
- [ ] S42 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:10-13 "shows this ... thus ... nobody wants" Verbs and approved words. The present tense claims that the current fixture build shows a probe value in the output, but the current test (bundleCredentials.test.mjs:155-159) asserts that it does not. "thus" and "nobody" are not approved words (I am not sure). Write: "Before this change, a fixture build through the browser config showed this: a probe value on the environment got to the built output. So a later `.env` line can ship a value that the team does not want to expose."
- [ ] S43 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:9 "A second gap is in the build." (also design.md:10) One word, one meaning. "gap" also means a coverage gap in proposal.md:68-74. Write: "A second problem is in the build."
- [ ] S44 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:19 "The other checks that the registry or `.env.example` documents" Verbs. "checks" is a noun in "two checks" and a verb here, so the sentence can be misread. Write: "The second check finds each credential name that the server reads. It fails when the registry and `.env.example` do not document the name."
- [ ] S45 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:58 "Six test files get new tagged tests" Does not agree with tasks.md:38. `viteBuild.test.mjs` and `googleServerKey.test.mjs` get the tag `credential-boundary-003` on existing tests. Write: "Six test files get new tags or new tagged tests:".
- [ ] S46 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:80 "## Known limits and later changes" The section has no later change. Write: "## Known limits".
- [ ] S47 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:95 "in another way passes these tests" Two possible meanings. Write: "A change that removes only `VITE_AIS_LIVE_API_URL` or `VITE_AIS_LIVE_LABEL_MAX_ROWS`, and keeps the prefix, passes these tests." Also write "fails when the prefix changes" for "stops a change of the prefix" (:94).
- [ ] S48 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:119 "each test reads the output files as text" (also spec.md:9, :13, :28 "the built output") Does not agree with the code. The `before` hook builds and reads the files one time (bundleCredentials.test.mjs:130-132), and each test uses the result. The scan skips the `cesium/` asset folder (:112-114). Write: "The build and the read of the output files run one time, in a hook. Each test uses the result." and "the built output, without the `cesium/` folder that Vite copies from `node_modules`".
- [ ] S49 minor openspec/changes/archive/2026-09-24-credential-boundary/tasks.md:15 "Revert each mutation of this file after its check." (also :48, :86, :114, :132) "this file" is `tasks.md` (2.4), and no mutation edits it. Section 4 (:27-44) has mutations in 4.1, 4.5 and 4.6 and no revert line. Write: "Revert each mutation of this section after its check." in each section with a mutation, and add it to section 4. Write 9.5: "Run each mutation of sections 3 to 8 on the final tree, in one session."
- [ ] S50 minor openspec/changes/archive/2026-09-24-credential-boundary/tasks.md:37 "Export `standalonePlugins()` ..., and build the fixture with it." (also :74 "Install the route from `google.js`. Export it from `places.js`."; :94 "In each other test that mocks ...") Instructions. Tasks 4.3 and 5.7 give two instructions. In 4.3, "it" is not clear. Task 6.4 does not start with a verb. Write: split 4.3 and 5.7 into one task each, and write "Mock `/api/google/geocode` in each other test that mocks the direct Google host."
- [ ] S51 minor openspec/changes/archive/2026-09-24-credential-boundary/tasks.md:110 "State the narrower use of the browser key, the wider use of the server key and the OpenAI client secret." Two possible meanings. "the OpenAI client secret" can be the object of "State". Write: "Describe the narrower use of the browser key, the wider use of the server key and the design of the OpenAI client secret."
- [ ] S52 minor openspec/changes/archive/2026-09-24-credential-boundary/tasks.md:18 "Rules out:" (also :24, :30, :42, :51, :57, :63, :89, :98, :104) Phrasal verb with no subject. Write: "The test fails for: ...".
- [ ] S53 minor openspec/changes/archive/2026-09-24-credential-boundary/tasks.md:146 "each credential name that the server reads is documented" Voice. Write: "the registry or `.env.example` documents each credential name that the server reads; the positive control".
- [ ] S54 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:41 "a bracket read" (also tasks.md:20 "a destructured, a bracket and a quoted read of it"; proposal.md:89 "a dynamic read"; design.md:241 "dotted, destructured and quoted reads"; design.md:63 "both read it") Verbs used as nouns, and "read" has two meanings. Write: "`env['NAME']`", "a name that the code reads with `env[variable]`", "both import it".
- [ ] S55 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:11 "a matching name" (also tasks.md:24 "no matching `define` entry"; :22 "missing"; :38 and :78 "existing") Words that end in -ing. Write: "a name that matches", "no `define` entry for it", "the three commented names that `.env.example` lacks", "the current `define` tests".
- [ ] S56 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:104 "to find if photoreal tiles are available" Idiom. Write: "to check if photoreal tiles are available".
- [ ] S57 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:100 "with no key, also when the page has a browser key" (also proposal.md:87; tasks.md:104) "also when" is not a complete condition. Write: "with no key. The URL has no key when the page has a browser key too."
- [ ] S58 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:61 "Send a reverse lookup to Google with the selected key" (also openspec/specs/credential-boundary/spec.md:64) The body of 009 does not name a key. Write: "Send a reverse lookup to Google".
- [ ] S59 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:247 "The narrower `VITE_` prefix stops an AIS setting. Guard: ... stop a changed prefix." One word, one meaning. "stops" first means "breaks" and then "fails on". Write: "The narrower `VITE_` prefix breaks an AIS setting. Guard: ... fail when the prefix changes."
- [ ] S60 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:76 "a fixed error text" (also :77; design.md:57; src/googleGeocodeProxy.test.mjs:305 "a fixed error") Approved words. I am not sure about "fixed" in the meaning "the same each time". Write: "an error text of the route".
