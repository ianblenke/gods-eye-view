Verdict: PASS
- [ ] S61 minor openspec/changes/archive/2026-09-24-credential-boundary/tasks.md:9 "the `VITE_` gap" One word, one meaning. This diff renames "gap" to "problem" in proposal.md:9 and design.md:10 (S43). Task 2.2 still says "gap" for the same thing. Write: "the `VITE_` problem".
- [ ] S62 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:68 "a missing or out-of-range coordinate" (also openspec/specs/credential-boundary/spec.md:71) Words that end in -ing. This is the S55 class: the project already rewrote "missing" in tasks.md. I am not sure that "missing" is approved. Write: "a coordinate that is not in the request, or that is out of range".
- [ ] S63 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:68 "these are faults: neither mode, both modes" (also openspec/specs/credential-boundary/spec.md:71) Articles and nouns. The spec never defines "mode". Only design.md D1 does. "neither mode" is a noun phrase that names no fault. The spec names "forward lookup" and "reverse lookup", not "mode". Write: "these are faults: a request with no `address` and no `lat` or `lon`, and a request with an `address` and a `lat` or `lon`".
- [ ] S64 minor openspec/changes/archive/2026-09-24-credential-boundary/specs/credential-boundary/spec.md:70 "a request with a fault answers `400`" (also :71 "a request with a method other than `GET` answers `405`"; openspec/specs/credential-boundary/spec.md:73-74) Subjects. This is the S39 pattern. The round-2 text had "the route answers `400`", and the new wording lost "the route". The :71 line is the S39 text that the lead kept. Write: "**THEN** when the server has a Google key, the route answers a request with a fault with `400`, and it makes zero upstream calls" and "**AND** the route answers a request with a method other than `GET` with `405`, with or without a key, and it makes zero upstream calls".
- [ ] S65 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:10 "exposes each `.env` line with a name that matches to the bundle" Articles and nouns. "matches" has no object, and "to the bundle" can attach to "matches". This is the S55 fix, which has this side effect. Write: "exposes to the bundle each `.env` line with a name that starts with that prefix".
- [ ] S66 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:120 "The build and the read of the output files run one time" Verbs. "read" is a verb used as a noun. This is the S48 fix, which has this side effect. Write: "The test builds the fixture and reads the output files one time, in a hook. Each test uses the result."
- [ ] S67 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:134 "The fixture scan does not read the `cesium/` folder" One word, one meaning. The sentence is in the paragraph about the scans of 004 and 015, and it splits "data files" from the protobuf sentence that follows. "fixture scan" has no definition. The scan that skips `cesium/` is the scan of the built output, which the paragraph before describes. Write: move the sentence to the fixture paragraph, and write "The scan of the built output does not read the `cesium/` folder that Vite copies from `node_modules`. The known limit `bundle-scan-skips-cesium` records this."
- [ ] S68 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:209 "The search must then give `answered:false` when Photon finds no place, and it uses Photon." Verbs. "must give" and "uses" do not match, and "when Photon finds no place, and it uses Photon" can read as one condition. Write: "The search must then use Photon. It must give `answered:false` when Photon also finds no place."
- [ ] S69 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:245 "finds dotted, destructured and quoted reads" Verbs. "reads" is a verb used as a noun. This is the design.md part of S54, and the lead did not correct it. Write: "finds a credential name in a dotted form, a destructured form and a quoted form".
- [ ] S70 minor openspec/changes/archive/2026-09-24-credential-boundary/design.md:257 "A change that stops only `VITE_AIS_LIVE_API_URL`" One word, one meaning. Risk 4 now says "breaks" (S59). Risk 5 uses "stops" for the same thing. Write: "A change that breaks only `VITE_AIS_LIVE_API_URL` or `VITE_AIS_LIVE_LABEL_MAX_ROWS` passes this test."
- [ ] S71 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:19 "scans the build output" One word, one meaning. Line 13 of the same section and spec.md:9 say "built output". Write: "scans the built output".
- [ ] S72 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:21 "It fails when the registry and `.env.example` do not document the name." Articles and nouns. The text can mean "not both document it". Spec 006 passes a name that only one of them documents. Write: "It fails when neither the registry nor `.env.example` documents the name."
- [ ] S73 minor openspec/changes/archive/2026-09-24-credential-boundary/proposal.md:88 "with no key, also when no server key exists" "also when" is not a complete condition. This is the S57 text, and the lead corrected it in spec.md but not here. Write: "with no key. It sends no key when no server key exists too."
