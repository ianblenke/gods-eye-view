Verdict: FAIL
- [ ] S57 major proposal.md:45 "The fix sets blocking mode only in a test-file child process of a gate run." Does not agree with other text in the change. proposal.md:46 (`blocking-mode-in-started-processes`) says that a process that a test starts "then also goes to blocking mode". design.md:100 says the same. The code agrees with proposal.md:46: test-guard.mjs:267 checks only `GEV_SPEC_OUT` and `NODE_TEST_CONTEXT === 'child-v8'`. The same "only" is at design.md:35 and design.md:41. Write proposal.md:45: "The fix sets blocking mode only in a process that has `GEV_SPEC_OUT` and `NODE_TEST_CONTEXT=child-v8`. Such a process is a test-file child process of a gate run or a process that a test starts (`blocking-mode-in-started-processes`). The fix does not change the outer runner process." Write design.md:35: "D1. Set blocking mode on the standard output of a test-file child of a gate run". Write design.md:41: "The fix gives the same behavior on Linux, but only for a process with `GEV_SPEC_OUT` and `NODE_TEST_CONTEXT=child-v8`."
- [ ] S58 major design.md:49 "But the gate uses total counts only to calculate covered counts." Does not agree with the code. The gate also calculates the tolerance from the total count: ledger.mjs:196 `toleranceOf(gap.totals[metric])`, which ledger.mjs:283 uses. The gate also compares the total counts directly: ledger.mjs:178 and 361. Write: "For that cost, the gate gets a constant total count. The gate uses total counts to calculate covered counts and tolerances. For an unchanged file, the covered-count rule of D3 stops the false error at a lower cost."
- [ ] S59 minor specs/coverage-gate/spec.md:4 "Then `--test-force-exit` cannot end that process" "Then" can seem to refer to the mark, not to blocking mode, and the text uses "Then" with a different meaning at the end. Write: "With blocking mode, `--test-force-exit` cannot end that process before its queued reporter output reaches the parent."
- [ ] S60 minor proposal.md:13 "It merges the records in that sequence." No sentence before it names a sequence. Write: "It merges the records in the sequence that `opendirSync` gives."
- [ ] S61 minor proposal.md:13 and design.md:9 "all the test-file processes" Write: "all the test-file child processes".
- [ ] S62 minor design.md:69 "It also checks three fake processes with the same values that give the guard no error." Write: "It also checks that the guard gives no error for three fake processes with the same values."
- [ ] S63 minor design.md:76 "because a `_handle` without `setBlocking` throws" Write: "because the call on a `_handle` without `setBlocking` throws an error".
- [ ] S64 minor design.md:53 present tense describes the check before this change. Write: "Before this change, the `LEDGER-TOTALS-NOT-BASE` check of `compareWithBase` had no such rule. It stopped the build for each total that was not the base total. The only exception was a `totals` history line of the checked change for that file."
- [ ] S65 minor proposal.md:43 "So does a total drift of a changed file." leaves out words; "usual" may not be approved. Write: "A total drift with a changed covered count still needs the tolerance or an investigation. A total drift of a changed file also needs the tolerance or an investigation."
- [ ] S66 minor proposal.md:44 "a ledger entry can now raise a total ... the change that did it." Write: "For an unchanged file, a change can now raise a total and the not-covered count of a ledger entry by the same amount. ... Then no history line names the change that raised the total."
- [ ] S67 minor design.md:49 "sufficient" may not be approved. Write: "if the covered-count rule below is not enough".

Notes: S34 to S49 and S51 to S56 are corrected; S50 is partly corrected (S62). "blocking mode", "non-blocking mode" and "test-file child" are accepted. The test names have no findings.
