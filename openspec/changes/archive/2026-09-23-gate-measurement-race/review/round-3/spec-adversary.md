Verdict: PASS
- [ ] F1 minor src/tooling/spec/ledger.test.mjs:451 The `gap-ledger-056` test has no case where the covered count goes up while a total changes. Each drift case in the 056 and 088 tests (lines 451, 453, 470) lowers the covered count. So the change `covered >= coveredCount(base, metric)` at `scripts/spec/lib/ledger.mjs:51` passes all tests, but the 056 AND line says "is not equal". With that change, the ledger entry of an unchanged file can raise its branch total by 5 and its not-covered count by 2 with no `totals` history line. A later change that edits the file then starts from the larger not-covered count. The known limit `equal-rise-not-recorded` names only the equal rise. Also, no 056 case changes only the functions total. Add a case with `LOADED(5, 3, 1, { totals: { ...TOTALS, branches: 102 } })` (covered count 99, base 97). Add a second case with `{ ...TOTALS, functions: 9 }`. Assert `LEDGER-TOTALS-NOT-BASE` for each case.
- [ ] F2 minor openspec/changes/archive/2026-09-23-gate-measurement-race/proposal.md:46 The known limit `blocking-mode-in-started-processes` names only a process that a test starts "without its own `options.env`". A process with its own `options.env` that copies `process.env` also gets `NODE_TEST_CONTEXT=child-v8` and `GEV_SPEC_OUT`, so it also gets blocking mode. Four tests in the changed test file do this (`testGuard.test.mjs:240`, `251`, `266`, `462`). Also, `blocking-mode-child-only` at proposal.md:45 says "only in a test-file child process of a gate run", and the next item says the opposite. Write the condition as "a process whose environment has `NODE_TEST_CONTEXT=child-v8` and `GEV_SPEC_OUT`, with or without its own `options.env`". Remove "only" from proposal.md:45.
- [ ] F3 minor openspec/changes/archive/2026-09-23-gate-measurement-race/design.md:100 The Risks section and D1 (design.md:43) say that a write in blocking mode delays only the exit, by "some milliseconds", and only for a burst. But a write in blocking mode waits each time the socket buffer is full and the parent does not read. This can occur in the middle of a test, and it also applies to the console output of a test. The event loop of the child then stops, and its timers fire late. The parent stall is the same condition that causes the race: 150 ms in the reproduction, and possibly longer under whole-project load. A test with a timer race or a time limit can then fail more often. Correct the Risks text, and add this case to the known limits.
- [ ] F4 minor (gate output) `ERROR REVIEW-MISSING` stops the build until `/opsx:review` writes `review.md`. Tasks 6.2 (the mutation report) and 7.4 are still open. The STE warnings for "blocking" in "blocking mode" need the name of the person who accepts them in `review.md`. Of round-2 F10, only the "timing" part is corrected. I read each mutation at design.md:72-96 against the tests, and each one makes the named test fail. That reading does not replace the run that AGENTS.md rule 13 requires. Add the mutation report to `review.md`, then check 6.2 and 7.4.
