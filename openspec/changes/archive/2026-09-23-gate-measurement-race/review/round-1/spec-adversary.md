Verdict: PASS
- [ ] F1 minor src/tooling/spec/testGuard.test.mjs:527 The coverage-gate-054 test checks a `stdout` with no `_handle` and a process with no `stdout`. It does not check a `_handle` that has no `setBlocking`, which is the literal AND line ("no blocking-mode function"). If the last `?.` is removed (`_handle?.setBlocking(true)`), the tests still pass, and V8 records no branch for optional chaining. Add a case `{ stdout: { _handle: {} } }` with `NODE_TEST_CONTEXT: 'child-v8'` and assert doesNotThrow.
- [ ] F2 minor src/tooling/spec/ledger.test.mjs:455 The gap-ledger-088 test has no metric whose total is unchanged but whose covered count changed. Every metric in the test has the same covered count on both sides. If the clause `entry.totals[metric] === base.totals[metric] ||` is removed from ledger.mjs:48, the tests stay green. But the gate then stops a correct change, for example lines got better while the branch total flipped. That goes against the scenario's words "each metric whose total counts differ". Add a case with a smaller not-covered lines count and a flipped branch total, and assert [].
- [ ] F3 minor scripts/spec/lib/ledger.mjs:47 No test reaches `!base.totals` with an entry that has totals. In the gap-ledger-048 test, src/loaded.js and src/old.js skip that path because they have a totals history line. If `|| !base.totals` is removed, a legacy base entry with no history line makes the gate throw a TypeError instead of giving LEDGER-TOTALS-NOT-BASE. Add a gap-ledger-056 case with base `totals: undefined`, an entry with TOTALS, sameAsBase true and no history.
- [ ] F4 minor scripts/spec/lib/ledger.mjs:48 `coveredCount` gives null on both sides when both totals are numbers but the metric count is null. `null === null` then counts as agreement, so the totals change passes. design.md:69 says the function is true only "when neither total is missing". `currentGaps` cannot make such an entry, but a hand-edited ledger can. Treat a null covered count as disagreement, or state this case in the spec.
- [ ] F5 minor scripts/spec/lib/test-guard.mjs:266 The code ignores the return code of `setBlocking(true)`. When the call fails, the race comes back and nothing records it. This is not in "Known limits". Record it as a known limit, or report a non-zero return.
- [ ] F6 minor openspec/changes/archive/2026-09-23-gate-measurement-race/tasks.md:13 The mutation records in tasks.md are wrong. Task 2.1 says that removing the `child-v8` condition makes coverage-gate-053 fail, but 053 still gets setBlocking(true). Task 2.2 says that removing the line makes the first case of 054 fail, but `called` stays false. design.md:65 has the correct pairs, and the tests do catch those mutations. Correct 2.1 and 2.2 to match the design.
- [ ] F7 minor openspec/changes/archive/2026-09-23-gate-measurement-race/tasks.md:36 Task 6.2 is checked ("Report each mutation ... in the review notes"), but no review notes and no review.md exist. That breaks AGENTS.md rule 17. Uncheck the task, or add the mutation report with the test that failed for each mutation.
- [ ] F8 minor openspec/changes/archive/2026-09-23-gate-measurement-race/tasks.md:40 Tasks 7.1 to 7.4 are not checked, but the change is archived and `make gates CHANGE=` gave no error for them. Check each task after its command passes, or record why the archive came first.
- [ ] F9 minor openspec/changes/archive/2026-09-23-gate-measurement-race/design.md:37 "The guard already returns early for a worker thread before this point" is not true. The new line (test-guard.mjs:266) runs before the `mainThread` check at line 284, so a worker with `NODE_TEST_CONTEXT=child-v8` also runs it. A worker's stdout has no `_handle`, so nothing happens. Correct the sentence.
- [ ] F10 minor openspec/changes/archive/2026-09-23-gate-measurement-race/proposal.md:35 Impact lists `openspec/trace/history.jsonl` as a changed file. The diff does not change it, and no trace line names gate-measurement-race except the three in ids.json. Remove it from the list.
- [ ] F11 minor (gate output) "STE: 0 errors, warnings only". The gate output does not list the warnings, so this review cannot check each one. The STE adversary must list and resolve them in review.md.
