Verdict: FAIL
- [ ] F1 major openspec/specs/coverage-gate/spec.md:196 This change makes scenario `coverage-gate-027` wrong, and the change does not modify it. The scenario says: when a process loads the guard without `GEV_SPEC_OUT`, "the guard does nothing". `scripts/spec/lib/test-guard.mjs:266` now runs before the `GEV_SPEC_OUT` check. In such a process with `NODE_TEST_CONTEXT=child-v8`, it sets standard output to blocking mode. The `coverage-gate-053` test (`src/tooling/spec/testGuard.test.mjs:522`) uses exactly that input, with no `GEV_SPEC_OUT`, and it asserts that the guard does something. The `027` test uses only `env: {}`, so it still passes. The main spec now gives two contrary results for one input. The goal "Change no other rule of the gates" (design.md:24) is not true, and no known limit names this. Correct it in one of two ways. (a) Move the line after `if (!env.GEV_SPEC_OUT) return null;` and before the cached-guard return. Add `GEV_SPEC_OUT` to the WHEN line of `053`, and test it with `{ ...ENV, NODE_TEST_CONTEXT: 'child-v8' }`. In the gate, each `child-v8` child has `GEV_SPEC_OUT`. (b) Add a MODIFIED `coverage-gate-027` whose THEN line limits the one exception ("does nothing other than the blocking mode of `coverage-gate-053`"). Also add a changed `027` test that asserts that exception, and a task line for it.
- [ ] F2 minor src/tooling/spec/testGuard.test.mjs:522 The `053` test never has `GEV_SPEC_OUT`, but each `child-v8` child in a gate run has it. One mutation passes `053`, `054` and `027`: `if (!env.GEV_SPEC_OUT) { if (env.NODE_TEST_CONTEXT === 'child-v8') …setBlocking?.(true); return null; }`. With that mutation, the fix does nothing in a gate run. Add a case with the gate values and `child-v8`. Option (a) of F1 closes this finding.
- [ ] F3 minor src/tooling/spec/testGuard.test.mjs:534 The `054` test checks only `env: {}`. The mutation `if (env.NODE_TEST_CONTEXT)` passes `053` and `054`. It then sets blocking mode when `NODE_TEST_CONTEXT=child`, which is a value that `gates.test.mjs:134` uses. That is against the WHEN line of `054`. Add a case with `NODE_TEST_CONTEXT: 'child'` and assert no `setBlocking` call.
- [ ] F4 minor src/tooling/spec/ledger.test.mjs:451 No `056` case has a line total that is a number and differs from the base with a changed covered line count. Case 1 has no line total, so the unknown-count rule stops it. One mutation passes all tests: loop over `['branches', 'functions']` in `totalsAgreeOnCoverage`. This is the list that the other two loops of `compareWithBase` use, so it is a natural edit. With that mutation, a line-total drift that changes the covered line count does not stop the build. The `056` scenario says "one or more metrics". Add a case with `totals: { ...TOTALS, lines: 98 }` and 5 not-covered lines, and assert `LEDGER-TOTALS-NOT-BASE`.
- [ ] F5 minor openspec/specs/gap-ledger/spec.md:155 The new AND line of `056` says only that the covered count "is not equal". It does not name a covered count that is not known, or an entry without totals. The `056` test tags three such cases (`ledger.test.mjs:448`, `455`, `458`). proposal.md:20 and design.md:53 say "not known". Round-1 F4 asked for this case in the spec. Add "or is not known" to the AND line, in the delta and in the main spec. This change already modifies `056`, so only the ratchet must run again.
- [ ] F6 minor openspec/changes/archive/2026-09-23-gate-measurement-race/design.md:95 The Risks section says that `merge-order-not-fixed` records a risk: the tolerance can hide a real change of a total when the covered count stays equal. `proposal.md:43` does not name that risk. The unrecorded case is this. For an unchanged file, a ledger entry can now raise a total and its not-covered count by the same amount, with no totals history line. Then no record names the change that did it, and a later change that edits the file gets the larger not-covered allowance from that base. Add this to the known limit, or add a new limit.
- [ ] F7 minor scripts/spec/lib/test-guard.mjs:266 The condition also matches processes that a test starts. When the test gives no `options.env`, the child gets the test environment, so it also has `NODE_TEST_CONTEXT=child-v8` and gets the guard preload. Its standard output is then in blocking mode. When the parent does not read that pipe, a write stops the process, where before it queued the data, and the test waits until its time limit. design.md:95 gives the delay as "some milliseconds", and `blocking-mode-child-only` does not name these processes. Record this as a known limit, or make the condition apply only to the test-file child.
- [ ] F8 minor scripts/spec/lib/test-guard.mjs:266 Because of the optional chain, the fix does nothing and gives no sign when the real standard output of a `child-v8` process has no `_handle.setBlocking`. For example, a new Node version pinned in `.node-version` can change these internals. Each test uses a fake `processObject`, so each test still passes. `blocking-mode-not-checked` names only the return code. Add a check that does not depend on timing. In a real test process with `NODE_TEST_CONTEXT === 'child-v8'`, assert `typeof process.stdout._handle?.setBlocking === 'function'`. If you do not add it, record this case in the known limit.
- [ ] F9 minor openspec/changes/archive/2026-09-23-gate-measurement-race/tasks.md:40 Task 6.2 is checked, but `review.md` does not exist (the gate gives `REVIEW-MISSING`). So no report exists of each mutation with the test that failed. Round-1 F7 is not corrected. It still breaks AGENTS.md rule 17. Uncheck 6.2 until `review.md` has the report, or add the report now.
- [ ] F10 minor (gate output) There is 1 error and there are 17 warnings. `ERROR REVIEW-MISSING` stops the build until `/opsx:review` writes `review.md`. The STE warning "timing" at design.md:57 is an -ing noun and is still not corrected. Write, for example: "The result of such a test changes with the load on the machine." For the 16 "blocking" warnings, write in `review.md` the name of the person who accepts them.
