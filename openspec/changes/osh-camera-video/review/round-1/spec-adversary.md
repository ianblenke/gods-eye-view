Verdict: FAIL

Tree read: `/home/ianblenke/docker/gev-video`, branch `osh-camera-video`. The caller names HEAD `6e9e079`. I cannot run git, so I did not confirm the commit. I compared against `/home/ianblenke/docker/gods-eye-view` as main.

Findings:
- [ ] F1 (major) Slow client grows an unbounded write buffer: `server/providers/osh.js:713` passes `write: (text) => res.write(text)` and ignores the return value and `res.writableLength`. `server/providers/osh/live.js:74-76` (`broadcast`) and `:276` (group replay) do the same. Video messages can be 2 MB. A client that stops reading makes Node buffer every frame with no limit, until the socket dies. Design.md:89 counts only the hub group, and no known limit names this. Fix: when a client's buffer passes a fixed limit, end its response so the browser reconnects and gets the replay. Add a scenario and a test. Or name it in the Known limits.
- [ ] F2 (major) Nothing checks the markup that the panel needs: `index.html:836-839` holds `id="osh-panel"` with `hidden`, and the ids `osh-panel-video` and `osh-panel-detail`. Every osh-089 test in `src/data/osh.test.mjs:28-207` uses a fake document. Renaming one id in `index.html`, or dropping `hidden`, breaks the panel and fails no gate. `osh-panel-layout-unchecked` names the place of the panel, not these ids. Fix: add a test that reads `index.html` and asserts the three ids, their nesting and the initial `hidden`. Or name it as a known limit.
- [ ] F3 (major) The occluder line in `src/overlays/worldOverlay.js:106-109` has no scenario and no test, and the proposal does not name the file. `WORLD_OVERLAY_OCCLUDER_SELECTORS` gets `'#osh-panel'`. `worldOverlay.test.mjs` has no assertion for it, only the generic tests at :324 and :1513. Deleting the line fails nothing. This file has recorded gaps, so the ledger tolerates the new line (`history.jsonl:614-615`). `proposal.md:25-27` (Impact) omits the file. Fix: add an AND line to osh-086 or a new scenario. Add a test that asserts `'#osh-panel'` is in the list. List the file in the proposal Impact.
- [ ] F4 (major) Code, tests and scenarios disagree about a time stamp that is not finite. `src/data/oshVideo.js:32` returns null when the time stamp is NaN or infinite, even when the length field is right. Scenario osh-083 says the reader returns the data when the length field equals the size minus 12. Scenario osh-079 says the hub relays every message of at least 12 bytes whose length field is right. `design.md:40` says the hub drops "three kinds" of message. Tests tagged osh-083 (`oshVideo.test.mjs:52-61`) and osh-079 (`oshLive.test.mjs:1378-1379`) assert the opposite of the literal THEN. Fix: add the fourth case to osh-079, osh-083 and D74. Or remove the check.
- [ ] F5 (minor) The test for text messages cannot kill removal of the string guard at `server/providers/osh/live.js:185`. `oshLive.test.mjs:1354-1389` sends only non-numeric strings. Without the guard, `new Uint8Array('text')` is empty, so the code drops the message anyway. The string `'12'` gives 12 zero bytes. That is a valid message with time stamp 0 and length 0, so the hub would relay it as a `frame`. Fix: add `'12'` to the data list of that test.
- [ ] F6 (minor) A mutation survives in `src/data/oshVideo.js:62`. `nalTypeOf` masks with `0x1f`, and the test at `oshVideo.test.mjs:91-97` uses only bytes with bit 4 clear (`0xe5`, `0x67`, `0x68`, `0x65`, `0x41`). Changing the mask to `0x0f` passes. Then type 21 reads as an IDR slice and type 20 reads as type 4. Fix: assert `nalTypeOf([0x74]) === 20`, and check that `hasKeyNal` is false for `0x75`. A second, smaller survivor: `type >= NAL_SLICE_FIRST` in `toAvccSample` has no test with an empty NAL unit, which reads as type 0.
- [ ] F7 (minor) `src/layers/osh/videoPlayer.js` is not in `PROVIDER_FILES` at `src/data/oshRepositoryHygiene.test.mjs:11-27`. Scenario osh-034 says the layer files are scanned for real addresses. Task 4.4 added `oshVideo.js` only. Fix: add `src/layers/osh/videoPlayer.js` to the list.
- [ ] F8 (minor) The gate output has STE warnings in the new prose. `design.md:7` (framing), `:48` (is dropped), `:89` (is sixteen). `proposal.md:45` (is checked). `specs/osh/spec.md:94` (is dropped), `:144` (is hidden), `:162` (missing). `tasks.md:15` and `:21` (trailing), `:40` (is dropped), `:73` (missing), `:83` (decoding). Fix: rewrite them in the active voice, or accept them by name. `spec.md:94` and `:144` are in scenarios, so rewrite the delta and the synced spec together.
- [ ] F9 (minor) A wrong claim in the proposal: `proposal.md:36` (`osh-video-h264-only`) says another codec gives a decoder error and the panel shows `unavailable`. In `src/layers/osh/videoPlayer.js:83-95`, `error()` sets `error` and then `waiting` in the same call. So the view (`src/layers/osh/index.js:295-298`) never shows `error`. Only the layer shows `unavailable`, on the event `unsupported`. Data that has no SPS and PPS never gives an error, so the status stays `waiting` for ever. Fix: correct the claim, or show a status the user can see.
- [ ] F10 (minor) Two player behaviours are not named in the Known limits. First, `src/layers/osh/index.js:308-309` does not reset the player on `down`. After an upstream reconnect the hub group is empty (`live.js:102-103`), so the first frames can be deltas. The player decodes them against the old decoder state until the next key frame. Second, the replay of a group arrives in one burst. The queue rule at `videoPlayer.js:129` then drops every delta after about nine frames, until the next key frame. For a group longer than nine frames, D75 helps little and the picture freezes. Fix: name both as known limits, or reset the player on `down`.
- [ ] F11 (minor) The panel says `reconnecting` after a permanent failure. `src/layers/osh/source.js:38` maps the browser `error` event to `onDown`, and `index.js:309` shows `reconnecting`. An answer of 502 or 503 (`live_busy`, `live_unsupported`, `no_key`) closes an `EventSource` for good. The label then stays false. Fix: check `readyState === 2` in the source, or name it as a known limit.
- [ ] F12 (minor) Tested behaviour that no scenario states, or that a scenario contradicts. (a) `oshDatastreams.js:59` also cuts a definition at `#` and `:`, and tests at `oshDatastreams.test.mjs:241-258` assert it. Osh-076 says only "last path segment", and D72 repeats that. (b) The player time origin is the first decoded message (`videoPlayer.test.mjs:215-224`). Osh-084 says "from the first message". (c) These are tested but not in a scenario: the canvas resize, the frame close when the draw throws, the title `Video` when the name is empty, and the missing time and age in `detail.js` for osh-088. (d) The video route tests tagged osh-077 (`oshLive.test.mjs:1165-1305`) assert the Basic header, `binaryType`, the response head, `base_unresolved` and `live_busy`. The tests for osh-080 (`:1551`) assert the group is emptied. Osh-077 states only the refusals. No scenario covers the events `down` and `open`, the retry delays or the heartbeat for the video kind. Fix: add AND lines to the scenarios.
- [ ] F13 (minor) A wrong number in the design. `design.md:89` says the group holds "up to two megabytes" and "sixteen megabytes" for eight entries. `live.js:163-176` counts binary bytes, but the group holds base64 event text, which is about 1.33 times larger. The worst case is about 22 MB, before the per-client buffers of F1. Fix: correct the number, or count the length of the text.
- [ ] F14 (minor) The real WebSocket path of the video kind has no loopback test. The live kind has one (`oshLive.test.mjs:1960`, `:2010`), and it proves binary frames arrive as an `ArrayBuffer` with a 70000-byte frame. The video kind is tested only with a fake socket. The manual headless Chrome run is not repeatable evidence. Fix: add one loopback test for a video message, or add a known limit.

Checked:
- Read all four change files, the base spec on main and the synced spec, `AGENTS.md` and `openspec/config.yaml`.
- MODIFIED requirements: "GET only" and "Live layer" carry every base scenario (osh-004, 005, 006 and 072, 073, 074). Their requirement text is unchanged. The synced spec has one deleted line (the osh-005 count) and no revert of a merged fix.
- Read all changed code files, the changed tests and the new tests. Traced each scenario osh-076 to osh-089 to its tests.
- Pinned counts: 5 and 6 for osh-005. 17 OSH test files for hygiene: 14 in `src/data` plus 3 under `src/layers/osh`. Test counts per changed file against main show no removed test. The osh-071 test names are the same as on main.
- Trace: the `ids.json` hashes changed only for osh-005, 072 and 074, and 14 new ids were added. The two history lines name the change. The `gaps.json` change is limited to the `worldOverlay.js` hash and totals, with uncovered counts unchanged. The untrue count is 25, as on main.
- Constraints: no `send` word outside comments in the scanned files. Only GET, and `oshOpenStream` is the only socket site. The credential goes in a header, not in the URL or the browser. All fixtures are synthetic (`sensorml.com` is an allowed vocabulary host). `package-boundaries.json` covers both new modules in the right packages.
- A sample of the mutation ledger against the tests: H1 to H7, V1 to V15, P1 to P13 and L1 to L14. The named tests fail for these except F5 and F6.
- Timers and stale callbacks: the generation guard, close on selection end, `destroy()`, and view removal.

Could not check:
- I cannot run code or git. So I did not run any mutation, the gates, `make lint`, `npm run format:check` or `npm run check:boundaries`.
- The second gate run after the archive is not confirmed. I read only `gates1.out`, which has one error, `REVIEW-MISSING`.
- I cannot say whether `style.css` passes Prettier. It is in the format scope, and the lead runs task 8.4.
- The real decoder and the panel look are a known limit. I did not check `links.json` line by line.
